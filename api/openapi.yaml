openapi: 3.1.0
info:
  title: DataBolsa API
  description: >-
    API aberta de dados do mercado financeiro brasileiro.


    Convenções:

    - Datas em ISO 8601; valores monetários em BRL salvo campo `currency`.

    - Preços de ações **ajustados por proventos por default** (`adjusted=true`).

    - Indicadores fundamentalistas **TTM por default**, de demonstrações
    consolidadas.

    - **Toda listagem e toda série** respondem `{ data, meta }`: os itens em
    `data` e, em `meta`,
      sempre a paginação (`next_cursor`, `count`). Parte das rotas ecoa também o contexto da
      consulta em `meta` (ticker, sessão, indicador, data-base...) — confira o schema da
      operação antes de depender disso, porque não é universal. Rotas sem cursor trazem
      `next_cursor: null` — nunca um envelope diferente.
    - Paginação por cursor onde ela existe: `?cursor=&limit=`; siga
    `meta.next_cursor` até null.

    - Recursos singulares (um papel, uma companhia, um fundo, uma thread de
    eventos, a saúde da
      ingestão) respondem o objeto direto, sem `data`.
    - Erros seguem RFC 9457 (`application/problem+json`).

    - Toda métrica é rastreável à fonte primária (campo `lineage`).
  version: 3.6.5
  contact:
    name: DataBolsa
  license:
    name: Apache-2.0
servers:
  - url: https://api.databolsa.com
    description: Produção
  - url: http://localhost:8080
    description: Self-hosted
security:
  - bearerApiKey: []
components:
  securitySchemes:
    bearerApiKey:
      type: http
      scheme: bearer
  schemas:
    Health:
      type: object
      properties:
        status:
          type: string
          enum:
            - ok
            - degraded
        version:
          type: string
        build:
          type:
            - string
            - "null"
          description: SHA do commit da imagem que respondeu.
        instance:
          type:
            - string
            - "null"
          description: Identificador do processo/pod que respondeu.
        data_freshness:
          type: object
          additionalProperties:
            type: string
      required:
        - status
        - version
        - build
        - instance
        - data_freshness
    Problem:
      type: object
      properties:
        type:
          type: string
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        instance:
          type: string
        details:
          type: object
          additionalProperties: {}
          description: O erro em forma LEGÍVEL POR PROGRAMA (membro de extensão do RFC
            9457). `detail` é a frase humana e pode mudar; o que um cliente
            precisa decidir por ele mora aqui — `code` (`not_found`, `split`,
            `budget_exceeded`…), `successors` num 409 de cisão, os números
            recebidos e os tetos num 422 de orçamento.
      required:
        - type
        - title
        - status
    Company:
      type: object
      properties:
        cvm_code:
          type: integer
        cnpj:
          type:
            - string
            - "null"
        name:
          type:
            - string
            - "null"
        trade_name:
          type:
            - string
            - "null"
          description: Nome de pregão — null nas fontes abertas
        sector:
          type:
            - string
            - "null"
        subsector:
          type:
            - string
            - "null"
        listing_segment:
          type:
            - string
            - "null"
        tickers:
          type: array
          items:
            type: string
        primary_ticker:
          type:
            - string
            - "null"
          description: Ticker de exibição da companhia (o mais líquido no pregão recente;
            renomeações já resolvidas). null para companhias sem papel
            negociando. `tickers` pode incluir códigos históricos.
        shares_outstanding:
          type: object
          properties:
            common:
              type:
                - number
                - "null"
            preferred:
              type:
                - number
                - "null"
            total:
              type:
                - number
                - "null"
          required:
            - common
            - preferred
            - total
          description: Capital emitido (fontes cruzadas FRE/CVM e B3)
        free_float_pct:
          type:
            - number
            - "null"
          description: "% de ações em circulação (FRE)"
        status:
          type:
            - string
            - "null"
        has_active_ticker:
          type:
            - boolean
            - "null"
      required:
        - cvm_code
        - cnpj
        - name
        - trade_name
        - sector
        - subsector
        - listing_segment
        - tickers
        - primary_ticker
        - shares_outstanding
        - free_float_pct
        - status
        - has_active_ticker
    Document:
      type: object
      properties:
        cvm_code:
          type: integer
        kind:
          $ref: "#/components/schemas/DocumentKind"
          description: Tipo estável do documento, derivado de `category`. Use-o para
            filtrar (`kind=material_fact`) em vez de casar a string em
            português, que varia com o vocabulário da fonte.
        category:
          type:
            - string
            - "null"
          description: Categoria como publicada no IPE/CVM (texto livre)
        type:
          type:
            - string
            - "null"
        subject:
          type:
            - string
            - "null"
        reference_date:
          type:
            - string
            - "null"
        filed_at:
          type:
            - string
            - "null"
        protocol:
          type:
            - string
            - "null"
        download_url:
          type:
            - string
            - "null"
      required:
        - cvm_code
        - kind
        - category
        - type
        - subject
        - reference_date
        - filed_at
        - protocol
        - download_url
    DocumentKind:
      type: string
      enum:
        - material_fact
        - market_communication
        - financial_report
        - shareholder_notice
        - shareholder_meeting
        - periodic_report
        - management_report
        - debt_instrument
        - governing_document
        - other
      description: Classificação estável. debt_instrument = escritura/termo de
        securitização/aditamento; governing_document = regulamento ou documento
        constitutivo.
    CompanyStatement:
      type: object
      properties:
        cvm_code:
          type: integer
        cnpj:
          type:
            - string
            - "null"
        company_name:
          type:
            - string
            - "null"
        ref_date:
          type: string
          description: Fim do período contábil (DT_REFER)
        filed_at:
          type:
            - string
            - "null"
          description: "1ª publicação na CVM (DT_RECEB) — use para corte point-in-time sem
            lookahead. null: data de entrega desconhecida para o período."
        scope:
          type: string
          description: con (consolidado) | ind (controladora)
        quarters_available:
          type:
            - number
            - "null"
        is_latest:
          type:
            - boolean
            - "null"
          description: Período mais recente da companhia neste escopo
        revenue_ttm:
          type:
            - number
            - "null"
        gross_profit_ttm:
          type:
            - number
            - "null"
        ebit_ttm:
          type:
            - number
            - "null"
        ebitda_ttm:
          type:
            - number
            - "null"
        net_income_ttm:
          type:
            - number
            - "null"
        ocf_ttm:
          type:
            - number
            - "null"
          description: Fluxo de caixa operacional TTM
        fcf_ttm:
          type:
            - number
            - "null"
          description: Fluxo de caixa livre TTM (OCF - capex)
        dna_ttm:
          type:
            - number
            - "null"
          description: Depreciação e amortização TTM
        capex_ttm:
          type:
            - number
            - "null"
        total_assets:
          type:
            - number
            - "null"
        current_assets:
          type:
            - number
            - "null"
        cash:
          type:
            - number
            - "null"
        st_investments:
          type:
            - number
            - "null"
        current_liabilities:
          type:
            - number
            - "null"
        noncurrent_liabilities:
          type:
            - number
            - "null"
        st_debt:
          type:
            - number
            - "null"
        lt_debt:
          type:
            - number
            - "null"
        gross_debt:
          type:
            - number
            - "null"
        net_debt:
          type:
            - number
            - "null"
        equity:
          type:
            - number
            - "null"
        working_capital:
          type:
            - number
            - "null"
        net_current_assets:
          type:
            - number
            - "null"
        invested_capital:
          type:
            - number
            - "null"
        nopat_ttm:
          type:
            - number
            - "null"
      required:
        - cvm_code
        - cnpj
        - company_name
        - ref_date
        - filed_at
        - scope
        - quarters_available
        - is_latest
        - revenue_ttm
        - gross_profit_ttm
        - ebit_ttm
        - ebitda_ttm
        - net_income_ttm
        - ocf_ttm
        - fcf_ttm
        - dna_ttm
        - capex_ttm
        - total_assets
        - current_assets
        - cash
        - st_investments
        - current_liabilities
        - noncurrent_liabilities
        - st_debt
        - lt_debt
        - gross_debt
        - net_debt
        - equity
        - working_capital
        - net_current_assets
        - invested_capital
        - nopat_ttm
    EntityLinkObservationRow:
      type: object
      properties:
        rel:
          type: string
          description: "O verbo da relação, como o servidor o publica. É `string` e não
            enum de propósito: verbo novo tem de chegar a você como valor
            desconhecido, nunca derrubando a resposta. `listObjectRelations`
            lista os vigentes com forma temporal, domínio e alcance."
        shape:
          type: string
          const: snapshot
        direction:
          type: string
          enum:
            - out
            - in
        other_id:
          type: string
        other_kind:
          type: string
          description: "O tipo do objeto, como o servidor o publica. `string` e não enum
            pelo mesmo motivo de `rel`: `offering` e `fund_share_class` nasceram
            em 19/08/2026 e um cliente que validasse contra a lista anterior
            recusaria a resposta inteira. `getObjectCensus` lista os vigentes."
        other_name:
          type:
            - string
            - "null"
        other_key_type:
          type:
            - string
            - "null"
          description: "Tipo de identificador, como o servidor o publica. `string` e não
            enum pelo mesmo motivo de `kind` e `rel`: `commodity_key`, `iso2` e
            `iso3` nasceram em 20/08/2026 e um cliente que validasse contra a
            lista anterior recusaria a resposta inteira — foi o que aconteceu.
            `getObjectCensus` lista os tipos de chave vigentes."
        other_key:
          type:
            - string
            - "null"
        source:
          type: string
        observed_at:
          type: string
          description: Competência desta magnitude publicada.
        magnitude:
          type:
            - number
            - "null"
          description: Tamanho publicado NESTA competência. Nulo não é zero.
        magnitude_unit:
          type:
            - string
            - "null"
        confidence:
          type: string
          enum:
            - high
            - low
      required:
        - rel
        - shape
        - direction
        - other_id
        - other_kind
        - other_name
        - other_key_type
        - other_key
        - source
        - observed_at
        - magnitude
        - magnitude_unit
        - confidence
    EntityEvidence:
      type: object
      properties:
        protocol:
          type: string
        source:
          type: string
          description: Conector que trouxe o documento (IPE da CVM, FNET, DFP/ITR).
        url:
          type:
            - string
            - "null"
          description: Link da fonte, como capturado. Nulo quando a fonte não publica link
            direto.
        category:
          type:
            - string
            - "null"
        type:
          type:
            - string
            - "null"
        reference_date:
          type:
            - string
            - "null"
        filed_at:
          type:
            - string
            - "null"
          description: Quando foi PROTOCOLADO. É o que vale para leitura point-in-time.
        page_start:
          type:
            - integer
            - "null"
        page_end:
          type:
            - integer
            - "null"
        heading:
          type:
            - string
            - "null"
          description: Caminho de títulos até o trecho, quando o documento tem estrutura.
        excerpt:
          type: string
          description: O TRECHO. Texto do documento, não resumo nosso.
      required:
        - protocol
        - source
        - url
        - category
        - type
        - reference_date
        - filed_at
        - page_start
        - page_end
        - heading
        - excerpt
    FactAxes:
      type:
        - object
        - "null"
      properties:
        dimension:
          type:
            - string
            - "null"
          description: "O que o número É: `currency`, `rate`, `share`, `ratio`, `index`,
            `points`, `count`, `duration`. É ela que distingue o que `unit`
            colapsa — `duration_bd` sai como `count` porque o enum de `unit` não
            tem casa para duration, e somar duration não é duration de nada."
        scale:
          type:
            - string
            - "null"
          description: "Em que escala o valor VEM: 5,5 (`percent`), 0,055 (`unit`) e 550
            (`bps`) podem ser a mesma taxa. Também `thousand`, `million`,
            `billion` e `business_days`. **O valor é servido as-filed e a escala
            diz como lê-lo — ela não foi aplicada.** `portfolio_value_kbrl` é
            `currency` em `thousand`: 508.272.696 são 508 bilhões de reais."
        period:
          type:
            - string
            - "null"
          description: "Que JANELA o número cobre — `none`, `daily`, `monthly`,
            `quarterly`, `annual`, `ttm_12m`, `cagr_3y`. **NÃO é a cadência de
            publicação**, e as duas divergem de rotina: `fii_dy_12m` cobre doze
            meses e `fii_dividend_yield_month` cobre um, as duas em percentual e
            na mesma resposta. `none` é declaração — o número é um NÍVEL, não
            cobre janela — e é diferente de nulo."
        seasonal_adjustment:
          type:
            - string
            - "null"
          description: "`nsa` sem ajuste, `sa` com, `saar` com ajuste e já anualizado pela
            fonte. Nulo em medida que não é série macro: ali o eixo não se
            aplica, e afirmar `nsa` diria que a fonte declarou ausência de
            ajuste."
        expected_range:
          type:
            - object
            - "null"
          properties:
            min:
              type: number
            max:
              type: number
          required:
            - min
            - max
          description: Faixa plausível declarada, na MESMA escala do valor. Alimenta
            `out_of_prior`.
        concept:
          type:
            - string
            - "null"
          description: "O CONCEITO compartilhado entre tipos, quando existe: `dy_12m`
            (ação, fração) e `fii_dy_12m` (FII, percentual) declaram ambos
            `dividend_yield_12m` — são a MESMA pergunta em escalas diferentes, e
            a conversão é mecânica pela `scale` declarada. Medidas com o mesmo
            conceito têm garantidamente a mesma `dimension` e o mesmo `period`;
            moeda e escala podem divergir, e estão declaradas. Nulo = medida sem
            contraparte em outro tipo, não ausência de régua."
      required:
        - dimension
        - scale
        - period
        - seasonal_adjustment
        - expected_range
        - concept
    PublicMarketEvent:
      type: object
      properties:
        event_id:
          type: number
        day:
          type: string
          description: Dia do evento (YYYY-MM-DD, BRT).
        layer:
          type: string
          description: estrutural | setorial | corporativa
        category:
          type: string
          description: macro | politica | internacional | commodities | corporate | fii |
            cripto | mercado | outros
        title:
          type: string
        summary:
          type:
            - string
            - "null"
        entities:
          type: array
          items:
            type: string
          description: Atores/empresas citados (ticker NÃO é obrigatório).
        tickers:
          type: array
          items:
            type: string
        transmission_channels:
          type: array
          items:
            type: string
          description: "Canais de transmissão a preço: dolar | ibov | brent | curva |
            selic."
        score:
          type:
            - number
            - "null"
        detectors:
          type: array
          items:
            type: string
          description: "Quais detectores confirmaram: official | press | market. 2+ =
            evento forte."
        thread_slug:
          type:
            - string
            - "null"
          description: Thread (evento-pai) quando o evento faz parte de uma história em
            curso.
        source_refs:
          type: array
          items:
            $ref: "#/components/schemas/MarketEventSourceRef"
      required:
        - event_id
        - day
        - layer
        - category
        - title
        - summary
        - entities
        - tickers
        - transmission_channels
        - score
        - detectors
        - thread_slug
        - source_refs
    MarketEventSourceRef:
      type: object
      properties:
        kind:
          type: string
          description: "Origem da evidência: dou | rss | copom | fedreg | camara | gdelt |
            price"
        id:
          anyOf:
            - type: string
            - type: number
        url:
          type:
            - string
            - "null"
        title:
          type: string
      required:
        - kind
        - id
        - url
        - title
    LiveQuote:
      type: object
      properties:
        ticker:
          type: string
        price:
          type:
            - number
            - "null"
        change_pct:
          type:
            - number
            - "null"
        ts:
          type: string
        session_date:
          type: string
        prev_close:
          type:
            - number
            - "null"
        source:
          type: string
          enum:
            - intraday
            - eod
        sessions_behind:
          type: integer
          description: Pregões entre `session_date` e a última sessão do mercado. 0 =
            preço da sessão corrente. > 0 = último negócio conhecido do papel,
            congelado — `change_pct` vem null nesse caso.
      required:
        - ticker
        - price
        - change_pct
        - ts
        - session_date
        - prev_close
        - source
        - sessions_behind
    Stock:
      type: object
      properties:
        ticker:
          type: string
        isin:
          type:
            - string
            - "null"
        type:
          oneOf:
            - $ref: "#/components/schemas/StockType"
            - type: "null"
        active:
          type: boolean
          description: "false = código fora de circulação: sucedido (ver
            `successor`/`renamed_at`) ou sem negócio há mais de um ano de
            pregões. Não confundir com `trading_status`: companhia em
            recuperação segue com o papel ATIVO."
        successor:
          type:
            - string
            - "null"
          description: O código que sucedeu este NA data de `renamed_at` — o próximo elo
            da cadeia. Para o código negociável hoje use `current_ticker`.
        current_ticker:
          type:
            - string
            - "null"
          description: O código vivo no fim da cadeia. Igual a `successor` quando a cadeia
            tem um salto só.
        renamed_at:
          type:
            - string
            - "null"
        succession_type:
          type:
            - string
            - "null"
          enum:
            - rename
            - incorporation
            - null
          description: rename = troca de código (1:1); incorporation = M&A, a posição
            converte pelo ratio
        succession_ratio:
          type:
            - number
            - "null"
        trading_status:
          type: string
          enum:
            - regular
            - sancionada
            - concordataria
            - recuperacao_extrajudicial
            - recuperacao_judicial
          description: Situação da emissora na B3. `regular` = operação normal. Um valor
            diferente NÃO significa papel suspenso — só que há processo em curso
            na companhia.
        trading_status_since:
          type:
            - string
            - "null"
          description: Início do período ATUAL da situação (AAAA-MM-DD); null quando
            `regular`. Reentrada em recuperação reporta a data da reentrada, não
            a da primeira vez.
        class_shares_outstanding:
          type:
            - number
            - "null"
          description: "Ações emitidas DESTA classe. null quando a fonte não separa: UNIT
            não é espécie própria e o capital preferencial vem agregado (uma
            companhia com PNA e PNB compartilha o mesmo total). O total da
            companhia está em company.shares_outstanding."
        company:
          type:
            - object
            - "null"
          properties:
            cvm_code:
              type:
                - integer
                - "null"
            cnpj:
              type:
                - string
                - "null"
            name:
              type:
                - string
                - "null"
            trade_name:
              type:
                - string
                - "null"
              description: Nome de pregão da B3 — a marca pela qual a companhia é conhecida
                ("P.ACUCAR-CBD", "CASAS BAHIA"), não a razão social da CVM
            sector:
              type:
                - string
                - "null"
            subsector:
              type:
                - string
                - "null"
            listing_segment:
              type:
                - string
                - "null"
            primary_ticker:
              type:
                - string
                - "null"
              description: Ticker de exibição da companhia
            tickers:
              type: array
              items:
                $ref: "#/components/schemas/StockPaper"
              description: Papéis (classes) da empresa — cada um analisável em
                /stocks/{ticker}/indicators
            shares_outstanding:
              type: object
              properties:
                common:
                  type:
                    - number
                    - "null"
                preferred:
                  type:
                    - number
                    - "null"
                total:
                  type:
                    - number
                    - "null"
              required:
                - common
                - preferred
                - total
              description: Capital emitido da COMPANHIA inteira (fontes cruzadas FRE/CVM e B3)
            free_float_pct:
              type:
                - number
                - "null"
              description: "% de ações em circulação (FRE)"
            status:
              type:
                - string
                - "null"
            has_active_ticker:
              type:
                - boolean
                - "null"
          required:
            - cvm_code
            - cnpj
            - name
            - trade_name
            - sector
            - subsector
            - listing_segment
            - primary_ticker
            - tickers
            - shares_outstanding
            - free_float_pct
            - status
            - has_active_ticker
        latest_quote:
          type: object
          properties:
            date:
              type: string
            close:
              type:
                - number
                - "null"
            adjust_type:
              type:
                - string
                - "null"
            adjust_quality:
              type:
                - string
                - "null"
            sessions_behind:
              type: integer
              description: Pregões entre `date` e a última sessão do mercado. 0 = cotação da
                sessão mais recente; > 0 = papel sem negócio desde então.
          required:
            - date
            - close
            - adjust_type
            - adjust_quality
            - sessions_behind
      required:
        - ticker
        - isin
        - type
        - active
        - successor
        - current_ticker
        - renamed_at
        - succession_type
        - succession_ratio
        - trading_status
        - trading_status_since
        - class_shares_outstanding
        - company
        - latest_quote
    StockType:
      type: string
      enum:
        - ON
        - PN
        - PNA
        - PNB
        - UNIT
    StockPaper:
      type: object
      properties:
        ticker:
          type: string
        type:
          oneOf:
            - $ref: "#/components/schemas/StockType"
            - type: "null"
        active:
          type: boolean
        successor:
          type:
            - string
            - "null"
          description: O código que sucedeu este NA data de `renamed_at` — o PRÓXIMO elo,
            não o fim da cadeia. Para o código negociável hoje use
            `current_ticker`.
        current_ticker:
          type:
            - string
            - "null"
          description: O código vivo no fim da cadeia — o que a carteira usa para valorar
            a posição hoje. Igual a `successor` quando a cadeia tem um salto só.
        renamed_at:
          type:
            - string
            - "null"
          description: Data ex da sucessão (AAAA-MM-DD)
        succession_type:
          type:
            - string
            - "null"
          enum:
            - rename
            - incorporation
            - null
          description: rename = troca de código (1:1); incorporation = M&A, a posição
            converte pelo ratio
        succession_ratio:
          type:
            - number
            - "null"
          description: Ações/cotas do sucessor por unidade do código antigo. 1 =
            renomeação pura
      required:
        - ticker
        - type
        - active
        - successor
        - current_ticker
        - renamed_at
        - succession_type
        - succession_ratio
    Quote:
      type: object
      properties:
        date:
          type: string
        open:
          type:
            - number
            - "null"
        high:
          type:
            - number
            - "null"
        low:
          type:
            - number
            - "null"
        close:
          type:
            - number
            - "null"
        close_raw:
          type:
            - number
            - "null"
          description: Fechamento bruto (não ajustado), como reportado pela B3
        close_tr:
          type:
            - number
            - "null"
          description: "Fechamento de retorno total: ajustado por eventos + proventos
            reinvestidos (bruto). = close quando não há fonte de proventos
            (units/FII)"
        volume:
          type:
            - number
            - "null"
          description: Volume financeiro (R$)
        quantity:
          type:
            - number
            - "null"
        trades:
          type:
            - number
            - "null"
          description: "Nº de negócios — null: fonte aberta dá quantidade, não contagem"
        adjusted:
          type: boolean
        adjust_type:
          type:
            - string
            - "null"
        adjust_quality:
          type:
            - string
            - "null"
      required:
        - date
        - open
        - high
        - low
        - close
        - close_raw
        - close_tr
        - volume
        - quantity
        - trades
        - adjusted
        - adjust_type
        - adjust_quality
    IntradayPoint:
      type: object
      properties:
        ts:
          type: string
        price:
          type:
            - number
            - "null"
        change_pct:
          type:
            - number
            - "null"
      required:
        - ts
        - price
        - change_pct
    IndicatorValue:
      type: object
      properties:
        name:
          type: string
        label:
          type: string
        value:
          type:
            - number
            - "null"
          description: null quando não calculável — ver reason
        unit:
          type: string
          enum:
            - ratio
            - percent
            - brl
            - brl_per_share
            - count
            - days
        reason:
          type:
            - string
            - "null"
        reference_date:
          type:
            - string
            - "null"
        ttm:
          type: boolean
        lineage:
          $ref: "#/components/schemas/Lineage"
        methodology_url:
          type: string
      required:
        - name
        - label
        - value
        - unit
        - reason
        - reference_date
        - ttm
        - lineage
        - methodology_url
    Lineage:
      type: object
      properties:
        source:
          type: string
        reference:
          type: string
        url:
          type:
            - string
            - "null"
      required:
        - source
        - reference
    Observation:
      type: object
      properties:
        date:
          type: string
        value:
          type:
            - number
            - "null"
      required:
        - date
        - value
    Dividend:
      type: object
      properties:
        type:
          type:
            - string
            - "null"
        ex_date:
          type:
            - string
            - "null"
        payment_date:
          type:
            - string
            - "null"
        value_per_share_gross:
          type:
            - number
            - "null"
        value_per_share_net:
          type:
            - number
            - "null"
      required:
        - type
        - ex_date
        - payment_date
        - value_per_share_gross
        - value_per_share_net
    CorporateEvent:
      type: object
      properties:
        ticker:
          type: string
          description: "O código sob o qual o evento foi gravado — pode ser um código
            ANTIGO do mesmo papel: a rota resolve o papel pelo grafo de
            identidade e devolve o histórico inteiro, renomeações incluídas"
        entity_id:
          type:
            - string
            - "null"
          description: O objeto do PAPEL no grafo de identidade. Nulo quando o código não
            resolveu no spine
        type:
          type:
            - string
            - "null"
        approved_date:
          type:
            - string
            - "null"
        ex_date:
          type:
            - string
            - "null"
        factor:
          type:
            - number
            - "null"
        detail:
          type:
            - string
            - "null"
      required:
        - ticker
        - entity_id
        - type
        - approved_date
        - ex_date
        - factor
        - detail
    InsiderMove:
      type: object
      properties:
        reference_month:
          type: string
          description: AAAA-MM
        net_shares:
          type:
            - number
            - "null"
        net_value_brl:
          type:
            - number
            - "null"
        buy_value_brl:
          type:
            - number
            - "null"
        sell_value_brl:
          type:
            - number
            - "null"
        corporate_event_shares:
          type:
            - number
            - "null"
          description: "NÃO SOMAR a net_shares. Saldo de eventos NÃO-mercado do mesmo mês
            — bonificação, doação, herança, grupamento/desdobramento, empréstimo
            de ações e posse/desligamento de cargo — que mudam a posição do
            insider sem nenhuma compra ou venda. Vem separado porque a magnitude
            é maior que a do fluxo: 719.464 Mi de ações em módulo contra 184.689
            Mi do net de mercado (3,9×), presente em 39,1% dos meses do acervo."
        lineage:
          $ref: "#/components/schemas/Lineage"
      required:
        - reference_month
        - net_shares
        - net_value_brl
        - buy_value_brl
        - sell_value_brl
        - corporate_event_shares
        - lineage
    FundHolding:
      type: object
      properties:
        cnpj:
          type: string
        comptc_date:
          type: string
        cd_ativo:
          type:
            - string
            - "null"
        ticker:
          type:
            - string
            - "null"
          description: cd_ativo resolvido ao nosso universo (ação/BDR); null caso contrário
        tp_aplic:
          type:
            - string
            - "null"
        ds_ativo:
          type:
            - string
            - "null"
        cd_isin:
          type:
            - string
            - "null"
        qty:
          type:
            - number
            - "null"
        market_value:
          type:
            - number
            - "null"
          description: posição a mercado (R$)
        fund_name:
          type:
            - string
            - "null"
        fund_manager:
          type:
            - string
            - "null"
        fund_classificacao:
          type:
            - string
            - "null"
      required:
        - cnpj
        - comptc_date
        - cd_ativo
        - ticker
        - tp_aplic
        - ds_ativo
        - cd_isin
        - qty
        - market_value
        - fund_name
        - fund_manager
        - fund_classificacao
    FundOwnershipSummary:
      type: object
      properties:
        comptc_date:
          type: string
          description: competência do CDA (AAAA-MM-DD)
        n_funds:
          type:
            - number
            - "null"
        total_value_brl:
          type:
            - number
            - "null"
          description: posição a mercado somada (R$)
        total_shares:
          type:
            - number
            - "null"
      required:
        - comptc_date
        - n_funds
        - total_value_brl
        - total_shares
    FundTickerMove:
      type: object
      properties:
        ticker:
          type: string
        comptc_date:
          type: string
          description: competência do CDA (AAAA-MM-DD)
        cnpj:
          type: string
          description: CNPJ da classe que se moveu
        prev_comptc_date:
          type:
            - string
            - "null"
          description: competência anterior COMPARÁVEL, não o mês calendário
        fund_name:
          type:
            - string
            - "null"
        fund_manager:
          type:
            - string
            - "null"
        qty_prev:
          type:
            - number
            - "null"
        qty_cur:
          type:
            - number
            - "null"
        qty_delta:
          type:
            - number
            - "null"
          description: Variação em QUANTIDADE, posição econômica (direta + cedida em
            empréstimo − obrigação por recebida). Negativo = posição reduzida ou
            vendida.
        value_prev_brl:
          type:
            - number
            - "null"
        value_cur_brl:
          type:
            - number
            - "null"
        move_kind:
          type:
            - string
            - "null"
          enum:
            - entrou
            - aumentou
            - reduziu
            - saiu
            - abriu_venda
            - zerou_venda
            - manteve
            - indeterminado
            - null
          description: "Classificado pelo SINAL da posição econômica, não pelo zero: a
            posição pode ser negativa (obrigação por ações recebidas > posse),
            então abrir venda é `abriu_venda` e não `entrou`. `indeterminado` =
            um dos meses declarou R$ sem quantidade, e aí o zero não significa
            ausência de posição."
        delta_reason:
          type:
            - string
            - "null"
          enum:
            - posicao_liquida_negativa
            - somente_emprestimo
            - quantidade_nao_divulgada
            - null
          description: Anotação de leitura da linha; null = posição comprada, declarada e
            com quantidade.
        lineage:
          type:
            - string
            - "null"
      required:
        - ticker
        - comptc_date
        - cnpj
        - prev_comptc_date
        - fund_name
        - fund_manager
        - qty_prev
        - qty_cur
        - qty_delta
        - value_prev_brl
        - value_cur_brl
        - move_kind
        - delta_reason
        - lineage
    TickerDemand:
      type: object
      properties:
        ticker:
          type: string
        month_ref:
          type: string
          description: AAAA-MM
        comptc_date:
          type:
            - string
            - "null"
          description: competência do CDA; null quando o mês só tem insider
        asset_kind:
          type:
            - string
            - "null"
          enum:
            - equity
            - bdr
            - null
        funds_holders:
          type:
            - number
            - "null"
          description: fundos com posição econômica POSITIVA no ativo; o vendido está em
            funds_short_holders
        funds_short_holders:
          type:
            - number
            - "null"
          description: fundos com posição econômica negativa (obrigação por ações
            recebidas > posse)
        funds_shares:
          type:
            - number
            - "null"
        funds_value_brl:
          type:
            - number
            - "null"
        funds_implied_price:
          type:
            - number
            - "null"
          description: preço implícito do próprio CDA (R$/qtd), não o fechamento
        funds_shares_delta:
          type:
            - number
            - "null"
          description: Variação de posição em QUANTIDADE, medida só no painel balanceado;
            null quando não é comparável
        funds_value_flow_brl:
          type:
            - number
            - "null"
          description: "O fluxo em R$ (Δqtd × preço implícito). O Δ do valor bruto NÃO
            serve como fluxo: em ITUB4 (04→05/2026) o bruto dá −44,2 Mi enquanto
            o fluxo é +153,2 Mi — sinal trocado."
        funds_value_price_effect_brl:
          type:
            - number
            - "null"
          description: parcela do Δ em R$ que veio só da marcação
        funds_entered:
          type:
            - number
            - "null"
          description: fundos que abriram posição COMPRADA (short entra em
            funds_opened_short)
        funds_exited:
          type:
            - number
            - "null"
          description: fundos que zeraram posição COMPRADA
        funds_increased:
          type:
            - number
            - "null"
        funds_decreased:
          type:
            - number
            - "null"
        funds_opened_short:
          type:
            - number
            - "null"
          description: fundos que abriram posição vendida
        funds_closed_short:
          type:
            - number
            - "null"
          description: fundos que zeraram posição vendida
        funds_qty_undisclosed:
          type:
            - number
            - "null"
          description: fundos cortados do delta por declararem R$ sem quantidade num dos
            meses
        funds_partial_disclosure:
          type:
            - number
            - "null"
          description: fundos cortados do painel por divulgação parcial
        funds_appeared:
          type:
            - number
            - "null"
          description: fundos cortados do delta por terem COMEÇADO a divulgar carteira —
            entrada na base, não compra
        funds_panel_n:
          type:
            - number
            - "null"
          description: fundos presentes nas DUAS competências
        funds_panel_coverage_pct:
          type:
            - number
            - "null"
          description: PERCENTUAL (0–100) do universo coberto pelo painel — 98,16
            significa 98,16%, não 0,98%. Abaixo do limiar de 90 o delta sai null
            com o motivo em funds_delta_reason. Para o TAMANHO do painel use
            funds_panel_n, que é a contagem de fundos.
        funds_delta_reason:
          type:
            - string
            - "null"
          enum:
            - fora_da_janela_cda
            - primeira_competencia
            - competencia_ausente
            - painel_incompleto
            - null
          description: por que o delta não existe; null = delta válido
        issuer_cnpj:
          type:
            - string
            - "null"
        is_issuer_anchor_ticker:
          type:
            - boolean
            - "null"
          description: true no ÚNICO ticker somável do emissor — filtre por ele antes de
            agregar insider
        insider_net_shares:
          type:
            - number
            - "null"
        insider_net_value_brl:
          type:
            - number
            - "null"
        insider_buy_value_brl:
          type:
            - number
            - "null"
        insider_sell_value_brl:
          type:
            - number
            - "null"
        insider_corporate_event_shares:
          type:
            - number
            - "null"
          description: NÃO SOMAR a insider_net_shares — eventos não-mercado (bonificação,
            herança, grupamento)
        insider_reason:
          type:
            - string
            - "null"
          enum:
            - sem_cnpj_no_cadastro
            - ativo_sem_insider
            - sem_vlmo_no_mes
            - null
          description: por que não há insider no mês; null = há dado
        lineage:
          type:
            - string
            - "null"
      required:
        - ticker
        - month_ref
        - comptc_date
        - asset_kind
        - funds_holders
        - funds_short_holders
        - funds_shares
        - funds_value_brl
        - funds_implied_price
        - funds_shares_delta
        - funds_value_flow_brl
        - funds_value_price_effect_brl
        - funds_entered
        - funds_exited
        - funds_increased
        - funds_decreased
        - funds_opened_short
        - funds_closed_short
        - funds_qty_undisclosed
        - funds_partial_disclosure
        - funds_appeared
        - funds_panel_n
        - funds_panel_coverage_pct
        - funds_delta_reason
        - issuer_cnpj
        - is_issuer_anchor_ticker
        - insider_net_shares
        - insider_net_value_brl
        - insider_buy_value_brl
        - insider_sell_value_brl
        - insider_corporate_event_shares
        - insider_reason
        - lineage
    TradeStat:
      type: object
      properties:
        ticker:
          type: string
        trade_date:
          type: string
        segment:
          type:
            - string
            - "null"
        vwap:
          type:
            - number
            - "null"
          description: preço médio de negociação (VWAP oficial B3)
        min_price:
          type:
            - number
            - "null"
        max_price:
          type:
            - number
            - "null"
        last_price:
          type:
            - number
            - "null"
        n_trades:
          type:
            - number
            - "null"
          description: nº de negócios no pregão
        quantity:
          type:
            - number
            - "null"
        financial_volume:
          type:
            - number
            - "null"
          description: volume financeiro (R$)
        lineage:
          type:
            - string
            - "null"
      required:
        - ticker
        - trade_date
        - segment
        - vwap
        - min_price
        - max_price
        - last_price
        - n_trades
        - quantity
        - financial_volume
        - lineage
    ScreenerRow:
      type: object
      properties:
        ticker:
          type: string
        name:
          type:
            - string
            - "null"
        sector:
          type:
            - string
            - "null"
        indicators:
          type: object
          properties:
            market_cap:
              type:
                - number
                - "null"
            pl:
              type:
                - number
                - "null"
            pvp:
              type:
                - number
                - "null"
            dy:
              type:
                - number
                - "null"
            payout:
              type:
                - number
                - "null"
            roe:
              type:
                - number
                - "null"
            roic:
              type:
                - number
                - "null"
            ev_ebitda:
              type:
                - number
                - "null"
            div_liq_ebitda:
              type:
                - number
                - "null"
            margem_liquida:
              type:
                - number
                - "null"
            volume_medio_2m:
              type:
                - number
                - "null"
          required:
            - market_cap
            - pl
            - pvp
            - dy
            - payout
            - roe
            - roic
            - ev_ebitda
            - div_liq_ebitda
            - margem_liquida
            - volume_medio_2m
        quality_flags:
          type: array
          items:
            $ref: "#/components/schemas/ScreenerQualityFlag"
      required:
        - ticker
        - name
        - sector
        - indicators
        - quality_flags
    ScreenerQualityFlag:
      type: object
      properties:
        code:
          type: string
          enum:
            - dy_outlier
            - pl_outlier
            - roe_outlier
            - payout_outlier
        indicator:
          type: string
        value:
          type: number
        reason:
          type: string
      required:
        - code
        - indicator
        - value
        - reason
    FiiListRow:
      type: object
      properties:
        ticker:
          type: string
        name:
          type:
            - string
            - "null"
        segment:
          type:
            - string
            - "null"
        is_paper:
          type: boolean
        reference_date:
          type:
            - string
            - "null"
        preco:
          type:
            - number
            - "null"
        dy_12m:
          type:
            - number
            - "null"
        pvp:
          type:
            - number
            - "null"
        vacancia_fisica:
          type:
            - number
            - "null"
        patrimonio_liquido:
          type:
            - number
            - "null"
      required:
        - ticker
        - name
        - segment
        - is_paper
        - reference_date
        - preco
        - dy_12m
        - pvp
        - vacancia_fisica
        - patrimonio_liquido
    FundScreenerItem:
      type: object
      properties:
        cnpj:
          type: string
        name:
          type:
            - string
            - "null"
        tipo_classe:
          type:
            - string
            - "null"
        classificacao:
          type:
            - string
            - "null"
        classificacao_anbima:
          type:
            - string
            - "null"
        manager:
          type:
            - string
            - "null"
        admin_fee_pct:
          type:
            - number
            - "null"
        situacao:
          type:
            - string
            - "null"
        last_date:
          type:
            - string
            - "null"
        last_quota:
          type:
            - number
            - "null"
        net_worth:
          type:
            - number
            - "null"
        shareholders:
          type:
            - number
            - "null"
        return_1m:
          type:
            - number
            - "null"
          description: retorno no mês em FRAÇÃO (0,0117 = 1,17%)
        return_12m:
          type:
            - number
            - "null"
          description: retorno em 12m em FRAÇÃO (0,1453 = 14,53%); null quando o fundo tem
            menos de 12m de cota
        return_ytd:
          type:
            - number
            - "null"
          description: retorno no ano em FRAÇÃO (0,082 = 8,2%)
        net_flow_accumulated:
          type:
            - number
            - "null"
          description: captação líquida (aplicações − resgates) nos últimos 12 meses do
            fundo
      required:
        - cnpj
        - name
        - tipo_classe
        - classificacao
        - classificacao_anbima
        - manager
        - admin_fee_pct
        - situacao
        - last_date
        - last_quota
        - net_worth
        - shareholders
        - return_1m
        - return_12m
        - return_ytd
        - net_flow_accumulated
    DebentureScreenerRow:
      type: object
      properties:
        code:
          type: string
        isin:
          type:
            - string
            - "null"
        issuer_name:
          type: string
        issuer_cnpj:
          type: string
        issuer_cd_cvm:
          type:
            - number
            - "null"
        issuer_primary_ticker:
          type:
            - string
            - "null"
        issuer_categ_reg:
          type:
            - string
            - "null"
        issuer_match_level:
          type:
            - string
            - "null"
        is_active:
          type: boolean
        indexer:
          type:
            - string
            - "null"
        indexer_pct:
          type:
            - number
            - "null"
        spread_pct_aa:
          type:
            - number
            - "null"
        is_incentivada:
          type:
            - boolean
            - "null"
        guarantee_type:
          type:
            - string
            - "null"
        maturity_date:
          type:
            - string
            - "null"
        years_to_maturity:
          type:
            - number
            - "null"
        face_value_current:
          type:
            - number
            - "null"
        qty_outstanding:
          type:
            - number
            - "null"
        qty_outstanding_zero_reason:
          type:
            - string
            - "null"
          description: "Motivo do zero arquivado na fonte, mesma taxonomia de Debenture:
            'not_placed' e 'fully_redeemed' mantêm 0;
            'contradicted_by_recent_trades',
            'redeemed_before_maturity_unverifiable', 'unreported_no_movement',
            'unreported_partial_movement' e 'unparseable' vêm com
            qty_outstanding null."
        listed_b3:
          type:
            - boolean
            - "null"
        last_trade_date:
          type:
            - string
            - "null"
        last_pu_avg:
          type:
            - number
            - "null"
        last_pct_of_curve:
          type:
            - number
            - "null"
        trades_30d:
          type:
            - number
            - "null"
        qty_traded_30d:
          type:
            - number
            - "null"
        volume_brl_30d:
          type:
            - number
            - "null"
        trades_90d:
          type:
            - number
            - "null"
        volume_brl_90d:
          type:
            - number
            - "null"
        fund_holders:
          type:
            - number
            - "null"
        funds_value_brl:
          type:
            - number
            - "null"
      required:
        - code
        - isin
        - issuer_name
        - issuer_cnpj
        - issuer_cd_cvm
        - issuer_primary_ticker
        - issuer_categ_reg
        - issuer_match_level
        - is_active
        - indexer
        - indexer_pct
        - spread_pct_aa
        - is_incentivada
        - guarantee_type
        - maturity_date
        - years_to_maturity
        - face_value_current
        - qty_outstanding
        - qty_outstanding_zero_reason
        - listed_b3
        - last_trade_date
        - last_pu_avg
        - last_pct_of_curve
        - trades_30d
        - qty_traded_30d
        - volume_brl_30d
        - trades_90d
        - volume_brl_90d
        - fund_holders
        - funds_value_brl
    MacroGear:
      type: object
      properties:
        gear:
          type: string
        indicators:
          type: array
          items:
            $ref: "#/components/schemas/RegimeSignal"
      required:
        - gear
        - indicators
    RegimeSignal:
      type: object
      properties:
        name:
          type: string
        value:
          type:
            - number
            - "null"
        direction:
          type:
            - string
            - "null"
          enum:
            - up
            - down
            - flat
            - null
        lineage:
          $ref: "#/components/schemas/Lineage"
        unit:
          type:
            - string
            - "null"
          description: Rótulo de unidade como o mart escreveu, para exibição — são 33
            grafias distintas em 72 indicadores. Para decidir comparabilidade
            use `dimension`/`scale`/`period`.
        label:
          type:
            - string
            - "null"
        date:
          type:
            - string
            - "null"
        dimension:
          type:
            - string
            - "null"
          enum:
            - rate
            - index
            - currency
            - share
            - ratio
            - score
            - ordinal
            - null
          description: O que o número É, no MESMO vocabulário de `SeriesMeta` — é isso que
            torna esta rota e `getSeries` comparáveis. `score` é o par de
            regime, em [-1,1]; `ordinal` é o quadrante 1–4, que não se soma nem
            se tira média.
        scale:
          type:
            - string
            - "null"
          enum:
            - unit
            - thousand
            - million
            - billion
            - percent
            - bps
            - null
          description: "Quase tudo aqui é `unit` (decimal: 0,5576 e não 55,76) porque esta
            é a camada derivada. A série CRUA equivalente sai em `getSeries` com
            `scale: percent` — o mesmo número em escalas 100× diferentes, e
            antes deste campo nada dizia isso."
        period:
          type:
            - string
            - "null"
          enum:
            - none
            - monthly
            - quarterly
            - annual
            - ttm_12m
            - null
          description: Que janela o número cobre. `ipca_mensal` e `ipca_12m` são o mesmo
            indicador em janelas diferentes.
        expected_range:
          type:
            - object
            - "null"
          properties:
            min:
              type: number
            max:
              type: number
          required:
            - min
            - max
          description: Faixa plausível declarada, na escala deste campo. Guarda-corpo
            contra erro dimensional, não previsão.
        prior_note:
          type:
            - string
            - "null"
        entity_id:
          type:
            - string
            - "null"
          description: 'O ENDEREÇO deste indicador no grafo. Esta rota é um SNAPSHOT por
            seção: ela diz quanto é hoje e não tem eixo de tempo por indicador.
            Com o id, `getObjectHistory` devolve a série inteira e `rankObjects`
            ordena a coorte — sem precisar resolver o nome de novo, que é onde
            se perde o objeto certo (existem `ipca_mensal` e `ipca_12m`, e
            buscar "IPCA" não diz qual dos dois).'
      required:
        - name
        - value
        - direction
        - lineage
    RegimeSnapshot:
      type: object
      properties:
        as_of:
          type:
            - string
            - "null"
        quadrant:
          type:
            - string
            - "null"
          enum:
            - growth_up_inflation_up
            - growth_up_inflation_down
            - growth_down_inflation_up
            - growth_down_inflation_down
            - null
        growth:
          type: object
          properties:
            direction:
              type:
                - string
                - "null"
              enum:
                - up
                - down
                - flat
                - null
            signals:
              type: array
              items:
                $ref: "#/components/schemas/RegimeSignal"
          required:
            - direction
            - signals
        inflation:
          type: object
          properties:
            direction:
              type:
                - string
                - "null"
              enum:
                - up
                - down
                - flat
                - null
            signals:
              type: array
              items:
                $ref: "#/components/schemas/RegimeSignal"
          required:
            - direction
            - signals
        cross_asset:
          type: object
          properties:
            dy_vs_selic_spread:
              type:
                - number
                - "null"
            equity_risk_premium:
              type:
                - number
                - "null"
            real_selic:
              type:
                - number
                - "null"
          required:
            - dy_vs_selic_spread
            - equity_risk_premium
            - real_selic
      required:
        - as_of
        - quadrant
        - growth
        - inflation
        - cross_asset
    Expectation:
      type: object
      properties:
        survey_date:
          type: string
        reference:
          type: string
        median:
          type:
            - number
            - "null"
        mean:
          type:
            - number
            - "null"
        std_dev:
          type:
            - number
            - "null"
        respondents:
          type:
            - number
            - "null"
        base:
          type:
            - number
            - "null"
      required:
        - survey_date
        - reference
        - median
        - mean
        - std_dev
        - respondents
        - base
    YieldCurvePoint:
      type: object
      properties:
        maturity:
          type: string
        years:
          type:
            - number
            - "null"
        rate:
          type: number
      required:
        - maturity
        - years
        - rate
    BondCurvePoint:
      type: object
      properties:
        tenor_days:
          type: number
        tenor_years:
          type:
            - number
            - "null"
        rate_pct_aa:
          type:
            - number
            - "null"
      required:
        - tenor_days
        - tenor_years
        - rate_pct_aa
    TesouroAuction:
      type: object
      properties:
        auction_date:
          type: string
        bond_type:
          type: string
        maturity_date:
          type:
            - string
            - "null"
        offered_qty:
          type:
            - number
            - "null"
        accepted_qty:
          type:
            - number
            - "null"
        cut_rate_pct:
          type:
            - number
            - "null"
        avg_rate_pct:
          type:
            - number
            - "null"
        financial_brl:
          type:
            - number
            - "null"
        second_round_qty:
          type:
            - number
            - "null"
        bid_to_cover:
          type:
            - number
            - "null"
      required:
        - auction_date
        - bond_type
        - maturity_date
        - offered_qty
        - accepted_qty
        - cut_rate_pct
        - avg_rate_pct
        - financial_brl
        - second_round_qty
        - bid_to_cover
    TesouroBondQuote:
      type: object
      properties:
        type:
          type:
            - string
            - "null"
        entity_id:
          type:
            - string
            - "null"
          description: "O objeto do TÍTULO no grafo de identidade (tipo + vencimento, ex.:
            NTN-B 2035) — não o da família. Nulo quando o título ainda não
            resolveu no spine"
        name:
          type:
            - string
            - "null"
        maturity:
          type:
            - string
            - "null"
        date:
          type:
            - string
            - "null"
        buy_rate:
          type:
            - number
            - "null"
        sell_rate:
          type:
            - number
            - "null"
        buy_price:
          type:
            - number
            - "null"
        sell_price:
          type:
            - number
            - "null"
        is_negotiable:
          type:
            - boolean
            - "null"
        coupon:
          type:
            - string
            - "null"
          enum:
            - unique
            - semiannual
            - monthly
            - null
          description: "Fluxo CONTRATUAL, inclusive no histórico: `unique` distingue
            NTN-B-Principal (`Tesouro IPCA+`) de `semiannual` na NTN-B (`com
            Juros Semestrais`)."
        min_investment:
          type:
            - number
            - "null"
        live_buy_rate:
          type:
            - number
            - "null"
        live_sell_rate:
          type:
            - number
            - "null"
        live_buy_price:
          type:
            - number
            - "null"
        live_sell_price:
          type:
            - number
            - "null"
        priced_at:
          type:
            - string
            - "null"
      required:
        - type
        - entity_id
        - name
        - maturity
        - date
        - buy_rate
        - sell_rate
        - buy_price
        - sell_price
        - is_negotiable
        - coupon
        - min_investment
        - live_buy_rate
        - live_sell_rate
        - live_buy_price
        - live_sell_price
        - priced_at
    Commodity:
      type: object
      properties:
        commodity:
          type: string
        product_name:
          type:
            - string
            - "null"
        root:
          type: string
        reference_owner:
          type:
            - string
            - "null"
        contracts_open:
          type:
            - number
            - "null"
        first_trade_date:
          type:
            - string
            - "null"
        last_trade_date:
          type:
            - string
            - "null"
      required:
        - commodity
        - product_name
        - root
        - reference_owner
        - contracts_open
        - first_trade_date
        - last_trade_date
    CommodityCurvePoint:
      type: object
      properties:
        contract_code:
          type: string
        expiry_date:
          type:
            - string
            - "null"
        settlement_price:
          type:
            - number
            - "null"
        contracts:
          type:
            - number
            - "null"
      required:
        - contract_code
        - expiry_date
        - settlement_price
        - contracts
    CommoditySettlement:
      type: object
      properties:
        trade_date:
          type: string
        contract_code:
          type: string
        expiry_date:
          type:
            - string
            - "null"
        settlement_price:
          type:
            - number
            - "null"
        prev_settlement_price:
          type:
            - number
            - "null"
        close_price:
          type:
            - number
            - "null"
        contracts:
          type:
            - number
            - "null"
        trades:
          type:
            - number
            - "null"
        financial_volume:
          type:
            - number
            - "null"
      required:
        - trade_date
        - contract_code
        - expiry_date
        - settlement_price
        - prev_settlement_price
        - close_price
        - contracts
        - trades
        - financial_volume
    MineralCommodity:
      type: object
      properties:
        mineral:
          type: string
          description: "Chave estável do mineral, ex.: lithium, niobium_columbium."
        label:
          type: string
          description: "Nome em português, ex.: Lítio, Nióbio (colúmbio)."
        commodity_usgs:
          type:
            - string
            - "null"
          description: Nome da commodity como o USGS publica.
        group:
          type:
            - string
            - "null"
          description: "Agrupamento editorial: Metais básicos, Metais preciosos, Metais
            ferrosos e ligas, Terras raras e críticos, Minerais industriais,
            Fertilizantes, Materiais de construção, Energia."
        is_critical:
          type: boolean
          description: Consta da lista de minerais críticos do USGS (relevância
            estratégica).
        first_year:
          type:
            - number
            - "null"
        last_year:
          type:
            - number
            - "null"
        countries:
          type: number
          description: Países com pelo menos uma observação da commodity.
        has_production:
          type: boolean
        has_reserves:
          type: boolean
        units:
          type:
            - string
            - "null"
          description: Unidades observadas, separadas por ' | ' quando há mais de uma.
        countries_withheld:
          type: number
          description: Maior número de países com valor retido pelo USGS em algum recorte
            da commodity. Acima de zero, os rankings dela estão incompletos e os
            shares não somam 100%.
        top_producer:
          type:
            - string
            - "null"
          description: Maior produtor no ano mais recente, entre os países com share
            calculável.
        top_producer_share_pct:
          type:
            - number
            - "null"
          description: Participação do maior produtor. Nulo quando o share não é
            calculável para nenhum país da commodity (ver `share_basis` no
            ranking).
        edition:
          type:
            - string
            - "null"
          description: "Edição da publicação, ex.: MCS2026."
        edition_year:
          type:
            - number
            - "null"
      required:
        - mineral
        - label
        - commodity_usgs
        - group
        - is_critical
        - first_year
        - last_year
        - countries
        - has_production
        - has_reserves
        - units
        - countries_withheld
        - top_producer
        - top_producer_share_pct
        - edition
        - edition_year
    MineralObservation:
      type: object
      properties:
        mineral:
          type: string
        label:
          type:
            - string
            - "null"
        group:
          type:
            - string
            - "null"
        country:
          type: string
        country_iso3:
          type:
            - string
            - "null"
        country_label:
          type: string
        is_world_total:
          type: boolean
          description: Linha do total mundial, não de um país.
        statistic:
          type: string
        statistic_detail:
          type:
            - string
            - "null"
        statistic_detail_base:
          type:
            - string
            - "null"
        unit:
          type:
            - string
            - "null"
        year:
          type: number
        value:
          type:
            - number
            - "null"
        value_raw:
          type:
            - string
            - "null"
        value_flag:
          type:
            - string
            - "null"
        value_flag_reason:
          type:
            - string
            - "null"
        world_total:
          type:
            - number
            - "null"
        world_total_flag:
          type:
            - string
            - "null"
        countries_withheld:
          type: number
        is_estimate:
          type: boolean
        is_critical:
          type: boolean
        share_of_world_pct:
          type:
            - number
            - "null"
          description: Nulo quando não calculável, com motivo.
        share_basis:
          type: string
          description: "Por que o share existe ou não existe. Sempre preenchido, mesmo
            quando o share é nulo. `ok`: share calculado contra o total mundial
            da MESMA base e unidade. `incomparable_basis`: existe total mundial
            para a commodity, mas os países reportam bases diferentes e não
            somáveis. É o caso das reservas de boro (Turquia em boratos
            refinados, Chile em ulexita, China em óxido bórico equivalente); o
            próprio USGS marca o total como `XX`. Dividir um país por esse total
            produz o tipo de número redondo e falso que circula por aí.
            `world_total_not_calculable`: o USGS publica o total como `XX`, ou
            seja, declara-o incalculável. `no_world_total`: não há total mundial
            publicado para o recorte (comum nas séries só domésticas dos EUA,
            que a publicação traz com mais anos que a tabela mundial).
            `is_world_total`: a própria linha é o total mundial, então não há
            share de si mesma. `value_<flag>` (`value_na`, `value_w`, `value_s`,
            `value_large`, ...): o país não tem valor numérico; a sentinela
            publicada está em `value_raw` e o motivo em `value_flag_reason`."
        notes:
          type:
            - string
            - "null"
        revision:
          type:
            - string
            - "null"
        edition:
          type: string
        edition_year:
          type: number
      required:
        - mineral
        - label
        - group
        - country
        - country_iso3
        - country_label
        - is_world_total
        - statistic
        - statistic_detail
        - statistic_detail_base
        - unit
        - year
        - value
        - value_raw
        - value_flag
        - value_flag_reason
        - world_total
        - world_total_flag
        - countries_withheld
        - is_estimate
        - is_critical
        - share_of_world_pct
        - share_basis
        - notes
        - revision
        - edition
        - edition_year
    MineralProducer:
      type: object
      properties:
        rank:
          type:
            - number
            - "null"
          description: "Posição por valor, do maior para o menor. Nulo quando a linha não
            entra em ranking: país sem valor numérico (sentinela) ou linha
            agregada ('Outros países')."
        country:
          type: string
          description: País como o USGS nomeia.
        country_iso3:
          type:
            - string
            - "null"
          description: ISO 3166-1 alfa-3. Nulo em linhas agregadas como 'Outros países'.
        country_label:
          type: string
          description: País em português.
        is_aggregate:
          type: boolean
          description: Linha agregada ('Outros países'), não um país. Some do ranking
            numerado.
        value:
          type:
            - number
            - "null"
          description: Quantidade na unidade de `unit`. Nulo quando o USGS publicou
            sentinela.
        value_raw:
          type:
            - string
            - "null"
          description: O texto exatamente como o USGS publicou, inclusive sentinela ('W',
            'NA', '—'). É o que preserva o motivo quando `value` é nulo.
        value_flag:
          type:
            - string
            - "null"
          description: "Sentinela isolada: W, NA, s, XX, ..."
        value_flag_reason:
          type:
            - string
            - "null"
          description: Leitura da sentinela, em português.
        unit:
          type:
            - string
            - "null"
        share_of_world_pct:
          type:
            - number
            - "null"
          description: Participação no total mundial, em %. NULO quando não é calculável,
            e nesse caso `share_basis` diz o motivo. Nunca zero por ausência.
        share_basis:
          type: string
          description: "Por que o share existe ou não existe. Sempre preenchido, mesmo
            quando o share é nulo. `ok`: share calculado contra o total mundial
            da MESMA base e unidade. `incomparable_basis`: existe total mundial
            para a commodity, mas os países reportam bases diferentes e não
            somáveis. É o caso das reservas de boro (Turquia em boratos
            refinados, Chile em ulexita, China em óxido bórico equivalente); o
            próprio USGS marca o total como `XX`. Dividir um país por esse total
            produz o tipo de número redondo e falso que circula por aí.
            `world_total_not_calculable`: o USGS publica o total como `XX`, ou
            seja, declara-o incalculável. `no_world_total`: não há total mundial
            publicado para o recorte (comum nas séries só domésticas dos EUA,
            que a publicação traz com mais anos que a tabela mundial).
            `is_world_total`: a própria linha é o total mundial, então não há
            share de si mesma. `value_<flag>` (`value_na`, `value_w`, `value_s`,
            `value_large`, ...): o país não tem valor numérico; a sentinela
            publicada está em `value_raw` e o motivo em `value_flag_reason`."
        statistic_detail:
          type:
            - string
            - "null"
          description: Detalhe do agregado exatamente como publicado.
        statistic_detail_base:
          type:
            - string
            - "null"
          description: Detalhe normalizado. É a chave que torna dois países comparáveis.
        is_estimate:
          type: boolean
          description: Valor estimado pelo USGS (vale para o ano mais recente).
        notes:
          type:
            - string
            - "null"
      required:
        - rank
        - country
        - country_iso3
        - country_label
        - is_aggregate
        - value
        - value_raw
        - value_flag
        - value_flag_reason
        - unit
        - share_of_world_pct
        - share_basis
        - statistic_detail
        - statistic_detail_base
        - is_estimate
        - notes
    MineralDetailOption:
      type: object
      properties:
        detail:
          type: string
          description: "Base do agregado, ex.: Mine production, Refinery production."
        countries:
          type: number
        unit:
          type:
            - string
            - "null"
        world_total:
          type:
            - number
            - "null"
        world_total_flag:
          type:
            - string
            - "null"
      required:
        - detail
        - countries
        - unit
        - world_total
        - world_total_flag
    Debenture:
      type: object
      properties:
        code:
          type: string
        isin:
          type:
            - string
            - "null"
        issuer_name:
          type: string
        issuer_cnpj:
          type: string
        issuer_cd_cvm:
          type:
            - number
            - "null"
        issuer_primary_ticker:
          type:
            - string
            - "null"
        issuer_categ_reg:
          type:
            - string
            - "null"
        issuer_sector:
          type:
            - string
            - "null"
          description: Setor oficial da CVM do emissor; null quando o emissor não tem
            registro.
        issuer_match_level:
          type: string
          description: "Nunca nulo: 'exact' (CNPJ de 14 dígitos casou),
            'none_cnpj_root_only' (só a raiz de 8 dígitos casa — SPE de grupo
            registrado; o balanço da holding NÃO é atribuído a ela) ou
            'none_issuer_not_in_cvm_registry'. Os dois 'none_*' são limite da
            FONTE, não lacuna de ingest: ~48% das emissões de 2021+ vêm de
            emissor sem registro na CVM, e sem registro não há setor,
            demonstração nem documento."
        situation:
          type:
            - string
            - "null"
        is_active:
          type: boolean
        issue_date:
          type:
            - string
            - "null"
        maturity_date:
          type:
            - string
            - "null"
        effective_maturity_date:
          type:
            - string
            - "null"
        maturity_is_sentinel:
          type:
            - boolean
            - "null"
        term_years_implausible:
          type:
            - boolean
            - "null"
          description: Prazo acima de 40 anos que NÃO é sentinela (concessão longa, p.ex.)
            — plausível, mas verifique.
        exit_date:
          type:
            - string
            - "null"
        exit_reason:
          type:
            - string
            - "null"
        cvm_registration_raw:
          type:
            - string
            - "null"
        registration_regime:
          type:
            - string
            - "null"
          description: "Regime de registro normalizado: rito_automatico_rcvm_160 |
            dispensa_icvm_476 | registro_ordinario | dispensa_outra | pendente |
            outro. As dispensadas pela ICVM 476 não constam do registro de
            ofertas (listOfferings)."
        indexer:
          type:
            - string
            - "null"
        indexer_pct:
          type:
            - number
            - "null"
        spread_pct_aa:
          type:
            - number
            - "null"
        spread_pct_aa_as_filed:
          type:
            - number
            - "null"
          description: Campo moderno de taxa do SND, cru. Pode ser 0 como placeholder —
            não use para média.
        spread_pct_aa_source:
          type:
            - string
            - "null"
          description: "De onde a taxa veio: 'juros_novo' | 'juros_antigo_parsed'."
        spread_day_count:
          type:
            - number
            - "null"
          description: Base de contagem da taxa (252 | 360 | 365). Taxas em bases
            diferentes não são comparáveis.
        spread_absent_reason:
          type:
            - string
            - "null"
          description: "Motivo da taxa ausente: 'not_filed' | 'no_index_no_rate' |
            'unparseable_legacy' | 'filed_zero_unverifiable'. Preenchido se e
            somente se spread_pct_aa é null."
        is_incentivada:
          type:
            - boolean
            - "null"
        guarantee_type:
          type:
            - string
            - "null"
        debenture_class:
          type:
            - string
            - "null"
        face_value_issue:
          type:
            - number
            - "null"
        face_value_current:
          type:
            - number
            - "null"
        face_value_date:
          type:
            - string
            - "null"
        qty_issued:
          type:
            - number
            - "null"
        qty_issued_zero_reason:
          type:
            - string
            - "null"
          description: "Só quando qty_issued = 0 e há valor nominal: 'not_placed' (série
            registrada e não colocada) | 'unreported'. O volume da emissão é
            face_value_issue x qty_issued, então sem isto a emissão parece valer
            R$ 0."
        qty_outstanding:
          type:
            - number
            - "null"
        qty_outstanding_as_filed:
          type:
            - number
            - "null"
          description: Quantidade em mercado exatamente como o SND arquiva, inclusive
            quando é 0. Serve para auditar a diferença contra qty_outstanding;
            para cálculo, use qty_outstanding.
        qty_outstanding_zero_reason:
          type:
            - string
            - "null"
          description: "Só quando a fonte arquiva 0 em mercado. O campo do SND tem largura
            fixa ('000000000000'), então zero e 'não informado' são a mesma
            string — este motivo é o que separa os dois. 'not_placed' (nada
            emitido) e 'fully_redeemed' (baixas fecham com o emitido, papel
            vencido e sem negócio recente) são zeros verificáveis e mantêm
            qty_outstanding = 0. 'contradicted_by_recent_trades' (negociou nos
            últimos 90 dias, logo não pode ter 0 em circulação),
            'redeemed_before_maturity_unverifiable' (baixas fecham mas a emissão
            segue registrada com vencimento à frente), 'unreported_no_movement'
            (nenhuma baixa arquivada), 'unreported_partial_movement' (baixas
            menores que o emitido) e 'unparseable' vêm com qty_outstanding null:
            não trate como zero."
        early_redemption:
          type:
            - boolean
            - "null"
        fiduciary_agent:
          type:
            - string
            - "null"
          description: "Agente fiduciário da emissão, as-filed. É quem representa os
            debenturistas e assina os relatórios periódicos sobre a emissão,
            então é por ele que se chega ao documento quando a escritura está
            arquivada sob o CNPJ do emissor e não sob o código do papel. Texto
            livre da fonte: a mesma casa aparece com grafias diferentes, e nós
            não canonizamos para não inventar entidade que a fonte não declara."
        mandate_bank:
          type:
            - string
            - "null"
          description: Banco mandatário (liquidação e pagamento da emissão), as-filed.
            Texto livre.
        lead_coordinator:
          type:
            - string
            - "null"
          description: "Coordenador líder da distribuição, as-filed. Texto livre, e não
            confunda com o `nome_lider` de `listOfferings`: aqui é o líder
            declarado no cadastro da emissão, lá é o da oferta registrada, e os
            dois divergem quando a emissão foi redistribuída."
        listed_b3:
          type:
            - boolean
            - "null"
        last_trade_date:
          type:
            - string
            - "null"
        last_pu_avg:
          type:
            - number
            - "null"
        last_pct_of_curve:
          type:
            - number
            - "null"
      required:
        - code
        - isin
        - issuer_name
        - issuer_cnpj
        - issuer_cd_cvm
        - issuer_primary_ticker
        - issuer_categ_reg
        - issuer_sector
        - issuer_match_level
        - situation
        - is_active
        - issue_date
        - maturity_date
        - effective_maturity_date
        - maturity_is_sentinel
        - term_years_implausible
        - exit_date
        - exit_reason
        - cvm_registration_raw
        - registration_regime
        - indexer
        - indexer_pct
        - spread_pct_aa
        - spread_pct_aa_as_filed
        - spread_pct_aa_source
        - spread_day_count
        - spread_absent_reason
        - is_incentivada
        - guarantee_type
        - debenture_class
        - face_value_issue
        - face_value_current
        - face_value_date
        - qty_issued
        - qty_issued_zero_reason
        - qty_outstanding
        - qty_outstanding_as_filed
        - qty_outstanding_zero_reason
        - early_redemption
        - fiduciary_agent
        - mandate_bank
        - lead_coordinator
        - listed_b3
        - last_trade_date
        - last_pu_avg
        - last_pct_of_curve
    DebentureQuote:
      type: object
      properties:
        trade_date:
          type: string
        code:
          type: string
        isin:
          type:
            - string
            - "null"
        quantity:
          type:
            - number
            - "null"
        trade_count:
          type:
            - number
            - "null"
        pu_min:
          type:
            - number
            - "null"
        pu_avg:
          type:
            - number
            - "null"
        pu_max:
          type:
            - number
            - "null"
        pct_of_curve:
          type:
            - number
            - "null"
      required:
        - trade_date
        - code
        - isin
        - quantity
        - trade_count
        - pu_min
        - pu_avg
        - pu_max
        - pct_of_curve
    DebentureHolder:
      type: object
      properties:
        comptc_date:
          type: string
        code:
          type: string
        fund_cnpj:
          type: string
        fund_name:
          type:
            - string
            - "null"
        qty:
          type:
            - number
            - "null"
        value_brl:
          type:
            - number
            - "null"
        cost_brl:
          type:
            - number
            - "null"
        buy_value_brl:
          type:
            - number
            - "null"
        sell_value_brl:
          type:
            - number
            - "null"
      required:
        - comptc_date
        - code
        - fund_cnpj
        - fund_name
        - qty
        - value_brl
        - cost_brl
        - buy_value_brl
        - sell_value_brl
    DebentureDemandPoint:
      type: object
      properties:
        comptc_date:
          type: string
        code:
          type: string
        funds_holders:
          type:
            - number
            - "null"
        funds_qty:
          type:
            - number
            - "null"
        funds_value_brl:
          type:
            - number
            - "null"
        buy_value_brl:
          type:
            - number
            - "null"
        sell_value_brl:
          type:
            - number
            - "null"
        net_traded_brl:
          type:
            - number
            - "null"
        funds_panel_n:
          type: number
      required:
        - comptc_date
        - code
        - funds_holders
        - funds_qty
        - funds_value_brl
        - buy_value_brl
        - sell_value_brl
        - net_traded_brl
        - funds_panel_n
    CreditOtcQuote:
      type: object
      properties:
        trade_date:
          type: string
        instrument_code:
          type: string
        family:
          type: string
        isin:
          type:
            - string
            - "null"
        issuer_name:
          type:
            - string
            - "null"
        quantity:
          type:
            - number
            - "null"
        trade_count:
          type:
            - number
            - "null"
        price_min:
          type:
            - number
            - "null"
        price_avg:
          type:
            - number
            - "null"
        price_max:
          type:
            - number
            - "null"
        price_last:
          type:
            - number
            - "null"
        volume_brl:
          type:
            - number
            - "null"
      required:
        - trade_date
        - instrument_code
        - family
        - isin
        - issuer_name
        - quantity
        - trade_count
        - price_min
        - price_avg
        - price_max
        - price_last
        - volume_brl
    CreditCurvePoint:
      type: object
      properties:
        bucket:
          type: string
          enum:
            - 0-2
            - 2-4
            - 4-7
            - 7+
        min_years:
          type: number
        max_years:
          type:
            - number
            - "null"
        spread_median_pct_aa:
          type:
            - number
            - "null"
        pct_of_curve_median:
          type:
            - number
            - "null"
        n_papers:
          type: number
        volume_brl:
          type:
            - number
            - "null"
      required:
        - bucket
        - min_years
        - max_years
        - spread_median_pct_aa
        - pct_of_curve_median
        - n_papers
        - volume_brl
    IssuerRiskPoint:
      type: object
      properties:
        quarter:
          type: string
        cnpj_root:
          type:
            - string
            - "null"
        institution_name:
          type:
            - string
            - "null"
        basileia:
          type:
            - number
            - "null"
        capital_pr:
          type:
            - number
            - "null"
        total_assets:
          type:
            - number
            - "null"
        equity:
          type:
            - number
            - "null"
        net_income:
          type:
            - number
            - "null"
        credit_portfolio:
          type:
            - number
            - "null"
      required:
        - quarter
        - cnpj_root
        - institution_name
        - basileia
        - capital_pr
        - total_assets
        - equity
        - net_income
        - credit_portfolio
    IssuerProfile:
      type: object
      properties:
        cnpj:
          type: string
        issuer_name:
          type: string
        kind:
          type: string
          enum:
            - bank
            - corporate
            - unknown
        cd_cvm:
          type:
            - number
            - "null"
        primary_ticker:
          type:
            - string
            - "null"
        categ_reg:
          type:
            - string
            - "null"
        match_level:
          type:
            - string
            - "null"
        has_bank_risk:
          type: boolean
        debentures_total:
          type: number
        debentures_active:
          type: number
        last_trade_date:
          type:
            - string
            - "null"
      required:
        - cnpj
        - issuer_name
        - kind
        - cd_cvm
        - primary_ticker
        - categ_reg
        - match_level
        - has_bank_risk
        - debentures_total
        - debentures_active
        - last_trade_date
    CreditRatingAction:
      type: object
      properties:
        entity_type:
          type: string
        issuer_cnpj:
          type:
            - string
            - "null"
        asset_code:
          type:
            - string
            - "null"
        agency:
          type: string
        agency_raw:
          type:
            - string
            - "null"
        scale:
          type: string
          enum:
            - national_br
            - global
        rating_raw:
          type: string
        rating:
          type:
            - string
            - "null"
        rating_notch:
          type:
            - number
            - "null"
        outlook:
          type:
            - string
            - "null"
        watch:
          type:
            - string
            - "null"
        action:
          type:
            - string
            - "null"
        action_date:
          type:
            - string
            - "null"
        doc_filed_at:
          type:
            - string
            - "null"
        action_date_known:
          type: boolean
        source:
          type: string
        protocol:
          type: string
        download_url:
          type:
            - string
            - "null"
        page_start:
          type:
            - number
            - "null"
        evidence_quote:
          type:
            - string
            - "null"
        extraction_confidence:
          type:
            - string
            - "null"
        extracted_at:
          type: string
        declared:
          oneOf:
            - $ref: "#/components/schemas/CreditRatingDeclared"
            - type: "null"
      required:
        - entity_type
        - issuer_cnpj
        - asset_code
        - agency
        - agency_raw
        - scale
        - rating_raw
        - rating
        - rating_notch
        - outlook
        - watch
        - action
        - action_date
        - doc_filed_at
        - action_date_known
        - source
        - protocol
        - download_url
        - page_start
        - evidence_quote
        - extraction_confidence
        - extracted_at
        - declared
    CreditRatingDeclared:
      type: object
      properties:
        rating_raw:
          type:
            - string
            - "null"
        agency_raw:
          type:
            - string
            - "null"
        reference_date:
          type: string
        is_parseable:
          type:
            - boolean
            - "null"
      required:
        - rating_raw
        - agency_raw
        - reference_date
        - is_parseable
    CreditRatingHistoryRow:
      allOf:
        - $ref: "#/components/schemas/CreditRatingAction"
      properties:
        superseded_by:
          type:
            - number
            - "null"
      required:
        - superseded_by
    FidcMonthly:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        entity_kind:
          type:
            - string
            - "null"
        name:
          type:
            - string
            - "null"
        net_worth:
          type:
            - number
            - "null"
        net_worth_avg:
          type:
            - number
            - "null"
        total_assets:
          type:
            - number
            - "null"
        cash:
          type:
            - number
            - "null"
        portfolio:
          type:
            - number
            - "null"
        receivables_with_risk:
          type:
            - number
            - "null"
        overdue_with_risk:
          type:
            - number
            - "null"
        impaired_with_risk:
          type:
            - number
            - "null"
        overdue_no_risk:
          type:
            - number
            - "null"
        impaired_no_risk:
          type:
            - number
            - "null"
        impaired_total:
          type:
            - number
            - "null"
        delinquency_scope:
          type:
            - string
            - "null"
          enum:
            - with_risk_only
            - both
            - null
        impaired_ratio:
          type:
            - number
            - "null"
          description: "FRAÇÃO da carteira, não percentual: 0,05 = 5%, e o máximo possível
            é 1. Não confunda com `top_originator_pct`, que na MESMA linha vem
            em percentual (35,2 = 35,2%). Não é fração do patrimônio. Nulo tem
            duas causas — veja `impaired_ratio_absent_reason`."
        overdue_ratio:
          type:
            - number
            - "null"
          description: FRAÇÃO da carteira (0,05 = 5%), mesma escala de `impaired_ratio`.
        impaired_exceeds_portfolio:
          type:
            - boolean
            - "null"
          description: true = os dois números são as-filed e corretos, mas não estão na
            mesma base (carteira baixada com inadimplente ainda declarado). A
            razão vem nula de propósito.
        impaired_ratio_absent_reason:
          type:
            - string
            - "null"
          description: >-
            POR QUE `impaired_ratio` veio nulo. `impaired_exceeds_portfolio`:
            inadimplente declarado maior que a carteira. `portfolio_zero`:
            carteira igual a zero — divisão por zero, típico de fundo em
            liquidação ou que baixou o estoque. `portfolio_missing`: a
            competência não traz a carteira.


            Medido na competência mais recente: 309 nulos, 125 do primeiro
            motivo e 184 do segundo. Sem este campo os 184 se leem como "o fundo
            não informou", e carteira zerada é justamente o que se quer saber.
        portfolio_to_net_worth:
          type:
            - number
            - "null"
        top_originator_doc:
          type:
            - string
            - "null"
        top_originator_pct:
          type:
            - number
            - "null"
          description: "PERCENTUAL as-filed: 35,2 = 35,2%. Escala OPOSTA à de
            `impaired_ratio`/`overdue_ratio`, que vêm em fração — as três na
            mesma linha."
        top_originator_pct_implausible:
          type:
            - boolean
            - "null"
      required:
        - cnpj
        - reference_date
        - entity_kind
        - name
        - net_worth
        - net_worth_avg
        - total_assets
        - cash
        - portfolio
        - receivables_with_risk
        - overdue_with_risk
        - impaired_with_risk
        - overdue_no_risk
        - impaired_no_risk
        - impaired_total
        - delinquency_scope
        - impaired_ratio
        - overdue_ratio
        - impaired_exceeds_portfolio
        - impaired_ratio_absent_reason
        - portfolio_to_net_worth
        - top_originator_doc
        - top_originator_pct
        - top_originator_pct_implausible
    FidcSeries:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        series_label:
          type: string
        seniority_rank:
          type:
            - number
            - "null"
        seniority:
          type:
            - string
            - "null"
        quota_count:
          type:
            - number
            - "null"
        quota_value:
          type:
            - number
            - "null"
        series_net_worth:
          type:
            - number
            - "null"
        return_month_pct:
          type:
            - number
            - "null"
        holder_count:
          type:
            - number
            - "null"
        class_net_worth:
          type:
            - number
            - "null"
        pct_of_class:
          type:
            - number
            - "null"
        pct_of_class_implausible:
          type:
            - boolean
            - "null"
        cascade_observable:
          type:
            - boolean
            - "null"
      required:
        - cnpj
        - reference_date
        - series_label
        - seniority_rank
        - seniority
        - quota_count
        - quota_value
        - series_net_worth
        - return_month_pct
        - holder_count
        - class_net_worth
        - pct_of_class
        - pct_of_class_implausible
        - cascade_observable
    FidcDelinquency:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        risk_retained:
          type: boolean
        bucket:
          type: string
        bucket_label:
          type:
            - string
            - "null"
        bucket_order:
          type: number
        amount_due:
          type:
            - number
            - "null"
        amount_overdue:
          type:
            - number
            - "null"
        amount_prepaid:
          type:
            - number
            - "null"
        pct_of_due:
          type:
            - number
            - "null"
        pct_of_overdue:
          type:
            - number
            - "null"
        pct_of_prepaid:
          type:
            - number
            - "null"
        pct_implausible:
          type:
            - boolean
            - "null"
        total_due:
          type:
            - number
            - "null"
        total_overdue:
          type:
            - number
            - "null"
        total_prepaid:
          type:
            - number
            - "null"
      required:
        - cnpj
        - reference_date
        - risk_retained
        - bucket
        - bucket_label
        - bucket_order
        - amount_due
        - amount_overdue
        - amount_prepaid
        - pct_of_due
        - pct_of_overdue
        - pct_of_prepaid
        - pct_implausible
        - total_due
        - total_overdue
        - total_prepaid
    FidcScrLevel:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        scr_level:
          type: string
        scr_order:
          type: number
        amount_debtor:
          type:
            - number
            - "null"
        amount_operation:
          type:
            - number
            - "null"
        pct_debtor:
          type:
            - number
            - "null"
        pct_operation:
          type:
            - number
            - "null"
        total_debtor:
          type:
            - number
            - "null"
        total_operation:
          type:
            - number
            - "null"
        declares_scr:
          type:
            - boolean
            - "null"
      required:
        - cnpj
        - reference_date
        - scr_level
        - scr_order
        - amount_debtor
        - amount_operation
        - pct_debtor
        - pct_operation
        - total_debtor
        - total_operation
        - declares_scr
    FidcSecondaryTrade:
      type: object
      properties:
        trade_date:
          type: string
        instrument_code:
          type: string
        isin:
          type:
            - string
            - "null"
        issuer_name:
          type:
            - string
            - "null"
        cnpj:
          type:
            - string
            - "null"
        match_level:
          type: string
          enum:
            - exact_name
            - none_ambiguous_name
            - none_not_in_informe
            - none_not_fidc
        quantity:
          type:
            - number
            - "null"
        trade_count:
          type:
            - number
            - "null"
        price_min:
          type:
            - number
            - "null"
        price_avg:
          type:
            - number
            - "null"
        price_max:
          type:
            - number
            - "null"
        price_last:
          type:
            - number
            - "null"
        volume_brl:
          type:
            - number
            - "null"
      required:
        - trade_date
        - instrument_code
        - isin
        - issuer_name
        - cnpj
        - match_level
        - quantity
        - trade_count
        - price_min
        - price_avg
        - price_max
        - price_last
        - volume_brl
    FidcSector:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        sector_code:
          type: string
        parent_code:
          type:
            - string
            - "null"
        is_subcategory:
          type: boolean
        sector_label:
          type: string
        amount:
          type: number
        pct_of_portfolio:
          type:
            - number
            - "null"
      required:
        - cnpj
        - reference_date
        - sector_code
        - parent_code
        - is_subcategory
        - sector_label
        - amount
        - pct_of_portfolio
    FidcOriginator:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        origin_block:
          type: string
          enum:
            - with_risk
            - no_risk
            - legacy
        risk_retained:
          type:
            - boolean
            - "null"
        originator_rank:
          type: number
        originator_cnpj:
          type:
            - string
            - "null"
        document_kind:
          type:
            - string
            - "null"
          enum:
            - cnpj
            - cpf
            - invalido
            - null
        share_pct:
          type:
            - number
            - "null"
          description: "PERCENTUAL as-filed: 35,2 = 35,2%, não fração. Atenção ao compor
            com `listFidcs`, onde `impaired_ratio` e `overdue_ratio` vêm em
            FRAÇÃO — o mesmo raciocínio, escalas opostas, e nada além desta
            linha avisa."
        share_pct_implausible:
          type:
            - boolean
            - "null"
        declared_count:
          type:
            - number
            - "null"
        identified_count:
          type:
            - number
            - "null"
        declared_share_total:
          type:
            - number
            - "null"
          description: Soma dos `share_pct` declarados no bloco, em PERCENTUAL (98,54 =
            98,54%).
        declared_share_total_implausible:
          type:
            - boolean
            - "null"
        undisclosed_share_pct:
          type:
            - number
            - "null"
          description: Quanto da carteira vem de cedente que a classe NÃO identificou, em
            PERCENTUAL.
      required:
        - cnpj
        - reference_date
        - origin_block
        - risk_retained
        - originator_rank
        - originator_cnpj
        - document_kind
        - share_pct
        - share_pct_implausible
        - declared_count
        - identified_count
        - declared_share_total
        - declared_share_total_implausible
        - undisclosed_share_pct
    FidcFlow:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        series_count:
          type:
            - number
            - "null"
        subscriptions:
          type:
            - number
            - "null"
        amortizations:
          type:
            - number
            - "null"
        redemptions_paid:
          type:
            - number
            - "null"
        redemptions_requested:
          type:
            - number
            - "null"
        net_flow:
          type:
            - number
            - "null"
        liquid_now:
          type:
            - number
            - "null"
        liquid_30d:
          type:
            - number
            - "null"
        liquid_360d:
          type:
            - number
            - "null"
        net_worth:
          type:
            - number
            - "null"
        net_flow_to_net_worth:
          type:
            - number
            - "null"
        redemption_coverage_30d:
          type:
            - number
            - "null"
      required:
        - cnpj
        - reference_date
        - series_count
        - subscriptions
        - amortizations
        - redemptions_paid
        - redemptions_requested
        - net_flow
        - liquid_now
        - liquid_30d
        - liquid_360d
        - net_worth
        - net_flow_to_net_worth
        - redemption_coverage_30d
    FidcPortfolioOps:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        acquired_amount:
          type:
            - number
            - "null"
        acquired_count:
          type:
            - number
            - "null"
        avg_ticket:
          type:
            - number
            - "null"
        acquired_impaired:
          type:
            - number
            - "null"
        acquired_impaired_pct:
          type:
            - number
            - "null"
        repurchased_amount:
          type:
            - number
            - "null"
        repurchased_count:
          type:
            - number
            - "null"
        repurchase_to_portfolio:
          type:
            - number
            - "null"
        substituted_amount:
          type:
            - number
            - "null"
        sold_amount:
          type:
            - number
            - "null"
        sold_to_related_party:
          type:
            - number
            - "null"
        related_party_sale_pct:
          type:
            - number
            - "null"
        sale_gain:
          type:
            - number
            - "null"
        collateral_amount:
          type:
            - number
            - "null"
        collateral_pct:
          type:
            - number
            - "null"
        collateral_pct_implausible:
          type:
            - boolean
            - "null"
        portfolio:
          type:
            - number
            - "null"
        net_worth:
          type:
            - number
            - "null"
      required:
        - cnpj
        - reference_date
        - acquired_amount
        - acquired_count
        - avg_ticket
        - acquired_impaired
        - acquired_impaired_pct
        - repurchased_amount
        - repurchased_count
        - repurchase_to_portfolio
        - substituted_amount
        - sold_amount
        - sold_to_related_party
        - related_party_sale_pct
        - sale_gain
        - collateral_amount
        - collateral_pct
        - collateral_pct_implausible
        - portfolio
        - net_worth
    FidcInvestor:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        seniority:
          type: string
          enum:
            - senior
            - subordinada
        investor_type:
          type: string
        investor_type_label:
          type:
            - string
            - "null"
        investor_count:
          type:
            - number
            - "null"
        seniority_total:
          type:
            - number
            - "null"
        pct_of_seniority:
          type:
            - number
            - "null"
        is_institutional:
          type:
            - boolean
            - "null"
      required:
        - cnpj
        - reference_date
        - seniority
        - investor_type
        - investor_type_label
        - investor_count
        - seniority_total
        - pct_of_seniority
        - is_institutional
    FidcPerformance:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        series_label:
          type: string
        seniority:
          type:
            - string
            - "null"
          enum:
            - senior
            - mezanino
            - subordinada
            - unica
            - null
        expected_pct:
          type:
            - number
            - "null"
        realized_pct:
          type:
            - number
            - "null"
        declares_target:
          type:
            - boolean
            - "null"
        performance_gap_pct:
          type:
            - number
            - "null"
        performance_implausible:
          type:
            - boolean
            - "null"
        shortfall_streak:
          type:
            - number
            - "null"
      required:
        - cnpj
        - reference_date
        - series_label
        - seniority
        - expected_pct
        - realized_pct
        - declares_target
        - performance_gap_pct
        - performance_implausible
        - shortfall_streak
    FidcReturn:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        series_label:
          type: string
        seniority:
          type:
            - string
            - "null"
          enum:
            - senior
            - mezanino
            - subordinada
            - unica
            - null
        return_month_pct:
          type:
            - number
            - "null"
        return_implausible:
          type:
            - boolean
            - "null"
        cdi_month_pct:
          type:
            - number
            - "null"
        excess_month_pp:
          type:
            - number
            - "null"
        pct_of_cdi:
          type:
            - number
            - "null"
        spread_annual_pp:
          type:
            - number
            - "null"
        cum_year:
          type:
            - number
            - "null"
        cdi_cum_year:
          type:
            - number
            - "null"
        cum_since_inception:
          type:
            - number
            - "null"
        cdi_cum_since_inception:
          type:
            - number
            - "null"
        months_in_window:
          type:
            - number
            - "null"
        is_reported:
          type:
            - boolean
            - "null"
        has_position:
          type:
            - boolean
            - "null"
      required:
        - cnpj
        - reference_date
        - series_label
        - seniority
        - return_month_pct
        - return_implausible
        - cdi_month_pct
        - excess_month_pp
        - pct_of_cdi
        - spread_annual_pp
        - cum_year
        - cdi_cum_year
        - cum_since_inception
        - cdi_cum_since_inception
        - months_in_window
        - is_reported
        - has_position
    OriginatorProfile:
      type: object
      properties:
        cnpj:
          type: string
        razao_social:
          type:
            - string
            - "null"
        nome_fantasia:
          type:
            - string
            - "null"
        is_matriz:
          type:
            - boolean
            - "null"
        situacao:
          type:
            - string
            - "null"
        situacao_adversa:
          type:
            - boolean
            - "null"
        situacao_desde:
          type:
            - string
            - "null"
        atividade_desde:
          type:
            - string
            - "null"
        cnae_principal:
          type:
            - string
            - "null"
        cnae_descricao:
          type:
            - string
            - "null"
        uf:
          type:
            - string
            - "null"
        municipio:
          type:
            - string
            - "null"
        porte:
          type:
            - string
            - "null"
        capital_social:
          type:
            - number
            - "null"
        natureza_juridica:
          type:
            - string
            - "null"
        socios_total:
          type:
            - number
            - "null"
        socios_pj:
          type:
            - number
            - "null"
        socios_nomes:
          type:
            - array
            - "null"
          items:
            type: string
        donos_pj:
          type:
            - array
            - "null"
          items:
            type: string
        cedentes_mesmo_dono:
          type:
            - array
            - "null"
          items:
            type: string
        vinculo_verificavel:
          type:
            - boolean
            - "null"
        encontrado_no_cadastro:
          type:
            - boolean
            - "null"
      required:
        - cnpj
        - razao_social
        - nome_fantasia
        - is_matriz
        - situacao
        - situacao_adversa
        - situacao_desde
        - atividade_desde
        - cnae_principal
        - cnae_descricao
        - uf
        - municipio
        - porte
        - capital_social
        - natureza_juridica
        - socios_total
        - socios_pj
        - socios_nomes
        - donos_pj
        - cedentes_mesmo_dono
        - vinculo_verificavel
        - encontrado_no_cadastro
    FidcPricing:
      type: object
      properties:
        cnpj:
          type: string
        reference_date:
          type: string
        asset_class:
          type: string
          enum:
            - direitos_com_risco
            - direitos_sem_risco
            - valores_mobiliarios
            - titulos_publicos
            - cdb
            - outra_renda_fixa
        asset_class_label:
          type:
            - string
            - "null"
        risk_retained:
          type:
            - boolean
            - "null"
        rate_kind:
          type: string
          enum:
            - desconto_aquisicao
            - juros
        buy_min:
          type:
            - number
            - "null"
        buy_avg:
          type:
            - number
            - "null"
        buy_max:
          type:
            - number
            - "null"
        sell_min:
          type:
            - number
            - "null"
        sell_avg:
          type:
            - number
            - "null"
        sell_max:
          type:
            - number
            - "null"
        buy_spread:
          type:
            - number
            - "null"
        rate_implausible:
          type:
            - boolean
            - "null"
        peer_median_buy_avg:
          type:
            - number
            - "null"
        peer_count:
          type:
            - number
            - "null"
        buy_vs_peer_pp:
          type:
            - number
            - "null"
      required:
        - cnpj
        - reference_date
        - asset_class
        - asset_class_label
        - risk_retained
        - rate_kind
        - buy_min
        - buy_avg
        - buy_max
        - sell_min
        - sell_avg
        - sell_max
        - buy_spread
        - rate_implausible
        - peer_median_buy_avg
        - peer_count
        - buy_vs_peer_pp
    FidcOriginatorExposure:
      type: object
      properties:
        cnpj:
          type: string
        name:
          type:
            - string
            - "null"
        reference_date:
          type: string
        origin_block:
          type: string
          enum:
            - with_risk
            - no_risk
            - legacy
        risk_retained:
          type:
            - boolean
            - "null"
        originator_rank:
          type: number
        share_pct:
          type:
            - number
            - "null"
          description: "PERCENTUAL as-filed da carteira da classe atribuído a este
            cedente: 35,2 = 35,2%. Escala OPOSTA à de `impaired_ratio`, nesta
            mesma linha."
        share_pct_implausible:
          type:
            - boolean
            - "null"
        net_worth:
          type:
            - number
            - "null"
        portfolio:
          type:
            - number
            - "null"
        impaired_ratio:
          type:
            - number
            - "null"
          description: "FRAÇÃO da carteira da classe: 0,05 = 5%, máximo 1. Escala OPOSTA à
            de `share_pct`, nesta mesma linha — multiplicar por 100 antes de
            comparar."
      required:
        - cnpj
        - name
        - reference_date
        - origin_block
        - risk_retained
        - originator_rank
        - share_pct
        - share_pct_implausible
        - net_worth
        - portfolio
        - impaired_ratio
    FundCreditHolding:
      type: object
      properties:
        cnpj:
          type: string
        comptc_date:
          type: string
        source_block:
          type:
            - string
            - "null"
          description: "bloco do CDA: BLC_6 (crédito e agronegócio) ou BLC_8 (demais
            ativos, onde vive o CRI)"
        asset_family:
          type:
            - string
            - "null"
          description: CRI, CRA, DEB, NP, CCB, CCI, CPR, CDCA, NCA, LCA, CCCB, OUTROS_CR
            ou OUTROS
        tp_aplic:
          type:
            - string
            - "null"
          description: rótulo bruto da fonte
        tp_ativo:
          type:
            - string
            - "null"
          description: rótulo bruto da fonte
        ds_ativo:
          type:
            - string
            - "null"
          description: descritivo as-filed do BLC_8; texto livre, formato varia por
            administrador
        cetip_code:
          type:
            - string
            - "null"
          description: código CETIP extraído do descritivo, quando presente
        isin:
          type:
            - string
            - "null"
          description: preenchido apenas em match_level 'cetip' e 'issuer_maturity'; null
            nos demais
        debenture_code:
          type:
            - string
            - "null"
          description: código da debênture quando a resolução chegou à emissão
        match_level:
          type: string
          enum:
            - cetip
            - issuer_maturity
            - issuer
            - none
          description: "até onde a resolução de identidade chegou. 'cetip': código do
            papel achado no descritivo e confirmado no cadastro.
            'issuer_maturity': emissor + vencimento resolvendo série única.
            'issuer': só a casa emissora. 'none': não amarra em cadastro
            nenhum."
        issuer_name:
          type:
            - string
            - "null"
          description: as-filed. Alterna entre securitizadora e devedor econômico do
            lastro — não normalizado
        issuer_doc:
          type:
            - string
            - "null"
          description: CNPJ do emissor; null quando o emissor é pessoa física
        issuer_person_type:
          type:
            - string
            - "null"
          description: PF ou PJ
        issuer_related:
          type:
            - string
            - "null"
          description: emissor ligado à gestora/administradora, as-filed (S/N)
        maturity_date:
          type:
            - string
            - "null"
          description: só o BLC_6 declara vencimento
        indexer_label:
          type:
            - string
            - "null"
        indexer_pct:
          type:
            - number
            - "null"
        prefixed_rate_pct:
          type:
            - number
            - "null"
        qty:
          type:
            - number
            - "null"
        market_value:
          type:
            - number
            - "null"
          description: posição a mercado (R$)
        fund_name:
          type:
            - string
            - "null"
        fund_manager:
          type:
            - string
            - "null"
        fund_classificacao:
          type:
            - string
            - "null"
      required:
        - cnpj
        - comptc_date
        - source_block
        - asset_family
        - tp_aplic
        - tp_ativo
        - ds_ativo
        - cetip_code
        - isin
        - debenture_code
        - match_level
        - issuer_name
        - issuer_doc
        - issuer_person_type
        - issuer_related
        - maturity_date
        - indexer_label
        - indexer_pct
        - prefixed_rate_pct
        - qty
        - market_value
        - fund_name
        - fund_manager
        - fund_classificacao
    CreditFundHoldersSummary:
      type: object
      properties:
        isin:
          type: string
        comptc_date:
          type: string
          description: competência do CDA (AAAA-MM-DD)
        n_funds:
          type: number
          description: fundos com posição resolvida a ESTE ISIN
        total_value_brl:
          type:
            - number
            - "null"
          description: posição a mercado somada (R$)
        resolved_only:
          type: boolean
          const: true
          description: "sempre true. O CDA não publica ISIN em BLC_6/BLC_8: a soma cobre
            apenas as posições cuja identidade foi resolvida, e é PISO, nunca
            total."
      required:
        - isin
        - comptc_date
        - n_funds
        - total_value_brl
        - resolved_only
    CoeInstrument:
      type: object
      properties:
        instrument_code:
          type: string
        isin:
          type:
            - string
            - "null"
        issuer_name:
          type:
            - string
            - "null"
        is_incentivada:
          type:
            - boolean
            - "null"
        indexer:
          type:
            - string
            - "null"
        indexer_pct:
          type:
            - number
            - "null"
        additional_rate_pct:
          type:
            - number
            - "null"
        maturity_date:
          type:
            - string
            - "null"
        qty_issued:
          type:
            - number
            - "null"
        issue_price:
          type:
            - number
            - "null"
        issue_size_brl:
          type:
            - number
            - "null"
        issue_type:
          type:
            - string
            - "null"
        registered_ref_date:
          type:
            - string
            - "null"
        trade_days:
          type:
            - number
            - "null"
        trade_count_total:
          type:
            - number
            - "null"
        volume_brl_total:
          type:
            - number
            - "null"
        last_trade_date:
          type:
            - string
            - "null"
      required:
        - instrument_code
        - isin
        - issuer_name
        - is_incentivada
        - indexer
        - indexer_pct
        - additional_rate_pct
        - maturity_date
        - qty_issued
        - issue_price
        - issue_size_brl
        - issue_type
        - registered_ref_date
        - trade_days
        - trade_count_total
        - volume_brl_total
        - last_trade_date
    CoeSecondaryWindow:
      type: object
      properties:
        from:
          type:
            - string
            - "null"
          description: Primeiro pregão de balcão coberto (YYYY-MM-DD).
        to:
          type:
            - string
            - "null"
          description: Último pregão de balcão coberto (YYYY-MM-DD).
        sessions:
          type: integer
          description: "Pregões distintos de balcão cobertos. É o denominador de
            `trade_days`: janela curta significa que o secundário null diz pouco
            sobre o papel."
      required:
        - from
        - to
        - sessions
    IndexMeta:
      type: object
      properties:
        code:
          type: string
        name:
          type: string
        rebalancing:
          type: string
      required:
        - code
        - name
        - rebalancing
    BenchmarkIndexPoint:
      type: object
      properties:
        date:
          type: string
        index_code:
          type: string
        index_value:
          type:
            - number
            - "null"
        daily_change_pct:
          type:
            - number
            - "null"
        duration_bd:
          type:
            - number
            - "null"
        portfolio_value_kbrl:
          type:
            - number
            - "null"
      required:
        - date
        - index_code
        - index_value
        - daily_change_pct
        - duration_bd
        - portfolio_value_kbrl
    IndexComponent:
      type: object
      properties:
        ticker:
          type: string
        weight:
          type: number
        theoretical_quantity:
          type: number
      required:
        - ticker
        - weight
        - theoretical_quantity
    SeriesMeta:
      type: object
      properties:
        source:
          type: string
        series_id:
          type: string
        name:
          type:
            - string
            - "null"
        label:
          type:
            - string
            - "null"
        unit:
          type:
            - string
            - "null"
          description: 'Rótulo de unidade COMO A FONTE PUBLICA, para exibição. Não use
            para decidir se duas séries são comparáveis: no World Bank ele traz
            o nome do indicador ("GDP (current US$)") e no SGS convivem "%", "%
            a.a." e "% a.m.". Para isso existem `dimension`, `scale` e
            `period`.'
        frequency:
          type:
            - string
            - "null"
          description: "Cadência de PUBLICAÇÃO: daily, weekly, monthly, quarterly, annual.
            Não confunda com `period`."
        dimension:
          type:
            - string
            - "null"
          description: O que o número É. `rate` é taxa, `index` é número-índice (o CPI
            americano vale 322, não 3,2%), `share` é participação, `currency` é
            dinheiro, `count` é contagem. Comparar séries de dimensões
            diferentes é aritmética correta sobre coisas incomparáveis.
        scale:
          type:
            - string
            - "null"
          description: "Em que escala o valor vem: 5,5 (percent), 0,055 (unit) e 550 (bps)
            podem ser a mesma taxa. `thousand`/`million`/`billion` multiplicam o
            valor servido."
        period:
          type:
            - string
            - "null"
          description: Que JANELA o número cobre, que NÃO é a cadência de publicação. A
            série 433 é o IPCA do MÊS (`monthly`, ~0,39) e a 13522 é o acumulado
            em 12 MESES (`ttm_12m`, ~4,72) — responder "a inflação do ano" com a
            433 é o erro que este campo existe para impedir. A TR (226) e a
            poupança (195) são publicadas todo dia e o número é do mês.
        seasonal_adjustment:
          type:
            - string
            - "null"
          description: '`nsa` sem ajuste sazonal, `sa` com, `saar` com ajuste E já
            anualizado pela fonte (o PIB americano é assim: o nível TRIMESTRAL
            já vem em ritmo anual e não se compara com um trimestre brasileiro
            sem multiplicar por quatro). Antes disso a informação vivia só no
            sufixo do nome — `ibc_br` e `ibc_br_dessaz` têm a mesma última data
            e quem pedisse "o IBC-Br" tinha 50% de chance de pegar a série
            errada.'
        expected_range:
          type:
            - object
            - "null"
          properties:
            min:
              type: number
            max:
              type: number
          required:
            - min
            - max
          description: "Faixa plausível declarada, NA MESMA UNIDADE dos valores servidos.
            Guarda-corpo contra erro de unidade, não previsão: valor fora dela é
            MARCADO, nunca omitido — ruptura de regime é dado verdadeiro."
        prior_note:
          type:
            - string
            - "null"
          description: Ressalva conhecida sobre a série, quando existe.
        served_by:
          type:
            - string
            - "null"
          description: "QUAL operação serve as observações desta série — `getSeries`,
            `getMarketExpectations` ou `getBondCurves`. O catálogo é a lista de
            quem EXISTE, não a promessa de que `getSeries` responde por todas:
            desde que a expectativa Focus e o vértice de curva viraram objetos
            do grafo, eles entraram aqui (é onde a régua declarada mora) mas as
            observações continuam na tabela de origem. Chamar a rota errada
            devolveria cabeçalho com zero pontos, e zero pontos com 200 lê-se
            como 'o dado não existe'."
        first_date:
          type:
            - string
            - "null"
        last_date:
          type:
            - string
            - "null"
      required:
        - source
        - series_id
        - name
        - label
        - unit
        - frequency
        - dimension
        - scale
        - period
        - seasonal_adjustment
        - expected_range
        - prior_note
        - served_by
        - first_date
        - last_date
    Fii:
      type: object
      properties:
        ticker:
          type: string
        cnpj:
          type:
            - string
            - "null"
        name:
          type:
            - string
            - "null"
        segment:
          type:
            - string
            - "null"
        administrator:
          type:
            - string
            - "null"
        manager:
          type:
            - string
            - "null"
        is_paper:
          type: boolean
      required:
        - ticker
        - cnpj
        - name
        - segment
        - administrator
        - manager
        - is_paper
    FiiDistribution:
      type: object
      properties:
        ex_date:
          type:
            - string
            - "null"
        payment_date:
          type:
            - string
            - "null"
        value_per_share:
          type:
            - number
            - "null"
          description: rendimento por cota NOMINAL pago na época (base de cotas da data-com)
        value_per_share_adj:
          type:
            - number
            - "null"
          description: rendimento por cota ajustado por desdobramentos/grupamentos para a
            base de cotas ATUAL — comparável/somável através de splits (o
            value_per_share permanece nominal da época)
        tax_free:
          type: boolean
      required:
        - ex_date
        - payment_date
        - value_per_share
        - value_per_share_adj
        - tax_free
    FiiMonthlyReport:
      type: object
      properties:
        reference_month:
          type:
            - string
            - "null"
        net_asset_value:
          type:
            - number
            - "null"
        value_per_share:
          type:
            - number
            - "null"
        monthly_dividend_yield_pct:
          type:
            - number
            - "null"
        shareholders:
          type:
            - number
            - "null"
        shares_issued:
          type:
            - number
            - "null"
      required:
        - reference_month
        - net_asset_value
        - value_per_share
        - monthly_dividend_yield_pct
        - shareholders
        - shares_issued
    BdrProfile:
      type: object
      properties:
        ticker:
          type: string
        name:
          type:
            - string
            - "null"
        isin:
          type:
            - string
            - "null"
        kind:
          type:
            - string
            - "null"
          description: patrocinado | nao_patrocinado | etf (recibo de ETF estrangeiro,
            CODBDI 36)
        spec:
          type:
            - string
            - "null"
        first_traded:
          type:
            - string
            - "null"
        last_traded:
          type:
            - string
            - "null"
        sessions:
          type:
            - number
            - "null"
      required:
        - ticker
        - name
        - isin
        - kind
        - spec
        - first_traded
        - last_traded
        - sessions
    BdrQuote:
      type: object
      properties:
        date:
          type: string
        open:
          type:
            - number
            - "null"
        high:
          type:
            - number
            - "null"
        low:
          type:
            - number
            - "null"
        close:
          type:
            - number
            - "null"
        close_raw:
          type:
            - number
            - "null"
        volume:
          type:
            - number
            - "null"
        adjust_quality:
          type:
            - string
            - "null"
      required:
        - date
        - open
        - high
        - low
        - close
        - close_raw
        - volume
        - adjust_quality
    EtfProfile:
      type: object
      properties:
        ticker:
          type: string
        name:
          type:
            - string
            - "null"
        cnpj:
          type:
            - string
            - "null"
        manager:
          type:
            - string
            - "null"
          description: gestor (positionManager)
        administrator:
          type:
            - string
            - "null"
        segment:
          type:
            - string
            - "null"
        classification:
          type:
            - string
            - "null"
        trading_code_others:
          type:
            - string
            - "null"
          description: lote fracionário / código alternativo
        quota_count_authorized:
          type:
            - number
            - "null"
          description: teto autorizado de cotas (cadastro B3)
        net_worth:
          type:
            - number
            - "null"
          description: Patrimônio líquido do registro CVM 175, em R$. SNAPSHOT da data de
            registro, não valor de hoje — leia sempre junto de net_worth_date.
        net_worth_date:
          type:
            - string
            - "null"
          description: Data de registro a que net_worth se refere (varia de 2023 a 2026
            entre ETFs).
        first_traded:
          type:
            - string
            - "null"
        last_traded:
          type:
            - string
            - "null"
        sessions:
          type:
            - number
            - "null"
      required:
        - ticker
        - name
        - cnpj
        - manager
        - administrator
        - segment
        - classification
        - trading_code_others
        - quota_count_authorized
        - net_worth
        - net_worth_date
        - first_traded
        - last_traded
        - sessions
    FundProfile:
      type: object
      properties:
        cnpj:
          type: string
        codigo_cvm:
          type:
            - number
            - "null"
        name:
          type:
            - string
            - "null"
        tipo_classe:
          type:
            - string
            - "null"
        situacao:
          type:
            - string
            - "null"
        classificacao:
          type:
            - string
            - "null"
        classificacao_anbima:
          type:
            - string
            - "null"
        entidade_investimento:
          type:
            - boolean
            - "null"
          description: Entidade de investimento na acepção contábil (CPC 18/IFRS 10), o
            que muda como as participadas são avaliadas. **Era `"S"`/`"N"` até
            20/08/2026** — a letra da CVM ia crua até aqui. Nulo é ausência de
            declaração na fonte, nunca `false`.
        net_worth:
          type:
            - number
            - "null"
          description: "patrimônio líquido (R$). É o PL DA CLASSE. Somar `net_worth` de
            várias classes conta o mesmo dinheiro mais de uma vez: um FIC entra
            com o PL dele e o fundo investido também — no universo CVM 175 isso
            infla o agregado da indústria em cerca de 37%. Para um total,
            desconte a posição em cotas de fundos (`listFundLookThrough`)."
        cnpj_fundo:
          type:
            - string
            - "null"
        tipo_fundo:
          type:
            - string
            - "null"
        administrator:
          type:
            - string
            - "null"
        manager:
          type:
            - string
            - "null"
          description: gestor
        manager_cnpj:
          type:
            - string
            - "null"
          description: CNPJ do gestor (14 dígitos). É a chave estável da casa — use-a em
            vez de casar `manager` por razão social. Null quando o gestor é
            pessoa física ou quando a CVM não declara.
        admin_fee_pct:
          type:
            - number
            - "null"
          description: taxa de administração (herdada do cadastro legado)
        performance_fee_pct:
          type:
            - number
            - "null"
        data_registro:
          type:
            - string
            - "null"
      required:
        - cnpj
        - codigo_cvm
        - name
        - tipo_classe
        - situacao
        - classificacao
        - classificacao_anbima
        - entidade_investimento
        - net_worth
        - cnpj_fundo
        - tipo_fundo
        - administrator
        - manager
        - manager_cnpj
        - admin_fee_pct
        - performance_fee_pct
        - data_registro
    FundManager:
      type: object
      properties:
        manager:
          type: string
          description: nome do gestor exatamente como a CVM publica
        n_classes:
          type: number
          description: classes registradas com este gestor, em qualquer situação
        n_active:
          type: number
          description: subconjunto em 'Em Funcionamento Normal'
        net_worth_sum:
          type:
            - number
            - "null"
          description: soma BRUTA do PL das classes ativas. Conta o mesmo dinheiro mais de
            uma vez sempre que a casa opera master/feeder — use `net_worth_net`.
        net_worth_net:
          type:
            - number
            - "null"
          description: PL sob gestão descontando o que uma classe da PRÓPRIA casa aplica
            em cotas de outra classe da casa, pela última competência divulgada
            de cada investidor. É o número comparável entre gestoras; ainda é
            PISO, porque só desconta a aplicação em cotas que a carteira
            divulgada revela.
        internal_feeder_brl:
          type:
            - number
            - "null"
          description: o desconto aplicado, isto é, quanto do bruto era aplicação da casa
            nela mesma
        vehicle_types:
          type: array
          items:
            type: object
            properties:
              tipo_fundo:
                type:
                  - string
                  - "null"
              n:
                type: number
            required:
              - tipo_fundo
              - n
          description: distribuição por tipo de veículo (FI, FIDC, FII, FIP, FIAGRO...)
        largest_class:
          type:
            - object
            - "null"
          properties:
            cnpj:
              type: string
            name:
              type:
                - string
                - "null"
            net_worth:
              type:
                - number
                - "null"
          required:
            - cnpj
            - name
            - net_worth
          description: maior classe ativa por PL
        registry:
          oneOf:
            - $ref: "#/components/schemas/FundManagerRegistry"
            - type: "null"
          description: cadastro da casa na CVM (ADM_CART), resolvido pelo CNPJ que as
            classes declaram. Null quando o gestor é pessoa física, quando as
            classes não declaram CNPJ ou quando a casa não tem registro próprio
            de administrador de carteira — caso de banco ou DTVM que exerce a
            gestão sob outro registro.
      required:
        - manager
        - n_classes
        - n_active
        - net_worth_sum
        - net_worth_net
        - internal_feeder_brl
        - vehicle_types
        - largest_class
        - registry
    FundManagerRegistry:
      type: object
      properties:
        cnpj:
          type: string
          description: CNPJ da gestora (14 dígitos)
        name:
          type: string
          description: razão social no cadastro de administradores de carteira
        trade_name:
          type:
            - string
            - "null"
          description: nome comercial, quando declarado
        category:
          type:
            - string
            - "null"
          description: "categoria de registro: 'Gestor de Carteira', 'Administrador
            Fiduciário e Gestor de Carteira' ou 'Administrador Fiduciário'. Diz
            o que a casa pode fazer, não o que ela faz."
        shareholder_control:
          type:
            - string
            - "null"
          description: controle acionário (privado, estrangeiro...)
        city:
          type:
            - string
            - "null"
        uf:
          type:
            - string
            - "null"
        website:
          type:
            - string
            - "null"
        registered_at:
          type:
            - string
            - "null"
          description: data do registro na CVM
        is_active:
          type:
            - boolean
            - "null"
          description: registro em funcionamento normal
      required:
        - cnpj
        - name
        - trade_name
        - category
        - shareholder_control
        - city
        - uf
        - website
        - registered_at
        - is_active
    FundQuote:
      type: object
      properties:
        comptc_date:
          type: string
        quota_value:
          type:
            - number
            - "null"
        net_worth:
          type:
            - number
            - "null"
        total_value:
          type:
            - number
            - "null"
        inflow:
          type:
            - number
            - "null"
        outflow:
          type:
            - number
            - "null"
        net_flow:
          type:
            - number
            - "null"
        shareholders:
          type:
            - number
            - "null"
      required:
        - comptc_date
        - quota_value
        - net_worth
        - total_value
        - inflow
        - outflow
        - net_flow
        - shareholders
    FundOfFundsEdge:
      type: object
      properties:
        comptc_date:
          type: string
        investor_cnpj:
          type: string
        invested_cnpj:
          type: string
        invested_name:
          type:
            - string
            - "null"
        market_value:
          type:
            - number
            - "null"
          description: posição do investidor nas cotas do investido (R$)
        invested_net_worth:
          type:
            - number
            - "null"
          description: PL do investido NA COMPETÊNCIA (nunca o PL de hoje); null em 27,3%
            das arestas
        invested_weight_pct:
          type:
            - number
            - "null"
          description: PERCENTUAL (0–100) do PL do INVESTIDO que o investidor representa
        investor_share_pct:
          type:
            - number
            - "null"
          description: PERCENTUAL (0–100) do PL do INVESTIDOR aplicado nesta posição
        weight_over_100:
          type:
            - boolean
            - "null"
          description: posição declarada entre 1,0× e 1,5× o PL do investido —
            descasamento de base, não erro
        weight_reason:
          type:
            - string
            - "null"
          enum:
            - sem_pl_no_informe
            - peso_acima_do_cap
            - null
          description: por que o peso não foi calculado; null = peso válido
        nav_date:
          type:
            - string
            - "null"
          description: data do PL usado no denominador
        invested_manager:
          type:
            - string
            - "null"
        invested_classificacao:
          type:
            - string
            - "null"
        lineage:
          type:
            - string
            - "null"
      required:
        - comptc_date
        - investor_cnpj
        - invested_cnpj
        - invested_name
        - market_value
        - invested_net_worth
        - invested_weight_pct
        - investor_share_pct
        - weight_over_100
        - weight_reason
        - nav_date
        - invested_manager
        - invested_classificacao
        - lineage
    FundLookThrough:
      type: object
      properties:
        investor_cnpj:
          type: string
        comptc_date:
          type: string
        ticker:
          type: string
        exposure_direct_brl:
          type:
            - number
            - "null"
          description: posição do próprio fundo no ativo (R$)
        exposure_indirect_brl:
          type:
            - number
            - "null"
          description: parcela que chega via cotas de outros fundos (R$)
        exposure_total_brl:
          type:
            - number
            - "null"
        shares_direct:
          type:
            - number
            - "null"
        shares_indirect:
          type:
            - number
            - "null"
        indirect_via_n_funds:
          type:
            - number
            - "null"
          description: quantos fundos investidos carregam o ativo
        indirect_coverage_pct:
          type:
            - number
            - "null"
          description: PERCENTUAL (0–100) do valor em cotas cujo BLC_4 foi possível abrir;
            null = não sabemos abrir nada. Exposição baixa com cobertura baixa
            NÃO quer dizer exposição ausente.
        cotas_value_brl:
          type:
            - number
            - "null"
          description: total em cotas do investidor na competência
        look_through_depth:
          type:
            - number
            - "null"
          description: saltos percorridos; hoje sempre 1
        lineage:
          type:
            - string
            - "null"
      required:
        - investor_cnpj
        - comptc_date
        - ticker
        - exposure_direct_brl
        - exposure_indirect_brl
        - exposure_total_brl
        - shares_direct
        - shares_indirect
        - indirect_via_n_funds
        - indirect_coverage_pct
        - cotas_value_brl
        - look_through_depth
        - lineage
    InvestorFlow:
      type: object
      properties:
        as_of_date:
          type: string
        investor_type:
          type: string
          description: Estrangeiro | Institucionais | ...
        buy_brl_thousand:
          type:
            - number
            - "null"
        sell_brl_thousand:
          type:
            - number
            - "null"
        buy_share_pct:
          type:
            - number
            - "null"
        sell_share_pct:
          type:
            - number
            - "null"
        net_mtd_brl_thousand:
          type:
            - number
            - "null"
        net_daily_brl_thousand:
          type:
            - number
            - "null"
        lineage:
          type:
            - string
            - "null"
      required:
        - as_of_date
        - investor_type
        - buy_brl_thousand
        - sell_brl_thousand
        - buy_share_pct
        - sell_share_pct
        - net_mtd_brl_thousand
        - net_daily_brl_thousand
        - lineage
    InvestorFlowMonthly:
      type: object
      properties:
        month_ref:
          type: string
        investor_type:
          type: string
        segment:
          type: string
        brl_reais:
          type:
            - number
            - "null"
        share_pct:
          type:
            - number
            - "null"
        lineage:
          type:
            - string
            - "null"
      required:
        - month_ref
        - investor_type
        - segment
        - brl_reais
        - share_pct
        - lineage
    PublicMarketAnomaly:
      type: object
      properties:
        day:
          type: string
        series_code:
          type: string
          description: IBOV | IFIX | USDBRL | BRENT | VIX | SP500 | NASDAQ
        return_pct:
          type: number
          description: Retorno do dia em % (a partir do log-retorno).
        zscore:
          type: number
          description: Desvios-padrão do retorno BRUTO vs σ rolling de 252 pregões. Quando
            local_zscore está presente, é ELE que define o dia como anômalo —
            este campo fica para auditoria.
        direction:
          type: string
          description: alta | queda
        factor_model:
          type:
            - string
            - "null"
          description: "Séries de controle descontadas (ex.: 'NASDAQ+USDBRL'). null =
            série sem modelo de fatores (as próprias séries globais) ou período
            sem os fatores disponíveis."
        local_return_pct:
          type:
            - number
            - "null"
          description: "Quanto a série andou ALÉM do que os fatores explicam, em %. É o
            descolamento próprio do dia: um IBOV que cai 5% num dia de queda
            global de 5% tem local_return_pct ≈ 0."
        local_zscore:
          type:
            - number
            - "null"
          description: "Desvios-padrão do descolamento local. É o critério de anomalia
            quando presente: separa 'o mundo caiu' de 'o Brasil caiu'."
        explained_by:
          oneOf:
            - $ref: "#/components/schemas/PublicMarketEvent"
            - type: "null"
          description: Evento que explica o dia anômalo, quando casado.
      required:
        - day
        - series_code
        - return_pct
        - zscore
        - direction
        - factor_model
        - local_return_pct
        - local_zscore
        - explained_by
    PublicEventThread:
      type: object
      properties:
        slug:
          type: string
        kind:
          type: string
          description: mp | copom | tarifa | eleicao | conflito | fiscal | outro
        title:
          type: string
        status:
          type: string
          description: em_curso | resolvido
        started_at:
          type: string
        resolved_at:
          type:
            - string
            - "null"
        last_event_day:
          type:
            - string
            - "null"
        entities:
          type: array
          items:
            type: string
        events:
          type: array
          items:
            $ref: "#/components/schemas/PublicMarketEvent"
          description: Timeline em ordem cronológica.
      required:
        - slug
        - kind
        - title
        - status
        - started_at
        - resolved_at
        - last_event_day
        - entities
        - events
    PublicSimilarEvent:
      allOf:
        - $ref: "#/components/schemas/PublicMarketEvent"
      properties:
        similarity:
          type: number
          description: Similaridade semântica (cosseno, 0-1) com o evento de referência.
      required:
        - similarity
    PublicOffering:
      type: object
      properties:
        offering_id:
          type: string
        regime:
          type:
            - string
            - "null"
          description: ICVM_400_476 | RCVM_160
        numero_processo:
          type:
            - string
            - "null"
          description: Null em todo o acervo ICVM 476 — o rito de esforços restritos não
            gera processo.
        numero_requerimento:
          type:
            - string
            - "null"
          description: Só RCVM 160; null em todo o regime antigo.
        numero_registro:
          type:
            - string
            - "null"
        serie:
          type:
            - string
            - "null"
        emissao:
          type:
            - string
            - "null"
        cnpj_emissor:
          type:
            - string
            - "null"
        nome_emissor:
          type:
            - string
            - "null"
        nome_lider:
          type:
            - string
            - "null"
        tipo_ativo:
          type:
            - string
            - "null"
        instrumento:
          type:
            - string
            - "null"
          description: Instrumento canônico (debenture, cri, cra, nota_comercial, acao,
            cota_fii…).
        familia:
          type:
            - string
            - "null"
          description: "Família do instrumento: divida | securitizacao | equity | fundo |
            outro."
        tipo_oferta:
          type:
            - string
            - "null"
        rito:
          type:
            - string
            - "null"
        modalidade_registro:
          type:
            - string
            - "null"
        regime_registro:
          type:
            - string
            - "null"
          description: "Regime normalizado: dispensa_icvm_476 | rito_automatico_rcvm_160 |
            registro_icvm_400 | registro_automatico_icvm_555 | dispensa_icvm_400
            | registro_ordinario_rcvm_160."
        status:
          type:
            - string
            - "null"
          description: Só RCVM 160; null em todo o regime antigo.
        data_processo:
          type:
            - string
            - "null"
        data_registro:
          type:
            - string
            - "null"
          description: Null no acervo ICVM 476, que é dispensado de registro. Para série
            temporal use data_referencia.
        data_inicio:
          type:
            - string
            - "null"
          description: Início da oferta; a única data do acervo ICVM 476.
        data_referencia:
          type:
            - string
            - "null"
          description: "Data única para série temporal: data_registro > data_inicio >
            data_processo."
        data_encerramento:
          type:
            - string
            - "null"
        valor_total:
          type:
            - number
            - "null"
        value_is_sentinel:
          type:
            - boolean
            - "null"
          description: Valor de preenchimento (sequência de noves ou acima de 5x o maior
            ano do mercado). Exclua ao agregar.
        value_pre_real_currency:
          type:
            - boolean
            - "null"
          description: Valor anterior a jul/1994, em cruzeiro/cruzado, não convertido para
            real. Exclua ao agregar.
        quantidade_total:
          type:
            - number
            - "null"
        preco_unitario:
          type:
            - number
            - "null"
        incentivo_fiscal:
          type:
            - boolean
            - "null"
      required:
        - offering_id
    OptionContract:
      type: object
      properties:
        option_ticker:
          type: string
        underlying_ticker:
          type:
            - string
            - "null"
        underlying_root:
          type:
            - string
            - "null"
        option_type:
          type:
            - string
            - "null"
          description: call | put
        strike:
          type:
            - number
            - "null"
        expiry:
          type:
            - string
            - "null"
        date:
          type:
            - string
            - "null"
        last:
          type:
            - number
            - "null"
        volume_brl:
          type:
            - number
            - "null"
        trades:
          type:
            - number
            - "null"
        underlying_spot:
          type:
            - number
            - "null"
        days_to_expiry:
          type:
            - number
            - "null"
        moneyness:
          type:
            - number
            - "null"
          description: spot / strike
        intrinsic:
          type:
            - number
            - "null"
        time_value:
          type:
            - number
            - "null"
        iv:
          type:
            - number
            - "null"
          description: vol implícita anualizada (Black-Scholes europeu)
        delta:
          type:
            - number
            - "null"
        gamma:
          type:
            - number
            - "null"
        vega:
          type:
            - number
            - "null"
          description: por 1% de vol
        theta:
          type:
            - number
            - "null"
          description: por dia corrido
        iv_amer:
          type:
            - number
            - "null"
          description: IV americana (binomial CRR) — corrige exercício antecipado;
            relevante em puts
        delta_amer:
          type:
            - number
            - "null"
        gamma_amer:
          type:
            - number
            - "null"
        vega_amer:
          type:
            - number
            - "null"
          description: por 1% de vol
        theta_amer:
          type:
            - number
            - "null"
          description: por dia corrido
        early_ex_premium:
          type:
            - number
            - "null"
          description: "prêmio de exercício antecipado: preço americano − europeu (mesmo σ)"
      required:
        - option_ticker
        - underlying_ticker
        - underlying_root
        - option_type
        - strike
        - expiry
        - date
        - last
        - volume_brl
        - trades
        - underlying_spot
        - days_to_expiry
        - moneyness
        - intrinsic
        - time_value
        - iv
        - delta
        - gamma
        - vega
        - theta
        - iv_amer
        - delta_amer
        - gamma_amer
        - vega_amer
        - theta_amer
        - early_ex_premium
    OptionExpiry:
      type: object
      properties:
        expiry:
          type: string
        count:
          type: number
          description: Séries abertas neste vencimento
      required:
        - expiry
        - count
    OptionQuote:
      type: object
      properties:
        date:
          type: string
        option_ticker:
          type: string
        option_type:
          type:
            - string
            - "null"
        strike:
          type:
            - number
            - "null"
        expiry:
          type:
            - string
            - "null"
        open:
          type:
            - number
            - "null"
        high:
          type:
            - number
            - "null"
        low:
          type:
            - number
            - "null"
        last:
          type:
            - number
            - "null"
        volume_brl:
          type:
            - number
            - "null"
        trades:
          type:
            - number
            - "null"
        underlying_spot:
          type:
            - number
            - "null"
        moneyness:
          type:
            - number
            - "null"
        intrinsic:
          type:
            - number
            - "null"
        time_value:
          type:
            - number
            - "null"
        iv:
          type:
            - number
            - "null"
        delta:
          type:
            - number
            - "null"
        gamma:
          type:
            - number
            - "null"
        vega:
          type:
            - number
            - "null"
        theta:
          type:
            - number
            - "null"
      required:
        - date
        - option_ticker
        - option_type
        - strike
        - expiry
        - open
        - high
        - low
        - last
        - volume_brl
        - trades
        - underlying_spot
        - moneyness
        - intrinsic
        - time_value
        - iv
        - delta
        - gamma
        - vega
        - theta
    CryptoAsset:
      type: object
      properties:
        symbol:
          type: string
          description: "Ativo-base canônico, ex.: BTC"
        name:
          type:
            - string
            - "null"
        rank:
          type:
            - number
            - "null"
          description: Ordem curada por relevância/market cap
        first_date:
          type:
            - string
            - "null"
        last_date:
          type:
            - string
            - "null"
        close_brl:
          type:
            - number
            - "null"
        close_usd:
          type:
            - number
            - "null"
        change_pct_1d:
          type:
            - number
            - "null"
        change_pct_7d:
          type:
            - number
            - "null"
        volume_brl_1d:
          type:
            - number
            - "null"
          description: Volume negociado em BRL na última vela diária
      required:
        - symbol
        - name
        - rank
        - first_date
        - last_date
        - close_brl
        - close_usd
        - change_pct_1d
        - change_pct_7d
        - volume_brl_1d
    CryptoLiveQuote:
      type: object
      properties:
        symbol:
          type: string
        price_brl:
          type: number
        price_usd:
          type:
            - number
            - "null"
        change_pct_24h:
          type:
            - number
            - "null"
          description: Variação % em 24h rolantes
        volume_usd_24h:
          type:
            - number
            - "null"
        ts:
          type: string
          description: Timestamp do snapshot (UTC)
      required:
        - symbol
        - price_brl
        - price_usd
        - change_pct_24h
        - volume_usd_24h
        - ts
    CryptoCandle:
      type: object
      properties:
        open_time:
          type: string
        close_time:
          type: string
        open:
          type:
            - number
            - "null"
        high:
          type:
            - number
            - "null"
        low:
          type:
            - number
            - "null"
        close:
          type:
            - number
            - "null"
        volume:
          type:
            - number
            - "null"
          description: Volume na moeda base
        quote_volume:
          type:
            - number
            - "null"
          description: Volume em BRL
        trades:
          type:
            - number
            - "null"
        close_usd:
          type:
            - number
            - "null"
          description: Fechamento em USD
      required:
        - open_time
        - close_time
        - open
        - high
        - low
        - close
        - volume
        - quote_volume
        - trades
        - close_usd
    UsAsset:
      type: object
      properties:
        ticker:
          type: string
        name:
          type:
            - string
            - "null"
        type:
          type: string
          enum:
            - stock
            - etf
        in_sp500:
          type:
            - boolean
            - "null"
        in_ndx100:
          type:
            - boolean
            - "null"
        bdr_ticker:
          type:
            - string
            - "null"
          description: BDR correspondente na B3, quando mapeado
        first_date:
          type:
            - string
            - "null"
        last_date:
          type:
            - string
            - "null"
        close_usd:
          type:
            - number
            - "null"
        change_pct_1d:
          type:
            - number
            - "null"
        change_pct_1m:
          type:
            - number
            - "null"
        change_pct_1y:
          type:
            - number
            - "null"
        high_52w:
          type:
            - number
            - "null"
        low_52w:
          type:
            - number
            - "null"
        volume_1d:
          type:
            - number
            - "null"
      required:
        - ticker
        - name
        - type
        - in_sp500
        - in_ndx100
        - bdr_ticker
        - first_date
        - last_date
        - close_usd
        - change_pct_1d
        - change_pct_1m
        - change_pct_1y
        - high_52w
        - low_52w
        - volume_1d
    UsAssetDetail:
      allOf:
        - $ref: "#/components/schemas/UsAsset"
      properties:
        fundamentals:
          type: array
          items:
            $ref: "#/components/schemas/UsFundamental"
      required:
        - fundamentals
    UsFundamental:
      type: object
      properties:
        metric:
          type: string
          enum:
            - revenue
            - net_income
            - eps_diluted
        unit:
          type:
            - string
            - "null"
        fy:
          type:
            - number
            - "null"
          description: Ano fiscal
        fp:
          type:
            - string
            - "null"
          description: Período fiscal (FY, Q1..Q4)
        period_start:
          type:
            - string
            - "null"
        period_end:
          type:
            - string
            - "null"
        value:
          type:
            - number
            - "null"
        form:
          type:
            - string
            - "null"
          description: Formulário de origem (10-K, 10-Q…)
        filed:
          type:
            - string
            - "null"
      required:
        - metric
        - unit
        - fy
        - fp
        - period_start
        - period_end
        - value
        - form
        - filed
    UsQuote:
      type: object
      properties:
        date:
          type: string
        open:
          type:
            - number
            - "null"
        high:
          type:
            - number
            - "null"
        low:
          type:
            - number
            - "null"
        close:
          type:
            - number
            - "null"
        volume:
          type:
            - number
            - "null"
      required:
        - date
        - open
        - high
        - low
        - close
        - volume
    UsFiling:
      type: object
      properties:
        accession:
          type: string
        form:
          type: string
          description: 10-K, 10-Q, 8-K, 20-F, 6-K, DEF 14A
        filed_at:
          type: string
        report_date:
          type:
            - string
            - "null"
        description:
          type:
            - string
            - "null"
        url:
          type: string
          description: Documento principal no site da SEC (EDGAR)
      required:
        - accession
        - form
        - filed_at
        - report_date
        - description
        - url
    SearchResult:
      type: object
      properties:
        kind:
          type: string
          enum:
            - stock
            - fii
            - fiagro
            - index
            - bond
            - macro
            - crypto
            - us
            - etf
            - bdr
            - debenture
            - cri
            - cra
            - lci
            - lca
        ticker:
          type: string
        title:
          type: string
        subtitle:
          type:
            - string
            - "null"
        href:
          type: string
        score:
          type: number
        cvm_code:
          type:
            - integer
            - "null"
          description: Código CVM da companhia (só ações) — abre documentos e demonstrações
      required:
        - kind
        - ticker
        - title
        - subtitle
        - href
        - score
        - cvm_code
    IngestRunSummary:
      type: object
      properties:
        run_id:
          type: string
        trigger:
          type: string
        started_at:
          type: string
        finished_at:
          type: string
        duration_s:
          type: number
        exit:
          type: number
        ok:
          type: boolean
        error_count:
          type: number
        error_sources:
          type: array
          items:
            type: string
          description: "QUAIS FONTES falharam neste run. `ok: false` fala do RUN inteiro,
            e um run vermelho por causa de uma série do Banco Central não diz
            nada sobre ofertas da CVM — mas quem lê só `ok` conclui que a base
            está quebrada e recusa uma pergunta que ela responde. Aconteceu numa
            sondagem em 20/08/2026. Cruze com `sources[]` para a saúde da fonte
            que te interessa."
      required:
        - run_id
        - trigger
        - started_at
        - finished_at
        - duration_s
        - exit
        - ok
        - error_count
        - error_sources
    IngestRunDetail:
      allOf:
        - $ref: "#/components/schemas/IngestRunSummary"
      properties:
        errors:
          type: array
          items:
            type: string
      required:
        - errors
    IngestSourceHealth:
      type: object
      properties:
        source:
          type: string
        status:
          type: string
          enum:
            - ok
            - incomplete
            - stale
            - error
            - no_data
        last_fetch:
          type:
            - string
            - "null"
        age_days:
          type:
            - number
            - "null"
        datasets:
          type: number
        missing:
          type: number
        missing_ratio:
          type:
            - number
            - "null"
        unavailable:
          type: number
        failed_validation:
          type: number
        expected_max_age_days:
          type:
            - number
            - "null"
        stale_after_days:
          type: number
        ok:
          type: number
        skip:
          type: number
        miss:
          type: number
        err:
          type: number
        rows:
          type: number
        duration_s:
          type:
            - number
            - "null"
      required:
        - source
        - status
        - last_fetch
        - age_days
        - datasets
        - missing
        - missing_ratio
        - unavailable
        - failed_validation
        - expected_max_age_days
        - stale_after_days
        - ok
        - skip
        - miss
        - err
        - rows
        - duration_s
    IngestPollerHealth:
      type: object
      properties:
        source:
          type: string
        status:
          type: string
          enum:
            - ok
            - sleeping
            - stale
            - error
        last_cycle_at:
          type: string
        last_write_at:
          type:
            - string
            - "null"
        age_seconds:
          type: number
        cycle_seconds:
          type: number
        sleeping_until:
          type:
            - string
            - "null"
        in_window:
          type: boolean
        universe_size:
          type:
            - number
            - "null"
        rows_written:
          type: number
        consecutive_errors:
          type: number
        last_error:
          type:
            - string
            - "null"
        last_error_at:
          type:
            - string
            - "null"
      required:
        - source
        - status
        - last_cycle_at
        - last_write_at
        - age_seconds
        - cycle_seconds
        - sleeping_until
        - in_window
        - universe_size
        - rows_written
        - consecutive_errors
        - last_error
        - last_error_at
    ThesisSummary:
      type: object
      properties:
        id:
          type: string
        slug:
          type: string
          description: Slug estável por usuário — compõe a URL pública
            /teses/{handle}/{slug}.
        title:
          type: string
        subtitle:
          type: string
          nullable: true
        ticker:
          type: string
          nullable: true
        kind:
          type: string
          description: equity | fii | macro | setor | carta | carteira
        visibility:
          type: string
          enum:
            - private
            - unlisted
            - public
        exportStatus:
          type: string
          description: idle | queued | processing | ready | failed
        pdfUrl:
          type: string
          nullable: true
        previewUrl:
          type: string
          nullable: true
        publishedAt:
          type: string
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string
        archivedAt:
          type: string
          nullable: true
          description: Arquivada (some do público) quando preenchido.
        position:
          type: number
          nullable: true
          description: Ordem manual na vitrine (null = por data).
        tags:
          type: array
          items:
            type: string
          description: Rótulos privados de organização (só o dono vê).
    ThesisDetail:
      allOf:
        - $ref: "#/components/schemas/ThesisSummary"
        - type: object
          properties:
            md:
              type: string
              description: Markdown da tese — é este o campo para ler e editar. Teses criadas
                antes do markdown não têm fonte gravada e vêm serializadas do
                próprio documento, então `md` sempre existe.
            doc:
              allOf:
                - $ref: "#/components/schemas/ReportDocInput"
              description: ReportDoc compilado do `md`. Só presente quando pedido com
                `include=doc` — é conteúdo derivado, e numa leitura por agente
                dobra o payload sem acrescentar informação.
            exportError:
              type: string
              nullable: true
    ThesisWarning:
      type: object
      description: Algo que degradou na compilação do markdown sem impedir a tese de
        existir.
      properties:
        line:
          type: number
          description: Linha do markdown onde está o problema.
        at:
          type: string
          description: "Construção afetada: db:chart, table, callout, frontmatter…"
        message:
          type: string
          description: O que houve, com o caminho do campo quando aplicável.
    ThesisExportStatus:
      type: object
      properties:
        status:
          type: string
          enum:
            - ready
            - queued
        pdfUrl:
          type: string
        previewUrl:
          type: string
    ReportDocInput:
      type: object
      properties:
        id:
          type: string
          pattern: ^[a-z0-9][a-z0-9-]*$
        kind:
          type: string
          enum:
            - equity
            - fii
            - etf
            - bdr
            - fundo
            - credito
            - tesouro
            - cripto
            - global
            - macro
            - setor
            - carteira
            - carta
            - outros
        title:
          type: string
          maxLength: 120
        subtitle:
          type: string
          maxLength: 160
        date:
          type: string
          pattern: ^\d{4}-\d{2}-\d{2}$
        authors:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                maxLength: 60
              role:
                type: string
                maxLength: 60
              credential:
                type: string
                maxLength: 30
            required:
              - name
            additionalProperties: false
          minItems: 1
          maxItems: 4
        ticker:
          type: string
          maxLength: 12
        tags:
          type: array
          items:
            type: string
            maxLength: 24
          maxItems: 8
        sections:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                pattern: ^[a-z0-9][a-z0-9-]*$
                description: âncora p/ TOC/deep-link
              title:
                type: string
                maxLength: 80
              numbered:
                type: boolean
              blocks:
                type: array
                items:
                  oneOf:
                    - oneOf:
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - rating
                            rating:
                              type: string
                              enum:
                                - compra
                                - neutro
                                - venda
                                - em revisão
                            priceTarget:
                              type: object
                              properties:
                                value:
                                  type: string
                                  maxLength: 20
                                horizon:
                                  type: string
                                  maxLength: 20
                              required:
                                - value
                                - horizon
                              additionalProperties: false
                            lastPrice:
                              type: string
                              maxLength: 20
                            upside:
                              type: number
                              description: variação % crua (a viz põe ▲/▼ e cor)
                            prev:
                              type: object
                              properties:
                                rating:
                                  type: string
                                  maxLength: 20
                                priceTarget:
                                  type: string
                                  maxLength: 20
                              additionalProperties: false
                              description: 'revisão: "de Neutro", "de R$ 41,00"'
                          required:
                            - kind
                            - rating
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - summary
                            title:
                              type: string
                              maxLength: 60
                            points:
                              type: array
                              items:
                                type: object
                                properties:
                                  text:
                                    type: string
                                    maxLength: 280
                                    description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                                  tone:
                                    type: string
                                    enum:
                                      - pos
                                      - neg
                                      - neutral
                                required:
                                  - text
                                additionalProperties: false
                              minItems: 2
                              maxItems: 8
                          required:
                            - kind
                            - points
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - keyStats
                            title:
                              type: string
                              maxLength: 60
                            stats:
                              type: array
                              items:
                                type: object
                                properties:
                                  label:
                                    type: string
                                    maxLength: 40
                                  value:
                                    type: string
                                    maxLength: 20
                                  change:
                                    type: number
                                    description: variação % crua (a viz põe ▲/▼ e cor)
                                  hint:
                                    type: string
                                    maxLength: 60
                                required:
                                  - label
                                  - value
                                additionalProperties: false
                              minItems: 2
                              maxItems: 10
                            cols:
                              anyOf:
                                - type: number
                                  enum:
                                    - 3
                                - type: number
                                  enum:
                                    - 4
                                - type: number
                                  enum:
                                    - 5
                          required:
                            - kind
                            - stats
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - prose
                            md:
                              type: string
                              minLength: 1
                              description: markdown GFM completo (sem h1 — título é da seção)
                          required:
                            - kind
                            - md
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - chart
                            spec:
                              type: object
                              properties:
                                type:
                                  type: string
                                  enum:
                                    - line
                                    - area
                                    - bars
                                    - stackedBars
                                    - netBars
                                    - sparkline
                                    - donut
                                title:
                                  type: string
                                  maxLength: 90
                                source:
                                  type: string
                                  maxLength: 90
                                  description: 'vira "Fonte: …" sob o gráfico'
                                height:
                                  type: integer
                                  minimum: 60
                                  maximum: 480
                                series:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      name:
                                        type: string
                                        maxLength: 40
                                      data:
                                        type: array
                                        items:
                                          type: number
                                        minItems: 1
                                      color:
                                        type: string
                                        enum:
                                          - accent
                                          - alta
                                          - baixa
                                          - ink
                                          - warn
                                          - class-acao
                                          - class-fii
                                          - class-bdr
                                          - class-tesouro
                                          - class-rf
                                          - class-cripto
                                          - class-outros
                                    required:
                                      - name
                                      - data
                                    additionalProperties: false
                                  minItems: 1
                                  maxItems: 8
                                  description: "donut: 1 série por fatia, data = [valor]; demais: séries com ≥2
                                    pontos"
                                labels:
                                  type: array
                                  items:
                                    type: string
                                  description: rótulos do eixo x, pareados com series[].data
                                yFormat:
                                  type: string
                                  enum:
                                    - num
                                    - pct
                                    - brl
                                refLine:
                                  type: object
                                  properties:
                                    value:
                                      type: number
                                    label:
                                      type: string
                                      maxLength: 40
                                  required:
                                    - value
                                  additionalProperties: false
                                up:
                                  type: boolean
                              required:
                                - type
                                - series
                              additionalProperties: false
                          required:
                            - kind
                            - spec
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - table
                            title:
                              type: string
                              maxLength: 80
                            note:
                              type: string
                              maxLength: 200
                              description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                            columns:
                              type: array
                              items:
                                type: object
                                properties:
                                  label:
                                    type: string
                                    maxLength: 30
                                  align:
                                    type: string
                                    enum:
                                      - left
                                      - right
                                      - center
                                  kind:
                                    type: string
                                    enum:
                                      - text
                                      - num
                                      - delta
                                      - deltaPp
                                required:
                                  - label
                                additionalProperties: false
                              minItems: 2
                              maxItems: 8
                            rows:
                              type: array
                              items:
                                type: array
                                items:
                                  anyOf:
                                    - type: string
                                    - type: number
                              minItems: 1
                              maxItems: 40
                            highlightRow:
                              type: integer
                              minimum: 0
                          required:
                            - kind
                            - columns
                            - rows
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - callout
                            tone:
                              type: string
                              enum:
                                - risco
                                - tese
                                - catalisador
                                - atenção
                                - info
                            title:
                              type: string
                              maxLength: 80
                            body:
                              type: string
                              maxLength: 600
                              description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                            items:
                              type: array
                              items:
                                type: string
                                maxLength: 200
                                description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                              maxItems: 8
                          required:
                            - kind
                            - tone
                            - body
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - scenarios
                            title:
                              type: string
                              maxLength: 60
                            cases:
                              type: array
                              items:
                                type: object
                                properties:
                                  label:
                                    type: string
                                    maxLength: 30
                                  probability:
                                    type: string
                                    maxLength: 10
                                  target:
                                    type: string
                                    maxLength: 20
                                  upside:
                                    type: number
                                    description: variação % crua (a viz põe ▲/▼ e cor)
                                  summary:
                                    type: string
                                    maxLength: 320
                                required:
                                  - label
                                  - summary
                                additionalProperties: false
                              minItems: 2
                              maxItems: 4
                          required:
                            - kind
                            - cases
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - quote
                            text:
                              type: string
                              maxLength: 280
                            who:
                              type: string
                              maxLength: 60
                            role:
                              type: string
                              maxLength: 60
                          required:
                            - kind
                            - text
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - figure
                            src:
                              type: string
                            alt:
                              type: string
                              maxLength: 200
                            caption:
                              type: string
                              maxLength: 200
                            source:
                              type: string
                              maxLength: 90
                          required:
                            - kind
                            - src
                            - alt
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - sources
                            title:
                              type: string
                              maxLength: 60
                            items:
                              type: array
                              items:
                                type: object
                                properties:
                                  ref:
                                    type: string
                                    maxLength: 8
                                  label:
                                    type: string
                                    maxLength: 200
                                  href:
                                    type: string
                                required:
                                  - ref
                                  - label
                                additionalProperties: false
                              minItems: 1
                              maxItems: 24
                          required:
                            - kind
                            - items
                          additionalProperties: false
                        - type: object
                          properties:
                            kind:
                              type: string
                              enum:
                                - disclaimer
                            title:
                              type: string
                              maxLength: 60
                            paragraphs:
                              type: array
                              items:
                                type: string
                              minItems: 1
                              maxItems: 8
                          required:
                            - kind
                            - paragraphs
                          additionalProperties: false
                    - type: object
                      properties:
                        kind:
                          type: string
                          enum:
                            - cover
                        eyebrow:
                          type: string
                          maxLength: 60
                        headline:
                          type: string
                          maxLength: 140
                        sub:
                          type: string
                          maxLength: 280
                        hero:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - line
                                - area
                                - bars
                                - stackedBars
                                - netBars
                                - sparkline
                                - donut
                            title:
                              type: string
                              maxLength: 90
                            source:
                              type: string
                              maxLength: 90
                              description: 'vira "Fonte: …" sob o gráfico'
                            height:
                              type: integer
                              minimum: 60
                              maximum: 480
                            series:
                              type: array
                              items:
                                type: object
                                properties:
                                  name:
                                    type: string
                                    maxLength: 40
                                  data:
                                    type: array
                                    items:
                                      type: number
                                    minItems: 1
                                  color:
                                    type: string
                                    enum:
                                      - accent
                                      - alta
                                      - baixa
                                      - ink
                                      - warn
                                      - class-acao
                                      - class-fii
                                      - class-bdr
                                      - class-tesouro
                                      - class-rf
                                      - class-cripto
                                      - class-outros
                                required:
                                  - name
                                  - data
                                additionalProperties: false
                              minItems: 1
                              maxItems: 8
                              description: "donut: 1 série por fatia, data = [valor]; demais: séries com ≥2
                                pontos"
                            labels:
                              type: array
                              items:
                                type: string
                              description: rótulos do eixo x, pareados com series[].data
                            yFormat:
                              type: string
                              enum:
                                - num
                                - pct
                                - brl
                            refLine:
                              type: object
                              properties:
                                value:
                                  type: number
                                label:
                                  type: string
                                  maxLength: 40
                              required:
                                - value
                              additionalProperties: false
                            up:
                              type: boolean
                          required:
                            - type
                            - series
                          additionalProperties: false
                        meta:
                          type: array
                          items:
                            type: object
                            properties:
                              label:
                                type: string
                                maxLength: 30
                              value:
                                type: string
                                maxLength: 60
                            required:
                              - label
                              - value
                            additionalProperties: false
                          maxItems: 6
                      required:
                        - kind
                        - headline
                      additionalProperties: false
                    - type: object
                      properties:
                        kind:
                          type: string
                          enum:
                            - columns
                        ratio:
                          type: string
                          enum:
                            - 1-1
                            - 2-1
                            - 1-2
                        left:
                          type: array
                          items:
                            oneOf:
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - rating
                                  rating:
                                    type: string
                                    enum:
                                      - compra
                                      - neutro
                                      - venda
                                      - em revisão
                                  priceTarget:
                                    type: object
                                    properties:
                                      value:
                                        type: string
                                        maxLength: 20
                                      horizon:
                                        type: string
                                        maxLength: 20
                                    required:
                                      - value
                                      - horizon
                                    additionalProperties: false
                                  lastPrice:
                                    type: string
                                    maxLength: 20
                                  upside:
                                    type: number
                                    description: variação % crua (a viz põe ▲/▼ e cor)
                                  prev:
                                    type: object
                                    properties:
                                      rating:
                                        type: string
                                        maxLength: 20
                                      priceTarget:
                                        type: string
                                        maxLength: 20
                                    additionalProperties: false
                                    description: 'revisão: "de Neutro", "de R$ 41,00"'
                                required:
                                  - kind
                                  - rating
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - summary
                                  title:
                                    type: string
                                    maxLength: 60
                                  points:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        text:
                                          type: string
                                          maxLength: 280
                                          description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                                        tone:
                                          type: string
                                          enum:
                                            - pos
                                            - neg
                                            - neutral
                                      required:
                                        - text
                                      additionalProperties: false
                                    minItems: 2
                                    maxItems: 8
                                required:
                                  - kind
                                  - points
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - keyStats
                                  title:
                                    type: string
                                    maxLength: 60
                                  stats:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        label:
                                          type: string
                                          maxLength: 40
                                        value:
                                          type: string
                                          maxLength: 20
                                        change:
                                          type: number
                                          description: variação % crua (a viz põe ▲/▼ e cor)
                                        hint:
                                          type: string
                                          maxLength: 60
                                      required:
                                        - label
                                        - value
                                      additionalProperties: false
                                    minItems: 2
                                    maxItems: 10
                                  cols:
                                    anyOf:
                                      - type: number
                                        enum:
                                          - 3
                                      - type: number
                                        enum:
                                          - 4
                                      - type: number
                                        enum:
                                          - 5
                                required:
                                  - kind
                                  - stats
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - prose
                                  md:
                                    type: string
                                    minLength: 1
                                    description: markdown GFM completo (sem h1 — título é da seção)
                                required:
                                  - kind
                                  - md
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - chart
                                  spec:
                                    type: object
                                    properties:
                                      type:
                                        type: string
                                        enum:
                                          - line
                                          - area
                                          - bars
                                          - stackedBars
                                          - netBars
                                          - sparkline
                                          - donut
                                      title:
                                        type: string
                                        maxLength: 90
                                      source:
                                        type: string
                                        maxLength: 90
                                        description: 'vira "Fonte: …" sob o gráfico'
                                      height:
                                        type: integer
                                        minimum: 60
                                        maximum: 480
                                      series:
                                        type: array
                                        items:
                                          type: object
                                          properties:
                                            name:
                                              type: string
                                              maxLength: 40
                                            data:
                                              type: array
                                              items:
                                                type: number
                                              minItems: 1
                                            color:
                                              type: string
                                              enum:
                                                - accent
                                                - alta
                                                - baixa
                                                - ink
                                                - warn
                                                - class-acao
                                                - class-fii
                                                - class-bdr
                                                - class-tesouro
                                                - class-rf
                                                - class-cripto
                                                - class-outros
                                          required:
                                            - name
                                            - data
                                          additionalProperties: false
                                        minItems: 1
                                        maxItems: 8
                                        description: "donut: 1 série por fatia, data = [valor]; demais: séries com ≥2
                                          pontos"
                                      labels:
                                        type: array
                                        items:
                                          type: string
                                        description: rótulos do eixo x, pareados com series[].data
                                      yFormat:
                                        type: string
                                        enum:
                                          - num
                                          - pct
                                          - brl
                                      refLine:
                                        type: object
                                        properties:
                                          value:
                                            type: number
                                          label:
                                            type: string
                                            maxLength: 40
                                        required:
                                          - value
                                        additionalProperties: false
                                      up:
                                        type: boolean
                                    required:
                                      - type
                                      - series
                                    additionalProperties: false
                                required:
                                  - kind
                                  - spec
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - table
                                  title:
                                    type: string
                                    maxLength: 80
                                  note:
                                    type: string
                                    maxLength: 200
                                    description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                                  columns:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        label:
                                          type: string
                                          maxLength: 30
                                        align:
                                          type: string
                                          enum:
                                            - left
                                            - right
                                            - center
                                        kind:
                                          type: string
                                          enum:
                                            - text
                                            - num
                                            - delta
                                            - deltaPp
                                      required:
                                        - label
                                      additionalProperties: false
                                    minItems: 2
                                    maxItems: 8
                                  rows:
                                    type: array
                                    items:
                                      type: array
                                      items:
                                        anyOf:
                                          - type: string
                                          - type: number
                                    minItems: 1
                                    maxItems: 40
                                  highlightRow:
                                    type: integer
                                    minimum: 0
                                required:
                                  - kind
                                  - columns
                                  - rows
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - callout
                                  tone:
                                    type: string
                                    enum:
                                      - risco
                                      - tese
                                      - catalisador
                                      - atenção
                                      - info
                                  title:
                                    type: string
                                    maxLength: 80
                                  body:
                                    type: string
                                    maxLength: 600
                                    description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                                  items:
                                    type: array
                                    items:
                                      type: string
                                      maxLength: 200
                                      description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                                    maxItems: 8
                                required:
                                  - kind
                                  - tone
                                  - body
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - scenarios
                                  title:
                                    type: string
                                    maxLength: 60
                                  cases:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        label:
                                          type: string
                                          maxLength: 30
                                        probability:
                                          type: string
                                          maxLength: 10
                                        target:
                                          type: string
                                          maxLength: 20
                                        upside:
                                          type: number
                                          description: variação % crua (a viz põe ▲/▼ e cor)
                                        summary:
                                          type: string
                                          maxLength: 320
                                      required:
                                        - label
                                        - summary
                                      additionalProperties: false
                                    minItems: 2
                                    maxItems: 4
                                required:
                                  - kind
                                  - cases
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - quote
                                  text:
                                    type: string
                                    maxLength: 280
                                  who:
                                    type: string
                                    maxLength: 60
                                  role:
                                    type: string
                                    maxLength: 60
                                required:
                                  - kind
                                  - text
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - figure
                                  src:
                                    type: string
                                  alt:
                                    type: string
                                    maxLength: 200
                                  caption:
                                    type: string
                                    maxLength: 200
                                  source:
                                    type: string
                                    maxLength: 90
                                required:
                                  - kind
                                  - src
                                  - alt
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - sources
                                  title:
                                    type: string
                                    maxLength: 60
                                  items:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        ref:
                                          type: string
                                          maxLength: 8
                                        label:
                                          type: string
                                          maxLength: 200
                                        href:
                                          type: string
                                      required:
                                        - ref
                                        - label
                                      additionalProperties: false
                                    minItems: 1
                                    maxItems: 24
                                required:
                                  - kind
                                  - items
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - disclaimer
                                  title:
                                    type: string
                                    maxLength: 60
                                  paragraphs:
                                    type: array
                                    items:
                                      type: string
                                    minItems: 1
                                    maxItems: 8
                                required:
                                  - kind
                                  - paragraphs
                                additionalProperties: false
                          minItems: 1
                          maxItems: 4
                        right:
                          type: array
                          items:
                            oneOf:
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - rating
                                  rating:
                                    type: string
                                    enum:
                                      - compra
                                      - neutro
                                      - venda
                                      - em revisão
                                  priceTarget:
                                    type: object
                                    properties:
                                      value:
                                        type: string
                                        maxLength: 20
                                      horizon:
                                        type: string
                                        maxLength: 20
                                    required:
                                      - value
                                      - horizon
                                    additionalProperties: false
                                  lastPrice:
                                    type: string
                                    maxLength: 20
                                  upside:
                                    type: number
                                    description: variação % crua (a viz põe ▲/▼ e cor)
                                  prev:
                                    type: object
                                    properties:
                                      rating:
                                        type: string
                                        maxLength: 20
                                      priceTarget:
                                        type: string
                                        maxLength: 20
                                    additionalProperties: false
                                    description: 'revisão: "de Neutro", "de R$ 41,00"'
                                required:
                                  - kind
                                  - rating
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - summary
                                  title:
                                    type: string
                                    maxLength: 60
                                  points:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        text:
                                          type: string
                                          maxLength: 280
                                          description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                                        tone:
                                          type: string
                                          enum:
                                            - pos
                                            - neg
                                            - neutral
                                      required:
                                        - text
                                      additionalProperties: false
                                    minItems: 2
                                    maxItems: 8
                                required:
                                  - kind
                                  - points
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - keyStats
                                  title:
                                    type: string
                                    maxLength: 60
                                  stats:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        label:
                                          type: string
                                          maxLength: 40
                                        value:
                                          type: string
                                          maxLength: 20
                                        change:
                                          type: number
                                          description: variação % crua (a viz põe ▲/▼ e cor)
                                        hint:
                                          type: string
                                          maxLength: 60
                                      required:
                                        - label
                                        - value
                                      additionalProperties: false
                                    minItems: 2
                                    maxItems: 10
                                  cols:
                                    anyOf:
                                      - type: number
                                        enum:
                                          - 3
                                      - type: number
                                        enum:
                                          - 4
                                      - type: number
                                        enum:
                                          - 5
                                required:
                                  - kind
                                  - stats
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - prose
                                  md:
                                    type: string
                                    minLength: 1
                                    description: markdown GFM completo (sem h1 — título é da seção)
                                required:
                                  - kind
                                  - md
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - chart
                                  spec:
                                    type: object
                                    properties:
                                      type:
                                        type: string
                                        enum:
                                          - line
                                          - area
                                          - bars
                                          - stackedBars
                                          - netBars
                                          - sparkline
                                          - donut
                                      title:
                                        type: string
                                        maxLength: 90
                                      source:
                                        type: string
                                        maxLength: 90
                                        description: 'vira "Fonte: …" sob o gráfico'
                                      height:
                                        type: integer
                                        minimum: 60
                                        maximum: 480
                                      series:
                                        type: array
                                        items:
                                          type: object
                                          properties:
                                            name:
                                              type: string
                                              maxLength: 40
                                            data:
                                              type: array
                                              items:
                                                type: number
                                              minItems: 1
                                            color:
                                              type: string
                                              enum:
                                                - accent
                                                - alta
                                                - baixa
                                                - ink
                                                - warn
                                                - class-acao
                                                - class-fii
                                                - class-bdr
                                                - class-tesouro
                                                - class-rf
                                                - class-cripto
                                                - class-outros
                                          required:
                                            - name
                                            - data
                                          additionalProperties: false
                                        minItems: 1
                                        maxItems: 8
                                        description: "donut: 1 série por fatia, data = [valor]; demais: séries com ≥2
                                          pontos"
                                      labels:
                                        type: array
                                        items:
                                          type: string
                                        description: rótulos do eixo x, pareados com series[].data
                                      yFormat:
                                        type: string
                                        enum:
                                          - num
                                          - pct
                                          - brl
                                      refLine:
                                        type: object
                                        properties:
                                          value:
                                            type: number
                                          label:
                                            type: string
                                            maxLength: 40
                                        required:
                                          - value
                                        additionalProperties: false
                                      up:
                                        type: boolean
                                    required:
                                      - type
                                      - series
                                    additionalProperties: false
                                required:
                                  - kind
                                  - spec
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - table
                                  title:
                                    type: string
                                    maxLength: 80
                                  note:
                                    type: string
                                    maxLength: 200
                                    description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                                  columns:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        label:
                                          type: string
                                          maxLength: 30
                                        align:
                                          type: string
                                          enum:
                                            - left
                                            - right
                                            - center
                                        kind:
                                          type: string
                                          enum:
                                            - text
                                            - num
                                            - delta
                                            - deltaPp
                                      required:
                                        - label
                                      additionalProperties: false
                                    minItems: 2
                                    maxItems: 8
                                  rows:
                                    type: array
                                    items:
                                      type: array
                                      items:
                                        anyOf:
                                          - type: string
                                          - type: number
                                    minItems: 1
                                    maxItems: 40
                                  highlightRow:
                                    type: integer
                                    minimum: 0
                                required:
                                  - kind
                                  - columns
                                  - rows
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - callout
                                  tone:
                                    type: string
                                    enum:
                                      - risco
                                      - tese
                                      - catalisador
                                      - atenção
                                      - info
                                  title:
                                    type: string
                                    maxLength: 80
                                  body:
                                    type: string
                                    maxLength: 600
                                    description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                                  items:
                                    type: array
                                    items:
                                      type: string
                                      maxLength: 200
                                      description: "md inline: **negrito**, *itálico*, `código`, [link](url)"
                                    maxItems: 8
                                required:
                                  - kind
                                  - tone
                                  - body
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - scenarios
                                  title:
                                    type: string
                                    maxLength: 60
                                  cases:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        label:
                                          type: string
                                          maxLength: 30
                                        probability:
                                          type: string
                                          maxLength: 10
                                        target:
                                          type: string
                                          maxLength: 20
                                        upside:
                                          type: number
                                          description: variação % crua (a viz põe ▲/▼ e cor)
                                        summary:
                                          type: string
                                          maxLength: 320
                                      required:
                                        - label
                                        - summary
                                      additionalProperties: false
                                    minItems: 2
                                    maxItems: 4
                                required:
                                  - kind
                                  - cases
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - quote
                                  text:
                                    type: string
                                    maxLength: 280
                                  who:
                                    type: string
                                    maxLength: 60
                                  role:
                                    type: string
                                    maxLength: 60
                                required:
                                  - kind
                                  - text
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - figure
                                  src:
                                    type: string
                                  alt:
                                    type: string
                                    maxLength: 200
                                  caption:
                                    type: string
                                    maxLength: 200
                                  source:
                                    type: string
                                    maxLength: 90
                                required:
                                  - kind
                                  - src
                                  - alt
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - sources
                                  title:
                                    type: string
                                    maxLength: 60
                                  items:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        ref:
                                          type: string
                                          maxLength: 8
                                        label:
                                          type: string
                                          maxLength: 200
                                        href:
                                          type: string
                                      required:
                                        - ref
                                        - label
                                      additionalProperties: false
                                    minItems: 1
                                    maxItems: 24
                                required:
                                  - kind
                                  - items
                                additionalProperties: false
                              - type: object
                                properties:
                                  kind:
                                    type: string
                                    enum:
                                      - disclaimer
                                  title:
                                    type: string
                                    maxLength: 60
                                  paragraphs:
                                    type: array
                                    items:
                                      type: string
                                    minItems: 1
                                    maxItems: 8
                                required:
                                  - kind
                                  - paragraphs
                                additionalProperties: false
                          minItems: 1
                          maxItems: 4
                      required:
                        - kind
                        - left
                        - right
                      additionalProperties: false
                    - type: object
                      properties:
                        kind:
                          type: string
                          enum:
                            - pageBreak
                      required:
                        - kind
                      additionalProperties: false
                minItems: 1
            required:
              - id
              - blocks
            additionalProperties: false
          minItems: 1
          maxItems: 24
      required:
        - id
        - kind
        - title
        - date
        - authors
        - sections
      additionalProperties: false
      description: Documento de report estruturado, com seções e blocos discriminados
        por `kind`. O schema é gerado do mesmo validador que aceita escritas no
        servidor; erros voltam com o caminho do campo violado.
    PortfolioHolding:
      type: object
      description: Posição computada da carteira (snake_case, como todo o /v1).
        `asset_type` e `transactions[].trade_date` são os MESMOS campos do
        ledger (listPortfolioTransactions). Valores monetários sempre em BRL;
        ativos em moeda estrangeira (asset_type=us) expõem também currency,
        price_native (US$), fx_rate e o bloco `native` com a MESMA posição em
        dólar (custo, valor de mercado e P&L).
      properties:
        symbol:
          type: string
        asset_type:
          type: string
          description: stock | fii | etf | bdr | index | tesouro | crypto | option |
            renda_fixa | debenture | fund | us. `debenture` = crédito privado
            com catálogo próprio (símbolo = código do papel); `fund` = fundo CVM
            175 (símbolo = CNPJ da classe, 14 dígitos).
        name:
          type: string
        qty:
          type: number
        avg_price:
          type:
            - number
            - "null"
          description: Preço médio de custo (null = custo desconhecido/watchlist).
        invested_cost:
          type:
            - number
            - "null"
        cost_basis_known:
          type: boolean
          description: true quando todos os buy/sell têm preço → P&L confiável.
        price:
          type:
            - number
            - "null"
          description: Cotação atual em BRL (null = sem cotação).
        change_pct:
          type:
            - number
            - "null"
          description: Variação do dia em BRL. Em posição estrangeira é o COMPOSTO do
            retorno do ativo com o do câmbio, (1+ativo)×(1+câmbio)−1 — a
            variação só do ativo, na moeda dele, está em native.change_pct.
        market_value:
          type:
            - number
            - "null"
        unrealized_pl:
          type:
            - number
            - "null"
          description: Resultado não-realizado em R$. Com valuation=accrual (renda fixa
            privada) é RENDIMENTO acruado pela taxa contratada, não marcação a
            mercado.
        unrealized_pct:
          type:
            - number
            - "null"
          description: Percentual sobre o custo. null quando o percentual é indefinido —
            veja unrealized_pct_reason.
        unrealized_pct_reason:
          type:
            - string
            - "null"
          enum:
            - zero_cost
            - cost_unknown
            - null
          description: "Por que unrealized_pct é null numa posição aberta e valorada:
            zero_cost = custo abaixo de um centavo (resíduo de fusão/bonificação
            — o percentual é indefinido, não 0 nem centenas de por cento);
            cost_unknown = há buy/sell sem preço no ledger. null = percentual
            definido."
        valuation:
          type:
            - string
            - "null"
          enum:
            - market
            - accrual
            - cost
            - null
          description: "Como a posição foi valorada (linhagem do número): market = cotação
            (para debênture, o PU do secundário com no máx. ~30 dias); accrual =
            renda fixa/debênture acruada pela taxa contratada (contrato do
            usuário ou pré-preenchido do catálogo — veja meta.rf.source); cost =
            sem cotação, marcada pelo custo remanescente; null = sem valor de
            mercado."
        realized_pl:
          type:
            - number
            - "null"
        realized_by_month:
          type: object
          additionalProperties:
            type: number
          description: "'AAAA-MM' → R$ realizado no mês. Só presente com `include=monthly`."
        income_received:
          type: number
          description: Proventos recebidos acumulados (R$).
        income_by_month:
          type: object
          additionalProperties:
            type: number
          description: "'AAAA-MM' → R$ de proventos no mês. Só presente com
            `include=monthly`."
        weight_pct:
          type:
            - number
            - "null"
        day_change_brl:
          type:
            - number
            - "null"
        added_at:
          type:
            - string
            - "null"
          description: Data da 1ª transação (AAAA-MM-DD).
        tx_count:
          type: integer
          description: Nº de transações da posição.
        closed:
          type: boolean
          description: qty = 0 com transações (posição encerrada).
        is_watchlist:
          type: boolean
          description: Entrada sem nenhuma transação (radar).
        priced:
          type: boolean
        renamed_to:
          type:
            - string
            - "null"
          description: Ticker vigente quando o papel foi renomeado/incorporado.
        merged_from:
          type:
            - array
            - "null"
          items:
            type: string
          description: "Tickers antigos fundidos nesta posição por sucessão (ex.:
            ['BCFF11'] num holding BTHF11). O ledger em `transactions` já inclui
            as transações dos predecessores. null = nenhum."
        currency:
          type: string
          description: Moeda nativa de negociação (BRL | USD).
        price_native:
          type:
            - number
            - "null"
          description: Cotação na moeda nativa quando ≠ BRL.
        fx_rate:
          type:
            - number
            - "null"
          description: Câmbio usado na marcação (price = price_native × fx_rate).
        native:
          $ref: "#/components/schemas/PortfolioHoldingNative"
        asset_id:
          type:
            - string
            - "null"
          description: Id do ativo no ledger (correlaciona com /assets e /transactions).
        meta:
          description: "portfolio_assets.meta (ex.: { rf: { indexer, rate } })."
        transactions:
          type: array
          description: Ledger da posição, ordenado por trade_date. Só presente com
            `include=transactions`; o mesmo dado, por ativo, está em
            listPortfolioTransactions.
          items:
            $ref: "#/components/schemas/PortfolioTransaction"
      required:
        - symbol
        - asset_type
        - qty
    PortfolioHoldingNative:
      type:
        - object
        - "null"
      description: >-
        A MESMA posição na moeda em que ela é negociada, quando não é o real
        (hoje, asset_type=us em USD). null para o que negocia em BRL.


        NÃO é o bloco em BRL dividido por fx_rate: é o mesmo cálculo rodado
        sobre o ledger cru, porque cada compra foi convertida pela PTAX da SUA
        data. Por isso invested_cost é o que se pagou em dólar e unrealized_pct
        é o retorno do ATIVO, enquanto o unrealized_pct em BRL embute também a
        variação do câmbio no período. Os dois estão certos e respondem a
        perguntas diferentes.


        Não repete qty (idêntico), weight_pct (adimensional) nem day_change_brl
        (escopo BRL).
      properties:
        currency:
          type: string
          description: "Moeda da negociação (ex.: USD)."
        avg_price:
          type:
            - number
            - "null"
          description: Preço médio pago na moeda nativa.
        invested_cost:
          type:
            - number
            - "null"
        cost_basis_known:
          type: boolean
          description: "Pode divergir do cost_basis_known em BRL: numa data sem PTAX o
            custo em real fica desconhecido enquanto o em dólar segue
            conhecido."
        price:
          type:
            - number
            - "null"
          description: Cotação na moeda nativa (= price_native).
        change_pct:
          type:
            - number
            - "null"
          description: Variação do dia do ativo, sem o câmbio.
        market_value:
          type:
            - number
            - "null"
        unrealized_pl:
          type:
            - number
            - "null"
        unrealized_pct:
          type:
            - number
            - "null"
          description: Retorno do ativo, sem o efeito do câmbio.
        realized_pl:
          type:
            - number
            - "null"
        income_received:
          type: number
      required:
        - currency
    PortfolioFx:
      type:
        - object
        - "null"
      description: >-
        Câmbio USD/BRL aplicado na marcação a mercado das posições estrangeiras
        desta carteira. Publicado para a conversão ser conferível: os totais da
        carteira são BRL e dependem desta taxa. null quando a carteira não tem
        ativo em moeda estrangeira.


        Atenção: esta é a taxa SPOT, usada só na marcação. O custo de cada
        compra foi convertido pela PTAX da data do trade, então invested_cost ÷
        usd_brl não devolve o custo em dólar — para isso use
        holdings[].native.invested_cost.
      properties:
        usd_brl:
          type: number
          description: Reais por dólar.
        as_of:
          type: string
          description: Timestamp da taxa. Com source=usdt é ISO 8601 completo (o par é
            atualizado a cada minuto); com source=bcb é a data do pregão
            (AAAA-MM-DD).
        source:
          type: string
          enum:
            - usdt
            - bcb
          description: usdt = par USDT/BRL, atualizado 24/7 e fresco mesmo fora do horário
            de pregão; bcb = PTAX de venda do BCB (série 1), último fechamento —
            o fallback.
        change_pct:
          type:
            - number
            - "null"
          description: Variação da PRÓPRIA fonte acima (24h do par × pregão a pregão da
            PTAX), nunca uma mistura das duas. Compõe change_pct das posições
            estrangeiras.
      required:
        - usd_brl
        - as_of
        - source
    PortfolioByCarteira:
      type: object
      properties:
        name:
          type: string
        patrimonio:
          type: number
        unrealized_pct:
          type: number
        open_positions:
          type: integer
          description: Posições com quantidade > 0 nesta carteira.
        watchlist_entries:
          type: integer
          description: Entradas sem nenhuma transação (radar) nesta carteira.
      required:
        - name
        - patrimonio
        - unrealized_pct
        - open_positions
        - watchlist_entries
    PortfolioListItem:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        ledger_assets:
          type: integer
          description: Nº de LINHAS DE ATIVO no ledger da carteira. Inclui posições já
            encerradas e tickers predecessores fundidos por sucessão, que não
            têm holding próprio — por isso é ≥ holdings_count do detalhe, que
            por sua vez é ≥ open_positions.
        created_at:
          type: string
          format: date-time
        exclude_from_consolidated:
          type: boolean
          description: true = carteira de simulação.
      required:
        - id
        - name
    PortfolioDetail:
      type: object
      description: "Carteira computada: posições derivadas do ledger + métricas
        agregadas (snake_case)."
      properties:
        id:
          type: string
        name:
          type: string
        visibility:
          type: string
        created_at:
          type:
            - string
            - "null"
        exclude_from_consolidated:
          type: boolean
          description: true = carteira de simulação.
        holdings_count:
          type: integer
          description: "Linhas devolvidas em holdings[]. Default: abertas + watchlist; com
            `include=closed` soma também as encerradas."
        open_positions:
          type: integer
          description: Posições com quantidade > 0.
        closed_positions:
          type: integer
          description: Posições zeradas por venda (qty = 0 com transações).
        watchlist_entries:
          type: integer
          description: Entradas sem nenhuma transação (radar).
        merged_assets:
          type: integer
          description: Tickers predecessores fundidos nas posições por sucessão. Contam em
            ledger_assets (listPortfolios) e não em holdings_count — é a
            diferença entre as duas contagens.
        patrimonio:
          type: number
        invested:
          type: number
        unrealized_pl:
          type: number
        unrealized_pct:
          type: number
        realized_pl:
          type: number
        realized_by_month:
          type: object
          additionalProperties:
            type: number
        income_received:
          type: number
        income_by_month:
          type: object
          additionalProperties:
            type: number
        total_pl:
          type: number
          description: unrealized_pl + realized_pl.
        xirr:
          type:
            - number
            - "null"
          description: "XIRR (TIR anualizada, money-weighted): o retorno REAL do
            investidor sobre os fluxos datados (aportes/resgates/proventos) +
            patrimônio a mercado como valor terminal. Decimal a.a. (0,1423 =
            14,23%). null quando custo desconhecido, fluxo sem TIR definível ou
            implausível (> +300% a.a., artefato de giro/curto prazo → use o
            twr). Mesmo valor de returns.xirr, onde ele aparece ao lado do
            time-weighted."
        returns:
          $ref: "#/components/schemas/PortfolioReturns"
        day_change_brl:
          type: number
        day_change_pct:
          type: number
        kind_mix:
          type: string
          description: "Resumo humano, ex.: '3 ações · 2 FIIs'."
        is_watchlist:
          type: boolean
        any_unpriced:
          type: boolean
        any_cost_unknown:
          type: boolean
        fx:
          $ref: "#/components/schemas/PortfolioFx"
        holdings:
          type: array
          description: "Posições com qty, preço médio, valor de mercado, P&L, proventos e
            peso. Default compacto: só abertas + watchlist (posições encerradas
            exigem `include=closed`) e sem as séries mensais por posição
            (`include=monthly`)."
          items:
            $ref: "#/components/schemas/PortfolioHolding"
    PortfolioTransaction:
      type: object
      description: Transação do ledger. É o MESMO shape das transações embutidas em
        PortfolioHolding.transactions.
      properties:
        id:
          type: string
        kind:
          type: string
          description: buy | sell | split | adjust
        trade_date:
          type: string
          description: AAAA-MM-DD
        quantity:
          type: number
        price:
          type:
            - number
            - "null"
          description: Preço unitário (null = custo desconhecido).
        fees:
          type: number
        ratio:
          type:
            - number
            - "null"
          description: Fator do split (só kind=split).
        currency:
          type: string
          description: "Moeda de price/fees DESTA linha. O ledger é devolvido CRU, como
            lançado: o de um ativo americano vem em USD e NÃO passou pela
            conversão que as posições sofrem. Sem ler este campo o consumidor
            assume real e exibe uma compra de US$ 178,40 como R$ 178,40."
        note:
          type:
            - string
            - "null"
        event_kind:
          type:
            - string
            - "null"
          description: "Subtipo do evento societário de origem quando a transação veio de
            evento (import da Movimentação B3): split | bonus | incorporation |
            subscription | fraction. null = lançamento comum."
      required:
        - id
        - kind
        - trade_date
        - quantity
    PortfolioTwr:
      type:
        - object
        - "null"
      description: TWR (time-weighted return) derivado da série mensal — neutraliza o
        efeito do timing dos aportes, o número comparável a benchmark
        (CDI/IBOV/IPCA). null quando não há período válido.
      properties:
        cumulative:
          type: number
          description: Retorno acumulado na janela (decimal; 0,25 = +25%).
        annualized:
          type:
            - number
            - "null"
          description: Equivalente anualizado (decimal).
        months:
          type: integer
          description: Meses na grade.
    PortfolioReturns:
      type: object
      description: As DUAS medidas de retorno da carteira no mesmo objeto. Elas
        divergem sempre que os aportes não são uniformes — é esperado, não é
        erro, e `explain` diz isso em uma linha.
      properties:
        xirr:
          type:
            - number
            - "null"
          description: Money-weighted anualizada (decimal a.a.).
        twr:
          $ref: "#/components/schemas/PortfolioTwr"
        explain:
          type: string
          description: Nota curta sobre a diferença entre as duas medidas.
    PortfolioHistoryResponse:
      type: object
      properties:
        data:
          type: array
          items:
            type: object
            description: "Um mês: patrimônio, aporte acumulado, proventos e realizado."
        meta:
          type: object
          properties:
            next_cursor:
              type:
                - string
                - "null"
              description: "Sempre null: resposta em página única."
            count:
              type: integer
              description: Itens nesta página.
            twr:
              $ref: "#/components/schemas/PortfolioTwr"
          required:
            - next_cursor
            - count
      required:
        - data
        - meta
    PortfolioHistoryDetailResponse:
      type: object
      description: "Histórico de UMA carteira: a série mensal em `data`, o TWR e a
        nota que o separa da xirr do detalhe em `meta`."
      properties:
        data:
          type: array
          items:
            type: object
            description: "Um mês: patrimônio, aporte acumulado, proventos e realizado."
        meta:
          type: object
          properties:
            next_cursor:
              type:
                - string
                - "null"
              description: "Sempre null: resposta em página única."
            count:
              type: integer
              description: Itens nesta página.
            twr:
              $ref: "#/components/schemas/PortfolioTwr"
            returns_explain:
              type: string
              description: Por que o twr daqui difere da xirr do detalhe da carteira
                (time-weighted × money-weighted).
          required:
            - next_cursor
            - count
      required:
        - data
        - meta
    PortfolioXraySlice:
      type: object
      properties:
        value_brl:
          type: number
          description: Valor cru em BRL.
        pct:
          type: number
          description: Percentual 0-100 (em by_indexer, sobre o total de renda fixa).
      required:
        - value_brl
        - pct
    PortfolioXray:
      type: object
      description: "Raio-X de concentração: agregações determinísticas + flags
        factuais. Só posições abertas com valor; arrays ordenados por valor
        (maiores primeiro)."
      properties:
        by_class:
          type: array
          items:
            allOf:
              - $ref: "#/components/schemas/PortfolioXraySlice"
              - type: object
                properties:
                  class:
                    type: string
                    description: stock | fii | etf | bdr | tesouro | renda_fixa | debenture | fund |
                      crypto | us | option
                required:
                  - class
        by_sector:
          type: array
          description: Só posições de bolsa (ações/FIIs/ETFs/BDRs); sem setor conhecido =
            'Sem classificação'.
          items:
            allOf:
              - $ref: "#/components/schemas/PortfolioXraySlice"
              - type: object
                properties:
                  sector:
                    type: string
                required:
                  - sector
        by_indexer:
          type: array
          description: Exposição da RENDA FIXA por indexador — pct sobre o total de renda
            fixa, não da carteira.
          items:
            allOf:
              - $ref: "#/components/schemas/PortfolioXraySlice"
              - type: object
                properties:
                  indexer:
                    type: string
                    description: selic | ipca | prefixado | cdi | outro
                required:
                  - indexer
        by_currency:
          type: array
          items:
            allOf:
              - $ref: "#/components/schemas/PortfolioXraySlice"
              - type: object
                properties:
                  currency:
                    type: string
                    description: BRL | USD
                required:
                  - currency
        top_positions:
          type: array
          description: As 10 maiores posições por valor.
          items:
            allOf:
              - $ref: "#/components/schemas/PortfolioXraySlice"
              - type: object
                properties:
                  symbol:
                    type: string
                  class:
                    type: string
                required:
                  - symbol
                  - class
        flags:
          type: array
          items:
            type: string
          description: Fatos pt-BR quando limites de concentração estouram (posição >10%,
            setor >25%, classe >60%, indexador >70% da RF, moeda estrangeira
            >30%). Sem opinião — cite como estão.
        meta:
          type: object
          properties:
            patrimonio_brl:
              type: number
              description: "Base dos percentuais: soma das posições consideradas."
            rf_total_brl:
              type: number
              description: Base dos percentuais de by_indexer.
            reference:
              type: string
              description: Data de referência da valoração (AAAA-MM-DD).
            positions_considered:
              type: integer
            unpriced_ignored:
              type: integer
              description: Posições abertas sem valor, fora do raio-X.
          required:
            - patrimonio_brl
            - rf_total_brl
            - reference
            - positions_considered
            - unpriced_ignored
      required:
        - by_class
        - by_sector
        - by_indexer
        - by_currency
        - top_positions
        - flags
        - meta
    PortfolioCosts:
      type: object
      description: "Raio-X de custos recorrentes: itens com destinatário e referência,
        agregação por destinatário, totais e o que ficou sem estimativa (com
        motivo). Estimativas determinísticas — não é recomendação."
      properties:
        items:
          type: array
          description: Custos estimados por posição, maiores primeiro.
          items:
            type: object
            properties:
              symbol:
                type: string
                description: CNPJ da classe (fund), nome do título (tesouro) ou código do papel
                  (renda_fixa).
              class:
                type: string
                description: fund | tesouro | renda_fixa
              name:
                type:
                  - string
                  - "null"
              value_brl:
                type: number
                description: Valor da posição (base do custo).
              cost_kind:
                type: string
                enum:
                  - fund_admin_fee
                  - tesouro_custody
                  - rf_cdi_gap
                description: fund_admin_fee = taxa de administração as-filed; tesouro_custody =
                  custódia B3; rf_cdi_gap = gap estimado de RF tributável
                  contratada abaixo de 100% do CDI.
              annual_cost_pct:
                type:
                  - number
                  - "null"
                description: Custo % a.a. efetivo sobre o valor (já líquido de isenção).
              annual_cost_brl:
                type: number
                description: Custo estimado em R$/ano.
              recipient:
                type: string
                description: Para quem o custo vai (administrador do fundo, B3,
                  emissor/distribuidor).
              benchmark:
                type:
                  - object
                  - "null"
                description: "Referência de comparação. Null = sem referência (ex.: custódia;
                  poucos pares)."
                properties:
                  label:
                    type: string
                    description: "Ex.: 'mediana dos pares (Renda Fixa)' | '100% do CDI'."
                  value_pct:
                    type: number
                  peers:
                    type: integer
                    description: Nº de pares da mediana (só em fund_admin_fee).
                required:
                  - label
                  - value_pct
              excess_cost_brl:
                type:
                  - number
                  - "null"
                description: Parcela ACIMA da referência (R$/ano). Null = sem referência.
              note:
                type:
                  - string
                  - "null"
                description: Fato adicional (taxa de performance no cadastro; regra de isenção;
                  base da estimativa).
            required:
              - symbol
              - class
              - name
              - value_brl
              - cost_kind
              - annual_cost_pct
              - annual_cost_brl
              - recipient
              - benchmark
              - excess_cost_brl
              - note
        not_estimated:
          type: array
          description: Posições SEM estimativa, agregadas por (classe, motivo) — o motivo
            é parte da resposta.
          items:
            type: object
            properties:
              class:
                type: string
              reason:
                type: string
              positions:
                type: integer
              symbols:
                type: array
                items:
                  type: string
                description: Até 10 símbolos de exemplo.
            required:
              - class
              - reason
              - positions
              - symbols
        by_recipient:
          type: array
          description: Custo anual agregado por destinatário, maiores primeiro.
          items:
            type: object
            properties:
              recipient:
                type: string
              annual_cost_brl:
                type: number
            required:
              - recipient
              - annual_cost_brl
        totals:
          type: object
          properties:
            annual_cost_brl:
              type: number
              description: Soma dos custos estimados (R$/ano).
            above_reference_brl:
              type: number
              description: Soma do que está acima das referências (mediana dos pares + gap vs
                CDI).
            pct_of_patrimonio:
              type:
                - number
                - "null"
              description: Custo anual como % do patrimônio considerado.
          required:
            - annual_cost_brl
            - above_reference_brl
            - pct_of_patrimonio
        flags:
          type: array
          items:
            type: string
          description: Fatos pt-BR (fundo acima da mediana dos pares; RF abaixo de 100% do
            CDI). Sem opinião — cite como estão.
        meta:
          type: object
          properties:
            patrimonio_brl:
              type: number
            reference:
              type: string
              description: Data de referência (AAAA-MM-DD).
            positions_considered:
              type: integer
            unpriced_ignored:
              type: integer
              description: Posições abertas sem valor, fora da estimativa.
            cdi_12m_pct:
              type:
                - number
                - "null"
              description: CDI acumulado 12m usado no gap de RF (%).
            caveat:
              type: string
              description: O que a estimativa cobre e o que fica de fora — repasse ao usuário.
          required:
            - patrimonio_brl
            - reference
            - positions_considered
            - unpriced_ignored
            - cdi_12m_pct
            - caveat
      required:
        - items
        - not_estimated
        - by_recipient
        - totals
        - flags
        - meta
    PortfolioLookThrough:
      type: object
      description: "Exposição efetiva: posições em fundos abertas na última carteira
        mensal divulgada de cada um (defasada — não é a posição de hoje).
        Exposição indireta é PISO: cobre só o que o fundo divulgou."
      properties:
        exposures:
          type: array
          description: Ativos alcançados VIA FUNDOS, com a posição direta ao lado (top 50
            por valor total).
          items:
            type: object
            properties:
              symbol:
                type: string
                description: Ticker (kind=stock) ou código da debênture (kind=debenture).
              kind:
                type: string
                enum:
                  - stock
                  - debenture
              direct_value_brl:
                type: number
                description: Posição direta da carteira no mesmo ativo (0 = só via fundos).
              indirect_value_brl:
                type: number
                description: Exposição proporcional via fundos.
              total_value_brl:
                type: number
              pct_of_portfolio:
                type:
                  - number
                  - "null"
                description: total_value_brl ÷ patrimônio, 0-100.
              via:
                type: array
                description: Por quais fundos DA CARTEIRA a exposição chega (2º nível de FoF
                  atribuído ao fundo direto).
                items:
                  type: object
                  properties:
                    fund_cnpj:
                      type: string
                    fund_name:
                      type:
                        - string
                        - "null"
                    value_brl:
                      type: number
                  required:
                    - fund_cnpj
                    - fund_name
                    - value_brl
            required:
              - symbol
              - kind
              - direct_value_brl
              - indirect_value_brl
              - total_value_brl
              - pct_of_portfolio
              - via
        funds_opened:
          type: array
          items:
            type: object
            properties:
              fund_cnpj:
                type: string
              fund_name:
                type:
                  - string
                  - "null"
              position_value_brl:
                type: number
                description: Valor da posição no fundo (BRL).
              comptc_date:
                type:
                  - string
                  - "null"
                description: Competência da carteira divulgada usada (AAAA-MM-DD).
              coverage_pct:
                type:
                  - number
                  - "null"
                description: Quanto do fundo os ativos divulgados explicam (0-100; pode passar
                  de 100 por descasamento de datas).
              method:
                type: string
                enum:
                  - pl
                  - disclosed_sum
                description: "Denominador dos pesos: patrimônio oficial na competência (pl) ou
                  soma dos ativos divulgados (disclosed_sum, quando o patrimônio
                  falta — coverage vira 100 por construção)."
            required:
              - fund_cnpj
              - fund_name
              - position_value_brl
              - comptc_date
              - coverage_pct
              - method
        not_opened:
          type: array
          description: Posições em fundo que NÃO puderam ser abertas (sem carteira
            divulgada ou sem valor).
          items:
            type: object
            properties:
              symbol:
                type: string
                description: CNPJ do fundo.
              fund_name:
                type:
                  - string
                  - "null"
              position_value_brl:
                type:
                  - number
                  - "null"
              reason:
                type: string
            required:
              - symbol
              - fund_name
              - position_value_brl
              - reason
        meta:
          type: object
          properties:
            patrimonio_brl:
              type: number
            comptc_dates:
              type: array
              items:
                type: string
              description: Competências das carteiras divulgadas usadas.
            exposures_truncated:
              type: integer
              description: Linhas além do top 50 (ausente quando não truncou).
            note:
              type: string
              description: Presente quando a carteira não tem fundos (resposta válida, vazia).
            caveat:
              type: string
              description: Limitação metodológica — repasse ao usuário quando relevante.
          required:
            - patrimonio_brl
            - comptc_dates
            - caveat
      required:
        - exposures
        - funds_opened
        - not_opened
        - meta
    PortfolioImportSummary:
      type: object
      description: Resumo de um import de planilha (o detalhe linha a linha fica em
        listPortfolioImportRows).
      properties:
        import_id:
          type: string
        source:
          type: string
          description: negociacao | movimentacao | manual (detectado pelo cabeçalho).
        parsed:
          type: integer
          description: Linhas entendidas no arquivo.
        imported:
          type: integer
          description: Lançamentos novos gravados.
        duplicates_skipped:
          type: integer
          description: Lançamentos já existentes (reimport) pulados.
        rows_ignored:
          type: integer
        cross_source_skipped:
          type: integer
          description: Trades pulados por já estarem cobertos por outra fonte.
        symbols:
          type: integer
        period:
          type: object
          properties:
            from:
              type:
                - string
                - "null"
            to:
              type:
                - string
                - "null"
        warnings:
          type: array
          items:
            type: string
          description: Até 10 avisos.
        warnings_truncated:
          type: integer
          description: Nº de avisos omitidos além dos 10 primeiros.
      required:
        - import_id
        - source
        - imported
    PortfolioImportListItem:
      type: object
      properties:
        id:
          type: string
        filename:
          type:
            - string
            - "null"
        source:
          type: string
        created_at:
          type: string
          format: date-time
        summary:
          type: object
        warnings:
          type: array
          items:
            type: string
      required:
        - id
        - source
    PortfolioImportRow:
      type: object
      properties:
        row_index:
          type: integer
        status:
          type: string
          description: imported | ignored | duplicate | error
        kind:
          type:
            - string
            - "null"
        label:
          type:
            - string
            - "null"
        reason:
          type:
            - string
            - "null"
        external_id:
          type:
            - string
            - "null"
        raw: {}
      required:
        - row_index
        - status
    PortfolioPosition:
      type: object
      description: Posição resumida do consolidado (o detalhe completo vem em
        getPortfolioDetail).
      properties:
        symbol:
          type: string
        asset_type:
          type: string
        qty:
          type: number
        weight_pct:
          type:
            - number
            - "null"
        market_value:
          type:
            - number
            - "null"
        unrealized_pct:
          type:
            - number
            - "null"
      required:
        - symbol
        - asset_type
        - qty
    PortfolioContext:
      type: object
      description: Carteira consolidada do usuário (mesmo resumo que o agente de IA usa).
      properties:
        has_portfolio:
          type: boolean
        carteiras:
          type: integer
        patrimonio:
          type: number
        invested:
          type: number
        unrealized_pl:
          type: number
        unrealized_pct:
          type: number
        realized_pl:
          type: number
        xirr:
          type:
            - number
            - "null"
          description: XIRR (TIR anualizada, money-weighted) do consolidado. Decimal a.a.
            (0,14 = 14%).
        day_change_brl:
          type: number
        kind_mix:
          type: string
        any_unpriced:
          type: boolean
        positions:
          type: array
          items:
            $ref: "#/components/schemas/PortfolioPosition"
          description: Todas as posições consolidadas (qty > 0), ordenadas por valor
            (maiores primeiro).
        by_carteira:
          type: array
          items:
            $ref: "#/components/schemas/PortfolioByCarteira"
      required:
        - has_portfolio
        - carteiras
        - patrimonio
        - invested
        - unrealized_pl
        - unrealized_pct
        - realized_pl
        - day_change_brl
        - kind_mix
        - any_unpriced
        - positions
        - by_carteira
    SuitabilityProfile:
      type: object
      properties:
        level:
          type: string
          enum:
            - Conservador
            - Moderado
            - Arrojado
        score:
          type: integer
          description: Posição na régua 0–100 (Conservador→Arrojado), não uma nota.
        answers:
          type: array
          items:
            type: object
            properties:
              q:
                type: string
              a:
                type: string
            required:
              - q
              - a
        updatedAt:
          type: string
      required:
        - level
        - score
        - answers
        - updatedAt
    SuitabilityEnvelope:
      type: object
      properties:
        profile:
          oneOf:
            - $ref: "#/components/schemas/SuitabilityProfile"
            - type: "null"
      required:
        - profile
    DocumentEntityType:
      type: string
      enum:
        - stock
        - fii
        - fiagro
        - issuer
        - securitization
        - fidc
      description: stock = companhia com ação negociada · fii = fundo imobiliário ·
        fiagro = fundo das cadeias produtivas agroindustriais · issuer =
        companhia registrada apenas para emitir dívida (não tem ticker) ·
        securitization = certificado CRI/CRA/OTS, identificado pelo ISIN · fidc
        = fundo de direitos creditórios.
    DocumentVersion:
      description: >-
        ONDE ESTE DOCUMENTO ESTÁ NA FILA DOS SEUS SEMELHANTES, ou o MOTIVO de
        não haver fila.


        Leia ANTES de citar. Uma companhia tem 7,1 estatutos e um fundo tem em
        média vários regulamentos — medido: 3.214 de 3.900 emissores têm mais de
        um. Citar o errado é citar termos revogados, e nada além deste campo
        avisa.


        A FILA MISTURA `base` E `amendment`. O texto em vigor de um regulamento
        é o último regulamento MAIS toda alteração protocolada depois dele, e as
        duas peças vêm em categorias diferentes da fonte. Por isso `is_current:
        true` é estrito: só o base em vigor SEM emendas posteriores. Nos demais
        casos leia `current_base` e `amendments_after` — juntos eles dizem
        exatamente o que compõe o texto vigente.


        NUNCA é omitido. Quando não há fila, vem o objeto de motivo — campo
        ausente se leria como "não há versão", e há quatro razões diferentes
        para não haver.
      oneOf:
        - type: object
          title: Posição na fila
          properties:
            key:
              type: string
              description: 'O que identificou "a mesma coisa": `cnpj:<emissor>` quando a
                família é chaveada pelo emissor (regulamento, estatuto),
                `asset:<instrumento>` quando é pelo instrumento (termo de
                securitização, escritura).'
            family:
              type: string
              description: A fila de que este documento participa. Documentos da mesma família
                e da mesma chave formam UMA linha do tempo, ainda que venham de
                categorias diferentes da fonte.
            role:
              type: string
              description: "`base` restabelece o texto inteiro (regulamento, estatuto, termo
                de securitização) e aposenta o base anterior junto com as
                emendas dele. `amendment` altera o base em vigor sem repetir o
                que não muda — ler uma emenda isolada devolve só o que ela
                mudou."
            index:
              type: integer
              minimum: 1
              description: Posição por data de protocolo na fila INTEIRA, base e emendas
                juntas. 1 = o mais antigo.
            total:
              type: integer
              minimum: 1
              description: Quantos documentos há na fila, incluindo os sem data.
            is_current:
              type:
                - boolean
                - "null"
              description: 'Se ESTE documento, SOZINHO, é o texto em vigor — mais estrito que
                "é o mais recente". `true` só quando é o último `base` e não há
                emenda depois dele. `false` quando foi substituído por um base
                posterior, quando é o base em vigor mas há emendas depois (veja
                `amendments_after`), ou quando é uma emenda. `null` quando não
                dá para afirmar: há documento sem data na fila (`undated > 0`)
                ou a fila não tem nenhum `base` a que somar as emendas.'
            current_base:
              type:
                - string
                - "null"
              description: Protocolo do `base` em vigor — o documento a que as emendas de
                `amendments_after` se somam. Nulo quando a fila não tem base
                nenhum.
            amendments_after:
              type: array
              items:
                type: string
              description: 'Protocolos que alteram `current_base`, do mais antigo ao mais
                recente. Lista vazia é medição, não omissão: o base em vigor não
                recebeu emenda. É este campo que separa "leia este documento" de
                "leia este documento MAIS estes três".'
            is_latest_in_chain:
              type: boolean
              description: Este é o último elo datado da fila. Afirmação do servidor, para não
                obrigar a derivá-la de `next_in_chain == null` com `index ==
                total`. Falso quando há documento sem data, porque aí não há
                último.
            undated:
              type: integer
              minimum: 0
              description: 'Documentos da fila sem data de protocolo, que por isso ficaram
                fora da ordenação. Empurrá-los para uma das pontas decidiria
                quem é o vigente — nulo primeiro faz do sem data "o mais
                antigo", nulo por último faz dele "o vigente". Medido hoje:
                zero.'
            unindexed:
              type: integer
              minimum: 0
              description: 'Documentos da fila que EXISTEM no acervo e ainda não foram lidos:
                contam para a ordem e para a vigência, mas `readDocument` ainda
                não os recupera. "Não existe" e "existe e ainda não foi
                processado" são a mesma ausência com significados opostos.'
            previous:
              type:
                - string
                - "null"
              description: Protocolo do imediatamente anterior na fila, seja base ou emenda.
            next_in_chain:
              type:
                - string
                - "null"
              description: Protocolo do imediatamente posterior na fila, seja base ou emenda.
            superseded_by:
              type:
                - string
                - "null"
              description: "Quem REVOGOU este. Existe só entre `base`: o base seguinte
                aposenta o anterior. Numa emenda vem nulo de propósito — o
                aditamento seguinte não revoga o anterior, ele soma. Para
                caminhar na cadeia use `next_in_chain`."
            amends:
              type:
                - string
                - "null"
              description: 'Para `role: "amendment"`, o `base` que ele altera — o último base
                anterior a ele.'
            shape:
              type: string
              description: "A forma temporal declarada da categoria/tipo deste documento.
                Texto livre de propósito: forma nova não deve virar erro de
                validação numa rota de leitura."
          required:
            - key
            - family
            - role
            - index
            - total
            - is_current
            - current_base
            - amendments_after
            - is_latest_in_chain
            - undated
            - unindexed
            - previous
            - next_in_chain
            - superseded_by
            - amends
            - shape
          additionalProperties: false
        - type: object
          title: Sem fila, com motivo
          properties:
            reason:
              type: string
              enum:
                - shape_has_no_versions
                - missing_key
                - missing_date
                - not_implemented
              description: "`shape_has_no_versions`: a categoria é `event` ou não declara
                forma — a pergunta não se aplica. `missing_key`: falta o
                identificador do grupo; numa família chaveada por instrumento
                isso é o INSTRUMENTO, e cair no emissor fundiria papéis
                diferentes numa cadeia só. `missing_date`: o documento não tem
                data de protocolo e não há posição a afirmar. `not_implemented`:
                a forma é `periodic` e a fila por competência ainda não é
                calculada, ou a categoria declara forma sem família de versão."
            detail:
              type: string
              description: O motivo em uma frase, com o campo que falta nomeado.
          required:
            - reason
            - detail
          additionalProperties: false
        - type: "null"
    DocumentCitation:
      type: object
      properties:
        ticker:
          type:
            - string
            - "null"
          description: Null para documento de crédito — use asset_code/issuer_cnpj.
        company_name:
          type:
            - string
            - "null"
        entity_type:
          type:
            - string
            - "null"
          enum:
            - stock
            - fii
            - fiagro
            - issuer
            - securitization
            - fidc
            - null
        issuer_cnpj:
          type:
            - string
            - "null"
          description: CNPJ do emissor (14 dígitos). Null para ação e FII, que se
            identificam por ticker.
        asset_code:
          type:
            - string
            - "null"
          description: "Papel a que o documento se refere: ISIN, código ANBIMA ou código
            de instrumento B3."
        asset_family:
          type:
            - string
            - "null"
          enum:
            - DEB
            - CRI
            - CRA
            - OTS
            - FIDC
            - FIAGRO
            - null
        doc_category:
          type:
            - string
            - "null"
          description: Categoria bruta da fonte.
        doc_type_raw:
          type:
            - string
            - "null"
          description: Subtipo bruto da fonte.
        document_kind:
          $ref: "#/components/schemas/DocumentKind"
        reference_date:
          type:
            - string
            - "null"
          format: date
          description: "Período a que o documento se REFERE (ex.: fim do trimestre
            coberto). Não é a data de publicação."
        filed_at:
          type:
            - string
            - "null"
          format: date
          description: Data em que o documento foi PROTOCOLADO/publicado na fonte
            (Data_Entrega). É a data que define disponibilidade point-in-time;
            pode ser semanas depois de reference_date.
        filed_at_source:
          type:
            - string
            - "null"
          enum:
            - source
            - null
          description: 'Qualidade de filed_at: "source" = veio da fonte oficial; null = a
            fonte não forneceu (nunca há fallback para reference_date).'
        protocol:
          type:
            - string
            - "null"
          description: Identificador do documento na fonte (protocolo CVM/FNET) —
            auditável em conjunto com source.
        source:
          type:
            - string
            - "null"
          description: "Conector de origem do documento (ex.: cvm_ipe, fnet_fii,
            fnet_fiagro)."
        page_start:
          type:
            - integer
            - "null"
          minimum: 1
        download_url:
          type:
            - string
            - "null"
          format: uri
        heading_path:
          type:
            - string
            - "null"
        version:
          $ref: "#/components/schemas/DocumentVersion"
      required:
        - ticker
        - company_name
        - entity_type
        - issuer_cnpj
        - asset_code
        - asset_family
        - doc_category
        - doc_type_raw
        - document_kind
        - reference_date
        - filed_at
        - filed_at_source
        - protocol
        - source
        - page_start
        - download_url
        - heading_path
        - version
      additionalProperties: false
    DocumentChunk:
      type: object
      properties:
        text:
          type: string
        text_expanded:
          type: string
          description: Entorno costurado (context-expansion), quando disponível.
        score:
          type: number
          minimum: -1
          maximum: 1
          description: Similaridade (1 - distância cosseno).
        ticker:
          type:
            - string
            - "null"
          deprecated: true
        company_name:
          type:
            - string
            - "null"
          deprecated: true
        doc_category:
          type:
            - string
            - "null"
          deprecated: true
        doc_type:
          type:
            - string
            - "null"
          deprecated: true
        reference_date:
          type:
            - string
            - "null"
          format: date
          deprecated: true
        page_start:
          type:
            - integer
            - "null"
          minimum: 1
          deprecated: true
        download_url:
          type:
            - string
            - "null"
          format: uri
          deprecated: true
        heading_path:
          type:
            - string
            - "null"
          deprecated: true
        citation:
          $ref: "#/components/schemas/DocumentCitation"
        is_table:
          type: boolean
      required:
        - text
        - score
        - citation
        - is_table
      additionalProperties: false
    DocumentTaxonomy:
      type: object
      properties:
        kinds:
          type: array
          items:
            type: object
            properties:
              kind:
                $ref: "#/components/schemas/DocumentKind"
              label:
                type: string
              document_count:
                type: integer
                minimum: 0
              categories:
                type: array
                items:
                  type: object
                  properties:
                    category:
                      type:
                        - string
                        - "null"
                    doc_type_raw:
                      type:
                        - string
                        - "null"
                    document_count:
                      type: integer
                      minimum: 0
                    shape:
                      type:
                        - string
                        - "null"
                      description: >-
                        O QUE UM SEGUNDO DOCUMENTO DESTA CATEGORIA SIGNIFICA, e
                        vem antes de "qual é o vigente". `event`: cada documento
                        é um fato próprio e nada substitui nada — a pergunta não
                        se aplica. `superseding`: o mais recente é o vigente.
                        `amending`: o vigente é o ORIGINAL mais todos os
                        aditamentos, e ler só o último perde o contrato.
                        `periodic`: chaveado por competência — 4T24 não
                        substitui 3T24, mas uma republicação do 4T24 sim.


                        Medido: uma companhia tem 7,1 estatutos e 33 fatos
                        relevantes. Sete estatutos são um vigente e seis
                        históricos; trinta e três fatos relevantes são trinta e
                        três fatos. Sem este campo os dois casos se leem igual.


                        NULO quer dizer que ninguém declarou a forma desta
                        categoria — nunca que ela é `event`.
                    shape_meaning:
                      type:
                        - string
                        - "null"
                      description: "O que a forma quer dizer, em uma frase. Viaja junto porque o nome
                        sozinho não é acionável: `amending` não avisa que ler
                        apenas o documento mais recente devolve o aditamento
                        isolado."
                    version_family:
                      type:
                        - string
                        - "null"
                      description: 'A FILA de que esta categoria participa. Duas categorias com a
                        MESMA família entram na mesma linha do tempo, ainda que
                        a fonte as rotule diferente: é assim que "Instrumento
                        Particular de Alteração do Regulamento" encontra o
                        "Regulamento" que ele altera. Nulo quando a categoria
                        não versiona.'
                    version_role:
                      type:
                        - string
                        - "null"
                      description: '`base` restabelece o texto inteiro e aposenta o base anterior
                        junto com as emendas dele; `amendment` altera o base em
                        vigor sem repetir o que não muda. Uma categoria
                        `amendment` NUNCA responde sozinha "o que vale hoje".'
                  required:
                    - category
                    - doc_type_raw
                    - document_count
                    - shape
                    - shape_meaning
                    - version_family
                    - version_role
                  additionalProperties: false
            required:
              - kind
              - label
              - document_count
              - categories
            additionalProperties: false
        entity_types:
          type: array
          description: Tipos de entidade aceitos como filtro.
          items:
            $ref: "#/components/schemas/DocumentEntityType"
        asset_families:
          type: array
          description: Famílias aceitas para documentos de crédito e fundos.
          items:
            type: string
            enum:
              - DEB
              - CRI
              - CRA
              - OTS
              - FIDC
              - FIAGRO
        reference_years:
          type: object
          properties:
            min:
              type:
                - integer
                - "null"
            max:
              type:
                - integer
                - "null"
          required:
            - min
            - max
          additionalProperties: false
      required:
        - kinds
        - entity_types
        - asset_families
        - reference_years
      additionalProperties: false
    DocumentReadChunk:
      type: object
      properties:
        chunk_index:
          type: integer
          minimum: 0
          description: Posição do trecho no documento; a paginação preserva esta ordem.
        text:
          type: string
        heading_path:
          type:
            - string
            - "null"
        is_table:
          type: boolean
        page_start:
          type:
            - integer
            - "null"
          minimum: 1
        page_end:
          type:
            - integer
            - "null"
          minimum: 1
      required:
        - chunk_index
        - text
        - heading_path
        - is_table
        - page_start
        - page_end
      additionalProperties: false
    DocumentReadResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/DocumentReadChunk"
        meta:
          type: object
          properties:
            next_cursor:
              type:
                - string
                - "null"
              description: Passe como cursor na próxima chamada; null encerra o documento.
            count:
              type: integer
              minimum: 0
            protocol:
              type: string
            source:
              type: string
            citation:
              $ref: "#/components/schemas/DocumentCitation"
          required:
            - next_cursor
            - count
            - protocol
            - source
            - citation
          additionalProperties: false
      required:
        - data
        - meta
      additionalProperties: false
    MarketEventSearchResponse:
      type: object
      properties:
        data:
          type: array
          items:
            type: object
            properties:
              event_id:
                type: integer
                description: Use em /v1/events/{id} e /v1/events/{id}/similar.
              dedup_key:
                type: string
                description: Chave de idempotência do evento na origem.
              day:
                type: string
                description: Dia BRT do evento (AAAA-MM-DD).
              layer:
                type: string
                description: estrutural | setorial | corporativa
              category:
                type: string
                description: macro | politica | internacional | commodities | corporate | fii |
                  cripto | mercado | outros
              title:
                type: string
              summary:
                type:
                  - string
                  - "null"
              entities:
                type: array
                items:
                  type: string
              tickers:
                type: array
                items:
                  type: string
                description: Um papel por posição.
              transmission_channels:
                type: array
                items:
                  type: string
                description: dolar | ibov | brent | curva | selic
              score:
                type:
                  - number
                  - "null"
                description: Rank do funil (imprensa) ou |z| (mercado) — NÃO é a relevância da
                  busca.
              detectors:
                type: array
                items:
                  type: string
                description: official | press | market
              thread_slug:
                type:
                  - string
                  - "null"
                description: Use em /v1/events/threads/{slug}.
              source_refs:
                type: array
                items:
                  type: object
              similarity:
                type: number
                description: Relevância semântica desta busca (maior = mais próximo da
                  pergunta).
            required:
              - event_id
              - dedup_key
              - day
              - layer
              - category
              - title
              - similarity
        meta:
          type: object
          properties:
            next_cursor:
              type:
                - string
                - "null"
              description: "Sempre null: resposta em página única."
            count:
              type: integer
              description: Itens nesta página.
            query:
              type: string
          required:
            - next_cursor
            - count
      required:
        - data
        - meta
    DocumentSearchResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/DocumentChunk"
        meta:
          type: object
          properties:
            next_cursor:
              type:
                - string
                - "null"
              description: "Sempre null: resposta em página única."
            count:
              type: integer
              description: Itens nesta página.
            query:
              type: string
            context:
              type: string
              description: Bloco pronto p/ prompt, com citação por trecho. Vazio quando
                include_context=false.
            warnings:
              type: array
              items:
                type: string
              description: "Avisos de semântica da busca (ex.: exclusão de documentos sem
                filed_at no modo point-in-time). Ausente quando não há avisos."
          required:
            - next_cursor
            - count
      required:
        - data
        - meta
tags:
  - name: Companies
    description: Companhias abertas (cadastro CVM) e demonstrações
  - name: Stocks
    description: Ações — cotações, indicadores, proventos e eventos
  - name: Objects
    description: 'O grafo de identidade: resolve um texto para o objeto, lê seus
      apelidos e atravessa suas relações. Uma empresa, um fundo, um instrumento
      ou um indicador são UM objeto só, com todos os códigos apontando para ele.
      `getObject` traz o mapa do que dá para perguntar em seguida. **Resolva
      pelo NOME DO CONCEITO, não só pelo ticker** (`resolveObject("earnings
      yield agregado")`, "juro real", "IPCA") — uma sondagem de 23/08/2026
      reconstruiu na mão, com 14 chamadas e universo errado, um número que o
      grafo publicava pronto sob o nome do conceito. O que volta tem DUAS
      formas, e a diferença importa: conceito que fonte terceira mede (IPCA,
      Selic, CDI, IGP-M, INPC, TR, dólar) é `kind=indicator` e aponta em
      `measures`/`forecasts` as séries que o medem; agregado COMPUTADO pelo
      DataBolsa (earnings yield agregado, juro real ex-ante, spread de crédito…)
      é `kind=data_series` com o fato `indicator_value` e histórico completo —
      não procure `measures` nele, e `resolveObject` vazio com `kind=indicator`
      fixado NÃO significa que o conceito não existe: repita sem o filtro.'
  - name: Corporate
    description: "Eventos societários que valem para o mercado inteiro, não para um
      papel: quem está fora de operação regular (recuperação judicial e afins) e
      as sucessões de código. Os mesmos dados existem por papel em `getStock`;
      aqui são o conjunto."
  - name: Quotes
    description: Cotações ao vivo (delay de 15 min) — lote e séries intradiárias
  - name: FIIs
    description: Fundos imobiliários — indicadores e distribuições
  - name: Bonds
    description: Títulos públicos (Tesouro Direto) e curvas de juros
  - name: Credit
    description: Crédito privado — debêntures, CRI/CRA, FIDC, ratings, curvas de
      crédito, risco de emissor e negócios de balcão. É a maior superfície de
      renda fixa privada do contrato. Não confunda com Bonds (título público)
      nem com Estruturados (COE).
  - name: Commodities
    description: "Ajuste diário dos contratos FUTUROS de mercadoria na B3 (boi
      gordo, milho, café, etanol, soja, ouro). ATENÇÃO: é preço de ajuste por
      vencimento, NÃO preço à vista — o vencimento mais curto é base futura, não
      spot. Para o preço internacional de Brent, minério de ferro e soja use as
      séries de getSeries (fonte fred)."
  - name: Minerais
    description: "Produção e reservas de MINERAIS por país (lítio, cobre, nióbio,
      cobalto, terras raras, grafita, ferro, ouro, fosfato...), da publicação
      Mineral Commodity Summaries do USGS. Responde quem produz e quem detém
      reserva, em quantidade física e participação no total mundial. Não
      confunda com Commodities, que é ajuste de contrato FUTURO da B3 em BRL:
      aqui não há preço. 'Reserves' é estimativa geológica agregada por país,
      NÃO reserva provada sob SEC S-K 1300, JORC ou NI 43-101."
  - name: Crypto
    description: Criptoativos — catálogo, velas diárias em BRL e cotações quase-live
  - name: EUA
    description: Ativos dos EUA (S&P 500/Nasdaq-100 + ETFs) — EOD em USD,
      fundamentos e filings SEC
  - name: Indices
    description: Índices da B3 (IBOV, IFIX, ...) e composições
  - name: Events
    description: Ledger de eventos de mercado e mundo — o que foi relevante em cada
      dia (decisões de juros, medidas provisórias, tarifas, geopolítica,
      anomalias de preço), com fontes. Não confunda com eventos SOCIETÁRIOS
      (listCorporateEvents) nem com a agenda de resultados (earnings).
  - name: Market
    description: Microestrutura de mercado — fluxo por perfil de investidor
  - name: Primary Market
    description: Mercado primário — ofertas públicas de distribuição (CVM)
  - name: BDR
    description: BDRs (recibos de ações estrangeiras) — catálogo e cotações
  - name: ETFs
    description: ETFs listados na B3 — catálogo (gestor, CNPJ, segmento)
  - name: Funds
    description: Fundos de investimento (CVM 175) — perfil, cota diária e screener
  - name: Options
    description: Opções sobre ações — cadeia vigente e histórico EOD
  - name: Estruturados
    description: "COE (Certificado de Operações Estruturadas) registrado no balcão.
      Categoria à parte de Bonds e Credit de propósito: o retorno NÃO é fixo. É
      um payoff de derivativo embrulhado no risco de crédito do banco emissor —
      o investidor carrega os dois. O registro público dá emissor, tamanho,
      prazo e liquidez secundária; a estrutura (proteção de capital, subjacente,
      cenários) vive no DIE e não está aqui."
  - name: Macro
    description: Séries macroeconômicas, expectativas Focus e regime
  - name: Screener
    description: Filtros multi-critério sobre ações e FIIs
  - name: System
    description: Saúde e metadados da API
  - name: Carteira
    description: Carteira e perfil do usuário dono da chave de API
  - name: Documentos
    description: "Descoberta, busca semântica e leitura paginada de documentos
      oficiais CVM/B3. Fluxo para agentes: getDocumentTaxonomy descobre facetas;
      searchDocuments encontra documentos e passagens; readDocument percorre um
      protocolo na ordem, até meta.next_cursor=null. Em crédito privado,
      identifique emissor, papel e família separadamente e preserve
      protocol/source/página e as duas datas da citação."
  - name: Teses
    description: "Teses de investimento do dono da chave: documentos de research
      escritos em markdown (capa, rating, cenários, fontes) salvos na conta,
      compartilháveis por link público e exportáveis em PDF."
paths:
  /v1/health:
    get:
      responses:
        "200":
          description: Status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getHealth
      tags:
        - System
      parameters: []
      summary: Status da API e frescor dos dados
      security: []
      x-capability: system
  /v1/companies:
    get:
      responses:
        "200":
          description: Página de companhias
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Company"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCompanies
      tags:
        - Companies
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: sector
          schema:
            type: string
            description: Setor (match exato, case-sensitive). Valor desconhecido → página
              vazia.
        - in: query
          name: segment
          schema:
            type: string
            description: "Segmento de listagem B3 (substring, case-insensitive), ex.: 'Novo
              Mercado'."
        - in: query
          name: search
          schema:
            type: string
            description: Busca por nome ou ticker da companhia.
      summary: Lista companhias abertas (cadastro CVM)
      x-capability: market-data
  /v1/companies/{company}:
    get:
      responses:
        "200":
          description: Companhia
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Company"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getCompany
      tags:
        - Companies
      parameters:
        - in: path
          name: company
          schema:
            type: string
            minLength: 1
            maxLength: 32
            description: "Ticker do papel (BBAS3), código CVM numérico da companhia (1023)
              OU CNPJ de 14 dígitos (10531501000158). O CNPJ é a porta para a
              companhia SEM ação em bolsa: quem se registra na CVM apenas para
              emitir dívida (Categoria B) entrega demonstração financeira
              completa e não tem ticker. Debênture, FIDC e informe de
              securitização identificam o emissor por CNPJ, nunca por cd_cvm."
          required: true
      summary: Companhia por ticker, código CVM ou CNPJ
      x-capability: market-data
  /v1/companies/{company}/documents:
    get:
      responses:
        "200":
          description: Página de documentos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Document"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCompanyDocuments
      tags:
        - Companies
      parameters:
        - in: path
          name: company
          schema:
            type: string
            minLength: 1
            maxLength: 32
            description: "Ticker do papel (BBAS3), código CVM numérico da companhia (1023)
              OU CNPJ de 14 dígitos (10531501000158). O CNPJ é a porta para a
              companhia SEM ação em bolsa: quem se registra na CVM apenas para
              emitir dívida (Categoria B) entrega demonstração financeira
              completa e não tem ticker. Debênture, FIDC e informe de
              securitização identificam o emissor por CNPJ, nunca por cd_cvm."
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: total
          schema:
            type: string
            description: true = inclui `meta.total` (contagem do universo filtrado). Custa
              uma consulta a mais.
        - in: query
          name: kind
          schema:
            $ref: "#/components/schemas/DocumentKind"
            description: "Tipo estável do documento (mesma taxonomia do campo `kind` da
              resposta). Prefira este filtro a `category`: a categoria é texto
              livre em português e muda com a fonte."
        - in: query
          name: category
          schema:
            type: string
            description: "Categoria bruta do documento no IPE/CVM, ex.: 'Fato Relevante',
              'Comunicado ao Mercado', 'Aviso aos Acionistas', 'Assembleia',
              'Dados Econômico-Financeiros'."
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Filtra a DATA DE REFERÊNCIA do documento (período a que ele se
              refere), não a de protocolo — um DFP do exercício 2022 é entregue
              em 2023 e aparece em from/to de 2022. A data de entrega segue no
              campo `filed_at` de cada documento.
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Fim do período (data de referência, inclusive).
      summary: Documentos e comunicados (IPE/CVM)
      description: Fatos relevantes, comunicados e documentos protocolados na CVM
        (IPE) da companhia. Aceita ticker (BBAS3), código CVM (1023) ou CNPJ de
        14 dígitos (10531501000158). Filtre por `kind` (taxonomia estável),
        `category` (texto bruto da fonte) e período (`from`/`to`); paginação por
        cursor.
      x-capability: market-data
  /v1/companies/{company}/statements:
    get:
      responses:
        "200":
          description: Página de demonstrações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CompanyStatement"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCompanyStatements
      tags:
        - Companies
      parameters:
        - in: path
          name: company
          schema:
            type: string
            minLength: 1
            maxLength: 32
            description: "Ticker do papel (BBAS3), código CVM numérico da companhia (1023)
              OU CNPJ de 14 dígitos (10531501000158). O CNPJ é a porta para a
              companhia SEM ação em bolsa: quem se registra na CVM apenas para
              emitir dívida (Categoria B) entrega demonstração financeira
              completa e não tem ticker. Debênture, FIDC e informe de
              securitização identificam o emissor por CNPJ, nunca por cd_cvm."
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: scope
          schema:
            type: string
            enum:
              - con
              - ind
            description: con = consolidado, ind = controladora; omitir retorna ambos.
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Início do período (ref_date, fim do trimestre contábil).
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Fim do período (ref_date, inclusive).
      summary: Demonstrações financeiras estruturadas (TTM + balanço)
      description: "Aceita ticker (BBAS3), código CVM (1023) ou CNPJ de 14 dígitos
        (10531501000158). O CNPJ é a porta para a companhia SEM ação em bolsa:
        quem se registra na CVM apenas para emitir dívida (Categoria B) entrega
        a mesma demonstração trimestral e não tem ticker — e debênture, FIDC e
        informe de securitização identificam o emissor por CNPJ. Séries
        estruturadas por trimestre (CVM DFP/ITR): fluxos em TTM (receita, EBIT,
        EBITDA, lucro, caixa operacional/livre, capex) e balanço ponto-no-tempo
        (ativos, caixa, dívida, patrimônio). Valores em R$ (unidade), escopo
        consolidado e controladora. `filed_at` traz a 1ª publicação na CVM para
        corte point-in-time sem lookahead. `from`/`to` filtram ref_date;
        paginação por cursor."
      x-capability: market-data
  /v1/corporate/trading-status:
    get:
      responses:
        "200":
          description: Página de emissoras em situação excepcional
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        ticker:
                          type: string
                        entity_id:
                          type:
                            - string
                            - "null"
                          description: O objeto do PAPEL no grafo de identidade. Nulo quando o ticker
                            ainda não resolveu no spine
                        trading_status:
                          type: string
                          enum:
                            - regular
                            - sancionada
                            - concordataria
                            - recuperacao_extrajudicial
                            - recuperacao_judicial
                          description: Situação da emissora segundo a B3. `concordataria` só aparece no
                            histórico (o instituto foi extinto pela Lei
                            11.101/2005).
                        trading_status_since:
                          type:
                            - string
                            - "null"
                          description: Desde quando a situação vale (AAAA-MM-DD)
                        last_session:
                          type:
                            - string
                            - "null"
                          description: Último pregão observado para o papel
                      required:
                        - ticker
                        - entity_id
                        - trading_status
                        - trading_status_since
                        - last_session
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listTradingStatus
      tags:
        - Corporate
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: total
          schema:
            type: string
            description: true = inclui `meta.total` (contagem do universo filtrado). Custa
              uma consulta a mais.
        - in: query
          name: status
          schema:
            type: string
            enum:
              - sancionada
              - concordataria
              - recuperacao_extrajudicial
              - recuperacao_judicial
            description: Filtra por uma situação específica. Omitir = todas as
              não-regulares.
      summary: Empresas fora de operação regular — recuperação judicial e afins
      description: "Papéis cuja emissora está em situação excepcional carimbada pela
        B3 no CODBDI do pregão: recuperação judicial, extrajudicial, concordata,
        sancionada. **NÃO é suspensão de negociação** — a maioria segue com
        pregão todo dia. Use `?total=true` para a contagem do universo sem
        paginar."
      x-capability: market-data
  /v1/corporate/successions:
    get:
      responses:
        "200":
          description: Página de sucessões
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        old_ticker:
                          type: string
                        new_ticker:
                          type: string
                          description: "O código que sucedeu o antigo NA `ex_date` — o próximo elo da
                            cadeia, não o fim dela. O par (destino, data) tem de
                            ser verdadeiro junto: em 14/08/2021 VVAR3 virou
                            VIIA3, e BHIA3 só existe desde 2023. Para o código
                            negociável hoje use `current_ticker`; para
                            reconstruir a posição atravessando várias trocas,
                            caminhe por aqui."
                        current_ticker:
                          type: string
                          description: O código vivo no fim da cadeia. Igual a `new_ticker` quando a
                            cadeia tem um salto só — 48 das 51 renomeações.
                        old_entity_id:
                          type:
                            - string
                            - "null"
                          description: Objeto do antecessor. Linhas com o MESMO par de ids são um evento
                            só, visto por papéis diferentes.
                        new_entity_id:
                          type:
                            - string
                            - "null"
                          description: Objeto do sucessor.
                        ratio:
                          type:
                            - number
                            - "null"
                          description: Ações do sucessor por unidade do código antigo. 1 = renomeação pura
                        ex_date:
                          type:
                            - string
                            - "null"
                        event_type:
                          type:
                            - string
                            - "null"
                          enum:
                            - rename
                            - incorporation
                            - null
                          description: rename = troca de código (1:1); incorporation = M&A, a posição
                            converte pelo ratio
                        source:
                          type:
                            - string
                            - "null"
                        note:
                          type:
                            - string
                            - "null"
                      required:
                        - old_ticker
                        - new_ticker
                        - current_ticker
                        - old_entity_id
                        - new_entity_id
                        - ratio
                        - ex_date
                        - event_type
                        - source
                        - note
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCorporateSuccessions
      tags:
        - Corporate
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: total
          schema:
            type: string
            description: true = inclui `meta.total` (contagem do universo filtrado). Custa
              uma consulta a mais.
        - in: query
          name: ticker
          schema:
            type: string
            description: Sucessões que envolvem este código, como antecessor OU sucessor.
        - in: query
          name: event_type
          schema:
            type: string
            enum:
              - rename
              - incorporation
            description: rename = troca de código 1:1; incorporation = M&A, a posição
              converte pelo ratio.
      summary: Sucessões de código — esta empresa virou aquela
      description: >-
        Liga o código antigo ao novo, com a data ex e o `ratio` de conversão.
        `rename` é troca de código 1:1 (a empresa é a mesma); `incorporation` é
        M&A e a posição converte pelo ratio. É o que permite reconstruir
        histórico de posição atravessando a troca de código.


        `new_ticker` é o PRÓXIMO elo — o que casa com `ex_date` — e
        `current_ticker` é o código vivo no fim da cadeia. Os dois coincidem em
        48 das 51 renomeações; nas outras três a diferença é que VVAR3 virou
        VIIA3 em 2021 e só chegou a BHIA3 em 2023.
      x-capability: market-data
  /v1/objects/intersect:
    get:
      responses:
        "200":
          description: Objetos nos dois conjuntos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        kind:
                          type: string
                          description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                            pelo mesmo motivo de `rel`: `offering` e
                            `fund_share_class` nasceram em 19/08/2026 e um
                            cliente que validasse contra a lista anterior
                            recusaria a resposta inteira. `getObjectCensus`
                            lista os vigentes."
                        subkind:
                          type:
                            - string
                            - "null"
                          description: "Fundo: `fidc`, `fii`, `fip`, `fif`, `fiagro`, `fiim`, `etf`,
                            `fapi`, `funcine`. Instrumento: `debenture`,
                            `securitizado`, `bdr`, `coe`, `tesouro`. Papel:
                            `on`, `pn`, `unit` — a classe da ação, que é o que
                            ela é dentro do tipo. Prestador: `gestora`,
                            `administradora_fiduciaria`, `assessoria`,
                            `agencia_rating` — o que a casa declarou SER perante
                            a CVM; quem gere E administra sai `gestora`. Auditor
                            e custodiante ficam sem sub-tipo de propósito (só
                            existem como menção no cadastro de fundo de
                            terceiro) — descubra-os pelas arestas
                            `audits`/`custodies`. NULO é resposta honesta — a
                            fonte que classifica cobre só o universo ATIVO,
                            então fundo encerrado fica sem sub-tipo. Ausência
                            aqui é 'não sabemos o que é', nunca 'não é nada'."
                        name:
                          type:
                            - string
                            - "null"
                        cnpj:
                          type:
                            - string
                            - "null"
                        tickers:
                          type:
                            - array
                            - "null"
                          items:
                            type: string
                        a_count:
                          type: integer
                          description: "Quantas CONTRAPARTES DISTINTAS este objeto tem na relação A.
                            `count(distinct)`, não contagem de linhas —
                            `entity_link` tem uma linha por AFIRMAÇÃO, e a mesma
                            cessão é reafirmada a cada informe mensal: das
                            22.535 arestas de `assigned_to`, 13.719 são pares
                            distintos. Contar linhas publicaria um grau inflado
                            em 60%, plausível e errado."
                        b_count:
                          type:
                            - integer
                            - "null"
                          description: Idem na relação B. Nulo em `union` e `difference`, onde não há lado
                            B a pesar.
                      required:
                        - id
                        - kind
                        - subkind
                        - name
                        - cnpj
                        - tickers
                        - a_count
                        - b_count
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      as_of:
                        type:
                          - string
                          - "null"
                        description: "A data efetivamente aplicada, que pode ser anterior à pedida:
                          verbo de retrato recua até a última competência
                          publicada (veja `last_valid_to` em
                          `listObjectRelations`). Nulo quando a consulta mistura
                          formas. Ausente quando `at` não foi pedido."
                      as_of_by_rel:
                        type: object
                        additionalProperties:
                          type:
                            - string
                            - "null"
                        description: >-
                          A data EFETIVAMENTE aplicada a cada verbo, quando a
                          consulta prende mais de um — é onde o escalar `as_of`
                          seria nulo por não haver recorte comum.


                          Leia por FORMA, senão o campo engana: em verbo de
                          RETRATO (`holds`, `assigned_to`) o valor pode ter
                          RECUADO até a última competência publicada, e é essa a
                          informação. Em verbo de EVENTO (`issued`) ele é a data
                          que você pediu, porque tudo que aconteceu até lá conta
                          — não é 'a competência do issued', é o teto do corte.
                          Ausente quando há um verbo só (aí o escalar responde)
                          ou quando `at` não foi pedido.
                      excluded_shapes:
                        type: array
                        items:
                          type: string
                          enum:
                            - event
                            - snapshot
                            - static
                          description: "`event` = aconteceu numa data e não deixa de ter acontecido
                            (emissão, sucessão): vigência não se aplica e a
                            contagem é o total. `snapshot` = a fonte republica
                            um retrato por competência e o novo substitui o
                            velho (carteira de fundo, carteira de índice): a
                            contagem é o último retrato. `static` = o registro
                            publica o vigente, sem série (gestor, custodiante,
                            auditor, sócio): a contagem é o que vale hoje."
                        description: Formas removidas do conjunto por causa da data. `static` aparece
                          aqui quando `at` é passado e a consulta não prende um
                          verbo — o registro publica só o vigente, e devolvê-lo
                          apresentaria o estado de hoje como fato daquela data.
                          Vazio quando nada foi removido; ausente quando `at`
                          não foi pedido.
                      order:
                        type: string
                        enum:
                          - name
                          - a_count
                          - b_count
                          - total
                        description: Como a página foi ordenada. Ordenar por grau responde 'as três
                          maiores da interseção' numa chamada.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: intersectObjects
      tags:
        - Objects
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
          required: false
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - in: query
          name: total
          schema:
            type: string
            description: true = inclui `meta.total` (contagem do universo filtrado). Custa
              uma consulta a mais.
          required: false
        - in: query
          name: a
          schema:
            type: string
            enum:
              - issued
              - assigned_to
              - holds
              - manages
              - administers
              - custodies
              - audits
              - same_owner
              - shareholder_of
              - indexed_to
              - rates
              - mentions
              - measures
              - forecasts
              - contains
              - member_of
              - exposed_to_issuer
              - succeeded_by
              - produces
              - covers
            description: Primeira relação.
          required: true
        - in: query
          name: b
          schema:
            type: string
            enum:
              - issued
              - assigned_to
              - holds
              - manages
              - administers
              - custodies
              - audits
              - same_owner
              - shareholder_of
              - indexed_to
              - rates
              - mentions
              - measures
              - forecasts
              - contains
              - member_of
              - exposed_to_issuer
              - succeeded_by
              - produces
              - covers
            description: Segunda relação.
          required: true
        - in: query
          name: a_direction
          schema:
            type: string
            enum:
              - out
              - in
            default: out
          required: false
        - in: query
          name: b_direction
          schema:
            type: string
            enum:
              - out
              - in
            default: out
          required: false
        - in: query
          name: a_to_id
          schema:
            type: string
            description: Prende a outra ponta da relação A.
          required: false
        - in: query
          name: b_to_id
          schema:
            type: string
            description: Prende a outra ponta da relação B.
          required: false
        - in: query
          name: a_to_kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: "Recorta o TIPO da outra ponta de A. `issued` sai da companhia
              tanto para `instrument` quanto para `equity_security`: sem isto,
              'quem cede para FIDC e emite DÍVIDA' inclui quem só emitiu ação."
          required: false
        - in: query
          name: b_to_kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: Idem para a relação B.
          required: false
        - in: query
          name: kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: >-
              O que a coisa É. Papel (emissor, cedente, administrador) NÃO é
              kind — é aresta. `equity_security` é a ação (PETR4), distinta da
              companhia que a emitiu.


              `offering` é a oferta pública, e é o único que não é uma COISA: é
              uma RELAÇÃO que virou objeto. Ela liga emissor, instrumento e
              tempo, e carrega atributos próprios (volume, rito, status,
              quantidade, preço) que não cabem numa aresta. Atravesse `issued`
              na direção `in` para chegar ao emissor.


              `market_event` é o segundo do mesmo feitio: um ACONTECIMENTO que
              virou objeto. Ele existe porque aresta liga dois objetos, e sem nó
              do lado do evento não havia como compor — `getObjectEvents`
              responde 'o que aconteceu com este objeto' e nunca 'quais empresas
              foram citadas nos mesmos eventos que esta'. Atravesse `mentions`
              na direção `in` para ir do objeto aos eventos que falam dele, e
              `out` para ir do evento aos objetos citados. **Só o evento com
              objeto carimbado vira nó**: os demais seguem legíveis por
              `getObjectEvents` e não viram ilha no censo.


              `fund_share_class` é a SUBCLASSE de fundo da RCVM 175 — o que o
              investidor efetivamente subscreve, distinto da classe que tem o
              CNPJ. Mesma separação de `equity_security` × `company`: o que se
              compra não é o veículo. Atravesse `contains` na direção `in` para
              chegar à classe. Ela nasce sem medidas: os atributos (situação,
              público-alvo) ainda não são servidos, e ausência aqui é isso, não
              'a subclasse não tem'.


              **`indicator` × `data_series`, a regra, porque a pergunta se
              repete.** `indicator` é o CONCEITO — existe fora do DataBolsa, tem
              nome próprio no mercado e é o que um contrato cita: IPCA, Selic,
              CDI, TR, IGP-M, INPC, dólar PTAX. `data_series` é uma PUBLICAÇÃO
              numerada: uma fonte, uma unidade, uma transformação, um calendário
              — `bcb_sgs:433` é a variação mensal do IPCA e `bcb_sgs:13522` é o
              acumulado em 12 meses do MESMO conceito. O conceito tem uma série
              `headline` e quantas variantes a fonte publicar; a série nunca tem
              conceito além do que ela mede.


              Daí a consequência que surpreende: o que o DataBolsa CALCULA é
              `data_series`, não `indicator`, mesmo quando tem nome de conceito.
              `juro_real_ex_ante`, `earnings_yield_agregado` e os escores de
              regime são receitas nossas — a linhagem sai em `lineage` — e não
              coisas que uma escritura possa citar. São 7 conceitos e centenas
              de séries, e isso é o desenho, não cobertura faltando. **Resolva
              pelo NOME e leia o `kind` que voltar**, em vez de filtrar por
              `kind=indicator` esperando encontrar tudo.
          required: false
        - in: query
          name: subkind
          schema:
            type: string
            description: Recorta dentro do tipo — `fidc`, `fii`, `debenture`…
          required: false
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: >-
              Corte temporal (AAAA-MM-DD). SEM ele a resposta é 'quem JÁ se
              relacionou', não 'quem se relaciona' — a diferença é grande e
              silenciosa.


              **O RECORTE DEPENDE DA FORMA DO VERBO** (`shape`, publicado em
              `listObjectRelations`):


              - `event` (`issued`, `indexed_to`, `succeeded_by`): aconteceu numa
              data e não deixa de ter acontecido — entra tudo que ocorreu
              **até** `at`, sem limite superior. Aresta sem data declarada NÃO
              entra: o registro de ações não publica data de emissão, e afirmar
              que ela já existia numa data passada é afirmação que a fonte não
              faz.

              - `snapshot` (`holds`, `contains`, `assigned_to`,
              `exposed_to_issuer`): retrato por competência, e nenhum retrato se
              afirma válido depois de ser tirado. A janela é **contida**
              (relação que saiu e voltou não aparece no buraco), e `at` mais
              recente que a última competência **recua** até ela — a resposta
              diz em `meta.as_of` qual data foi realmente aplicada. Cada verbo
              tem a competência DELE: `holds` fecha no trimestre da CDA,
              `contains` no pregão do dia.

              - `static` (`manages`, `administers`, `custodies`, `audits`,
              `same_owner`, `shareholder_of`, `measures`, `forecasts`): o
              registro publica só o VIGENTE — 111.729 arestas sem início
              declarado. Com o verbo preso em `rel`, `at` no passado responde
              **400** em vez de devolver o gestor de hoje como gestor de 2020.
              Sem verbo preso, essas arestas são removidas do conjunto e
              `meta.excluded_shapes` diz que foram.


              **AS IRMÃS NÃO COMPARTILHAM O DEFAULT.** Sem `at`: esta operação,
              `listGlobalLinks`, `intersectObjects` e `findObjectPaths`
              respondem 'quem JÁ se relacionou'; `getObjectLinkStats` resume o
              retrato VIGENTE (última competência do próprio conjunto);
              `traverseObjectPath` atravessa o retrato vigente POR SUJEITO em
              cada salto. As diferenças são deliberadas — histórico acumulado,
              resumo e travessia respondem perguntas distintas — e cada rota
              declara a sua no próprio `at`. Medido em 23/08/2026: 1 aresta no
              resumo contra 100+ na listagem, para o mesmo objeto, os dois
              certos.
          required: false
        - in: query
          name: order
          schema:
            type: string
            enum:
              - name
              - a_count
              - b_count
              - total
            default: name
            description: "Como ordenar. `name` (default) é alfabética.
              `a_count`/`b_count`/`total` ordenam pelo GRAU — quantas
              contrapartes distintas o objeto tem em cada relação — e é assim
              que se pede 'as três maiores da interseção' numa chamada só, em
              vez de paginar o universo e agregar no cliente. Em `union` e
              `difference` só `a_count` faz sentido: não há lado B a pesar."
          required: false
        - in: query
          name: op
          schema:
            type: string
            enum:
              - intersect
              - union
              - difference
            default: intersect
            description: "`intersect` = nos dois; `union` = em qualquer um; `difference` =
              em A e não em B."
          required: false
      summary: "ÁLGEBRA DE CONJUNTOS sobre relações: interseção, união e diferença"
      description: |-
        Responde a classe de pergunta que dois montes separados não respondem. `op` escolhe a operação e o resto dos parâmetros é o mesmo nas três:
        - `op=intersect` (default) — quem está nos DOIS. Ex.: `a=assigned_to&b=issued` são as empresas que vendem recebível para FIDC **e** têm debênture emitida.
        - `op=union` — quem está em QUALQUER um dos dois.
        - `op=difference` — quem está em A e NÃO em B. É a pergunta de concentração: cedente EXCLUSIVO é risco que não aparece em média nenhuma.

        EXEMPLO COMPLETO da diferença, com os parâmetros exatos: cedentes do FIDC X que não cedem para o FIDC Y é `a=assigned_to&a_to_id=<X>&b=assigned_to&b_to_id=<Y>&op=difference&total=true`. Note que o VERBO é o mesmo nos dois lados — o que muda é a outra ponta. Comparar dois objetos concretos é para isso que `a_to_id`/`b_to_id` existem; sem eles você compara dois verbos, que é outra pergunta. E reporte `meta.total`, não o tamanho da página.

        `a_to_id`/`b_to_id` prendem a OUTRA ponta de cada relação — é o que permite comparar dois objetos concretos (cedentes do FIDC A contra os do FIDC B) em vez de dois verbos. Use `?total=true` para o tamanho do conjunto sem paginar.
      x-capability: object-context
  /v1/objects/path:
    get:
      responses:
        "200":
          description: Objetos de partida, com quantos alcançam
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        kind:
                          type: string
                          description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                            pelo mesmo motivo de `rel`: `offering` e
                            `fund_share_class` nasceram em 19/08/2026 e um
                            cliente que validasse contra a lista anterior
                            recusaria a resposta inteira. `getObjectCensus`
                            lista os vigentes."
                        name:
                          type:
                            - string
                            - "null"
                        reached_count:
                          type: integer
                          description: "Objetos DISTINTOS alcançados a partir deste, na posição escolhida
                            por `count_at`. Distintos, não arestas: dois
                            caminhos até o mesmo objeto são um alcance só."
                        count:
                          type: integer
                          description: "DEPRECIADO, sai na próxima versão maior: use `reached_count`, que
                            é o mesmo número."
                      required:
                        - id
                        - kind
                        - name
                        - reached_count
                        - count
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      as_of:
                        type:
                          - string
                          - "null"
                        description: "A data efetivamente aplicada, que pode ser anterior à pedida:
                          verbo de retrato recua até a última competência
                          publicada (veja `last_valid_to` em
                          `listObjectRelations`). Nulo quando a consulta mistura
                          formas. Ausente quando `at` não foi pedido."
                      as_of_by_rel:
                        type: object
                        additionalProperties:
                          type:
                            - string
                            - "null"
                        description: >-
                          A data EFETIVAMENTE aplicada a cada verbo, quando a
                          consulta prende mais de um — é onde o escalar `as_of`
                          seria nulo por não haver recorte comum.


                          Leia por FORMA, senão o campo engana: em verbo de
                          RETRATO (`holds`, `assigned_to`) o valor pode ter
                          RECUADO até a última competência publicada, e é essa a
                          informação. Em verbo de EVENTO (`issued`) ele é a data
                          que você pediu, porque tudo que aconteceu até lá conta
                          — não é 'a competência do issued', é o teto do corte.
                          Ausente quando há um verbo só (aí o escalar responde)
                          ou quando `at` não foi pedido.
                      excluded_shapes:
                        type: array
                        items:
                          type: string
                          enum:
                            - event
                            - snapshot
                            - static
                          description: "`event` = aconteceu numa data e não deixa de ter acontecido
                            (emissão, sucessão): vigência não se aplica e a
                            contagem é o total. `snapshot` = a fonte republica
                            um retrato por competência e o novo substitui o
                            velho (carteira de fundo, carteira de índice): a
                            contagem é o último retrato. `static` = o registro
                            publica o vigente, sem série (gestor, custodiante,
                            auditor, sócio): a contagem é o que vale hoje."
                        description: Formas removidas do conjunto por causa da data. `static` aparece
                          aqui quando `at` é passado e a consulta não prende um
                          verbo — o registro publica só o vigente, e devolvê-lo
                          apresentaria o estado de hoje como fato daquela data.
                          Vazio quando nada foi removido; ausente quando `at`
                          não foi pedido.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: traverseObjectPath
      tags:
        - Objects
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
          required: false
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - in: query
          name: total
          schema:
            type: string
            description: true = inclui `meta.total` (contagem do universo filtrado). Custa
              uma consulta a mais.
          required: false
        - in: query
          name: steps
          schema:
            type: string
            description: "Cadeia `verbo:direção` separada por vírgula, 1 a 3 passos. Ex.:
              `contains:out,issued:out`"
          required: true
        - in: query
          name: start_id
          schema:
            type: string
          required: false
        - in: query
          name: start_kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: >-
              O que a coisa É. Papel (emissor, cedente, administrador) NÃO é
              kind — é aresta. `equity_security` é a ação (PETR4), distinta da
              companhia que a emitiu.


              `offering` é a oferta pública, e é o único que não é uma COISA: é
              uma RELAÇÃO que virou objeto. Ela liga emissor, instrumento e
              tempo, e carrega atributos próprios (volume, rito, status,
              quantidade, preço) que não cabem numa aresta. Atravesse `issued`
              na direção `in` para chegar ao emissor.


              `market_event` é o segundo do mesmo feitio: um ACONTECIMENTO que
              virou objeto. Ele existe porque aresta liga dois objetos, e sem nó
              do lado do evento não havia como compor — `getObjectEvents`
              responde 'o que aconteceu com este objeto' e nunca 'quais empresas
              foram citadas nos mesmos eventos que esta'. Atravesse `mentions`
              na direção `in` para ir do objeto aos eventos que falam dele, e
              `out` para ir do evento aos objetos citados. **Só o evento com
              objeto carimbado vira nó**: os demais seguem legíveis por
              `getObjectEvents` e não viram ilha no censo.


              `fund_share_class` é a SUBCLASSE de fundo da RCVM 175 — o que o
              investidor efetivamente subscreve, distinto da classe que tem o
              CNPJ. Mesma separação de `equity_security` × `company`: o que se
              compra não é o veículo. Atravesse `contains` na direção `in` para
              chegar à classe. Ela nasce sem medidas: os atributos (situação,
              público-alvo) ainda não são servidos, e ausência aqui é isso, não
              'a subclasse não tem'.


              **`indicator` × `data_series`, a regra, porque a pergunta se
              repete.** `indicator` é o CONCEITO — existe fora do DataBolsa, tem
              nome próprio no mercado e é o que um contrato cita: IPCA, Selic,
              CDI, TR, IGP-M, INPC, dólar PTAX. `data_series` é uma PUBLICAÇÃO
              numerada: uma fonte, uma unidade, uma transformação, um calendário
              — `bcb_sgs:433` é a variação mensal do IPCA e `bcb_sgs:13522` é o
              acumulado em 12 meses do MESMO conceito. O conceito tem uma série
              `headline` e quantas variantes a fonte publicar; a série nunca tem
              conceito além do que ela mede.


              Daí a consequência que surpreende: o que o DataBolsa CALCULA é
              `data_series`, não `indicator`, mesmo quando tem nome de conceito.
              `juro_real_ex_ante`, `earnings_yield_agregado` e os escores de
              regime são receitas nossas — a linhagem sai em `lineage` — e não
              coisas que uma escritura possa citar. São 7 conceitos e centenas
              de séries, e isso é o desenho, não cobertura faltando. **Resolva
              pelo NOME e leia o `kind` que voltar**, em vez de filtrar por
              `kind=indicator` esperando encontrar tudo.
          required: false
        - in: query
          name: start_subkind
          schema:
            type: string
            description: "Recorte DENTRO do tipo na partida: `fidc` em vez de `fund`."
          required: false
        - in: query
          name: end_id
          schema:
            type: string
            description: Prende a ponta final a um objeto específico.
          required: false
        - in: query
          name: end_kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: >-
              O que a coisa É. Papel (emissor, cedente, administrador) NÃO é
              kind — é aresta. `equity_security` é a ação (PETR4), distinta da
              companhia que a emitiu.


              `offering` é a oferta pública, e é o único que não é uma COISA: é
              uma RELAÇÃO que virou objeto. Ela liga emissor, instrumento e
              tempo, e carrega atributos próprios (volume, rito, status,
              quantidade, preço) que não cabem numa aresta. Atravesse `issued`
              na direção `in` para chegar ao emissor.


              `market_event` é o segundo do mesmo feitio: um ACONTECIMENTO que
              virou objeto. Ele existe porque aresta liga dois objetos, e sem nó
              do lado do evento não havia como compor — `getObjectEvents`
              responde 'o que aconteceu com este objeto' e nunca 'quais empresas
              foram citadas nos mesmos eventos que esta'. Atravesse `mentions`
              na direção `in` para ir do objeto aos eventos que falam dele, e
              `out` para ir do evento aos objetos citados. **Só o evento com
              objeto carimbado vira nó**: os demais seguem legíveis por
              `getObjectEvents` e não viram ilha no censo.


              `fund_share_class` é a SUBCLASSE de fundo da RCVM 175 — o que o
              investidor efetivamente subscreve, distinto da classe que tem o
              CNPJ. Mesma separação de `equity_security` × `company`: o que se
              compra não é o veículo. Atravesse `contains` na direção `in` para
              chegar à classe. Ela nasce sem medidas: os atributos (situação,
              público-alvo) ainda não são servidos, e ausência aqui é isso, não
              'a subclasse não tem'.


              **`indicator` × `data_series`, a regra, porque a pergunta se
              repete.** `indicator` é o CONCEITO — existe fora do DataBolsa, tem
              nome próprio no mercado e é o que um contrato cita: IPCA, Selic,
              CDI, TR, IGP-M, INPC, dólar PTAX. `data_series` é uma PUBLICAÇÃO
              numerada: uma fonte, uma unidade, uma transformação, um calendário
              — `bcb_sgs:433` é a variação mensal do IPCA e `bcb_sgs:13522` é o
              acumulado em 12 meses do MESMO conceito. O conceito tem uma série
              `headline` e quantas variantes a fonte publicar; a série nunca tem
              conceito além do que ela mede.


              Daí a consequência que surpreende: o que o DataBolsa CALCULA é
              `data_series`, não `indicator`, mesmo quando tem nome de conceito.
              `juro_real_ex_ante`, `earnings_yield_agregado` e os escores de
              regime são receitas nossas — a linhagem sai em `lineage` — e não
              coisas que uma escritura possa citar. São 7 conceitos e centenas
              de séries, e isso é o desenho, não cobertura faltando. **Resolva
              pelo NOME e leia o `kind` que voltar**, em vez de filtrar por
              `kind=indicator` esperando encontrar tudo.
          required: false
        - in: query
          name: end_subkind
          schema:
            type: string
            description: "O mesmo recorte na ponta: `debenture` em vez de `instrument`."
          required: false
        - in: query
          name: count_at
          schema:
            type: integer
            minimum: 0
            maximum: 3
          required: false
        - in: query
          name: return_at
          schema:
            type: integer
            minimum: 0
            maximum: 3
            description: Posição LISTADA. 0 = a partida (default); use o meio quando a
              pergunta for sobre ele.
          required: false
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Corte temporal (AAAA-MM-DD), aplicado a TODOS os saltos. **Default
              DIFERENTE de `listObjectLinks`**: SEM `at`, cada salto de forma
              `snapshot` atravessa o retrato VIGENTE do PRÓPRIO SUJEITO da
              aresta — fundo cuja última CDA está uma competência atrás continua
              no caminho, com o retrato dele (o recuo pela competência GLOBAL do
              verbo o apagava em silêncio: medido em 23/08/2026, uma cadeia com
              66 emissores respondia vazio). `static` atravessa o vigente;
              `event` conta tudo que aconteceu. Para 'quem JÁ se relacionou' use
              `listObjectLinks` ou `findObjectPaths` sem `at`. COM `at`, vale a
              mesma regra por sujeito na data pedida."
          required: false
      summary: "Travessia em CADEIA: A → B → C, com o meio invisível"
      description: >-
        Para pergunta de dois ou três saltos, onde o objeto do meio existe só
        para ligar as pontas. Também responde SUPERLATIVO sobre relação — 'qual
        X tem mais Y' — porque a lista já vem ordenada por `reached_count`
        decrescente — objetos DISTINTOS alcançados, não caminhos: a resposta é a
        primeira linha.


        TRÊS EXEMPLOS COMPLETOS, com os parâmetros exatos:

        - 'qual auditor audita mais fundos que detêm papel emitido' →
        `steps=audits:out,holds:out,issued:in&count_at=1&total=true`.
        `count_at=1` porque a contagem pedida é de FUNDOS (posição 1); sem ele
        contaria PAPÉIS (a ponta) e daria outro número, maior e plausível.

        - 'qual índice tem mais empresas emissoras de debênture' →
        `steps=exposed_to_issuer:out,issued:out&count_at=1&total=true`.
        `exposed_to_issuer` salta detentor→papel→emissora de uma vez.

        - 'quais empresas têm debênture indexada ao IPCA' →
        `steps=issued:out,indexed_to:out&start_kind=company&end_id=<id do
        ipca>&total=true`. O alvo é o CONCEITO (`ipca`), não a série que o
        publica (`bcb_sgs:433`).


        **O ÍNDICE CONTÉM O PAPEL, NÃO A COMPANHIA** (desde 16/08/2026, quando
        PETR3 e PETR4 deixaram de ser a mesma coisa). Toda cadeia que sai de um
        índice e quer chegar na emissora paga um salto a mais: `contains:out`
        traz papéis e `issued:in` traz quem os emitiu, e cadeias que cabiam em
        três passaram a exigir quatro. O quarto NÃO existe: medido em produção,
        uma travessia de quatro saltos leva 86 segundos.


        Por isso existe **`exposed_to_issuer`**, que faz detentor→papel→emissora
        em UM salto. Use-a sempre que a pergunta for sobre exposição à EMPRESA.
        Ela NÃO tem `magnitude`, de propósito: tamanho é do papel, e agregar
        tamanho por emissora foi o que apagava R$ 4,54 bilhões antes da
        separação. Para valores, atravesse `holds` até o papel.


        POSIÇÕES: 0 é a partida, 1 é depois do primeiro salto, e assim por
        diante. `return_at` escolhe quem é LISTADO (default 0); `count_at`
        escolhe quem é CONTADO (default: a ponta). `end_id` prende a ponta final
        a um objeto; `end_kind` prende o tipo dela.


        O TOTAL É `meta.total` com `?total=true` — nunca conte as linhas da
        página, que são no máximo `limit`. A direção é obrigatória em cada
        passo: `holds:out` é 'este detém aquele' e `holds:in` é o inverso. O
        recorte temporal é POR PASSO, cada verbo pela sua forma; passar `at`
        força a mesma data em todos, o que só serve para pergunta datada.
      x-capability: object-context
  /v1/objects/paths:
    get:
      responses:
        "200":
          description: Cadeias que ligam os dois objetos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        hops:
                          type: integer
                          description: Saltos desta cadeia. Menos saltos = ligação mais direta.
                        chain:
                          type: string
                          description: "A cadeia lida da origem para o destino, ex.: `issued:out →
                            indexed_to:out`."
                        path_count:
                          type: integer
                          description: Quantos caminhos distintos seguem esta mesma cadeia.
                        paths:
                          type: integer
                          description: "DEPRECIADO, sai na próxima versão maior: use `path_count`."
                        examples:
                          type: array
                          items:
                            type: string
                          description: Até 3 objetos intermediários, para tornar a ligação concreta.
                      required:
                        - hops
                        - chain
                        - path_count
                        - paths
                        - examples
                  meta:
                    type: object
                    properties:
                      at:
                        type: string
                        description: A data pedida, ecoada.
                      as_of:
                        type:
                          - string
                          - "null"
                        description: "A data efetivamente aplicada, que pode ser anterior à pedida:
                          verbo de retrato recua até a última competência
                          publicada (veja `last_valid_to` em
                          `listObjectRelations`). Nulo quando a consulta mistura
                          formas. Ausente quando `at` não foi pedido."
                      as_of_by_rel:
                        type: object
                        additionalProperties:
                          type:
                            - string
                            - "null"
                        description: >-
                          A data EFETIVAMENTE aplicada a cada verbo, quando a
                          consulta prende mais de um — é onde o escalar `as_of`
                          seria nulo por não haver recorte comum.


                          Leia por FORMA, senão o campo engana: em verbo de
                          RETRATO (`holds`, `assigned_to`) o valor pode ter
                          RECUADO até a última competência publicada, e é essa a
                          informação. Em verbo de EVENTO (`issued`) ele é a data
                          que você pediu, porque tudo que aconteceu até lá conta
                          — não é 'a competência do issued', é o teto do corte.
                          Ausente quando há um verbo só (aí o escalar responde)
                          ou quando `at` não foi pedido.
                      excluded_shapes:
                        type: array
                        items:
                          type: string
                          enum:
                            - event
                            - snapshot
                            - static
                          description: "`event` = aconteceu numa data e não deixa de ter acontecido
                            (emissão, sucessão): vigência não se aplica e a
                            contagem é o total. `snapshot` = a fonte republica
                            um retrato por competência e o novo substitui o
                            velho (carteira de fundo, carteira de índice): a
                            contagem é o último retrato. `static` = o registro
                            publica o vigente, sem série (gestor, custodiante,
                            auditor, sócio): a contagem é o que vale hoje."
                        description: Formas removidas do conjunto por causa da data. `static` aparece
                          aqui quando `at` é passado e a consulta não prende um
                          verbo — o registro publica só o vigente, e devolvê-lo
                          apresentaria o estado de hoje como fato daquela data.
                          Vazio quando nada foi removido; ausente quando `at`
                          não foi pedido.
                    required:
                      - at
                    description: Presente só quando `at` foi pedido.
                required:
                  - data
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: findObjectPaths
      tags:
        - Objects
      parameters:
        - in: query
          name: from_id
          schema:
            type: string
          required: true
        - in: query
          name: to_id
          schema:
            type: string
          required: true
        - in: query
          name: max_hops
          schema:
            type: integer
            minimum: 1
            maximum: 3
            default: 2
          required: false
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 25
            default: 10
          required: false
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: >-
              Recorta CADA salto pela data. Sem ele a ligação pode ser
              inteiramente histórica — dois objetos que se cruzaram em 2019 e
              nunca mais.


              A CADEIA É DESCOBERTA, então esta rota não recusa a pergunta
              datada como as irmãs fazem: não há verbo preso para recusar. Com
              `at` no passado, aresta de forma `static` (gestor, custodiante,
              auditor, sócio) sai da busca — devolvê-la apresentaria o estado de
              hoje como caminho daquela data — e `meta.excluded_shapes` diz que
              saiu. Caminho composto só de `event`/`snapshot` continua
              respondendo normalmente.


              Corte temporal (AAAA-MM-DD). SEM ele a resposta é 'quem JÁ se
              relacionou', não 'quem se relaciona' — a diferença é grande e
              silenciosa.


              **O RECORTE DEPENDE DA FORMA DO VERBO** (`shape`, publicado em
              `listObjectRelations`):


              - `event` (`issued`, `indexed_to`, `succeeded_by`): aconteceu numa
              data e não deixa de ter acontecido — entra tudo que ocorreu
              **até** `at`, sem limite superior. Aresta sem data declarada NÃO
              entra: o registro de ações não publica data de emissão, e afirmar
              que ela já existia numa data passada é afirmação que a fonte não
              faz.

              - `snapshot` (`holds`, `contains`, `assigned_to`,
              `exposed_to_issuer`): retrato por competência, e nenhum retrato se
              afirma válido depois de ser tirado. A janela é **contida**
              (relação que saiu e voltou não aparece no buraco), e `at` mais
              recente que a última competência **recua** até ela — a resposta
              diz em `meta.as_of` qual data foi realmente aplicada. Cada verbo
              tem a competência DELE: `holds` fecha no trimestre da CDA,
              `contains` no pregão do dia.

              - `static` (`manages`, `administers`, `custodies`, `audits`,
              `same_owner`, `shareholder_of`, `measures`, `forecasts`): o
              registro publica só o VIGENTE — 111.729 arestas sem início
              declarado. Com o verbo preso em `rel`, `at` no passado responde
              **400** em vez de devolver o gestor de hoje como gestor de 2020.
              Sem verbo preso, essas arestas são removidas do conjunto e
              `meta.excluded_shapes` diz que foram.


              **AS IRMÃS NÃO COMPARTILHAM O DEFAULT.** Sem `at`: esta operação,
              `listGlobalLinks`, `intersectObjects` e `findObjectPaths`
              respondem 'quem JÁ se relacionou'; `getObjectLinkStats` resume o
              retrato VIGENTE (última competência do próprio conjunto);
              `traverseObjectPath` atravessa o retrato vigente POR SUJEITO em
              cada salto. As diferenças são deliberadas — histórico acumulado,
              resumo e travessia respondem perguntas distintas — e cada rota
              declara a sua no próprio `at`. Medido em 23/08/2026: 1 aresta no
              resumo contra 100+ na listagem, para o mesmo objeto, os dois
              certos.
          required: false
        - in: query
          name: resolve
          schema:
            type: string
            enum:
              - auto
              - exact
            default: auto
            description: >-
              Como interpretar o `id` recebido.


              - `auto` (default) — como REFERÊNCIA publicada: id fundido resolve
              sozinho para o sucessor (com `redirected_from` preenchido) e id
              cindido responde **409** com a lista.

              - `exact` — como o id do objeto que existe HOJE: nenhum histórico
              é consultado, e o id ou responde ou é 404.


              **Quando você precisa de `exact`:** numa cisão, um dos sucessores
              é o PRÓPRIO id pedido — o ramo que ficou com a chave de nascimento
              herda o identificador. O 409 manda consultar cada sucessor, e
              consultar esse cairia no mesmo 409. `exact` é o endereço dele.
              Serve também para desligar o redirect automático de fusão quando
              você quer saber se o id que guardou ainda é o id de alguma coisa.
          required: false
      summary: COMO dois objetos se ligam — descobre a cadeia, não a percorre
      description: >-
        Para a pergunta de quem viu dois objetos no mesmo lugar e não sabe por
        quê: 'como esta empresa se liga a este fundo'. Diferente de
        `traverseObjectPath`, que percorre uma cadeia que VOCÊ especifica — aqui
        a cadeia é o que se descobre.


        Agrupado por cadeia, com `paths` dizendo quantos caminhos a sustentam e
        `examples` trazendo intermediários concretos: a Petrobras chega ao IPCA
        por 10 debêntures, e dez linhas iguais seriam despejo em vez de
        resposta. Cadeias mais curtas vêm primeiro.


        `max_hops` é 2 por default. O terceiro salto é CARO (segundos) e o
        quarto não existe de propósito: com grau médio alto ele liga quase tudo
        a quase tudo, e caminho que sempre existe não é evidência de nada. A
        direção da aresta é ignorada na busca e anotada em cada salto (`:out` =
        o objeto anterior pratica o verbo).


        `path_count` conta CAMINHOS DISTINTOS — sequências de objetos —, não
        linhas de aresta: a mesma ligação afirmada por duas fontes é um caminho,
        e contá-la duas vezes transformava redundância de fonte em argumento de
        robustez. Nenhum intermediário repete, nem volta para a origem ou o
        destino.
      x-capability: object-context
  /v1/objects/stats:
    get:
      responses:
        "200":
          description: Censo do grafo
          content:
            application/json:
              schema:
                type: object
                properties:
                  objects:
                    type: integer
                  by_kind:
                    type: array
                    items:
                      type: object
                      properties:
                        kind:
                          type: string
                          description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                            pelo mesmo motivo de `rel`: `offering` e
                            `fund_share_class` nasceram em 19/08/2026 e um
                            cliente que validasse contra a lista anterior
                            recusaria a resposta inteira. `getObjectCensus`
                            lista os vigentes."
                        count:
                          type: integer
                      required:
                        - kind
                        - count
                  by_subkind:
                    type: array
                    items:
                      type: object
                      properties:
                        kind:
                          type: string
                          description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                            pelo mesmo motivo de `rel`: `offering` e
                            `fund_share_class` nasceram em 19/08/2026 e um
                            cliente que validasse contra a lista anterior
                            recusaria a resposta inteira. `getObjectCensus`
                            lista os vigentes."
                        subkind:
                          type:
                            - string
                            - "null"
                          description: "Fundo: `fidc`, `fii`, `fip`, `fif`, `fiagro`, `fiim`, `etf`,
                            `fapi`, `funcine`. Instrumento: `debenture`,
                            `securitizado`, `bdr`, `coe`, `tesouro`. Papel:
                            `on`, `pn`, `unit` — a classe da ação, que é o que
                            ela é dentro do tipo. Prestador: `gestora`,
                            `administradora_fiduciaria`, `assessoria`,
                            `agencia_rating` — o que a casa declarou SER perante
                            a CVM; quem gere E administra sai `gestora`. Auditor
                            e custodiante ficam sem sub-tipo de propósito (só
                            existem como menção no cadastro de fundo de
                            terceiro) — descubra-os pelas arestas
                            `audits`/`custodies`. NULO é resposta honesta — a
                            fonte que classifica cobre só o universo ATIVO,
                            então fundo encerrado fica sem sub-tipo. Ausência
                            aqui é 'não sabemos o que é', nunca 'não é nada'."
                        count:
                          type: integer
                      required:
                        - kind
                        - subkind
                        - count
                    description: "O recorte que `by_kind` não dava: quantos FIDCs, FIIs, ETFs,
                      debêntures e BDRs existem. Sub-tipo nulo aparece como
                      linha própria — é população real, não resto."
                  ambiguous_key_objects:
                    type: integer
                    description: Objetos com alguma chave que aponta para mais de um — trate com
                      cautela.
                  keys:
                    type: integer
                  by_key_type:
                    type: array
                    items:
                      type: object
                      properties:
                        key_type:
                          type: string
                          description: "Tipo de identificador, como o servidor o publica. `string` e não
                            enum pelo mesmo motivo de `kind` e `rel`:
                            `commodity_key`, `iso2` e `iso3` nasceram em
                            20/08/2026 e um cliente que validasse contra a lista
                            anterior recusaria a resposta inteira — foi o que
                            aconteceu. `getObjectCensus` lista os tipos de chave
                            vigentes."
                        count:
                          type: integer
                      required:
                        - key_type
                        - count
                  keys_via_alias:
                    type: integer
                    description: Chaves que chegaram ao objeto por cadeia de apelido.
                  keys_low_confidence:
                    type: integer
                  relationships:
                    type: integer
                    description: Relações DISTINTAS (origem, verbo, destino) no grafo inteiro, de
                      todos os tempos.
                  assertions:
                    type: integer
                    description: Linhas de aresta — uma por (fonte, período contíguo). É maior que
                      `relationships`, e a diferença é redundância de fonte.
                  links:
                    type: integer
                    description: "DEPRECIADO, sai na próxima versão maior: era `assertions` com nome
                      de `relationships`."
                required:
                  - objects
                  - by_kind
                  - by_subkind
                  - ambiguous_key_objects
                  - keys
                  - by_key_type
                  - keys_via_alias
                  - keys_low_confidence
                  - relationships
                  - assertions
                  - links
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getObjectCensus
      tags:
        - Objects
      parameters: []
      summary: Quantos objetos existem no grafo, por tipo e por identificador
      description: "O censo: objetos que EXISTEM, por `kind`, e as chaves que apontam
        para eles. **Não confundir com `getObjectLinkStats`**, que conta objetos
        com determinada RELAÇÃO — são perguntas diferentes e responder uma com a
        outra erra por ordem de grandeza. Traz também quantos objetos têm chave
        ambígua e quantas chaves chegaram por cadeia de apelido, que é a medida
        de quanto o grafo depende de resolução."
      x-capability: object-context
  /v1/objects/links/stats:
    get:
      responses:
        "200":
          description: Resumo do conjunto
          content:
            application/json:
              schema:
                type: object
                properties:
                  shape:
                    type:
                      - string
                      - "null"
                    enum:
                      - event
                      - snapshot
                      - static
                      - null
                    description: "null quando a consulta não fixa `rel`: verbos de formas diferentes
                      não têm recorte comum."
                  as_of:
                    type:
                      - string
                      - "null"
                    description: O recorte efetivamente aplicado. null = nenhum, e aí os números são
                      de todos os tempos.
                  relationship_count:
                    type: integer
                    description: Relações DISTINTAS (origem, verbo, destino), JÁ recortadas por
                      `as_of`. É o número a reportar.
                  assertion_count:
                    type: integer
                    description: Afirmações que sustentam essas relações — uma por (fonte, período
                      contíguo). Sempre >= `relationship_count`.
                  edges:
                    type: integer
                    description: "DEPRECIADO, sai na próxima versão maior: use `assertion_count`,
                      que é o mesmo número."
                  from_count:
                    type: integer
                    description: Objetos DISTINTOS na ponta que pratica o verbo.
                  to_count:
                    type: integer
                    description: Objetos DISTINTOS na ponta que recebe.
                  source_count:
                    type: integer
                    description: Fontes distintas que afirmam alguma dessas relações.
                  magnitude_max:
                    type:
                      - number
                      - "null"
                    description: "MÁXIMO, nunca soma: somar magnitude ao longo de competências
                      produz número sem sentido."
                  magnitude_unit:
                    type:
                      - string
                      - "null"
                  evidence_max:
                    type:
                      - number
                      - "null"
                    description: "DEPRECIADO: use `magnitude_max`."
                  evidence_unit:
                    type:
                      - string
                      - "null"
                    description: "DEPRECIADO: use `magnitude_unit`."
                  valid_from_min:
                    type:
                      - string
                      - "null"
                    description: Início mais antigo observado.
                  valid_to_max:
                    type:
                      - string
                      - "null"
                    description: Fim mais recente observado.
                required:
                  - shape
                  - as_of
                  - relationship_count
                  - assertion_count
                  - edges
                  - from_count
                  - to_count
                  - source_count
                  - magnitude_max
                  - magnitude_unit
                  - evidence_max
                  - evidence_unit
                  - valid_from_min
                  - valid_to_max
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getObjectLinkStats
      tags:
        - Objects
      parameters:
        - in: query
          name: rel
          schema:
            type: string
            enum:
              - issued
              - assigned_to
              - holds
              - manages
              - administers
              - custodies
              - audits
              - same_owner
              - shareholder_of
              - indexed_to
              - rates
              - mentions
              - measures
              - forecasts
              - contains
              - member_of
              - exposed_to_issuer
              - succeeded_by
              - produces
              - covers
            description: >-
              O verbo da relação. `from` é sempre quem PRATICA a ação.


              **`measures` e `forecasts` são coisas diferentes e foram separados
              em 20/08/2026.** `bcb_sgs:433 --measures--> IPCA` é a inflação que
              OCORREU; `bcb_focus:ipca:2027 --forecasts--> IPCA` é a que se
              espera. Enquanto dividiam um verbo só, as 45 arestas do IPCA saíam
              com `source`, `shape` e `confidence` idênticos — 4 mediam e 41
              projetavam — e quem atravessasse 'a inflação medida' recebia 41
              previsões junto, sem erro e com tudo plausível.


              **`mentions` liga o EVENTO ao objeto citado**, e a ponta do objeto
              vem carimbada pelo detector na escrita — não casada por nome em
              tempo de consulta. `magnitude` carrega quantos objetos aquele
              evento cita: 1 a 3 é uma notícia sobre eles, 65 é um apanhado de
              mercado, e `confidence` cai para `low` a partir de 9.


              **`covers` diz de que PAÍS a série fala** —
              `world_bank:NY.GDP.MKTP.CD:DE` cobre a Alemanha. Só onde a fonte
              declara o país, e não onde se poderia deduzi-lo de quem publica: o
              Banco Central publica a PTAX, que é sobre o par BRL/USD e não
              sobre o Brasil.


              **`produces` liga PAÍS a commodity mineral**, e é a primeira
              aresta entre dois tipos que antes não tocavam nada: país e
              commodity eram objetos que resolviam por nome, apareciam no censo
              e não tinham uma única relação. `magnitude` é a PARTICIPAÇÃO NO
              TOTAL MUNDIAL em pontos percentuais, e não a quantidade — tonelada
              de nióbio e quilate de diamante não se comparam, participação se
              compara sempre. Nula onde a própria fonte marca as bases como não
              somáveis, e nas commodities com mais de um agregado (cobre tem
              produção de mina e de refino) só o recorte que mais países
              reportam carrega número. NÃO liga empresa a produto: os produtores
              que o USGS publica são países, e ligar a Vale ao minério de ferro
              por semelhança de nome seria vínculo inventado.


              **`rates` é a primeira relação vinda de EXTRAÇÃO**, não de
              formulário: a nota sai do relatório da própria agência, lido por
              LLM, e a aresta carrega o `magnitude` como o NOTCH da nota com
              `magnitude_unit` na escala (`national_br` ou `global`). Notch de
              escalas diferentes não se compara — `AAA` nacional é relativo ao
              teto soberano.


              **`manages` e `administers` também são coisas diferentes.** O
              GESTOR decide a carteira; o ADMINISTRADOR FIDUCIÁRIO é o
              responsável legal — constitui o fundo, registra na CVM, calcula e
              divulga a cota e contrata os demais prestadores. Quase sempre são
              casas distintas, e a concentração é oposta: o maior administrador
              responde por 7.350 fundos.
        - in: query
          name: from_kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: Restringe o tipo do objeto que PRATICA o verbo.
        - in: query
          name: to_kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: Restringe o tipo do objeto que RECEBE.
        - in: query
          name: from_subkind
          schema:
            type: string
            description: "O mesmo recorte um nível abaixo: `arestas de FIDC para cedente`, e
              não `de fundo`."
        - in: query
          name: to_subkind
          schema:
            type: string
        - in: query
          name: from_id
          schema:
            type: string
        - in: query
          name: to_id
          schema:
            type: string
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Corte temporal (AAAA-MM-DD). **Default DIFERENTE de
              `listObjectLinks`**: SEM `at`, o verbo de forma `snapshot` é
              resumido na ÚLTIMA competência (o retrato VIGENTE), não no
              histórico acumulado — a listagem sem corte devolve 'quem JÁ se
              relacionou', e os dois números divergem MUITO (medido: 1 aresta
              aqui contra 100+ lá para o mesmo objeto). É deliberado: somar
              retratos de competências diferentes contaria a mesma posição
              várias vezes. `meta.as_of` diz a competência aplicada. COM `at`,
              valem as mesmas regras de forma do verbo da listagem."
      summary: O resumo de um conjunto de relações, sem paginar nada
      description: >-
        Responde perguntas sobre o CONJUNTO em uma chamada: quantas relações
        existem, quantos objetos distintos de cada lado, desde quando, e a maior
        magnitude observada. `relationship_count` conta relações DISTINTAS e é o
        número a reportar; `assertion_count` conta as afirmações que as
        sustentam, uma por (fonte, período contíguo), e é sempre maior ou igual
        — a diferença é redundância de fonte, não tamanho. `magnitude_max` é
        MÁXIMO e nunca soma: somar magnitude ao longo de competências produz
        número sem sentido. Use quando a pergunta for 'quantos/qual o
        maior/desde quando', em vez de percorrer as arestas uma a uma.


        CUSTO, medido contra produção em 16/08/2026: com `from_id` ou `to_id`
        responde em ~12ms, porque o recorte é indexado. SEM recorte, ou só com
        `rel`, a contagem de objetos distintos varre o conjunto inteiro e leva
        de 4 a 6 segundos. Prefira sempre recortar por objeto quando a pergunta
        for sobre um objeto.
      x-capability: object-context
  /v1/objects/links:
    get:
      responses:
        "200":
          description: Página de relações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        rel:
                          type: string
                          description: "O verbo da relação, como o servidor o publica. É `string` e não
                            enum de propósito: verbo novo tem de chegar a você
                            como valor desconhecido, nunca derrubando a
                            resposta. `listObjectRelations` lista os vigentes
                            com forma temporal, domínio e alcance."
                        shape:
                          type: string
                          enum:
                            - event
                            - snapshot
                            - static
                          description: "`event` = aconteceu numa data e não deixa de ter acontecido
                            (emissão, sucessão): vigência não se aplica e a
                            contagem é o total. `snapshot` = a fonte republica
                            um retrato por competência e o novo substitui o
                            velho (carteira de fundo, carteira de índice): a
                            contagem é o último retrato. `static` = o registro
                            publica o vigente, sem série (gestor, custodiante,
                            auditor, sócio): a contagem é o que vale hoje."
                        from_id:
                          type: string
                        to_id:
                          type: string
                        source:
                          type: string
                        valid_from:
                          type:
                            - string
                            - "null"
                        valid_to:
                          type:
                            - string
                            - "null"
                        observations:
                          type: integer
                        magnitude:
                          type:
                            - number
                            - "null"
                          description: Tamanho da relação no corte pedido; sem `at`, máximo do segmento
                            histórico. Nulo NÃO é zero.
                        magnitude_unit:
                          type:
                            - string
                            - "null"
                        evidence:
                          type:
                            - number
                            - "null"
                          description: "DEPRECIADO, sai na próxima versão maior: use `magnitude`."
                        evidence_unit:
                          type:
                            - string
                            - "null"
                          description: "DEPRECIADO: use `magnitude_unit`."
                        confidence:
                          type: string
                          enum:
                            - high
                            - low
                      required:
                        - rel
                        - shape
                        - from_id
                        - to_id
                        - source
                        - valid_from
                        - valid_to
                        - observations
                        - magnitude
                        - magnitude_unit
                        - evidence
                        - evidence_unit
                        - confidence
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      as_of:
                        type:
                          - string
                          - "null"
                        description: "A data efetivamente aplicada, que pode ser anterior à pedida:
                          verbo de retrato recua até a última competência
                          publicada (veja `last_valid_to` em
                          `listObjectRelations`). Nulo quando a consulta mistura
                          formas. Ausente quando `at` não foi pedido."
                      as_of_by_rel:
                        type: object
                        additionalProperties:
                          type:
                            - string
                            - "null"
                        description: >-
                          A data EFETIVAMENTE aplicada a cada verbo, quando a
                          consulta prende mais de um — é onde o escalar `as_of`
                          seria nulo por não haver recorte comum.


                          Leia por FORMA, senão o campo engana: em verbo de
                          RETRATO (`holds`, `assigned_to`) o valor pode ter
                          RECUADO até a última competência publicada, e é essa a
                          informação. Em verbo de EVENTO (`issued`) ele é a data
                          que você pediu, porque tudo que aconteceu até lá conta
                          — não é 'a competência do issued', é o teto do corte.
                          Ausente quando há um verbo só (aí o escalar responde)
                          ou quando `at` não foi pedido.
                      excluded_shapes:
                        type: array
                        items:
                          type: string
                          enum:
                            - event
                            - snapshot
                            - static
                          description: "`event` = aconteceu numa data e não deixa de ter acontecido
                            (emissão, sucessão): vigência não se aplica e a
                            contagem é o total. `snapshot` = a fonte republica
                            um retrato por competência e o novo substitui o
                            velho (carteira de fundo, carteira de índice): a
                            contagem é o último retrato. `static` = o registro
                            publica o vigente, sem série (gestor, custodiante,
                            auditor, sócio): a contagem é o que vale hoje."
                        description: Formas removidas do conjunto por causa da data. `static` aparece
                          aqui quando `at` é passado e a consulta não prende um
                          verbo — o registro publica só o vigente, e devolvê-lo
                          apresentaria o estado de hoje como fato daquela data.
                          Vazio quando nada foi removido; ausente quando `at`
                          não foi pedido.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listGlobalLinks
      tags:
        - Objects
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: total
          schema:
            type: string
            description: true = inclui `meta.total` (contagem do universo filtrado). Custa
              uma consulta a mais.
        - in: query
          name: rel
          schema:
            type: string
            enum:
              - issued
              - assigned_to
              - holds
              - manages
              - administers
              - custodies
              - audits
              - same_owner
              - shareholder_of
              - indexed_to
              - rates
              - mentions
              - measures
              - forecasts
              - contains
              - member_of
              - exposed_to_issuer
              - succeeded_by
              - produces
              - covers
            description: >-
              O verbo da relação. `from` é sempre quem PRATICA a ação.


              **`measures` e `forecasts` são coisas diferentes e foram separados
              em 20/08/2026.** `bcb_sgs:433 --measures--> IPCA` é a inflação que
              OCORREU; `bcb_focus:ipca:2027 --forecasts--> IPCA` é a que se
              espera. Enquanto dividiam um verbo só, as 45 arestas do IPCA saíam
              com `source`, `shape` e `confidence` idênticos — 4 mediam e 41
              projetavam — e quem atravessasse 'a inflação medida' recebia 41
              previsões junto, sem erro e com tudo plausível.


              **`mentions` liga o EVENTO ao objeto citado**, e a ponta do objeto
              vem carimbada pelo detector na escrita — não casada por nome em
              tempo de consulta. `magnitude` carrega quantos objetos aquele
              evento cita: 1 a 3 é uma notícia sobre eles, 65 é um apanhado de
              mercado, e `confidence` cai para `low` a partir de 9.


              **`covers` diz de que PAÍS a série fala** —
              `world_bank:NY.GDP.MKTP.CD:DE` cobre a Alemanha. Só onde a fonte
              declara o país, e não onde se poderia deduzi-lo de quem publica: o
              Banco Central publica a PTAX, que é sobre o par BRL/USD e não
              sobre o Brasil.


              **`produces` liga PAÍS a commodity mineral**, e é a primeira
              aresta entre dois tipos que antes não tocavam nada: país e
              commodity eram objetos que resolviam por nome, apareciam no censo
              e não tinham uma única relação. `magnitude` é a PARTICIPAÇÃO NO
              TOTAL MUNDIAL em pontos percentuais, e não a quantidade — tonelada
              de nióbio e quilate de diamante não se comparam, participação se
              compara sempre. Nula onde a própria fonte marca as bases como não
              somáveis, e nas commodities com mais de um agregado (cobre tem
              produção de mina e de refino) só o recorte que mais países
              reportam carrega número. NÃO liga empresa a produto: os produtores
              que o USGS publica são países, e ligar a Vale ao minério de ferro
              por semelhança de nome seria vínculo inventado.


              **`rates` é a primeira relação vinda de EXTRAÇÃO**, não de
              formulário: a nota sai do relatório da própria agência, lido por
              LLM, e a aresta carrega o `magnitude` como o NOTCH da nota com
              `magnitude_unit` na escala (`national_br` ou `global`). Notch de
              escalas diferentes não se compara — `AAA` nacional é relativo ao
              teto soberano.


              **`manages` e `administers` também são coisas diferentes.** O
              GESTOR decide a carteira; o ADMINISTRADOR FIDUCIÁRIO é o
              responsável legal — constitui o fundo, registra na CVM, calcula e
              divulga a cota e contrata os demais prestadores. Quase sempre são
              casas distintas, e a concentração é oposta: o maior administrador
              responde por 7.350 fundos.
        - in: query
          name: from_kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: Restringe o tipo do objeto que PRATICA o verbo.
        - in: query
          name: to_kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: Restringe o tipo do objeto que RECEBE.
        - in: query
          name: from_subkind
          schema:
            type: string
            description: "O mesmo recorte um nível abaixo: `arestas de FIDC para cedente`, e
              não `de fundo`."
        - in: query
          name: to_subkind
          schema:
            type: string
        - in: query
          name: from_id
          schema:
            type: string
        - in: query
          name: to_id
          schema:
            type: string
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: >-
              Corte temporal (AAAA-MM-DD). SEM ele a resposta é 'quem JÁ se
              relacionou', não 'quem se relaciona' — a diferença é grande e
              silenciosa.


              **O RECORTE DEPENDE DA FORMA DO VERBO** (`shape`, publicado em
              `listObjectRelations`):


              - `event` (`issued`, `indexed_to`, `succeeded_by`): aconteceu numa
              data e não deixa de ter acontecido — entra tudo que ocorreu
              **até** `at`, sem limite superior. Aresta sem data declarada NÃO
              entra: o registro de ações não publica data de emissão, e afirmar
              que ela já existia numa data passada é afirmação que a fonte não
              faz.

              - `snapshot` (`holds`, `contains`, `assigned_to`,
              `exposed_to_issuer`): retrato por competência, e nenhum retrato se
              afirma válido depois de ser tirado. A janela é **contida**
              (relação que saiu e voltou não aparece no buraco), e `at` mais
              recente que a última competência **recua** até ela — a resposta
              diz em `meta.as_of` qual data foi realmente aplicada. Cada verbo
              tem a competência DELE: `holds` fecha no trimestre da CDA,
              `contains` no pregão do dia.

              - `static` (`manages`, `administers`, `custodies`, `audits`,
              `same_owner`, `shareholder_of`, `measures`, `forecasts`): o
              registro publica só o VIGENTE — 111.729 arestas sem início
              declarado. Com o verbo preso em `rel`, `at` no passado responde
              **400** em vez de devolver o gestor de hoje como gestor de 2020.
              Sem verbo preso, essas arestas são removidas do conjunto e
              `meta.excluded_shapes` diz que foram.


              **AS IRMÃS NÃO COMPARTILHAM O DEFAULT.** Sem `at`: esta operação,
              `listGlobalLinks`, `intersectObjects` e `findObjectPaths`
              respondem 'quem JÁ se relacionou'; `getObjectLinkStats` resume o
              retrato VIGENTE (última competência do próprio conjunto);
              `traverseObjectPath` atravessa o retrato vigente POR SUJEITO em
              cada salto. As diferenças são deliberadas — histórico acumulado,
              resumo e travessia respondem perguntas distintas — e cada rota
              declara a sua no próprio `at`. Medido em 23/08/2026: 1 aresta no
              resumo contra 100+ na listagem, para o mesmo objeto, os dois
              certos.
      summary: Consulta relações no grafo inteiro, sem partir de um objeto
      description: "A travessia por objeto responde 'com quem ESTE se relaciona'. Esta
        responde sobre o conjunto: todas as relações de um tipo, opcionalmente
        presas a um lado (`from_id`/`to_id`) ou restritas por tipo de objeto
        (`from_kind`/`to_kind`). Para contagem e extremos, prefira
        `/links/stats` — não pagina."
      x-capability: object-context
  /v1/objects/resolve:
    get:
      responses:
        "200":
          description: Candidatos, chave exata primeiro
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        query:
                          type: string
                          description: A consulta que trouxe este candidato — com `q=a|b` a lista mistura
                            as duas.
                        id:
                          type: string
                        kind:
                          type: string
                          description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                            pelo mesmo motivo de `rel`: `offering` e
                            `fund_share_class` nasceram em 19/08/2026 e um
                            cliente que validasse contra a lista anterior
                            recusaria a resposta inteira. `getObjectCensus`
                            lista os vigentes."
                        subkind:
                          type:
                            - string
                            - "null"
                          description: "Fundo: `fidc`, `fii`, `fip`, `fif`, `fiagro`, `fiim`, `etf`,
                            `fapi`, `funcine`. Instrumento: `debenture`,
                            `securitizado`, `bdr`, `coe`, `tesouro`. Papel:
                            `on`, `pn`, `unit` — a classe da ação, que é o que
                            ela é dentro do tipo. Prestador: `gestora`,
                            `administradora_fiduciaria`, `assessoria`,
                            `agencia_rating` — o que a casa declarou SER perante
                            a CVM; quem gere E administra sai `gestora`. Auditor
                            e custodiante ficam sem sub-tipo de propósito (só
                            existem como menção no cadastro de fundo de
                            terceiro) — descubra-os pelas arestas
                            `audits`/`custodies`. NULO é resposta honesta — a
                            fonte que classifica cobre só o universo ATIVO,
                            então fundo encerrado fica sem sub-tipo. Ausência
                            aqui é 'não sabemos o que é', nunca 'não é nada'."
                        name:
                          type:
                            - string
                            - "null"
                        anchor_type:
                          type: string
                          description: "Tipo de identificador, como o servidor o publica. `string` e não
                            enum pelo mesmo motivo de `kind` e `rel`:
                            `commodity_key`, `iso2` e `iso3` nasceram em
                            20/08/2026 e um cliente que validasse contra a lista
                            anterior recusaria a resposta inteira — foi o que
                            aconteceu. `getObjectCensus` lista os tipos de chave
                            vigentes."
                        anchor_value:
                          type: string
                        match_kind:
                          type: string
                          enum:
                            - exact_key
                            - exact_name
                            - prefix_name
                            - fuzzy_name
                          description: "`exact_key` = casou um identificador (é o único em que
                            `matched_key_*` significa alguma coisa). Os três de
                            nome são palpite ordenado, do melhor para o pior —
                            trate `fuzzy_name` como candidato a confirmar, nunca
                            como resposta."
                        matched_key_type:
                          type: string
                          description: A chave por onde casou. Em `match_kind` de NOME é uma chave
                            qualquer do objeto, e não diz nada sobre a busca.
                        matched_key_value:
                          type: string
                        confidence:
                          type: string
                          enum:
                            - high
                            - low
                      required:
                        - query
                        - id
                        - kind
                        - subkind
                        - name
                        - anchor_type
                        - anchor_value
                        - match_kind
                        - matched_key_type
                        - matched_key_value
                        - confidence
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      queries:
                        type: array
                        items:
                          type: object
                          properties:
                            q:
                              type: string
                            best_id:
                              type:
                                - string
                                - "null"
                              description: O id resolvido, só quando o primeiro candidato casou por chave ou
                                nome EXATO. Nulo = escolha de quem pergunta.
                            candidates:
                              type: integer
                            reason:
                              type:
                                - string
                                - "null"
                              enum:
                                - no_match
                                - ambiguous
                                - null
                              description: Por que não há `best_id`. Nulo quando há.
                          required:
                            - q
                            - best_id
                            - candidates
                            - reason
                        description: "Um veredito por consulta, na ordem de `q`. Sem cursor: `limit`
                          vale por consulta e a lista é fechada."
                    required:
                      - next_cursor
                      - count
                      - queries
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: resolveObject
      tags:
        - Objects
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
            description: "Candidatos POR CONSULTA. Default 20. Sem cursor: a lista é
              fechada."
          required: false
        - in: query
          name: q
          schema:
            type: string
            minLength: 1
            description: |-
              Ticker, CNPJ, ISIN, código ou nome — ou VÁRIOS, separados por `|` (até 20): `q=Petrobras|Vale` resolve os dois numa chamada, cada candidato carimbado com `query` e `meta.queries` dizendo o veredito de cada uma (`best_id` só quando o topo é chave ou nome exato). Não há cursor: `limit` vale por consulta, e consultas × limit ficam sob o teto de 200 candidatos por chamada (422 acima disso).

              Chave exata tem prioridade sobre nome. O nome aceita palavras em QUALQUER ORDEM ("banco brasil" acha BANCO DO BRASIL) e termo de TIPO vira recorte ("FIDC Cielo", "debênture vale", "gestora kinea" — FIDC/FII/ETF/fundo/debênture/BDR/COE/empresa/índice/gestora/assessoria filtram em vez de casar texto). Entre nomes parecidos, vence o objeto mais REFERENCIADO pelas fontes. Se a primeira linha vier com `match_kind=exact_name` ou `exact_key`, a identidade JÁ está resolvida: use aquele `id`; não repita `search` nem tente transformar a chave em id.

              Para comparar a MESMA série macro entre países, busque o nome da medida UMA vez com `limit` suficiente (`pib_crescimento_real`), em vez de tentar uma chave por país. A lista devolve todas as variantes `— BR`, `— DE` etc.; país ausente fica provado pela mesma resposta e os ids encontrados vão direto para `getObjectHistory`.
          required: true
        - in: query
          name: kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: Restringe o tipo do objeto.
          required: false
        - in: query
          name: subkind
          schema:
            type: string
            description: "Restringe DENTRO do tipo: `fii`, `fidc`, `fip`, `etf`,
              `debenture`, `bdr`, `coe`, `tesouro`. É o recorte que `kind`
              sozinho não dá — `fund` mistura 26.846 FIF com 1.583 FIIs."
          required: false
      summary: De um texto para o objeto — ticker, CNPJ, ISIN, código ou nome
      description: >-
        Devolve CANDIDATOS, no plural e de propósito: 'Itaú' e 'BTG' são várias
        pessoas jurídicas distintas, e escolher uma em silêncio é o erro que o
        grafo existe para impedir. `confidence: low` marca chave ambígua (código
        reaproveitado após encerramento).


        **`match_kind` diz COMO casou**, e é por ele que se decide o quanto
        confiar. `exact_key` é o único em que o candidato foi identificado — e o
        único em que `matched_key_type`/`matched_key_value` significam alguma
        coisa. Os outros três são palpite ordenado do melhor para o pior: nome
        idêntico, nome que começa com o termo, e o resto. Trate `fuzzy_name`
        como candidato a confirmar, nunca como resposta.


        Buscar por NOME pode trazer o objeto certo fora da primeira posição
        (`Petrobras` casa a controlada Petroquisa e a própria companhia), então
        quem busca por nome deve olhar a lista. Quando existir identificador,
        use-o.


        **A AÇÃO É OBJETO SEPARADO DA COMPANHIA desde 16/08/2026.** `PETR4`
        resolve para o PAPEL (`kind: equity_security`), não para a Petrobras:
        preço, indicador e provento são dele e são diferentes dos de PETR3. Para
        chegar à emissora, atravesse `issued` na direção `in`; a companhia
        continua publicando `tickers` e `share_classes`. Código antigo do mesmo
        papel continua resolvendo — `VVAR3` e `VIIA3` chegam em `BHIA3`.


        Do mesmo jeito, o INDICADOR é o conceito (`ipca`) e a SÉRIE é a
        publicação dele (`bcb_sgs:433`, `kind: data_series`), ligados por
        `measures` — e por `forecasts` quando a série PROJETA o conceito em vez
        de medi-lo, como o Focus e o breakeven da curva. Código de série é
        MINÚSCULO e é encontrado como está — até 16/08/2026 a busca normalizava
        tudo para maiúscula e as 183 séries eram inalcançáveis por chave, sem
        erro nenhum.
      x-capability: object-context
  /v1/objects/relations:
    get:
      responses:
        "200":
          description: Os verbos declarados, com forma, domínio, range e nota
          content:
            application/json:
              schema:
                type: object
                properties:
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        rel:
                          type: string
                          description: "O verbo da relação, como o servidor o publica. É `string` e não
                            enum de propósito: verbo novo tem de chegar a você
                            como valor desconhecido, nunca derrubando a
                            resposta. `listObjectRelations` lista os vigentes
                            com forma temporal, domínio e alcance."
                        shape:
                          type: string
                          enum:
                            - event
                            - snapshot
                            - static
                          description: "`event` = aconteceu numa data e não deixa de ter acontecido
                            (emissão, sucessão): vigência não se aplica e a
                            contagem é o total. `snapshot` = a fonte republica
                            um retrato por competência e o novo substitui o
                            velho (carteira de fundo, carteira de índice): a
                            contagem é o último retrato. `static` = o registro
                            publica o vigente, sem série (gestor, custodiante,
                            auditor, sócio): a contagem é o que vale hoje."
                        max_gap_days:
                          type:
                            - integer
                            - "null"
                          description: Distância máxima entre competências que ainda conta como a MESMA
                            relação contínua. Acima dela a fonte parou de
                            afirmar e a aresta se parte em duas janelas. Nulo em
                            verbo sem série — e nulo aqui é 'não se aplica',
                            nunca zero.
                        last_valid_to:
                          type:
                            - string
                            - "null"
                          description: "A última competência que ESTE verbo publicou — a data mais recente
                            que faz sentido pedir em `at`. É por verbo e não da
                            base: `holds` fecha no trimestre da CDA enquanto
                            `contains` fecha no pregão do dia, e pedir a
                            competência de um no outro devolve vazio sem erro
                            nenhum. Pedir `at` mais recente que esta data não
                            devolve vazio: a consulta recua até aqui e diz em
                            `as_of` que recuou. Nulo em `event` e `static`, que
                            não têm retrato — e nulo aqui é 'não se aplica'."
                        domain_kinds:
                          type: array
                          items:
                            type: string
                            description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                              pelo mesmo motivo de `rel`: `offering` e
                              `fund_share_class` nasceram em 19/08/2026 e um
                              cliente que validasse contra a lista anterior
                              recusaria a resposta inteira. `getObjectCensus`
                              lista os vigentes."
                          description: Os tipos que podem PRATICAR o verbo. Aresta fora desta lista é
                            defeito, e há teste que reprova.
                        range_kinds:
                          type: array
                          items:
                            type: string
                            description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                              pelo mesmo motivo de `rel`: `offering` e
                              `fund_share_class` nasceram em 19/08/2026 e um
                              cliente que validasse contra a lista anterior
                              recusaria a resposta inteira. `getObjectCensus`
                              lista os vigentes."
                          description: Os tipos que podem RECEBER o verbo.
                        note:
                          type:
                            - string
                            - "null"
                          description: O que a fonte afirma, em uma frase — inclusive o que ela NÃO
                            afirma. É aqui que está a diferença entre `manages`
                            (gestor) e o administrador fiduciário, e o aviso de
                            que `shareholder_of` é quadro societário sem
                            percentual nem qualificação.
                      required:
                        - rel
                        - shape
                        - max_gap_days
                        - last_valid_to
                        - domain_kinds
                        - range_kinds
                        - note
                    description: Todos os verbos declarados, inclusive os que ainda não têm nenhuma
                      aresta.
                required:
                  - meta
                  - data
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listObjectRelations
      tags:
        - Objects
      parameters: []
      summary: "O VOCABULÁRIO dos verbos: o que cada relação afirma, e o que ela NÃO
        afirma"
      description: >-
        Leia isto ANTES de atravessar o grafo. O nome do verbo não basta:
        `manages` é o GESTOR e não o administrador fiduciário; `shareholder_of`
        é quadro societário SEM percentual e SEM qualificação, então nenhuma
        dessas arestas afirma controle; `same_owner` é simétrica e liga dois
        cedentes que compartilham sócio pessoa jurídica; `exposed_to_issuer`
        responde COM QUEM e nunca QUANTO.


        `shape` decide o que 'quantos' significa e qual `at` reproduz a
        contagem: `event` é total de todos os tempos, `snapshot` é o último
        retrato, `static` é o vigente — e verbo `static` RECUSA data passada,
        porque a fonte não publica série.


        `domain_kinds` e `range_kinds` dizem quais tipos podem estar em cada
        ponta, o que permite descartar uma travessia impossível antes de pagar
        por ela: `holds` termina em instrumento, fundo ou papel, nunca na
        companhia emissora.


        Catálogo fechado e pequeno, sem paginação. Traz também os verbos que
        ainda não têm nenhuma aresta — o range é declarado antes de o objeto
        nascer.
      x-capability: object-context
  /v1/objects/facts/catalog:
    get:
      responses:
        "200":
          description: Medidas declaradas no contrato
          content:
            application/json:
              schema:
                type: object
                properties:
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      kinds:
                        type: array
                        items:
                          type: string
                          description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                            pelo mesmo motivo de `rel`: `offering` e
                            `fund_share_class` nasceram em 19/08/2026 e um
                            cliente que validasse contra a lista anterior
                            recusaria a resposta inteira. `getObjectCensus`
                            lista os vigentes."
                        description: Todos os tipos que TÊM medida — a lista de valores que `kind`
                          aceita, dita na própria resposta.
                    required:
                      - next_cursor
                      - count
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        kind:
                          type: string
                          description: O tipo de objeto para o qual esta linha vale.
                        unit:
                          type: string
                          enum:
                            - brl
                            - usd
                            - pct
                            - ratio
                            - x
                            - count
                            - points
                            - native
                          description: "`ratio` = fração (0,08 é 8%); `pct` = percentual (8,0 é 8%) — os
                            dois existem no mesmo produto e NÃO são comparáveis
                            sem converter. `x` = múltiplo. `native` = a unidade
                            da própria série, que só o catálogo dela conhece."
                        dimension:
                          type:
                            - string
                            - "null"
                          description: "O que o número É: `currency`, `rate`, `share`, `ratio`, `index`,
                            `points`, `count`, `duration`. **`null` nos três
                            eixos = a régua é da SÉRIE, não da medida** — é o
                            caso de `data_series`, onde a mesma medida cobre 431
                            séries em escalas diferentes e só `getObjectFacts`
                            no objeto da série resolve. Não é ausência de
                            régua."
                        scale:
                          type:
                            - string
                            - "null"
                          description: "Em que escala o valor VEM: `unit` (o número é ele mesmo),
                            `percent` (8,0 é 8%), `thousand`, `million`,
                            `billion`, `bps`, `business_days`. O valor é servido
                            as-filed — a escala diz como lê-lo, não é aplicada
                            por nós. `portfolio_value_kbrl` é
                            `currency`+`thousand`: 508.272.696 são 508 BILHÕES
                            de reais."
                        period:
                          type:
                            - string
                            - "null"
                          description: "Que JANELA o número cobre — `none`, `daily`, `monthly`,
                            `quarterly`, `annual`, `ttm_12m`, `cagr_3y`. **NÃO é
                            `cadence`**, que é de quanto em quanto tempo a fonte
                            publica: as duas divergem de rotina. Confira antes
                            de comparar duas medidas — `fii_dy_12m` cobre doze
                            meses e `fii_dividend_yield_month` cobre um, e as
                            duas chegam na mesma resposta em percentual."
                        cadence:
                          type: string
                          enum:
                            - daily
                            - weekly
                            - monthly
                            - quarterly
                            - annual
                            - irregular
                          description: "De quanto em quanto tempo a fonte publica esta medida. Decide de
                            quanto em quanto tempo vale repetir a consulta, e
                            evita ler como parada uma série que é trimestral.
                            Não confunda com `axes.period`, que é a JANELA
                            coberta: uma taxa anualizada publicada diariamente
                            tem `period: annual` e `cadence: daily`. EXCEÇÃO
                            DECLARADA: em `data_series` esta linha vale
                            `irregular` porque o catálogo é por TIPO e o tipo
                            abrange séries diárias, semanais, mensais,
                            trimestrais e anuais — não existe uma cadência do
                            tipo. A cadência REAL de cada série vem em
                            `getObjectFacts`/`getObjectHistory`, lida do
                            catálogo daquela série, e por isso é o único caso em
                            que este campo e o do objeto legitimamente
                            divergem."
                        grain:
                          type: string
                          enum:
                            - object
                            - paper
                          description: "`paper` = a medida é da CLASSE DE AÇÃO e PETR3/PETR4 têm valores
                            diferentes; somar as classes duplica o emissor.
                            `object` = é do objeto inteiro e existe uma só."
                        expected_range:
                          type:
                            - object
                            - "null"
                          properties:
                            min:
                              type: number
                            max:
                              type: number
                          required:
                            - min
                            - max
                          description: "Faixa plausível, NA UNIDADE de `unit` — e a amarração importa:
                            `dy_12m` de ação é `ratio` (0,04) e `fii_dy_12m` é
                            `pct` (8,88), a mesma ideia em escalas 100×
                            diferentes. Só medida com incidente conhecido tem
                            faixa; `null` é ausência de régua, não aprovação."
                        concept:
                          type:
                            - string
                            - "null"
                          description: O CONCEITO compartilhado entre tipos — a ponte que o prefixo
                            escondia. Linhas com o mesmo `concept` são a MESMA
                            pergunta em tipos diferentes (`dy_12m` da ação e
                            `fii_dy_12m` do FII declaram `dividend_yield_12m`),
                            com `dimension` e `period` garantidamente iguais;
                            escala e moeda podem divergir e estão declaradas ao
                            lado. Para comparar entre tipos, filtre o catálogo
                            por este campo e converta pela `scale`. Nulo = sem
                            contraparte em outro tipo.
                      required:
                        - name
                        - kind
                        - unit
                        - dimension
                        - scale
                        - period
                        - cadence
                        - grain
                        - expected_range
                        - concept
                required:
                  - meta
                  - data
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFactCatalog
      tags:
        - Objects
      parameters:
        - in: query
          name: kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: Recorta o catálogo a um tipo de objeto. `meta.kinds` lista os que
              têm medida.
      summary: Que MEDIDAS existem no grafo, e em que escala cada uma
      description: >-
        O catálogo, independente de objeto: todo `fact` que `getObjectFacts`
        pode devolver e `getObjectHistory` pode servir, com a régua e os tipos
        de objeto a que se aplica.


        Leia a coluna `unit` antes de comparar duas medidas. `ratio` é fração
        (0,08 é 8%) e `pct` é percentual (8,0 é 8%): `dy_12m` de ação é fração e
        `fii_dy_12m` de FII é percentual, então comparar os dois crus erra por
        100×.


        **`dimension`, `scale` e `period` são a régua completa, e `period` é o
        eixo que `unit` não tem como dizer.** `dy_12m` cobre DOZE MESES,
        `fii_dividend_yield_month` cobre UM e `revenue_cagr_3y` é anualizado
        sobre TRÊS ANOS — três janelas que chegam na mesma resposta e que
        nenhuma unidade distingue. É a mesma armadilha do IPCA na `bcb_sgs:433`
        (o mês, ~0,4) contra a `bcb_sgs:13522` (doze meses, ~4,7).


        **O valor é servido AS-FILED e `scale` diz como lê-lo — ela não foi
        aplicada.** `portfolio_value_kbrl` é `currency` em `thousand`: os
        508.272.696 da mediana são 508 BILHÕES de reais, não 508 milhões. E
        `scale` desmente sufixo de coluna quando a fonte mente:
        `fidc_acquired_impaired_pct` termina em `_pct` e vem em `unit` (fração,
        mediana 0,0204), enquanto `fidc_collateral_pct`, na MESMA tabela, é
        percentual de verdade.


        **`unit: native` não é uma escala — é a ausência de uma.** Diz que a
        escala não é do FATO, é de cada SÉRIE: `value` e `indicator_value`
        cobrem 431 séries macro em que a mesma medida sai em percentual numa e
        em fração decimal noutra — o IPCA acumulado em 12 meses é 4,44 na
        `bcb_sgs:13522` e 0,0444 em `macro:ipca_12m`. O catálogo não tem como
        resolver isso sem saber de QUE série se fala; quem resolve é
        `getObjectFacts` no objeto da série, que devolve a `unit` já resolvida e
        os eixos declarados em `axes`. Tratar `native` como unidade é o erro de
        100× outra vez, agora sem nada na resposta para acusá-lo.
      x-capability: object-context
  /v1/objects/aggregate:
    get:
      responses:
        "200":
          description: Os grupos, com o resumo de cada um
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        key:
                          type: string
                          description: "A chave do grupo: o valor da propriedade, ou o `entity_id` do
                            vizinho."
                        label:
                          type: string
                          description: Como o grupo se chama para quem lê. Igual à chave quando o grupo é
                            palavra.
                        entity_id:
                          type:
                            - string
                            - "null"
                          description: "Preenchido SÓ quando o grupo é um OBJETO — agrupamento por
                            relação. Nulo quando o grupo é palavra. É por este
                            campo que se distingue a natureza do grupo, e é por
                            isso que o formato da linha nunca muda com o
                            parâmetro: uma linha só serve os dois casos."
                        value:
                          type:
                            - number
                            - "null"
                          description: O resultado da agregação. NULO quando nenhum objeto do grupo tem
                            valor na medida — nunca zero. Um grupo com 36
                            objetos e nenhuma cotação vale null, e zero seria
                            mentira.
                        objects:
                          type: integer
                          description: Quantos objetos da coorte caíram neste grupo.
                        with_value:
                          type: integer
                          description: Destes, quantos têm valor na medida. Menor que `objects` é
                            cobertura parcial.
                      required:
                        - key
                        - label
                        - entity_id
                        - value
                        - objects
                        - with_value
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type: "null"
                      count:
                        type: integer
                      fact:
                        type:
                          - string
                          - "null"
                        description: "A medida resumida. NULO no censo (pedido sem `fact`): `value` é
                          contagem de objetos, não há medida."
                      unit:
                        type: string
                        enum:
                          - brl
                          - usd
                          - pct
                          - ratio
                          - x
                          - count
                          - points
                          - native
                        description: "`ratio` = fração (0,08 é 8%); `pct` = percentual (8,0 é 8%) — os
                          dois existem no mesmo produto e NÃO são comparáveis
                          sem converter. `x` = múltiplo. `native` = a unidade da
                          própria série, que só o catálogo dela conhece."
                      agg:
                        type: string
                        enum:
                          - sum
                          - avg
                          - median
                          - min
                          - max
                          - count
                      group_by:
                        type: string
                      group_by_kind:
                        type: string
                        enum:
                          - property
                          - edge
                        description: A natureza do grupo, resolvida pelo servidor a partir do nome.
                      source:
                        type: string
                      cadence:
                        type: string
                        enum:
                          - daily
                          - weekly
                          - monthly
                          - quarterly
                          - annual
                          - irregular
                        description: "Com que frequência a fonte publica. `irregular` = papel que não
                          negocia todo dia, ou série sem calendário fixo: buraco
                          não é queda. **NÃO é a JANELA que o número cobre** —
                          essa é `axes.period`, e as duas divergem por regra: o
                          juro real ex-ante cobre um ano, porque é taxa
                          anualizada, e sai todo dia. Nos indicadores macro este
                          campo é MEDIDO do espaçamento das últimas publicações,
                          não declarado à mão, e por isso acompanha a fonte
                          quando ela muda de frequência."
                      grain:
                        type: string
                        enum:
                          - object
                          - paper
                        description: "`object` = uma resposta só para o objeto inteiro; somar os papéis
                          DUPLICA. `paper` = genuinamente diferente por classe
                          de ação; perguntar pelo objeto é malformado."
                      description:
                        type: string
                      cohort_objects:
                        type: integer
                        description: 'A coorte INTEIRA, tenha ou não valor na medida. NÃO se chama
                          `cohort_size` de propósito: em `rankObjects` esse nome
                          significa "objetos COM valor", e dois sentidos para o
                          mesmo nome em operações irmãs é como duas listas
                          divergem.'
                      cohort_with_value:
                        type: integer
                        description: Destes, quantos têm número na medida. É o que `rankObjects` chama
                          de `cohort_size`.
                      cohort_value:
                        type:
                          - number
                          - "null"
                        description: 'A agregação sobre a coorte INTEIRA, sem repartir — o total do
                          mercado. Calculado ANTES do agrupamento: inclui os
                          `ungrouped` e não depende do `limit`, então é o único
                          total que a soma de `data` não consegue prometer. Para
                          "quanto dá no mercado todo", leia este campo — não
                          some a página. Nulo quando nenhum objeto da coorte tem
                          valor na medida.'
                      order_by:
                        type: string
                        enum:
                          - value
                          - objects
                        description: Como a página foi ordenada e cortada, com o default explícito.
                          `value` ordena pela agregação; `objects` ordena pelo
                          TAMANHO do grupo — "as gestoras com mais fundos" é
                          pergunta de `objects`, e ordenar por `value` responde
                          outra ("as com mais fundos COM valor na medida").
                      total_groups:
                        type: integer
                        description: "Quantos grupos EXISTEM na coorte, contra os que couberam em
                          `data`. Diferente de `ungrouped`, que conta objeto sem
                          grupo nenhum: aqui o grupo existe e foi cortado pelo
                          teto. Somar `data` achando que é o total subestima em
                          silêncio."
                      groups_truncated:
                        type: boolean
                        description: "`true` quando `total_groups` é maior que o número de grupos
                          devolvidos. Antes disso, o único indício era `count`
                          bater exatamente com `limit` — e ler isso exige saber
                          o teto. Com ele verdadeiro, `data` responde 'os
                          maiores', nunca 'o total'."
                      ungrouped:
                        type: integer
                        description: "Objetos da coorte que ficaram FORA de todo grupo. Medido: 20.098
                          dos 57.090 fundos não declaram situação. Sem este
                          número, o total dos grupos parece completo sendo
                          parcial."
                      multi_group:
                        type: integer
                        description: Objetos que caíram em MAIS DE UM grupo. Zero num verbo funcional
                          como `manages`; grande em `issued`, onde a mesma
                          companhia entra uma vez por emissão e o `sum` a conta
                          várias. O número não fica errado — a leitura muda —, e
                          sem isto não há como saber qual dos dois casos se está
                          lendo.
                      filterable_properties:
                        type: array
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                            vocabulary:
                              type:
                                - array
                                - "null"
                              items:
                                type: string
                          required:
                            - name
                            - vocabulary
                        description: "O que dá para CORTAR por palavra nesta coorte, com os valores
                          possíveis — o mesmo campo de `rankObjects`. Faltava
                          aqui até 21/08/2026, e a resposta anunciava só o eixo
                          de agrupamento: quem quisesse filtrar tinha que sair
                          para `getObjectProperties` só para descobrir a grafia
                          do valor."
                      groupable:
                        type: array
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                            kind:
                              type: string
                              enum:
                                - property
                                - edge
                          required:
                            - name
                            - kind
                        description: O que dá para agrupar neste tipo. Sai da mesma fonte que valida
                          `group_by`.
                      empty_reason:
                        type: string
                        description: Por que `data` veio vazia. Ausente quando há grupos. Lista vazia
                          AFIRMA, para quem consome, que não há o que agrupar —
                          e o caso comum é direção invertida, que este campo
                          nomeia junto com a direção que funcionaria.
                    required:
                      - next_cursor
                      - count
                      - fact
                      - unit
                      - agg
                      - group_by
                      - group_by_kind
                      - source
                      - cadence
                      - grain
                      - description
                      - cohort_objects
                      - cohort_with_value
                      - cohort_value
                      - order_by
                      - total_groups
                      - groups_truncated
                      - ungrouped
                      - multi_group
                      - filterable_properties
                      - groupable
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: aggregateObjects
      tags:
        - Objects
      parameters:
        - in: query
          name: kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: O tipo dos objetos que entram na coorte.
          required: true
        - in: query
          name: subkind
          schema:
            type: string
            description: Recorte dentro do tipo.
          required: false
        - in: query
          name: fact
          schema:
            type: string
            description: "A medida a resumir, pelo nome do `listFactCatalog`. OMITA com
              `agg=count` para o CENSO: contar OBJETOS por grupo, sem medida
              nenhuma — é o único caminho de contagem para tipo que só tem
              propriedades (ex.:
              `kind=service_provider&agg=count&group_by=advisor_state&where=adv\
              isor_situation=EM FUNCIONAMENTO NORMAL`)."
          required: false
        - in: query
          name: agg
          schema:
            type: string
            enum:
              - sum
              - avg
              - median
              - min
              - max
              - count
            description: Como resumir. `sum` é RECUSADO em `pct`, `ratio`, `x` e `points` —
              somar percentual ou múltiplo devolve um número sem significado.
              `count` conta objetos COM valor na medida; sem `fact`, conta TODOS
              os objetos do grupo (censo).
          required: true
        - in: query
          name: group_by
          schema:
            type: string
            description: "Por que eixo repartir: nome de PROPRIEDADE (`situation`) ou verbo
              de RELAÇÃO (`manages`). `meta.groupable` lista os dois conjuntos,
              e `meta.group_by_kind` devolve qual foi usado."
          required: true
        - in: query
          name: group_by_direction
          schema:
            type: string
            enum:
              - in
              - out
            default: out
            description: "Onde a COORTE está na aresta, quando `group_by` é relação — a
              mesma leitura de `rel_direction`. `manages` liga gestora a fundo:
              coorte de fundos é `in`. Direção errada devolve lista vazia, e
              `meta.empty_reason` diz qual funcionaria."
          required: false
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Data-base da leitura. Ausente = o mais recente publicado por
              objeto.
          required: false
        - in: query
          name: rel
          schema:
            type: string
            description: Restringe a coorte a quem tem esta relação com `rel_to`.
          required: false
        - in: query
          name: rel_to
          schema:
            type: string
            description: O outro lado de `rel`.
          required: false
        - in: query
          name: rel_direction
          schema:
            type: string
            enum:
              - in
              - out
            default: in
            description: Onde a coorte está na aresta de `rel`.
          required: false
        - in: query
          name: where
          schema:
            type: string
            description: Os mesmos cortes de `rankObjects`, por número e por palavra, com a
              mesma sintaxe. Recortam a coorte ANTES de agrupar.
          required: false
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
            description: Quantos grupos devolver, do maior para o menor.
          required: false
        - in: query
          name: order_by
          schema:
            type: string
            enum:
              - value
              - objects
            default: value
            description: Por qual coluna ordenar e CORTAR a página. `value` (default) ordena
              pela agregação; `objects` ordena pelo tamanho do grupo em objetos
              — "as gestoras com mais fundos" é pergunta de `objects`, e com
              `value` a página cortada pelo teto pode ser OUTRA página.
          required: false
      summary: Resume uma coorte — soma, média ou mediana, repartida por propriedade
        ou relação
      description: |-
        A outra metade de `rankObjects`. Aquele responde QUEM se destaca; este responde QUANTO DÁ, repartido. "O patrimônio dos FIDCs somado por gestora" não tinha caminho: a saída era paginar o ranking inteiro e agrupar no cliente, o que só funciona se a coorte couber na página.

        **A COORTE é a mesma de `rankObjects`** — `kind`, `subkind`, `rel`/`rel_to` e `where`, com a mesma sintaxe e o mesmo significado. Troque `rankObjects` por `aggregateObjects` na mesma consulta e você resume exatamente o conjunto que ordenaria.

        **`group_by` aceita PROPRIEDADE ou RELAÇÃO**, e a resposta diz qual em `meta.group_by_kind`. `group_by=situation` reparte por atributo; `group_by=manages` reparte pelo objeto do outro lado da aresta — e aí cada grupo traz `entity_id`, porque gestora é objeto. Quando o grupo é palavra, `entity_id` vem nulo: é assim que se distingue, e o formato da linha nunca muda. `meta.groupable` lista, na própria resposta, o que cabe aqui.

        **`group_by_direction` diz onde a COORTE está na aresta**, igual a `rel_direction`. `manages` liga gestora a fundo, então uma coorte de fundos está na ponta de ENTRADA (`in`). Errar a direção devolve lista vazia — e nesse caso `meta.empty_reason` diz qual direção funcionaria, em vez de deixar a resposta parecer "não há dado".

        **`sum` é RECUSADO em percentual, razão, múltiplo e ponto de índice.** Somar o dividend yield de 300 FIIs devolve um número, responde 200 e não significa nada. `avg`, `median`, `min` e `max` valem para qualquer unidade — a média de um percentual é um percentual.

        **Três números de cobertura, e eles não são detalhe.** `cohort_objects` é a coorte inteira e `cohort_with_value` o subconjunto que tem número na medida — este segundo é o que `rankObjects` chama de `cohort_size`, e os nomes diferem de propósito para que ninguém leia um pelo outro. `ungrouped` conta quem ficou FORA de todo grupo (medido: 20.098 dos 57.090 fundos não declaram situação), e sem ele o total dos grupos parece completo sendo parcial. `multi_group` conta quem caiu em MAIS DE UM grupo: zero num verbo funcional, grande em `issued`, onde o mesmo objeto entra numa vez por emissão e o `sum` o conta várias — o número não fica errado, a leitura muda.

        Cada grupo traz `objects` e `with_value`. Diferença entre os dois é cobertura parcial: um grupo com 36 objetos e nenhum valor devolve `value: null`, nunca zero.

        **Para o TOTAL do mercado, leia `meta.cohort_value` — não some a página.** É a mesma agregação aplicada à coorte inteira, antes de repartir: inclui os `ungrouped` e não depende do `limit`. O `group_by` continua obrigatório, mas deixou de ser o único caminho até o total.

        **`order_by=objects` ordena pelos grupos MAIORES em objetos** — "as gestoras com mais fundos" é essa pergunta. O default `value` ordena pela agregação, e com o teto de grupos as duas ordens podem devolver PÁGINAS diferentes.

        **Sem `fact`, com `agg=count`, a operação vira CENSO**: conta OBJETOS por grupo. É o único caminho de contagem para tipo que só tem propriedades — "quantas assessorias ativas por UF" é `kind=service_provider&agg=count&group_by=advisor_state&where=advisor_situation=EM FUNCIONAMENTO NORMAL`, sem medida nenhuma envolvida. `meta.fact` volta nulo e `value` = `objects`.
      x-capability: object-context
  /v1/objects/rank:
    get:
      responses:
        "200":
          description: A coorte ordenada pela medida
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: O objeto ranqueado. É o mesmo id de `getObject` e de qualquer
                            travessia.
                        name:
                          type:
                            - string
                            - "null"
                        kind:
                          type: string
                          description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                            pelo mesmo motivo de `rel`: `offering` e
                            `fund_share_class` nasceram em 19/08/2026 e um
                            cliente que validasse contra a lista anterior
                            recusaria a resposta inteira. `getObjectCensus`
                            lista os vigentes."
                        subkind:
                          type:
                            - string
                            - "null"
                          description: "Fundo: `fidc`, `fii`, `fip`, `fif`, `fiagro`, `fiim`, `etf`,
                            `fapi`, `funcine`. Instrumento: `debenture`,
                            `securitizado`, `bdr`, `coe`, `tesouro`. Papel:
                            `on`, `pn`, `unit` — a classe da ação, que é o que
                            ela é dentro do tipo. Prestador: `gestora`,
                            `administradora_fiduciaria`, `assessoria`,
                            `agencia_rating` — o que a casa declarou SER perante
                            a CVM; quem gere E administra sai `gestora`. Auditor
                            e custodiante ficam sem sub-tipo de propósito (só
                            existem como menção no cadastro de fundo de
                            terceiro) — descubra-os pelas arestas
                            `audits`/`custodies`. NULO é resposta honesta — a
                            fonte que classifica cobre só o universo ATIVO,
                            então fundo encerrado fica sem sub-tipo. Ausência
                            aqui é 'não sabemos o que é', nunca 'não é nada'."
                        value:
                          type: number
                          description: "O número pelo qual a linha foi ordenada: o nível em `value`, a
                            variação em `delta` e `pct_change`."
                        value_from:
                          type:
                            - number
                            - "null"
                          description: Valor na ponta inicial da janela. Nulo em `measure=value`, que não
                            tem duas pontas.
                        value_to:
                          type:
                            - number
                            - "null"
                          description: Valor na ponta final da janela. Nulo em `measure=value`.
                        as_of:
                          type: string
                          description: Data-base do valor final. É dela que a resposta fala, não de
                            'hoje'.
                        statement_date:
                          type:
                            - string
                            - "null"
                          description: A COMPETÊNCIA do valor final, quando difere de `as_of`. **Para
                            datar um fundamento, cite este campo.** Múltiplo de
                            ação é reprecificado todo dia e `as_of` anda junto;
                            margem e retorno não dependem de preço e repetem o
                            valor do trimestre com data de hoje.
                        statement_date_from:
                          type:
                            - string
                            - "null"
                          description: A competência do valor inicial. Nula em `measure=value`.
                        as_of_from:
                          type:
                            - string
                            - "null"
                          description: "Data-base do valor inicial. **Pode não ser a data que você
                            pediu**: a medida é trimestral e a rota usa a última
                            competência publicada até `from`. Comparar duas
                            companhias cujas pontas caem em trimestres
                            diferentes é leitura sua, e por isso a data vai em
                            cada linha."
                        series:
                          type:
                            - string
                            - "null"
                          description: O papel de onde o número saiu, quando a medida vive no grão do
                            papel. Medida da EMISSORA sai UMA vez por companhia
                            mesmo quando ela tem várias classes — somar PETR3 e
                            PETR4 inflou 40% em 174 companhias quando o grão não
                            era declarado.
                        out_of_prior:
                          type:
                            - boolean
                            - "null"
                          description: >-
                            O valor caiu FORA da faixa plausível declarada para
                            esta medida — o mesmo sinal que `getObjectFacts`
                            publica, para o ranking não contradizer a leitura do
                            objeto.


                            Importa mais aqui do que lá: ordenar por variação
                            traz o denominador colapsado para o TOPO.
                            `fidc_top_originator_pct` passa de 100% em 6.876
                            competências, sempre com patrimônio perto de zero;
                            margem de empresa com receita residual dá milhares
                            de por cento. São valores as-filed, servidos sem
                            truncar — `true` diz para lê-los como indisponíveis,
                            não como recorde. Use `exclude_out_of_prior=true`
                            para tirá-los da ordenação. Nulo quando a medida não
                            declara faixa.
                        related:
                          type: array
                          items:
                            type: object
                            properties:
                              rel:
                                type: string
                              direction:
                                type: string
                                enum:
                                  - out
                                  - in
                              id:
                                type: string
                              kind:
                                type: string
                                description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                                  pelo mesmo motivo de `rel`: `offering` e
                                  `fund_share_class` nasceram em 19/08/2026 e um
                                  cliente que validasse contra a lista anterior
                                  recusaria a resposta inteira.
                                  `getObjectCensus` lista os vigentes."
                              name:
                                type:
                                  - string
                                  - "null"
                              key_type:
                                type:
                                  - string
                                  - "null"
                                description: "Que tipo de chave é `key`: `ticker`, `cnpj`, `series_code`…"
                              key:
                                type:
                                  - string
                                  - "null"
                                description: O código pelo qual um humano chama este objeto.
                            required:
                              - rel
                              - direction
                              - id
                              - kind
                              - name
                              - key_type
                              - key
                          description: >-
                            Os objetos ligados a esta linha pelo verbo de
                            `expand`. Ausente sem `expand`, e ausente quando a
                            linha não tem vizinho naquele verbo — lista vazia
                            diria 'não tem', que é afirmação diferente.


                            No máximo três por linha. O ranking não é o lugar de
                            listar 78 papéis de um índice; para a lista inteira
                            de UM objeto existe `listObjectLinks`, que pagina e
                            declara o total.
                        where_values:
                          type: array
                          items:
                            type: object
                            properties:
                              fact:
                                type: string
                                description: Qual corte. Vem NA linha e não por posição — lista paralela a
                                  `meta.applied_where` seria a mesma armadilha
                                  que este campo existe para fechar.
                              measure:
                                type: string
                                enum:
                                  - value
                                  - delta
                                  - pct_change
                                description: >-
                                  A REDUÇÃO aplicada sobre a coorte.


                                  - `value` — o valor na data `at` (ou o último
                                  publicado até ela). Ordena por NÍVEL.

                                  - `delta` — `valor(to) - valor(from)`, na
                                  unidade da medida. Ordena por VARIAÇÃO
                                  ABSOLUTA.

                                  - `pct_change` — variação relativa em
                                  percentual. **Não use com medida que cruza o
                                  zero**: margem indo de -1% para -2% dá +100%
                                  de 'crescimento'. Para essas, `delta`.
                              value:
                                type: number
                                description: O número desta linha na medida do corte, na escala que
                                  `meta.applied_where` declara para ela.
                              as_of:
                                type: string
                                description: "Data-base deste número. Pode diferir do `as_of` da medida
                                  ordenada: fundamento trimestral e preço diário
                                  caem em datas diferentes."
                            required:
                              - fact
                              - measure
                              - value
                              - as_of
                          description: >-
                            O VALOR DE CADA CORTE NESTA LINHA. Ausente quando
                            não houve `where`.


                            O servidor já calculou estes números para poder
                            filtrar; devolvê-los custa zero e evita a chamada
                            que só existia para relê-los. Medido em 20/08/2026:
                            uma triagem por duas medidas (dividendo alto E
                            múltiplo baixo) gastou um ranking INVERTIDO inteiro,
                            só para ler o segundo número de dez papéis que a
                            resposta anterior já conhecia.
                      required:
                        - id
                        - name
                        - kind
                        - subkind
                        - value
                        - value_from
                        - value_to
                        - as_of
                        - statement_date
                        - statement_date_from
                        - as_of_from
                        - series
                        - out_of_prior
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      fact:
                        type: string
                      unit:
                        type: string
                        enum:
                          - brl
                          - usd
                          - pct
                          - ratio
                          - x
                          - count
                          - points
                          - native
                        description: Leia antes de comparar com outra medida. `ratio` é fração (0,08 é
                          8%) e `pct` é percentual (8,0 é 8%). Em `pct_change` a
                          unidade do CAMPO `value` é sempre percentual, seja
                          qual for a da medida.
                      measure:
                        type: string
                        enum:
                          - value
                          - delta
                          - pct_change
                        description: >-
                          A REDUÇÃO aplicada sobre a coorte.


                          - `value` — o valor na data `at` (ou o último
                          publicado até ela). Ordena por NÍVEL.

                          - `delta` — `valor(to) - valor(from)`, na unidade da
                          medida. Ordena por VARIAÇÃO ABSOLUTA.

                          - `pct_change` — variação relativa em percentual.
                          **Não use com medida que cruza o zero**: margem indo
                          de -1% para -2% dá +100% de 'crescimento'. Para essas,
                          `delta`.
                      cadence:
                        type: string
                        enum:
                          - daily
                          - weekly
                          - monthly
                          - quarterly
                          - annual
                          - irregular
                        description: "Com que frequência a fonte publica. `irregular` = papel que não
                          negocia todo dia, ou série sem calendário fixo: buraco
                          não é queda. **NÃO é a JANELA que o número cobre** —
                          essa é `axes.period`, e as duas divergem por regra: o
                          juro real ex-ante cobre um ano, porque é taxa
                          anualizada, e sai todo dia. Nos indicadores macro este
                          campo é MEDIDO do espaçamento das últimas publicações,
                          não declarado à mão, e por isso acompanha a fonte
                          quando ela muda de frequência."
                      grain:
                        type: string
                        enum:
                          - object
                          - paper
                        description: "`object` = uma resposta só para o objeto inteiro; somar os papéis
                          DUPLICA. `paper` = genuinamente diferente por classe
                          de ação; perguntar pelo objeto é malformado."
                      source:
                        type: string
                      availability:
                        type: string
                        enum:
                          - filed
                          - unknown
                        description: "`filed` = a fonte publica data de entrega e o corte por `at`/`to`
                          é point-in-time completo. `unknown` = o corte é pela
                          data-base e o atraso de divulgação fica de fora. Um
                          ranking montado sobre `unknown` numa data passada
                          enxerga números que ainda não eram públicos."
                      order:
                        type: string
                        enum:
                          - asc
                          - desc
                      window_from:
                        type:
                          - string
                          - "null"
                        description: Ponta inicial PEDIDA. As datas efetivas variam por objeto e vão em
                          `as_of_from`.
                      window_to:
                        type:
                          - string
                          - "null"
                        description: Ponta final PEDIDA.
                      since:
                        type:
                          - string
                          - "null"
                        description: Data-base mínima exigida na ponta final, quando pedida. Nula =
                          objeto que parou de reportar também entra, carregando
                          o último valor publicado.
                      as_of_range:
                        type:
                          - object
                          - "null"
                        properties:
                          min:
                            type: string
                          max:
                            type: string
                        required:
                          - min
                          - max
                        description: "A faixa de data-base DESTA página. **`min` diferente de `max`
                          significa que a lista mistura competências** — objeto
                          que parou de reportar carrega o último valor para
                          sempre e disputa o topo com quem reportou este mês. Um
                          analista pediu a maior queda de margem 'no último ano'
                          e recebeu no topo uma empresa cujas duas pontas eram
                          de mais de um ano atrás: variação real, janela errada.
                          Use `since` para exigir frescor. Nulo quando a página
                          está vazia."
                      mixed_vintage:
                        type: boolean
                        description: "`true` quando a página mistura competências distantes demais para
                          a CADÊNCIA da medida — cerca de duas competências de
                          folga (10 dias em diária, 95 em mensal, 280 em
                          trimestral, 200 em irregular). Objeto que PAROU de
                          reportar carrega o último valor para sempre e ganha de
                          quem reportou ontem, sem erro nenhum; `since` é o
                          conserto. Leia `vintage_spread_days` para discordar do
                          critério em vez de aceitá-lo no escuro."
                      vintage_spread_days:
                        type:
                          - number
                          - "null"
                        description: Distância em DIAS entre a competência mais velha e a mais nova
                          desta página. Nulo quando a página é vazia.
                      as_of_from_range:
                        type:
                          - object
                          - "null"
                        properties:
                          min:
                            type: string
                          max:
                            type: string
                        required:
                          - min
                          - max
                        description: "A faixa de data-base da ponta INICIAL nesta página. **min distante
                          de max diz que a lista mistura JANELAS**, não só
                          competências: um ranking de 'maior piora no último
                          ano' pode trazer, na mesma lista, um objeto cuja ponta
                          inicial é de quatro anos atrás — e ele sobe ao topo
                          justamente por isso. Medido em 20/08/2026, com o sinal
                          invertido: +56 pp na lista contra −19 pp na janela
                          pedida. `since_from` é o conserto."
                      excluded_out_of_prior:
                        type:
                          - integer
                          - "null"
                        description: >-
                          Quantos objetos ficaram de fora por valor implausível.
                          **`null` = não houve medição** — o filtro não foi
                          pedido, ou a medida não declara faixa e não havia o
                          que aplicar. Zero significa 'apliquei e não derrubei
                          ninguém', que é outra afirmação: ler os dois como a
                          mesma coisa faz uma coorte não verificada passar por
                          coorte limpa.


                          Mesmo com número, ele NÃO cobre o denominador: a faixa
                          é da medida ordenada, e uma razão limitada a 0–1 é
                          plausível mesmo num fundo de carteira quase nula. Para
                          isso, `where`.
                      cohort_size:
                        type: integer
                        description: >-
                          Objetos da coorte com valor publicado nas DUAS pontas
                          — o denominador honesto do ranking. Quem não tem as
                          duas pontas não entra, e não entrar não é valer zero.


                          Com `where`, é a coorte DEPOIS do corte.
                          `filtered_out` diz quantos o corte derrubou.
                      filterable_facts:
                        type: array
                        items:
                          type: string
                        description: "As OUTRAS medidas desta coorte, prontas para entrar em `where` na
                          próxima chamada. Quase toda triagem tem duas medidas —
                          ordene por uma, corte pelas outras — e sem esta lista
                          o corte vira uma leitura por objeto: medido em
                          20/08/2026, a mesma pergunta custou 14 chamadas a quem
                          não viu o parâmetro. Escala de cada uma em
                          `listFactCatalog`."
                      filterable_properties:
                        type: array
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                              description: O nome que entra em `where`, o mesmo de `getObjectProperties`.
                            vocabulary:
                              type:
                                - array
                                - "null"
                              items:
                                type: string
                              description: Os valores possíveis. NULO quer dizer texto livre da fonte, não
                                ausência de valores — o `sector` da CVM tem 70
                                grafias e nenhuma norma, e listá-las aqui seria
                                publicar um vocabulário que a fonte não tem.
                          required:
                            - name
                            - vocabulary
                        description: >-
                          O QUE DÁ PARA CORTAR POR PALAVRA nesta coorte, com os
                          valores possíveis junto. É o irmão de
                          `filterable_facts` no eixo categórico: `situation=Em
                          Liquidação`, `area_type=country`, `is_critical=true`.


                          O vocabulário viaja aqui porque sem ele o nome não é
                          acionável — saber que existe `situation` não diz quais
                          são os valores, e descobrir custaria uma chamada
                          recusada. Sai da MESMA declaração que
                          `getObjectProperties` serve e que o `where` valida,
                          então esta lista nunca anuncia um corte que o filtro
                          recusa.
                      applied_property_where:
                        type: array
                        items:
                          type: object
                          properties:
                            property:
                              type: string
                            op:
                              type: string
                              enum:
                                - eq
                                - ne
                            values:
                              type: array
                              items:
                                type: string
                              description: O que foi PEDIDO, como veio.
                            matched:
                              type: array
                              items:
                                type: string
                              description: O que CASOU, na grafia canônica da fonte. Caixa e acento são
                                ignorados no casamento, então `em liquidacao` e
                                `Em Liquidação` produzem a mesma lista — e sem
                                este eco não haveria como saber sobre o que o
                                corte foi feito.
                            source:
                              type: string
                              description: De onde a propriedade vem.
                          required:
                            - property
                            - op
                            - values
                            - matched
                            - source
                        description: >-
                          Como cada corte por PALAVRA foi aplicado. Ausente
                          quando não houve nenhum.


                          Em `ne`, objeto que NÃO declara a propriedade passa:
                          "não está em liquidação" inclui quem não declarou
                          situação nenhuma. Sem isso o corte negativo devolveria
                          menos que o complemento do positivo e as duas listas
                          não fechariam com a coorte.
                      filtered_out:
                        type: integer
                        description: "Quantos objetos `where` derrubou. Uma lista de três é leitura
                          completamente diferente conforme venha de uma coorte
                          de 12 ou de 4.500: no primeiro caso o corte quase não
                          filtrou, no segundo ele É a resposta. Ausente quando
                          não houve `where` — um zero diria 'filtrei e não
                          derrubei ninguém', que é outra afirmação."
                      applied_where:
                        type: array
                        items:
                          type: object
                          properties:
                            fact:
                              type: string
                            op:
                              type: string
                              enum:
                                - lt
                                - lte
                                - gt
                                - gte
                            value:
                              type: number
                            measure:
                              type: string
                              enum:
                                - value
                                - delta
                                - pct_change
                              description: "O que da medida foi comparado. Muda o SENTIDO do corte, não só a
                                escala: `<5` é 'está abaixo de 5' em `value`,
                                'subiu menos de 5' em `delta` e 'subiu menos de
                                5%' em `pct_change`."
                            unit:
                              type: string
                              enum:
                                - brl
                                - usd
                                - pct
                                - ratio
                                - x
                                - count
                                - points
                                - native
                              description: "`ratio` = fração (0,08 é 8%); `pct` = percentual (8,0 é 8%) — os
                                dois existem no mesmo produto e NÃO são
                                comparáveis sem converter. `x` = múltiplo.
                                `native` = a unidade da própria série, que só o
                                catálogo dela conhece."
                            description:
                              type: string
                          required:
                            - fact
                            - op
                            - value
                            - measure
                            - unit
                            - description
                        description: "Como cada condição de `where` foi REALMENTE aplicada, com a
                          unidade contra a qual o número foi comparado — que é
                          onde isto erra. `fidc_impaired_ratio` é fração e
                          `fii_dy_12m` é percentual: o mesmo `<5` corta em 500%
                          numa e em 5% na outra, e as duas respondem 200 com uma
                          lista plausível. Confira aqui antes de acreditar no
                          corte."
                      description:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - fact
                      - unit
                      - measure
                      - cadence
                      - grain
                      - source
                      - availability
                      - order
                      - window_from
                      - window_to
                      - since
                      - as_of_range
                      - mixed_vintage
                      - vintage_spread_days
                      - excluded_out_of_prior
                      - cohort_size
                      - filterable_facts
                      - filterable_properties
                      - description
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: rankObjects
      tags:
        - Objects
      parameters:
        - in: query
          name: kind
          schema:
            type: string
            enum:
              - company
              - equity_security
              - fund
              - service_provider
              - instrument
              - index
              - crypto_asset
              - commodity
              - country
              - indicator
              - data_series
              - offering
              - fund_share_class
              - market_event
            description: O tipo dos objetos que entram na coorte.
          required: true
        - in: query
          name: subkind
          schema:
            type: string
            description: Recorte dentro do tipo.
          required: false
        - in: query
          name: fact
          schema:
            type: string
            description: A medida, pelo nome do `listFactCatalog`.
          required: true
        - in: query
          name: measure
          schema:
            type: string
            enum:
              - value
              - delta
              - pct_change
            default: value
            description: "`value` ordena por nível; `delta` pela variação absoluta;
              `pct_change` pela relativa."
          required: false
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: >-
              Data-base da leitura em `value`; ponta FINAL nas demais. Ausente =
              o mais recente publicado.


              **`at` + `since` é a JANELA.** `at` é o teto e `since` é o piso da
              data-base aceita, e os dois juntos recortam um período:
              `at=2024-12-31&since=2024-01-01` devolve o que tem competência EM
              2024 — 4.178 ofertas, medido. Sem `since`, um objeto que parou de
              reportar em 2020 entra com a competência dele e disputa o topo;
              sem `at`, entra o que veio depois. `meta.as_of_range` devolve a
              faixa efetiva da página, e ela é a conferência: min e max
              distantes dizem que a lista mistura competências.
          required: false
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Ponta INICIAL. Obrigatória em `delta` e `pct_change`.
          required: false
        - in: query
          name: since_from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: >-
              Piso da data-base aceita na ponta INICIAL — o que `since` faz pela
              ponta final.


              **Sem ele a janela que você pediu não é a janela que você
              recebe.** Objeto cuja última publicação antes de `from` é de anos
              atrás entra com aquele valor: medido em 20/08/2026, um FIDC subiu
              a 7º numa lista de 'maior piora no ÚLTIMO ANO' com ponta inicial
              em 2022-10-31 — e nos doze meses pedidos ele tinha MELHORADO 19
              pp. Sinal trocado, sem erro nenhum. `meta.as_of_from_range` é o
              alarme: min distante de max diz que a lista mistura janelas.
          required: false
        - in: query
          name: order
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
            description: "`asc` primeiro os menores — é assim que se pede 'a maior QUEDA'
              com `delta`."
          required: false
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
          required: false
        - in: query
          name: expand
          schema:
            type: string
            enum:
              - issued
              - assigned_to
              - holds
              - manages
              - administers
              - custodies
              - audits
              - same_owner
              - shareholder_of
              - indexed_to
              - rates
              - mentions
              - measures
              - forecasts
              - contains
              - member_of
              - exposed_to_issuer
              - succeeded_by
              - produces
              - covers
            description: "Traz, em CADA linha, os objetos ligados por este verbo — com id,
              nome e a CHAVE pela qual um humano os chama.
              `kind=offering&expand=issued` responde 'as maiores ofertas E quem
              emitiu cada uma' numa chamada; sem ele são N chamadas de
              `listObjectLinks`, medido em 5 de 12 numa sondagem de 20/08/2026.
              No máximo 3 vizinhos por linha: para a lista inteira de UM objeto,
              `listObjectLinks` pagina e declara o total."
          required: false
        - in: query
          name: expand_direction
          schema:
            type: string
            enum:
              - out
              - in
            default: in
            description: "De que lado da aresta está o vizinho. `in` (default) é quem APONTA
              para a linha, e é o caso da oferta: a aresta é `company
              --issued--> offering`, então o emissor entra por aí."
          required: false
        - in: query
          name: rel
          schema:
            type: string
            enum:
              - issued
              - assigned_to
              - holds
              - manages
              - administers
              - custodies
              - audits
              - same_owner
              - shareholder_of
              - indexed_to
              - rates
              - mentions
              - measures
              - forecasts
              - contains
              - member_of
              - exposed_to_issuer
              - succeeded_by
              - produces
              - covers
            description: Restringe a coorte a quem tem esta relação com `rel_to`. Sem ela, a
              coorte é o tipo inteiro.
          required: false
        - in: query
          name: rel_to
          schema:
            type: string
            description: O outro lado da relação — o índice, o fundo, a empresa.
          required: false
        - in: query
          name: rel_direction
          schema:
            type: string
            enum:
              - out
              - in
            default: in
            description: "Onde a COORTE está na aresta — não onde `rel_to` está. `in`
              (default) é o caso comum: `rel_to` aponta para os membros (o
              índice CONTÉM os papéis). `out` = os membros apontam para `rel_to`
              (os cedentes ATRIBUÍDOS a um fundo)."
          required: false
        - in: query
          name: since
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Data-base MÍNIMA aceita na ponta final. Objeto que PAROU de
              reportar carrega o último valor para sempre: sem `since`, um FIDC
              cujo último informe é de 2023 disputa o topo com quem reportou
              este mês, e a resposta compara competências a três anos de
              distância. Cada linha declara seu `as_of` justamente para isso ser
              visível — `since` é como se pede que não aconteça."
          required: false
        - in: query
          name: exclude_out_of_prior
          schema:
            type: boolean
            description: >-
              Tira da ordenação os valores fora da faixa plausível DA MEDIDA
              ORDENADA. Ordenar por variação traz o denominador colapsado para o
              TOPO — margem de empresa com receita residual, concentração de
              fundo com patrimônio perto de zero. `meta.excluded_out_of_prior`
              diz quantos saíram, porque 'nenhum implausível em 12' e 'nenhum em
              3.000' não são a mesma afirmação — e vem **null** quando não houve
              medição (filtro não pedido, ou medida sem faixa declarada). Zero
              significa 'apliquei e não derrubei ninguém'.


              **O QUE ELE NÃO ENXERGA: o denominador.** A faixa é da medida
              ORDENADA, e uma razão limitada a 0–1 é sempre plausível — mesmo
              num fundo cuja carteira é R$ 66,77. Medido em 20/08/2026:
              ranqueando piora de inadimplência, fundos com carteira de sessenta
              e seis reais e de ZERO entraram no top-16 com `out_of_prior:
              false`, porque 0,89 é um valor perfeitamente normal. O implausível
              estava no denominador, que este parâmetro não olha. Para isso use
              `where` sobre a medida de tamanho —
              `where=fidc_portfolio>50000000` — que é o corte que a pergunta
              realmente pedia. `meta.filterable_facts` lista o que cabe ali.
          required: false
        - in: query
          name: where
          schema:
            type: string
            description: >-
              CONDIÇÕES SOBRE OUTRAS MEDIDAS, na mesma coorte e na mesma janela
              do ranking. Formato `<medida><operador><número>`, várias separadas
              por vírgula — `fidc_impaired_ratio<0.05,fidc_portfolio>1000000`.
              Operadores: `<`, `<=`, `>`, `>=`.


              **NÍVEL ou VARIAÇÃO.** `x<5` compara o valor na data-base;
              `delta(x)>0.02` compara a VARIAÇÃO entre `from` e `at`, e
              `pct_change(x)>10` a variação relativa em %. Metade da triagem é
              sobre mudança e não sobre nível — inadimplência de 5% é normal em
              quem sempre esteve em 5% e é alarme em quem estava em 3%. Condição
              de variação exige `from`, e usa a MESMA janela do ranking.


              **Confira a ESCALA da medida antes de escrever o número.**
              `fidc_impaired_ratio` é fração (0,05 é 5%) e `fii_dy_12m` é
              percentual (5 é 5%): o mesmo `<5` corta em 500% numa e em 5% na
              outra, e as duas respondem 200 com uma lista plausível.
              `meta.applied_where` devolve a unidade contra a qual cada número
              foi comparado.


              Objeto SEM valor na medida do filtro não passa — ausência não é
              zero, e não dá para afirmar que quem não publicou inadimplência
              está abaixo do corte. `meta.filtered_out` diz quantos o corte
              derrubou.


              **CORTE POR PALAVRA**, no mesmo parâmetro: `<propriedade>=<valor>`
              e `!=`. As propriedades e os valores possíveis de cada tipo vêm em
              `meta.filterable_properties` desta mesma resposta, e são os mesmos
              de `getObjectProperties` — `situation=Em Liquidação`,
              `area_type=country`, `is_critical=true`.


              **`|` é OU dentro do mesmo campo**: `sector=Bancos|Seguros`. Duas
              condições separadas por vírgula são E, então sem o pipe "as
              financeiras" não teria como ser pedido.


              **ASPAS quando o valor tem vírgula** — `sector="Construção Civil,
              Mat. Constr. e Decoração"`. São 471 companhias assim; sem aspas a
              vírgula parte a condição e a requisição é RECUSADA, nunca cortada
              pela metade.


              Caixa e acento são ignorados no casamento, e
              `meta.applied_property_where` devolve o valor CANÔNICO que casou —
              é onde este corte engana. Em `!=`, objeto que não declara a
              propriedade PASSA: "não está em liquidação" inclui quem não
              declarou situação nenhuma.
          required: false
      summary: Ordena e FILTRA uma coorte por medidas — nível ou variação
      description: >-
        O quarto eixo. Os outros três já existiam e cada um sozinho: QUEM
        (`kind`, `subkind` e as relações do grafo), O QUÊ (`listFactCatalog`) e
        QUANDO (`at`, `from`). Faltava o que fazer com o CONJUNTO — e sem isso
        'a companhia que mais perdeu margem' exigia ler o histórico de milhares
        de companhias, uma chamada cada.


        **Quase toda triagem tem DUAS medidas, e `where` é a segunda.** Ordene
        por uma e corte pelas outras na mesma chamada:
        `fact=fidc_impaired_ratio&measure=delta&where=fidc_portfolio>50000000`
        responde 'quem mais piorou ENTRE os grandes'. Sem isso o corte vira uma
        leitura por objeto — medido em 20/08/2026, a mesma pergunta custou 14
        chamadas a quem não viu o parâmetro. `meta.filterable_facts` lista, na
        própria resposta, o que mais cabe ali, e cada linha volta com
        `where_values` — o valor dela em CADA corte, já calculado para filtrar.
        Não peça um segundo ranking para reler o segundo número.


        **`expand` traz o objeto ligado em cada linha.**
        `kind=offering&expand=issued` responde 'as maiores ofertas E quem emitiu
        cada uma' numa chamada só, com o nome e a chave do emissor na própria
        linha.


        **Para um PERÍODO, use `at` com `since`**: `at` é o teto da data-base e
        `since` o piso. Não existe `until` porque `at` já é ele. Em `delta` e
        `pct_change`, `since_from` é o piso da ponta INICIAL — sem ele a janela
        pedida não é a janela recebida.


        **Leia `unit` antes de comparar com outra medida.** `ratio` é fração
        (0,08 é 8%) e `pct` é percentual (8,0 é 8%).


        **`pct_change` não serve para medida que cruza o zero**: margem indo de
        -1% para -2% devolve +100% de 'crescimento'. Para essas, `delta`.


        **Quem não tem valor nas DUAS pontas não entra**, e não entrar não é
        valer zero — `cohort_size` é o denominador honesto. Cada linha declara
        `as_of` e `as_of_from` porque medida trimestral cai na última
        competência publicada até a data pedida, e duas companhias podem estar
        comparando trimestres diferentes.


        **`availability: unknown` significa que a fonte não publica data de
        entrega** e o corte é só pela data-base: um ranking numa data passada
        enxerga números que ainda não eram públicos. Com `filed`, o corte é
        point-in-time completo.
      x-capability: object-context
  /v1/objects/{id}:
    get:
      responses:
        "200":
          description: O objeto com apelidos e mapa de relações
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: "Determinístico na cunhagem (`pub_` = fonte pública) e CONGELADO
                      daí em diante: identificador melhor descoberto depois não
                      move o `id`, e código antigo continua resolvendo como
                      apelido. Pode sair de circulação em FUSÃO ou CISÃO de
                      objeto, que são raras: aí a rota redireciona sozinha (e
                      diz em `redirected_from`) quando há sucessor único, e
                      responde 409 com a lista quando há mais de um."
                  redirected_from:
                    type:
                      - string
                      - "null"
                    description: "Preenchido quando você pediu um id que saiu de circulação e ele
                      tinha sucessor ÚNICO: este é o objeto atual, e aquele era
                      o id pedido. Atualize o que você guardou. Nulo no caso
                      normal."
                  kind:
                    type: string
                    description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                      pelo mesmo motivo de `rel`: `offering` e
                      `fund_share_class` nasceram em 19/08/2026 e um cliente que
                      validasse contra a lista anterior recusaria a resposta
                      inteira. `getObjectCensus` lista os vigentes."
                  subkind:
                    type:
                      - string
                      - "null"
                    description: "Fundo: `fidc`, `fii`, `fip`, `fif`, `fiagro`, `fiim`, `etf`,
                      `fapi`, `funcine`. Instrumento: `debenture`,
                      `securitizado`, `bdr`, `coe`, `tesouro`. Papel: `on`,
                      `pn`, `unit` — a classe da ação, que é o que ela é dentro
                      do tipo. Prestador: `gestora`,
                      `administradora_fiduciaria`, `assessoria`,
                      `agencia_rating` — o que a casa declarou SER perante a
                      CVM; quem gere E administra sai `gestora`. Auditor e
                      custodiante ficam sem sub-tipo de propósito (só existem
                      como menção no cadastro de fundo de terceiro) —
                      descubra-os pelas arestas `audits`/`custodies`. NULO é
                      resposta honesta — a fonte que classifica cobre só o
                      universo ATIVO, então fundo encerrado fica sem sub-tipo.
                      Ausência aqui é 'não sabemos o que é', nunca 'não é
                      nada'."
                  name:
                    type:
                      - string
                      - "null"
                  anchor_type:
                    type: string
                    description: "A MELHOR chave de hoje, não a que originou o `id`. As duas
                      coincidem quase sempre e divergem justamente quando uma
                      fonte melhor apareceu depois da cunhagem — a chave
                      original não é publicada. Não derive o `id` daqui nem
                      presuma que ele muda quando esta muda: ele não muda."
                  anchor_value:
                    type: string
                  cnpj:
                    type:
                      - string
                      - "null"
                  cd_cvm:
                    type:
                      - string
                      - "null"
                  isin:
                    type:
                      - string
                      - "null"
                  tickers:
                    type:
                      - array
                      - "null"
                    items:
                      type: string
                  share_classes:
                    type:
                      - array
                      - "null"
                    items:
                      type: object
                      properties:
                        ticker:
                          type: string
                        share_class:
                          type:
                            - string
                            - "null"
                          enum:
                            - ON
                            - PN
                            - UNIT
                            - COTA
                            - BDR
                            - null
                          description: Nulo quando o código foge da convenção da B3 — 2 casos em 2.550, e
                            nulo é melhor que chute.
                      required:
                        - ticker
                        - share_class
                    description: "A classe de cada papel. É o que torna 'o preço da Petrobras'
                      respondível: PETR3 (ON) e PETR4 (PN), em vez de um dos
                      dois escolhido em silêncio."
                  has_ambiguous_key:
                    type: boolean
                    description: Alguma chave aponta para mais de um objeto — trate as respostas com
                      cautela.
                  keys:
                    type: array
                    items:
                      type: object
                      properties:
                        key_type:
                          type: string
                          description: "Tipo de identificador, como o servidor o publica. `string` e não
                            enum pelo mesmo motivo de `kind` e `rel`:
                            `commodity_key`, `iso2` e `iso3` nasceram em
                            20/08/2026 e um cliente que validasse contra a lista
                            anterior recusaria a resposta inteira — foi o que
                            aconteceu. `getObjectCensus` lista os tipos de chave
                            vigentes."
                        key_value:
                          type: string
                        confidence:
                          type: string
                          enum:
                            - high
                            - low
                          description: "`low` = a chave é ambígua (código reaproveitado após encerramento,
                            p.ex.)."
                        is_anchor:
                          type: boolean
                          description: A chave que define o `id` do objeto.
                      required:
                        - key_type
                        - key_value
                        - confidence
                        - is_anchor
                  links:
                    type: array
                    items:
                      type: object
                      properties:
                        rel:
                          type: string
                          description: "O verbo da relação, como o servidor o publica. É `string` e não
                            enum de propósito: verbo novo tem de chegar a você
                            como valor desconhecido, nunca derrubando a
                            resposta. `listObjectRelations` lista os vigentes
                            com forma temporal, domínio e alcance."
                        direction:
                          type: string
                          enum:
                            - out
                            - in
                          description: "`out` = este objeto pratica o verbo; `in` = sofre."
                        shape:
                          type: string
                          enum:
                            - event
                            - snapshot
                            - static
                          description: "`event` = aconteceu numa data e não deixa de ter acontecido
                            (emissão, sucessão): vigência não se aplica e a
                            contagem é o total. `snapshot` = a fonte republica
                            um retrato por competência e o novo substitui o
                            velho (carteira de fundo, carteira de índice): a
                            contagem é o último retrato. `static` = o registro
                            publica o vigente, sem série (gestor, custodiante,
                            auditor, sócio): a contagem é o que vale hoje."
                        relationship_count:
                          type: integer
                          description: O número a reportar para 'quantos'. Objetos DISTINTOS do outro
                            lado, sob o recorte que `shape` manda. Duas fontes
                            que afirmam a mesma relação são uma relação.
                        assertion_count:
                          type: integer
                          description: Quantas AFIRMAÇÕES sustentam essas relações — uma por (fonte,
                            período contíguo), no mesmo recorte. Sempre >=
                            `relationship_count`, e a diferença é redundância de
                            fonte, não tamanho.
                        relationship_count_ever:
                          type: integer
                          description: Objetos distintos de todos os tempos. Em `snapshot` e `static`
                            responde 'quantos JÁ', nunca 'quantos'.
                        count:
                          type: integer
                          description: "DEPRECIADO, sai na próxima versão maior: use `relationship_count`,
                            que é o mesmo número."
                        count_ever:
                          type: integer
                          description: "DEPRECIADO: use `relationship_count_ever`."
                        as_of:
                          type:
                            - string
                            - "null"
                          description: O valor a passar em `at` para reproduzir `relationship_count`. null
                            = não passe `at` nenhum (relação de evento, onde
                            recortar por data subestimaria).
                        note:
                          type:
                            - string
                            - "null"
                          description: O que este verbo AFIRMA e o que não afirma, do vocabulário —
                            `manages` é o gestor, não o administrador. Nulo só
                            se o verbo não estiver no vocabulário.
                        domain_kinds:
                          type: array
                          items:
                            type: string
                          description: Tipos que podem PRATICAR o verbo.
                        range_kinds:
                          type: array
                          items:
                            type: string
                          description: Tipos que podem estar do outro lado.
                        traverse:
                          type: object
                          properties:
                            operation:
                              type: string
                              const: listObjectLinks
                            id:
                              type: string
                            rel:
                              type: string
                              description: "O verbo da relação, como o servidor o publica. É `string` e não
                                enum de propósito: verbo novo tem de chegar a
                                você como valor desconhecido, nunca derrubando a
                                resposta. `listObjectRelations` lista os
                                vigentes com forma temporal, domínio e alcance."
                            direction:
                              type: string
                              enum:
                                - out
                                - in
                            at:
                              type:
                                - string
                                - "null"
                          required:
                            - operation
                            - id
                            - rel
                            - direction
                            - at
                          description: Receita executável do próximo salto. Copie estes argumentos para
                            `listObjectLinks`; `at=null` significa omitir o
                            parâmetro. Evita redescobrir direção e corte por
                            tentativa.
                      required:
                        - rel
                        - direction
                        - shape
                        - relationship_count
                        - assertion_count
                        - relationship_count_ever
                        - count
                        - count_ever
                        - as_of
                        - note
                        - domain_kinds
                        - range_kinds
                        - traverse
                    description: "O MAPA das relações: o que dá para atravessar."
                  aspects:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                          description: quotes, indicators, dividends, documents…
                        description:
                          type: string
                          description: O que este aspecto contém, com as convenções que valem (TTM,
                            ajustado…).
                        grain:
                          type: string
                          enum:
                            - object
                            - paper
                          description: "`object` = uma resposta só para o objeto inteiro; somar os papéis
                            DUPLICA. `paper` = genuinamente diferente por classe
                            de ação; perguntar pelo objeto é malformado."
                        operation:
                          type: string
                          description: A operação do contrato que serve este aspecto.
                        parameter:
                          type: string
                          description: Nome do parâmetro a passar.
                        value:
                          type: string
                          description: O valor JÁ resolvido para este objeto.
                      required:
                        - name
                        - description
                        - grain
                        - operation
                        - parameter
                        - value
                    description: "O MAPA dos dados: que capítulos este objeto tem, e como pedir cada
                      um. Só aparece aspecto que TEM dado — aspecto vazio na
                      lista faria quem consulta ler página vazia como 'o fato
                      não existe'."
                  declared_facts:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        unit:
                          type: string
                          enum:
                            - brl
                            - usd
                            - pct
                            - ratio
                            - x
                            - count
                            - points
                            - native
                          description: "`ratio` = fração (0,08 é 8%); `pct` = percentual (8,0 é 8%) — os
                            dois existem no mesmo produto e NÃO são comparáveis
                            sem converter. `x` = múltiplo. `native` = a unidade
                            da própria série, que só o catálogo dela conhece."
                        cadence:
                          type: string
                          enum:
                            - daily
                            - weekly
                            - monthly
                            - quarterly
                            - annual
                            - irregular
                          description: "Com que frequência a fonte publica. `irregular` = papel que não
                            negocia todo dia, ou série sem calendário fixo:
                            buraco não é queda. **NÃO é a JANELA que o número
                            cobre** — essa é `axes.period`, e as duas divergem
                            por regra: o juro real ex-ante cobre um ano, porque
                            é taxa anualizada, e sai todo dia. Nos indicadores
                            macro este campo é MEDIDO do espaçamento das últimas
                            publicações, não declarado à mão, e por isso
                            acompanha a fonte quando ela muda de frequência."
                        period:
                          type:
                            - string
                            - "null"
                        grain:
                          type: string
                          enum:
                            - object
                            - paper
                          description: "`object` = uma resposta só para o objeto inteiro; somar os papéis
                            DUPLICA. `paper` = genuinamente diferente por classe
                            de ação; perguntar pelo objeto é malformado."
                        series:
                          type:
                            - string
                            - "null"
                          description: A classe de ação, quando a medida é de papel.
                      required:
                        - name
                        - unit
                        - cadence
                        - period
                        - grain
                        - series
                    description: "As MEDIDAS que este recorte de objeto DECLARA — os nomes que
                      `getObjectFacts` pode devolver e `getObjectHistory` aceita
                      em `fact`, com unidade e janela. É DECLARAÇÃO, não valor:
                      a fonte pode não ter publicado para este objeto. Dispensa
                      ler `listFactCatalog` para descobrir o nome."
                required:
                  - id
                  - redirected_from
                  - kind
                  - subkind
                  - name
                  - anchor_type
                  - anchor_value
                  - cnpj
                  - cd_cvm
                  - isin
                  - tickers
                  - share_classes
                  - has_ambiguous_key
                  - keys
                  - links
                  - aspects
                  - declared_facts
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getObject
      tags:
        - Objects
      parameters:
        - in: query
          name: resolve
          schema:
            type: string
            enum:
              - auto
              - exact
            default: auto
            description: >-
              Como interpretar o `id` recebido.


              - `auto` (default) — como REFERÊNCIA publicada: id fundido resolve
              sozinho para o sucessor (com `redirected_from` preenchido) e id
              cindido responde **409** com a lista.

              - `exact` — como o id do objeto que existe HOJE: nenhum histórico
              é consultado, e o id ou responde ou é 404.


              **Quando você precisa de `exact`:** numa cisão, um dos sucessores
              é o PRÓPRIO id pedido — o ramo que ficou com a chave de nascimento
              herda o identificador. O 409 manda consultar cada sucessor, e
              consultar esse cairia no mesmo 409. `exact` é o endereço dele.
              Serve também para desligar o redirect automático de fusão quando
              você quer saber se o id que guardou ainda é o id de alguma coisa.
        - schema:
            type: string
          in: path
          name: id
          required: true
      summary: O objeto, seus apelidos e o MAPA do que dá para perguntar em seguida
      description: >-
        `keys` traz todos os identificadores que apontam para o mesmo objeto — é
        o que responde 'PETR3 e PETR4 são a mesma empresa?'. `links` é o mapa:
        quais relações existem para ESTE objeto, em que direção e quantas.
        Relação que não existe não aparece, em vez de devolver página vazia numa
        travessia — página vazia, para quem consulta, é a afirmação de que não
        há relação.


        **ID QUE SAIU DE CIRCULAÇÃO NÃO É 404.** Objeto se funde e se cinde —
        raramente, mas acontece. Com sucessor ÚNICO (fusão) esta rota resolve
        sozinha e devolve o objeto atual com `redirected_from` preenchido:
        atualize o id que você guardou. Com mais de um sucessor (cisão) responde
        **409** listando todos, porque escolher um por você acertaria parte das
        vezes e erraria o resto em silêncio. Vale igual em `listObjectLinks`,
        `getObjectFacts`, `getObjectHistory`, `getObjectEvents`,
        `getObjectEvidence` e `findObjectPaths`.


        **A lista do 409 pode conter o próprio id que você pediu**: numa cisão,
        um dos ramos herda o identificador por continuidade técnica. Para ler
        ESSE ramo use `resolve=exact`, que lê o id literalmente — sem ele,
        consultar esse sucessor voltaria ao mesmo 409 e o ramo seria
        inalcançável por qualquer rota. Os demais respondem pelo id deles.
      x-capability: object-context
  /v1/objects/{id}/links:
    get:
      responses:
        "200":
          description: Página de arestas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        rel:
                          type: string
                          description: "O verbo da relação, como o servidor o publica. É `string` e não
                            enum de propósito: verbo novo tem de chegar a você
                            como valor desconhecido, nunca derrubando a
                            resposta. `listObjectRelations` lista os vigentes
                            com forma temporal, domínio e alcance."
                        shape:
                          type: string
                          enum:
                            - event
                            - snapshot
                            - static
                          description: "`event` = aconteceu numa data e não deixa de ter acontecido
                            (emissão, sucessão): vigência não se aplica e a
                            contagem é o total. `snapshot` = a fonte republica
                            um retrato por competência e o novo substitui o
                            velho (carteira de fundo, carteira de índice): a
                            contagem é o último retrato. `static` = o registro
                            publica o vigente, sem série (gestor, custodiante,
                            auditor, sócio): a contagem é o que vale hoje."
                        direction:
                          type: string
                          enum:
                            - out
                            - in
                        other_id:
                          type: string
                        other_kind:
                          type: string
                          description: "O tipo do objeto, como o servidor o publica. `string` e não enum
                            pelo mesmo motivo de `rel`: `offering` e
                            `fund_share_class` nasceram em 19/08/2026 e um
                            cliente que validasse contra a lista anterior
                            recusaria a resposta inteira. `getObjectCensus`
                            lista os vigentes."
                        other_name:
                          type:
                            - string
                            - "null"
                        other_key_type:
                          type:
                            - string
                            - "null"
                          description: "Que tipo de chave é `other_key`: `ticker`, `cnpj`, `isin`,
                            `series_code`, `offering_id`…"
                        other_key:
                          type:
                            - string
                            - "null"
                          description: "A CHAVE do objeto do outro lado — o código pelo qual um humano o
                            chama. Sem ela a travessia devolve um id opaco e
                            quem quer o ticker, o CNPJ ou o `bcb_sgs:433`
                            precisa de uma chamada de `resolveObject` por
                            aresta: medido em 20/08/2026, caracterizar 45 séries
                            custaria 45 chamadas para ler o que a resposta já
                            tinha em mãos."
                        source:
                          type: string
                          description: O FORMULÁRIO que afirmou a relação — "CVM — Composição da Carteira
                            (CDA)", "CVM — Informe Mensal de FIDC, bloco de
                            cedentes". Duas fontes = duas afirmações, não uma
                            mais forte.
                        valid_from:
                          type:
                            - string
                            - "null"
                        valid_to:
                          type:
                            - string
                            - "null"
                        observations:
                          type: integer
                          description: Quantas vezes a fonte declarou a relação.
                        magnitude:
                          type:
                            - number
                            - "null"
                          description: Tamanho da relação, quando a fonte publica algo plausível. Com
                            corte `at`, é a última observação até a competência
                            dentro da janela; sem corte, é o máximo do segmento
                            histórico. Nulo NÃO é zero.
                        magnitude_unit:
                          type:
                            - string
                            - "null"
                          description: "`brl`, `share_pct`, `weight_pct`, `index_pct`, `ratio`."
                        evidence:
                          type:
                            - number
                            - "null"
                          description: "DEPRECIADO, sai na próxima versão maior: use `magnitude`."
                        evidence_unit:
                          type:
                            - string
                            - "null"
                          description: "DEPRECIADO: use `magnitude_unit`."
                        confidence:
                          type: string
                          enum:
                            - high
                            - low
                      required:
                        - rel
                        - shape
                        - direction
                        - other_id
                        - other_kind
                        - other_name
                        - other_key_type
                        - other_key
                        - source
                        - valid_from
                        - valid_to
                        - observations
                        - magnitude
                        - magnitude_unit
                        - evidence
                        - evidence_unit
                        - confidence
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      as_of:
                        type:
                          - string
                          - "null"
                        description: "A data efetivamente aplicada, que pode ser anterior à pedida:
                          verbo de retrato recua até a última competência
                          publicada (veja `last_valid_to` em
                          `listObjectRelations`). Nulo quando a consulta mistura
                          formas. Ausente quando `at` não foi pedido."
                      as_of_by_rel:
                        type: object
                        additionalProperties:
                          type:
                            - string
                            - "null"
                        description: >-
                          A data EFETIVAMENTE aplicada a cada verbo, quando a
                          consulta prende mais de um — é onde o escalar `as_of`
                          seria nulo por não haver recorte comum.


                          Leia por FORMA, senão o campo engana: em verbo de
                          RETRATO (`holds`, `assigned_to`) o valor pode ter
                          RECUADO até a última competência publicada, e é essa a
                          informação. Em verbo de EVENTO (`issued`) ele é a data
                          que você pediu, porque tudo que aconteceu até lá conta
                          — não é 'a competência do issued', é o teto do corte.
                          Ausente quando há um verbo só (aí o escalar responde)
                          ou quando `at` não foi pedido.
                      excluded_shapes:
                        type: array
                        items:
                          type: string
                          enum:
                            - event
                            - snapshot
                            - static
                          description: "`event` = aconteceu numa data e não deixa de ter acontecido
                            (emissão, sucessão): vigência não se aplica e a
                            contagem é o total. `snapshot` = a fonte republica
                            um retrato por competência e o novo substitui o
                            velho (carteira de fundo, carteira de índice): a
                            contagem é o último retrato. `static` = o registro
                            publica o vigente, sem série (gestor, custodiante,
                            auditor, sócio): a contagem é o que vale hoje."
                        description: Formas removidas do conjunto por causa da data. `static` aparece
                          aqui quando `at` é passado e a consulta não prende um
                          verbo — o registro publica só o vigente, e devolvê-lo
                          apresentaria o estado de hoje como fato daquela data.
                          Vazio quando nada foi removido; ausente quando `at`
                          não foi pedido.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listObjectLinks
      tags:
        - Objects
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: total
          schema:
            type: string
            description: true = inclui `meta.total` (contagem do universo filtrado). Custa
              uma consulta a mais.
        - in: query
          name: rel
          schema:
            type: string
            enum:
              - issued
              - assigned_to
              - holds
              - manages
              - administers
              - custodies
              - audits
              - same_owner
              - shareholder_of
              - indexed_to
              - rates
              - mentions
              - measures
              - forecasts
              - contains
              - member_of
              - exposed_to_issuer
              - succeeded_by
              - produces
              - covers
            description: Omitir = todas as relações desta direção.
        - in: query
          name: direction
          schema:
            type: string
            enum:
              - out
              - in
            default: out
            description: "`out` = este objeto PRATICA o verbo; `in` = sofre. Ex.: fundo `out
              holds` ativo; ativo `in holds` fundo."
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: >-
              Corte temporal (AAAA-MM-DD). SEM ele a resposta é 'quem JÁ se
              relacionou', não 'quem se relaciona' — a diferença é grande e
              silenciosa.


              **O RECORTE DEPENDE DA FORMA DO VERBO** (`shape`, publicado em
              `listObjectRelations`):


              - `event` (`issued`, `indexed_to`, `succeeded_by`): aconteceu numa
              data e não deixa de ter acontecido — entra tudo que ocorreu
              **até** `at`, sem limite superior. Aresta sem data declarada NÃO
              entra: o registro de ações não publica data de emissão, e afirmar
              que ela já existia numa data passada é afirmação que a fonte não
              faz.

              - `snapshot` (`holds`, `contains`, `assigned_to`,
              `exposed_to_issuer`): retrato por competência, e nenhum retrato se
              afirma válido depois de ser tirado. A janela é **contida**
              (relação que saiu e voltou não aparece no buraco), e `at` mais
              recente que a última competência **recua** até ela — a resposta
              diz em `meta.as_of` qual data foi realmente aplicada. Cada verbo
              tem a competência DELE: `holds` fecha no trimestre da CDA,
              `contains` no pregão do dia.

              - `static` (`manages`, `administers`, `custodies`, `audits`,
              `same_owner`, `shareholder_of`, `measures`, `forecasts`): o
              registro publica só o VIGENTE — 111.729 arestas sem início
              declarado. Com o verbo preso em `rel`, `at` no passado responde
              **400** em vez de devolver o gestor de hoje como gestor de 2020.
              Sem verbo preso, essas arestas são removidas do conjunto e
              `meta.excluded_shapes` diz que foram.


              **AS IRMÃS NÃO COMPARTILHAM O DEFAULT.** Sem `at`: esta operação,
              `listGlobalLinks`, `intersectObjects` e `findObjectPaths`
              respondem 'quem JÁ se relacionou'; `getObjectLinkStats` resume o
              retrato VIGENTE (última competência do próprio conjunto);
              `traverseObjectPath` atravessa o retrato vigente POR SUJEITO em
              cada salto. As diferenças são deliberadas — histórico acumulado,
              resumo e travessia respondem perguntas distintas — e cada rota
              declara a sua no próprio `at`. Medido em 23/08/2026: 1 aresta no
              resumo contra 100+ na listagem, para o mesmo objeto, os dois
              certos.
        - in: query
          name: resolve
          schema:
            type: string
            enum:
              - auto
              - exact
            default: auto
            description: >-
              Como interpretar o `id` recebido.


              - `auto` (default) — como REFERÊNCIA publicada: id fundido resolve
              sozinho para o sucessor (com `redirected_from` preenchido) e id
              cindido responde **409** com a lista.

              - `exact` — como o id do objeto que existe HOJE: nenhum histórico
              é consultado, e o id ou responde ou é 404.


              **Quando você precisa de `exact`:** numa cisão, um dos sucessores
              é o PRÓPRIO id pedido — o ramo que ficou com a chave de nascimento
              herda o identificador. O 409 manda consultar cada sucessor, e
              consultar esse cairia no mesmo 409. `exact` é o endereço dele.
              Serve também para desligar o redirect automático de fusão quando
              você quer saber se o id que guardou ainda é o id de alguma coisa.
        - schema:
            type: string
          in: path
          name: id
          required: true
      summary: Atravessa uma relação a partir (ou em direção a) este objeto
      description: >-
        Uma linha por AFIRMAÇÃO, com o outro lado já resolvido em nome e tipo —
        a mesma relação afirmada por duas fontes, ou interrompida e retomada,
        ocupa mais de uma linha. Para contar objetos, agrupe por `other_id`.
        `magnitude` é o tamanho que a fonte publicou (participação, peso no
        índice, valor de mercado) e **nulo não é zero**: significa que a fonte
        não publicou valor plausível. Prova documental é `getObjectEvidence`,
        não este campo. `valid_from`/`valid_to` delimitam o período — use `?at=`
        para o presente.


        **JÁ VEM ORDENADO POR `magnitude` DECRESCENTE, com nulo por último —
        então `limit=5` É o top-5.** Está escrito aqui porque a ausência custou
        caro: numa sondagem de 22/08/2026 um consumidor competente pediu "os
        cinco maiores donos da Vale", baixou as 341 arestas inteiras (160 mil
        caracteres) e ordenou por fora para chegar exatamente às cinco linhas
        que `limit=5` já devolveria. Capacidade que existe e não se anuncia é
        indistinguível de capacidade que não existe. No pior caso a diferença é
        entre uma chamada e o impossível: a ISA Energia tem **4.925**
        detentores, e a lista inteira não cabe em resposta nenhuma.


        O critério é o TAMANHO da aresta, não a data. Com `at`, `magnitude` é a
        última observação até aquela competência dentro da janela selecionada —
        logo `at` na última competência ordena a posição atual. Sem `at`, a
        listagem é histórica (quem JÁ se relacionou) e a magnitude segue sendo o
        máximo do segmento.
      x-capability: object-context
  /v1/objects/{id}/links/history:
    get:
      responses:
        "200":
          description: Página de observações da relação
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/EntityLinkObservationRow"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getObjectLinkHistory
      tags:
        - Objects
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
          required: false
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - in: query
          name: rel
          schema:
            type: string
            enum:
              - issued
              - assigned_to
              - holds
              - manages
              - administers
              - custodies
              - audits
              - same_owner
              - shareholder_of
              - indexed_to
              - rates
              - mentions
              - measures
              - forecasts
              - contains
              - member_of
              - exposed_to_issuer
              - succeeded_by
              - produces
              - covers
            description: Relação snapshot cuja magnitude será lida competência a
              competência.
          required: true
        - in: query
          name: direction
          schema:
            type: string
            enum:
              - out
              - in
            default: out
          required: false
        - in: query
          name: other_id
          schema:
            type: string
            description: Prende o outro lado a um objeto específico.
          required: false
        - in: query
          name: source
          schema:
            type: string
            description: Prende a afirmação a uma fonte específica.
          required: false
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
        - in: query
          name: resolve
          schema:
            type: string
            enum:
              - auto
              - exact
            default: auto
            description: >-
              Como interpretar o `id` recebido.


              - `auto` (default) — como REFERÊNCIA publicada: id fundido resolve
              sozinho para o sucessor (com `redirected_from` preenchido) e id
              cindido responde **409** com a lista.

              - `exact` — como o id do objeto que existe HOJE: nenhum histórico
              é consultado, e o id ou responde ou é 404.


              **Quando você precisa de `exact`:** numa cisão, um dos sucessores
              é o PRÓPRIO id pedido — o ramo que ficou com a chave de nascimento
              herda o identificador. O 409 manda consultar cada sucessor, e
              consultar esse cairia no mesmo 409. `exact` é o endereço dele.
              Serve também para desligar o redirect automático de fusão quando
              você quer saber se o id que guardou ainda é o id de alguma coisa.
          required: false
        - schema:
            type: string
          in: path
          name: id
          required: true
      summary: Histórico da MAGNITUDE de uma relação snapshot
      description: "Uma linha por competência publicada da aresta — o equivalente
        relacional de `getObjectHistory`. Evita repetir `listObjectLinks?at=`
        para cada data. Só aceita verbos `snapshot`; `event` não tem magnitude
        que varia por retrato e `static` não publica série. Mais recente
        primeiro. Observação ausente não é zero: significa que a fonte não
        publicou magnitude plausível naquela competência."
      x-capability: object-context
  /v1/objects/{id}/evidence:
    get:
      responses:
        "200":
          description: Página de trechos com proveniência
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/EntityEvidence"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      uncovered_reason:
                        type: string
                        description: "Presente só quando a lista está vazia POR AUSÊNCIA DE PONTE: o
                          objeto não tem cd_cvm, CNPJ, ISIN nem ticker, que são
                          as quatro chaves por onde a ligação documento↔objeto
                          resolve. Leia como 'não há caminho', nunca como 'não
                          há documento' — o documento que fala dele existe
                          ligado a outro objeto (a oferta, ao emissor; a
                          subclasse, ao fundo). Ausente quando o objeto TEM
                          chave: ali o vazio é a resposta certa."
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getObjectEvidence
      tags:
        - Objects
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Data de referência mínima do documento.
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Data de referência máxima do documento.
        - in: query
          name: resolve
          schema:
            type: string
            enum:
              - auto
              - exact
            default: auto
            description: >-
              Como interpretar o `id` recebido.


              - `auto` (default) — como REFERÊNCIA publicada: id fundido resolve
              sozinho para o sucessor (com `redirected_from` preenchido) e id
              cindido responde **409** com a lista.

              - `exact` — como o id do objeto que existe HOJE: nenhum histórico
              é consultado, e o id ou responde ou é 404.


              **Quando você precisa de `exact`:** numa cisão, um dos sucessores
              é o PRÓPRIO id pedido — o ramo que ficou com a chave de nascimento
              herda o identificador. O 409 manda consultar cada sucessor, e
              consultar esse cairia no mesmo 409. `exact` é o endereço dele.
              Serve também para desligar o redirect automático de fusão quando
              você quer saber se o id que guardou ainda é o id de alguma coisa.
        - schema:
            type: string
          in: path
          name: id
          required: true
      summary: Documento, página e trecho que sustentam o objeto
      description: >-
        Entrega o **parágrafo** de documentos ASSOCIADOS ao objeto, com
        protocolo, link da fonte e as páginas onde ele está. Página é o que
        separa citação de verificação: sem ela, "está no relatório" manda o
        leitor procurar em 300 páginas.


        **O QUE ISTO NÃO É.** A ligação é documento ↔ OBJETO, resolvida por
        cd_cvm, CNPJ do emissor, ISIN ou ticker. Ela NÃO prova uma aresta nem
        uma medida específica: um documento da companhia aparece aqui inteiro, e
        não porque contenha a afirmação que você está conferindo. Para saber
        qual fonte sustenta uma relação, leia `source` na própria aresta.


        `excerpt` é o TEXTO DO DOCUMENTO, não resumo nosso.


        COBERTURA: a ponte documento↔objeto é recalculada a cada noite contra o
        spine de identidade, com gate de volume e cobertura — ela não pode
        esvaziar em silêncio. Medido em 17/08/2026: **195.266 de 196.504
        documentos do acervo (99,4%)** resolvem para pelo menos um objeto.
        Objeto sem documento indexado responde vazio, e isso é ausência de
        documento, não falha de resolução.


        A ordem é a de ENTRADA no acervo, do mais recente para trás — **não** é
        relevância. Para relevância existe a busca semântica, que é outra
        superfície.


        O recorte é por DATA (`from`/`to`). Não há filtro por categoria de
        documento de propósito: medido, ele fazia a consulta varrer o acervo
        inteiro da empresa atrás de linhas que podem não existir, e a chamada
        expirava. Filtro que sempre expira é pior que filtro ausente.
      x-capability: object-context
  /v1/objects/{id}/properties:
    get:
      responses:
        "200":
          description: As propriedades declaradas do objeto
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                          description: "Nome canônico da propriedade. Estável entre tipos: `situation`
                            significa a mesma coisa num fundo e numa subclasse."
                        value:
                          type: string
                          description: 'O valor, sempre TEXTO — booleano vem como `"true"`/`"false"`. É o
                            valor como a tabela de serving o tem: onde a fonte
                            publica `S`/`N`, é `S`/`N` que sai. Traduzir aqui
                            faria esta rota discordar da rota de domínio sobre a
                            mesma coluna, e normalização é trabalho do
                            warehouse.'
                        description:
                          type: string
                          description: O que a propriedade significa e onde ela costuma ser mal lida.
                        source:
                          type: string
                          description: De que cadastro o valor veio.
                        as_of:
                          type:
                            - string
                            - "null"
                          description: "A COMPETÊNCIA da propriedade — em que data ela é verdade. Palavra
                            sem data-base é tão pouco auditável quanto número
                            sem competência: 'Em Liquidação' vale em alguma
                            publicação, não para sempre. **Nulo onde a fonte não
                            publica vintage**, e nulo é a resposta honesta —
                            carimbar 'hoje' afirmaria que o cadastro foi
                            conferido agora."
                        vocabulary:
                          type:
                            - array
                            - "null"
                          items:
                            type: string
                          description: "Os valores POSSÍVEIS, quando a fonte tem lista fechada — é o que
                            separa 'existem cinco situações' de 'existem vinte'.
                            `null` quando a lista é aberta ou grande demais para
                            declarar (70 setores, 66 classificações ANBIMA):
                            vocabulário declarado e desatualizado é pior que
                            vocabulário ausente."
                      required:
                        - name
                        - value
                        - description
                        - source
                        - as_of
                        - vocabulary
                    description: "Só propriedade COM valor. Coluna nula não vira linha: publicá-la
                      diria 'esta coisa não tem público-alvo', quando o que
                      houve foi a fonte não declarar."
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                  uncovered_reason:
                    type: string
                    description: "Por que a lista veio VAZIA. Presente só quando `data` é vazio, e
                      separa as duas causas que um `count: 0` seco confunde:
                      **não há folha para este recorte** (BDR, COE e
                      securitizado são `instrument`, e só debênture tem folha
                      hoje) ou **há folha e a fonte não declarou** nada para
                      este objeto. Nenhuma das duas significa que o objeto não
                      tem atributos."
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getObjectProperties
      tags:
        - Objects
      parameters:
        - in: query
          name: resolve
          schema:
            type: string
            enum:
              - auto
              - exact
            default: auto
            description: >-
              Como interpretar o `id` recebido.


              - `auto` (default) — como REFERÊNCIA publicada: id fundido resolve
              sozinho para o sucessor (com `redirected_from` preenchido) e id
              cindido responde **409** com a lista.

              - `exact` — como o id do objeto que existe HOJE: nenhum histórico
              é consultado, e o id ou responde ou é 404.


              **Quando você precisa de `exact`:** numa cisão, um dos sucessores
              é o PRÓPRIO id pedido — o ramo que ficou com a chave de nascimento
              herda o identificador. O 409 manda consultar cada sucessor, e
              consultar esse cairia no mesmo 409. `exact` é o endereço dele.
              Serve também para desligar o redirect automático de fusão quando
              você quer saber se o id que guardou ainda é o id de alguma coisa.
        - schema:
            type: string
          in: path
          name: id
          required: true
      summary: As PROPRIEDADES do objeto — o que ele é, em palavras
      description: >-
        A terceira forma de dizer algo sobre uma coisa. `getObjectFacts` serve
        NÚMERO (tem unidade, escala, data-base, anda no tempo);
        `listObjectLinks` serve LIGAÇÃO (aponta para outro objeto); esta rota
        serve PALAVRA de vocabulário fechado — situação, forma de condomínio,
        público-alvo, segmento de listagem, rito da oferta.


        **Leia `vocabulary` antes de concluir qualquer coisa de um valor.** Ele
        traz os valores POSSÍVEIS quando a fonte tem lista fechada, e é o que
        separa 'existem cinco situações' de 'existem vinte'. Um exemplo que
        morde: a situação de fundo tem cinco valores e NENHUM deles é
        'Encerrado' — referência de mercado que exibe isso está derivando de
        outro lugar. Onde a lista é grande e viva (70 setores, 66 classificações
        ANBIMA), `vocabulary` é nulo de propósito: vocabulário declarado e
        desatualizado é pior que vocabulário ausente.


        **O valor sai como a tabela o tem**, sempre texto — booleano vem
        `"true"`/`"false"`, e onde a CVM publica `S`/`N` é `S`/`N` que sai. Esta
        rota não traduz, porque traduzir aqui faria ela discordar da rota de
        domínio sobre a mesma coluna.


        **Propriedade nula não vira linha.** Publicá-la diria 'esta coisa não
        tem público-alvo' quando o que houve foi a fonte não declarar — 475 das
        9.119 subclasses não declaram previdência, e isso não as torna
        não-previdenciárias.


        **O que NÃO está aqui, e por quê:** administrador, gestor, auditor e
        custodiante de um fundo, e o coordenador líder de uma oferta, parecem
        propriedade e são ARESTA — apontam para outro objeto. Hoje ainda são
        texto na tabela de origem; publicá-los como propriedade normalizaria o
        erro em vez de resolvê-lo.
      x-capability: object-context
  /v1/objects/{id}/facts:
    get:
      responses:
        "200":
          description: Medidas com valor, mais recentes primeiro
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                          description: Identificador estável da medida. Passe-o em `fact` para pedir a
                            série.
                        series:
                          type:
                            - string
                            - "null"
                          description: Qualificador dentro do objeto — o papel, quando a medida é de
                            classe de ação. null = vale para o objeto inteiro.
                        value:
                          type: number
                        unit:
                          type: string
                          enum:
                            - brl
                            - usd
                            - pct
                            - ratio
                            - x
                            - count
                            - points
                            - native
                          description: "`ratio` = fração (0,08 é 8%); `pct` = percentual (8,0 é 8%) — os
                            dois existem no mesmo produto e NÃO são comparáveis
                            sem converter. `x` = múltiplo. `native` = a unidade
                            da própria série, que só o catálogo dela conhece."
                        as_of:
                          type: string
                          description: Data do REGISTRO que produziu este valor. Cadências diferentes têm
                            datas diferentes no MESMO objeto — não as alinhe.
                        statement_date:
                          type:
                            - string
                            - "null"
                          description: >-
                            A COMPETÊNCIA a que o número se refere, quando ela
                            difere de `as_of`.


                            Múltiplo de ação é reprecificado todo dia: a linha
                            de 17/08 traz o balanço de 30/06 com o preço de
                            hoje. Para `pl` e `market_cap` isso é correto, o
                            número mudou mesmo. Para `margem_ebit` e `roe`, que
                            não dependem de preço, o valor é IDÊNTICO ao do
                            fechamento do trimestre — e citá-lo com a data de
                            hoje afirma um trimestre que não existe.


                            Nulo quando registro e competência coincidem. **Ao
                            datar um fundamento, cite este campo, não `as_of`.**
                        available_at:
                          type:
                            - string
                            - "null"
                          description: "Quando este número ficou PÚBLICO — a data de entrega da
                            demonstração. É por ela que `at` corta, junto com
                            `as_of`. Nulo quando a fonte não publica entrega
                            (`availability: unknown`)."
                        availability:
                          type: string
                          enum:
                            - filed
                            - unknown
                          description: "`filed` = a fonte publica data de entrega, `available_at` vem
                            preenchido e o corte de `at` é point-in-time
                            completo. `unknown` = ela não publica; o corte é
                            pela data-base e o atraso de divulgação fica de
                            fora. NÃO trate `unknown` como point-in-time."
                        cadence:
                          type: string
                          enum:
                            - daily
                            - weekly
                            - monthly
                            - quarterly
                            - annual
                            - irregular
                          description: "Com que frequência a fonte publica. `irregular` = papel que não
                            negocia todo dia, ou série sem calendário fixo:
                            buraco não é queda. **NÃO é a JANELA que o número
                            cobre** — essa é `axes.period`, e as duas divergem
                            por regra: o juro real ex-ante cobre um ano, porque
                            é taxa anualizada, e sai todo dia. Nos indicadores
                            macro este campo é MEDIDO do espaçamento das últimas
                            publicações, não declarado à mão, e por isso
                            acompanha a fonte quando ela muda de frequência."
                        grain:
                          type: string
                          enum:
                            - object
                            - paper
                          description: "`object` = uma resposta só para o objeto inteiro; somar os papéis
                            DUPLICA. `paper` = genuinamente diferente por classe
                            de ação; perguntar pelo objeto é malformado."
                        source:
                          type: string
                          description: Formulário ou conjunto de dados que publicou o número.
                        lineage:
                          type:
                            - string
                            - "null"
                          description: 'A RECEITA do número, quando a fonte a publica por registro:
                            `source` diz o conjunto ("macro"), isto diz a
                            composição ("sgs:432;focus:inflacao_12m/ipca" — o
                            juro real ex-ante é Selic menos Focus). Nulo onde a
                            fonte não publica linhagem por linha; ali a
                            procedência é o próprio `source`.'
                        description:
                          type: string
                        out_of_prior:
                          type:
                            - boolean
                            - "null"
                          description: "O valor caiu fora da faixa plausível declarada para esta medida
                            (`expected_range` em `listFactCatalog`). Marca, não
                            omite: fora da faixa é ou erro de unidade na fonte —
                            a CVM já publicou percentual de 3×10¹⁶ — ou o
                            extremo verdadeiro, e omitir faria os dois virarem
                            ausência. `null` = medida sem faixa declarada, que
                            não é o mesmo que `false`."
                        axes:
                          $ref: "#/components/schemas/FactAxes"
                          description: "A RÉGUA DA MEDIDA: o que o número é (`dimension`), em que escala
                            vem (`scale`), que janela cobre (`period`) e a faixa
                            plausível. **Nulo apenas quando a régua é da SÉRIE e
                            não da medida** — o `value` de `data_series` cobre
                            431 séries em escalas diferentes, e só o catálogo
                            daquela série resolve. Toda medida de objeto declara
                            os eixos."
                        entity_id:
                          type: string
                          description: De QUEM é esta medida — o id resolvido. É o que separa as linhas
                            quando o caminho traz mais de um id.
                      required:
                        - name
                        - series
                        - value
                        - unit
                        - as_of
                        - statement_date
                        - available_at
                        - availability
                        - cadence
                        - grain
                        - source
                        - lineage
                        - description
                        - out_of_prior
                        - axes
                        - entity_id
                    description: "Só medida com valor. Nula não entra: ausência aqui seria lida como
                      'o fato não existe', quando significa 'a fonte não
                      publicou'."
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      subjects:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: O id como foi pedido.
                            resolved_id:
                              type:
                                - string
                                - "null"
                              description: O id que respondeu — difere de `id` numa fusão. Nulo quando nada
                                respondeu.
                            redirected_from:
                              type:
                                - string
                                - "null"
                            status:
                              type: string
                              enum:
                                - ok
                                - not_found
                                - split
                                - no_data
                                - unknown_fact
                              description: "`ok` = leu (numa fusão, `resolved_id` difere de `id` e
                                `redirected_from` diz de onde veio); `not_found`
                                = o id não existe; `split` = cindido, veja
                                `successors`; `no_data` = o objeto existe,
                                declara a medida e a fonte não publicou (NÃO é
                                zero); `unknown_fact` = NENHUMA das medidas
                                pedidas se aplica a este objeto — `reason` traz
                                as que existem. Quando só parte das medidas é
                                desconhecida o sujeito fica `ok` e o item
                                daquela medida carrega o erro."
                            reason:
                              type:
                                - string
                                - "null"
                              description: Por que não veio dado. Nulo quando `ok`.
                            successors:
                              type: array
                              items:
                                type: string
                              description: "Só em `split`: os objetos que herdaram a identidade."
                          required:
                            - id
                            - resolved_id
                            - redirected_from
                            - status
                            - reason
                        description: "Um por id pedido, na ordem pedida. É onde mora o motivo de uma
                          lista VAZIA: `status`/`reason` separam as causas que
                          um `count: 0` seco confunde — o tipo não tem medida, a
                          fonte não publicou, ou a medida pedida não existe para
                          ele. Nenhuma é zero."
                    required:
                      - next_cursor
                      - count
                      - subjects
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getObjectFacts
      tags:
        - Objects
      parameters:
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: facts
          schema:
            type: string
            description: "Recorte por NOME de medida, separado por vírgula (até 10):
              `facts=margem_ebit,roe`. Sem ele, todas."
        - in: query
          name: resolve
          schema:
            type: string
            enum:
              - auto
              - exact
            default: auto
            description: >-
              Como interpretar o `id` recebido.


              - `auto` (default) — como REFERÊNCIA publicada: id fundido resolve
              sozinho para o sucessor (com `redirected_from` preenchido) e id
              cindido responde **409** com a lista.

              - `exact` — como o id do objeto que existe HOJE: nenhum histórico
              é consultado, e o id ou responde ou é 404.


              **Quando você precisa de `exact`:** numa cisão, um dos sucessores
              é o PRÓPRIO id pedido — o ramo que ficou com a chave de nascimento
              herda o identificador. O 409 manda consultar cada sucessor, e
              consultar esse cairia no mesmo 409. `exact` é o endereço dele.
              Serve também para desligar o redirect automático de fusão quando
              você quer saber se o id que guardou ainda é o id de alguma coisa.
        - schema:
            type: string
          in: path
          name: id
          required: true
      summary: As MEDIDAS de um ou VÁRIOS objetos, no último valor publicado
      description: >-
        **Aceita VÁRIOS ids no caminho, separados por vírgula (até 50):**
        `/objects/pub_a,pub_b/facts?facts=margem_ebit` compara Petrobras e Vale
        numa chamada. Cada linha sai com `entity_id`, e `meta.subjects` diz o
        que aconteceu com cada id pedido (`ok`, `not_found`, `split`, `no_data`,
        `unknown_fact`) — o lote nunca encolhe em silêncio, e a forma é UMA só:
        com um id ou cinquenta, a resposta é 200 e o veredito está em
        `meta.subjects`. Id inexistente não é 404 aqui; é `status: not_found`.
        `facts` recorta por nome de medida (vírgula, até 10).


        O mapa de `aspects` diz onde olhar; isto diz quanto é. Uma chamada
        devolve todo número que a base tem sobre o objeto — múltiplos e margens
        de uma companhia, cota e patrimônio de um fundo, inadimplência e
        cotistas de um FIDC — cada um com a data-base em que foi apurado.


        **Leia `unit` antes de comparar qualquer coisa.** `ratio` é fração (0,08
        é 8%) e `pct` é percentual (8,0 é 8%); as duas escalas convivem no mesmo
        produto porque as fontes publicam assim. `dy_12m` de ação é fração,
        `fii_dy_12m` de FII é percentual.


        **`as_of` é por medida, não da resposta.** Preço é diário, indicador é
        trimestral e informe de FIDC é mensal: alinhar as datas na leitura
        inventa simultaneidade que não existe. E `series` diz a qual classe de
        ação a medida pertence quando `grain` é `paper` — PETR3 e PETR4 têm P/L
        diferente, e somar classes é a pergunta errada.


        Para a série de qualquer uma delas, passe o `name` em
        `getObjectHistory`.


        **`at` corta em POINT-IN-TIME**: devolve o último valor que já ERA
        PÚBLICO naquele dia, em vez do mais recente de hoje. É o que separa
        reconstituir uma decisão de junho de olhar o balanço de agosto.


        **Leia `availability` antes de usar `at` para backtest.** Onde ela é
        `filed` a fonte publica data de entrega, `available_at` vem preenchido e
        o corte exige as duas coisas — já entregue E com data-base até o dia.
        Isso importa: 21.591 das 21.949 linhas de indicador são entregues DEPOIS
        da data-base, com atraso mediano de 43 dias. Onde ela é `unknown`
        (informe mensal de FII, p. ex., que não traz data de entrega na fonte) o
        corte é só pela data-base e o atraso de divulgação continua de fora —
        não chame isso de point-in-time. Prova documental com data de protocolo
        é `getObjectEvidence`.
      x-capability: object-context
  /v1/objects/{id}/history:
    get:
      responses:
        "200":
          description: As séries, cada uma em ordem cronológica
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        entity_id:
                          type: string
                          description: De QUEM é esta série — o id resolvido.
                        fact:
                          type: string
                        series:
                          type:
                            - string
                            - "null"
                          description: Qual classe respondeu, quando a medida é de papel. Nunca some
                            classes.
                        unit:
                          type:
                            - string
                            - "null"
                          enum:
                            - brl
                            - usd
                            - pct
                            - ratio
                            - x
                            - count
                            - points
                            - native
                            - null
                          description: "`ratio` = fração (0,08 é 8%); `pct` = percentual (8,0 é 8%) — os
                            dois existem no mesmo produto e NÃO são comparáveis
                            sem converter. `x` = múltiplo. `native` = a unidade
                            da própria série, que só o catálogo dela conhece."
                        cadence:
                          type:
                            - string
                            - "null"
                          enum:
                            - daily
                            - weekly
                            - monthly
                            - quarterly
                            - annual
                            - irregular
                            - null
                          description: "Com que frequência a fonte publica. `irregular` = papel que não
                            negocia todo dia, ou série sem calendário fixo:
                            buraco não é queda. **NÃO é a JANELA que o número
                            cobre** — essa é `axes.period`, e as duas divergem
                            por regra: o juro real ex-ante cobre um ano, porque
                            é taxa anualizada, e sai todo dia. Nos indicadores
                            macro este campo é MEDIDO do espaçamento das últimas
                            publicações, não declarado à mão, e por isso
                            acompanha a fonte quando ela muda de frequência."
                        grain:
                          type:
                            - string
                            - "null"
                          enum:
                            - object
                            - paper
                            - null
                          description: "`object` = uma resposta só para o objeto inteiro; somar os papéis
                            DUPLICA. `paper` = genuinamente diferente por classe
                            de ação; perguntar pelo objeto é malformado."
                        source:
                          type:
                            - string
                            - "null"
                        lineage:
                          type:
                            - string
                            - "null"
                          description: A receita do número quando a fonte a publica por linha — ver o
                            mesmo campo em `getObjectFacts`.
                        availability:
                          type:
                            - string
                            - "null"
                          enum:
                            - filed
                            - unknown
                            - null
                          description: "`from`/`to` recortam a DATA-BASE, sempre — filtrar a série por
                            entrega abriria buracos onde a fonte atrasou. Este
                            campo diz se a leitura datada de
                            `getObjectFacts?at=` é point-in-time completa para
                            esta medida."
                        description:
                          type:
                            - string
                            - "null"
                        points:
                          type: array
                          items:
                            type: object
                            properties:
                              date:
                                type: string
                              value:
                                type: number
                            required:
                              - date
                              - value
                          description: Em ordem CRONOLÓGICA. `limit` corta pelos pontos mais RECENTES, não
                            pelos mais antigos.
                        count:
                          type: integer
                        first_date:
                          type:
                            - string
                            - "null"
                        last_date:
                          type:
                            - string
                            - "null"
                        truncated:
                          type: boolean
                          description: "`limit` cortou pontos que a janela pedida contém. `limit` fica
                            pelos mais RECENTES, que é o default certo — mas com
                            `from`/`to` você pediu uma JANELA, e receber o fim
                            dela em silêncio é receber outra janela. `true` =
                            peça de novo com `to` anterior a `first_date` para
                            varrer o resto."
                        error:
                          type:
                            - string
                            - "null"
                          description: "Por que esta série veio VAZIA: a medida não existe para este
                            objeto (com a lista do que existe) ou o objeto não
                            respondeu. Nulo quando a série é válida — e série
                            válida com zero pontos significa que não houve
                            observação na janela, que é outra coisa."
                      required:
                        - entity_id
                        - fact
                        - series
                        - unit
                        - cadence
                        - grain
                        - source
                        - lineage
                        - availability
                        - description
                        - points
                        - count
                        - first_date
                        - last_date
                        - truncated
                        - error
                    description: "Uma série por (objeto, medida), na ordem pedida: ids de fora,
                      medidas de dentro."
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      subjects:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: O id como foi pedido.
                            resolved_id:
                              type:
                                - string
                                - "null"
                              description: O id que respondeu — difere de `id` numa fusão. Nulo quando nada
                                respondeu.
                            redirected_from:
                              type:
                                - string
                                - "null"
                            status:
                              type: string
                              enum:
                                - ok
                                - not_found
                                - split
                                - no_data
                                - unknown_fact
                              description: "`ok` = leu (numa fusão, `resolved_id` difere de `id` e
                                `redirected_from` diz de onde veio); `not_found`
                                = o id não existe; `split` = cindido, veja
                                `successors`; `no_data` = o objeto existe,
                                declara a medida e a fonte não publicou (NÃO é
                                zero); `unknown_fact` = NENHUMA das medidas
                                pedidas se aplica a este objeto — `reason` traz
                                as que existem. Quando só parte das medidas é
                                desconhecida o sujeito fica `ok` e o item
                                daquela medida carrega o erro."
                            reason:
                              type:
                                - string
                                - "null"
                              description: Por que não veio dado. Nulo quando `ok`.
                            successors:
                              type: array
                              items:
                                type: string
                              description: "Só em `split`: os objetos que herdaram a identidade."
                          required:
                            - id
                            - resolved_id
                            - redirected_from
                            - status
                            - reason
                        description: Um por id pedido, na ordem pedida.
                      truncated:
                        type: boolean
                        description: Alguma série do lote foi cortada por `limit`.
                    required:
                      - next_cursor
                      - count
                      - subjects
                      - truncated
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getObjectHistory
      tags:
        - Objects
      parameters:
        - in: query
          name: facts
          schema:
            type: string
            description: O `name` da medida, como veio de `getObjectFacts` — ou VÁRIOS
              separados por vírgula (até 10).
          required: true
        - in: query
          name: series
          schema:
            type: string
            description: A classe de ação, quando a medida é de papel.
          required: false
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 2000
            default: 500
            description: Pontos mais RECENTES, POR SÉRIE. Default 500.
          required: false
        - in: query
          name: resolve
          schema:
            type: string
            enum:
              - auto
              - exact
            default: auto
            description: >-
              Como interpretar o `id` recebido.


              - `auto` (default) — como REFERÊNCIA publicada: id fundido resolve
              sozinho para o sucessor (com `redirected_from` preenchido) e id
              cindido responde **409** com a lista.

              - `exact` — como o id do objeto que existe HOJE: nenhum histórico
              é consultado, e o id ou responde ou é 404.


              **Quando você precisa de `exact`:** numa cisão, um dos sucessores
              é o PRÓPRIO id pedido — o ramo que ficou com a chave de nascimento
              herda o identificador. O 409 manda consultar cada sucessor, e
              consultar esse cairia no mesmo 409. `exact` é o endereço dele.
              Serve também para desligar o redirect automático de fusão quando
              você quer saber se o id que guardou ainda é o id de alguma coisa.
          required: false
        - schema:
            type: string
          in: path
          name: id
          required: true
      summary: As SÉRIES de uma ou várias medidas, de um ou VÁRIOS objetos
      description: >-
        **Aceita VÁRIOS ids no caminho (vírgula, até 50) e VÁRIAS medidas em
        `facts` (vírgula, até 10):**
        `/objects/pub_a,pub_b/history?facts=margem_ebit,roe&limit=6` devolve as
        quatro séries numa chamada, uma por (objeto, medida), cada uma com a
        própria régua. A forma é UMA só, com um id ou cinquenta: `data` é sempre
        a lista de séries e `meta.subjects` diz o que aconteceu com cada id.
        Medida que não existe para um objeto vem como item com `error` e a lista
        do que existe; id inexistente vem `status: not_found` no sujeito, não
        404.


        **Orçamento:** ids × medidas ≤ 100 séries e ids × medidas × `limit` ≤
        100.000 pontos por chamada; acima disso é 422 com os números em
        `details`.


        A mesma chamada para histórico de indicador fundamentalista, de cota de
        fundo e de cotistas de FIDC — o que muda é `fact`. Antes disso eram três
        contratos com três convenções, e quem perguntasse precisava conhecer os
        três.


        EXEMPLOS COMPLETOS, com os parâmetros exatos:

        - P/L da PETR4 nos últimos anos → `facts=pl&series=PETR4`

        - cotistas de um FIDC ao longo do tempo → `facts=fidc_investors`

        - cota ajustada de um fundo em 2025 →
        `facts=quota&from=2025-01-01&to=2025-12-31`

        - inadimplência mensal de um FIDC → `facts=fidc_impaired_ratio`


        `series` escolhe a classe de ação quando a medida é de papel; sem ele
        responde a âncora e `meta.series` diz qual foi — classes nunca são
        somadas. `limit` corta pelos pontos MAIS RECENTES e os dados voltam em
        ordem cronológica: receber os mais antigos sem aviso se lê como 'a série
        acaba aí'.


        Medida que não se aplica a este objeto responde **item com `error` e a
        lista do que existe**, não série vazia — vazio afirmaria que não houve
        observação, que é outra coisa.
      x-capability: object-context
  /v1/objects/{id}/events:
    get:
      responses:
        "200":
          description: Página de eventos do objeto
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PublicMarketEvent"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getObjectEvents
      tags:
        - Objects
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: total
          schema:
            type: string
            description: true = inclui `meta.total` (contagem do universo filtrado). Custa
              uma consulta a mais.
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: layer
          schema:
            type: string
            description: "`estrutural`, `setorial` ou `corporativa`."
        - in: query
          name: category
          schema:
            type: string
        - in: query
          name: resolve
          schema:
            type: string
            enum:
              - auto
              - exact
            default: auto
            description: >-
              Como interpretar o `id` recebido.


              - `auto` (default) — como REFERÊNCIA publicada: id fundido resolve
              sozinho para o sucessor (com `redirected_from` preenchido) e id
              cindido responde **409** com a lista.

              - `exact` — como o id do objeto que existe HOJE: nenhum histórico
              é consultado, e o id ou responde ou é 404.


              **Quando você precisa de `exact`:** numa cisão, um dos sucessores
              é o PRÓPRIO id pedido — o ramo que ficou com a chave de nascimento
              herda o identificador. O 409 manda consultar cada sucessor, e
              consultar esse cairia no mesmo 409. `exact` é o endereço dele.
              Serve também para desligar o redirect automático de fusão quando
              você quer saber se o id que guardou ainda é o id de alguma coisa.
        - schema:
            type: string
          in: path
          name: id
          required: true
      summary: O que ACONTECEU com este objeto — o ledger lido pelo grafo
      description: >-
        O ledger de eventos existe desde antes do spine e não sabia de quem era:
        para pegar os eventos da Petrobras era preciso escolher entre PETR3 e
        PETR4 pelo `listMarketEvents`, e perder o que estivesse marcado só com o
        outro papel. Aqui o objeto entrega TODOS os apelidos de uma vez.


        **Evento é do EMISSOR, não da classe.** Um fato da companhia vem marcado
        ora com um papel, ora com o outro, ora com os dois — e vir marcado com
        os dois não o conta duas vezes aqui. Somar as consultas por ticker faria
        exatamente isso.


        **A cobertura vem do TICKER.** Há também um casamento por nome canônico
        exato, mas ele rende pouco e isso está medido: o ledger grava nomes
        curtos e às vezes truncados (`Allos`, `Anima`, `Lojas`, `Petróleo`),
        então só 3 de 60 casam com a razão social. Ele fica porque é seguro e
        pega os poucos casos de nome curto (BRAVA, BRB, HAPVIDA); afrouxar para
        prefixo casaria `Rio` com dezenas de empresas, e errado com confiança é
        pior que ausente.


        Na prática: **objeto sem ticker tende a devolver lista vazia**, e isso é
        limite conhecido, não ausência de evento.


        Para o ledger inteiro sem partir de um objeto, use `listMarketEvents`.
        Estes NÃO são eventos societários (grupamento, bonificação) — esses
        vivem em `listCorporateEvents`.
      x-capability: object-context
  /v1/quotes/live:
    get:
      responses:
        "200":
          description: Lote de cotações ao vivo
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/LiveQuote"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      as_of:
                        type: string
                        description: Instante da resposta (ISO 8601).
                    required:
                      - next_cursor
                      - count
                      - as_of
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getLiveQuotes
      tags:
        - Quotes
      parameters:
        - in: query
          name: tickers
          schema:
            type: string
            description: Tickers separados por vírgula (máx 200). Exclusivo com 'index'.
        - in: query
          name: index
          schema:
            type: string
            description: "Código de índice (ex.: IBOV) — resolve os constituintes. Exclusivo
              com 'tickers'."
      summary: Cotações ao vivo (delay de 15 min) em lote
      description: Último preço de vários papéis de uma vez, com delay de 15 min
        durante o pregão. Informe `tickers` (CSV, máx 200) OU `index` (código,
        resolve os constituintes). Durante o pregão o preço vem do intradiário
        (`source=intraday`); fora dele, do fechamento oficial (`source=eod`).
        Tickers desconhecidos são omitidos.
      x-capability: market-data
  /v1/stocks/{ticker}:
    get:
      responses:
        "200":
          description: Ação
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Stock"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getStock
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
      summary: Perfil e última cotação do papel
      x-capability: market-data
  /v1/stocks/{ticker}/quotes:
    get:
      responses:
        "200":
          description: Página de cotações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Quote"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: "Do mais RECENTE para o mais antigo. Declarado porque `getSeries`
                          devolve CRESCENTE e nenhuma das duas dizia: quem
                          calcula `linha[i+1] − linha[i]` para achar a maior
                          QUEDA acha a maior ALTA se supuser o sentido errado —
                          mesmo código, sinal trocado, nenhuma exceção. Para a
                          série mais antiga primeiro, inverta no cliente ou use
                          `from`/`to`."
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listQuotes
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 20000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: adjusted
          schema:
            type: string
      summary: Série de cotações OHLCV
      description: Ajustada por eventos por default; `from`/`to` filtram o período
        (from <= to).
      x-capability: market-data
  /v1/stocks/{ticker}/intraday:
    get:
      responses:
        "200":
          description: Série intradiária
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/IntradayPoint"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      ticker:
                        type: string
                      session_date:
                        type: string
                      prev_close:
                        type:
                          - number
                          - "null"
                      sessions_behind:
                        type: integer
                        description: "Pregões entre `session_date` e a última sessão do mercado. 0 = a
                          série é do pregão corrente. > 0 = série ANTIGA: sem o
                          parâmetro `session`, esta rota devolve a última sessão
                          que o caminho intradiário conseguiu registrar, e ela
                          pode estar parada — em 07/2026 a B3 desativou o
                          endpoint que a alimentava (Comunicado Externo
                          01/2026-VTEC) e a série congelou. Os pontos seguem
                          fiéis ao pregão que descrevem; este campo diz QUAL
                          pregão é. O fechamento oficial não depende deste
                          caminho e continua fresco."
                    required:
                      - next_cursor
                      - count
                      - ticker
                      - session_date
                      - prev_close
                      - sessions_behind
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getStockIntraday
      tags:
        - Quotes
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: session
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Sessão (AAAA-MM-DD); default = sessão intradiária mais recente.
      summary: Série intradiária da sessão (delay de 15 min)
      description: Barras de minuto do pregão; `session` filtra o pregão (default =
        mais recente).
      x-capability: market-data
  /v1/stocks/{ticker}/indicators:
    get:
      responses:
        "200":
          description: Indicadores do papel
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/IndicatorValue"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      ticker:
                        type: string
                      reference_date:
                        type: string
                      statement_date:
                        type:
                          - string
                          - "null"
                        description: Fim do período contábil da demonstração usada (DT_REFER)
                      filed_at:
                        type:
                          - string
                          - "null"
                        description: "1ª publicação da demonstração na CVM (DT_RECEB) — é o corte que
                          `at` respeita. null: data de entrega desconhecida; o
                          corte de `at` cai no período contábil."
                      is_financial:
                        type: boolean
                    required:
                      - next_cursor
                      - count
                      - ticker
                      - reference_date
                      - statement_date
                      - filed_at
                      - is_financial
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getStockIndicators
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: names
          schema:
            type: string
            description: "Lista separada por vírgula de indicadores a retornar (omitir =
              todos). Ex.: 'pl,roe,dy'. Nomes válidos (abreviações SEM
              underscore — use 'pl', não 'p_l'): pl, pvp, psr, ev_ebitda,
              ev_ebit, lpa, vpa, roe, roa, roic, margem_bruta, margem_ebit,
              margem_liquida, div_liq_ebitda, div_liq_pl, dy, payout,
              market_cap, price; e dinâmicos: free_float, beta, volatilidade,
              retorno_12m, volume_medio_2m."
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Snapshot point-in-time SEM lookahead: retorna a última
              demonstração já PUBLICADA (filed_at, data de entrega à CVM) até
              esta data — não a de período contábil mais recente. O indicador
              `price` passa a refletir o fechamento do pregão mais próximo <=
              at. Demonstrações sem filed_at conhecido caem no corte por período
              contábil (ver `meta.filed_at`). Indicadores de preço (beta,
              volatilidade, retorno_12m, volume_medio_2m) não têm série
              histórica: sob `at` anterior à janela corrente saem como null com
              `reason` explícito, nunca o valor atual. `free_float` vem do
              cadastro ATUAL (FRE, sem data): sob QUALQUER `at` sai null com
              `reason`."
      summary: Indicadores fundamentalistas (snapshot TTM)
      x-capability: market-data
  /v1/stocks/{ticker}/indicators/history:
    get:
      responses:
        "200":
          description: Série do indicador
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Observation"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: asc
                        description: Do mais ANTIGO para o mais recente — o oposto de `listQuotes`, que
                          é a rota vizinha sobre o mesmo papel. Confira antes de
                          calcular variação entre linhas.
                      ticker:
                        type: string
                      name:
                        type: string
                      label:
                        type: string
                      unit:
                        type: string
                        enum:
                          - ratio
                          - percent
                          - brl
                          - brl_per_share
                          - count
                          - days
                      reason:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - order
                      - ticker
                      - name
                      - label
                      - unit
                      - reason
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getStockIndicatorHistory
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: name
          schema:
            type: string
            minLength: 1
            description: "Indicador único para a série histórica (ex.: 'pl', 'roe', 'dy').
              Mesmos nomes do snapshot."
          required: true
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
      summary: Histórico de um indicador (trimestral)
      description: "Série trimestral do indicador. Segue a MESMA política do snapshot
        (/stocks/{ticker}/indicators): indicadores que não se aplicam a
        instituições financeiras (margem_bruta, margem_ebit, margem_liquida,
        psr, p_ebit, p_fcf, ev_ebit, ev_ebitda, ebit_ativos, giro_ativos) vêm
        com os pontos em null e o motivo em `reason`, em vez de expor a série
        completa."
      x-capability: market-data
  /v1/stocks/{ticker}/dividends:
    get:
      responses:
        "200":
          description: Página de proventos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Dividend"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listDividends
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Proventos (dividendos e JCP)
      x-capability: market-data
  /v1/stocks/{ticker}/events:
    get:
      responses:
        "200":
          description: Lista de eventos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CorporateEvent"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      ticker:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - ticker
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCorporateEvents
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Eventos societários (desdobramentos, grupamentos, bonificações)
      description: "Os eventos MECÂNICOS que mudam a quantidade de ações/cotas e
        ajustam o preço, com o `factor` de conversão por data-ex. O papel é
        resolvido pelo grafo de identidade: pedir por um código vigente devolve
        também os eventos gravados sob os códigos antigos do mesmo papel (BHIA3
        traz a era VVAR3/VIIA3), cada linha dizendo sob qual `ticker` foi
        gravada. NÃO confundir com `getObjectEvents`, que é o ledger editorial
        de eventos de mercado (notícias, detecções, score) — nem com
        `listCorporateSuccessions`, que cobre M&A e renomeação de código."
      x-capability: market-data
  /v1/stocks/{ticker}/insider:
    get:
      responses:
        "200":
          description: Saldo mensal de insiders
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/InsiderMove"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      ticker:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - ticker
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listInsiderMoves
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Movimentações de insiders (CVM VLMO, por mês)
      description: "Saldo mensal de negociação de administradores, controladores e
        pessoas ligadas (VLMO/CVM). A série é nível-COMPANHIA: todos os papéis
        do emissor devolvem os MESMOS números, então somar PETR3 + PETR4 conta a
        mesma movimentação duas vezes — 174 das 433 companhias (40,2%) têm 2+
        papéis e a inflação medida ao agregar sem filtro é de 40,4%. Para
        agregar por emissor, use `/ownership-flow` e filtre
        `is_issuer_anchor_ticker`. `corporate_event_shares` sai SEPARADO e não
        soma a `net_shares`: é ruído não-mercado (bonificação, herança,
        grupamento), 3,9× maior em magnitude absoluta que o fluxo real."
      x-capability: market-data
  /v1/stocks/{ticker}/fund-holders:
    get:
      responses:
        "200":
          description: Página de fundos detentores
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundHolding"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      summary:
                        oneOf:
                          - $ref: "#/components/schemas/FundOwnershipSummary"
                          - type: "null"
                    required:
                      - next_cursor
                      - count
                      - summary
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFundHolders
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Competência do CDA (AAAA-MM-DD); default = mais recente.
      summary: Fundos de investimento que detêm o ativo (visão reversa do CDA)
      description: "Quais fundos têm o ativo em carteira e quanto (R$), da competência
        mais recente do CDA (CVM). Ordenado por posição a mercado. Use `date` p/
        outra competência. ESCOPO: é a posição DECLARADA em carteira — não
        inclui ações cedidas em empréstimo nem desconta obrigações por ações
        recebidas. Isso diverge de `/ownership-flow/movers`, que usa posição
        ECONÔMICA: em 2026-05 há R$ 10,53 bi cedidos e R$ 11,21 bi de obrigação,
        e 428 dos 2.770 pares fundo×ativo existem SÓ como empréstimo, logo
        aparecem numa rota e não na outra. As duas respondem perguntas
        diferentes; não some uma com a outra."
      x-capability: market-data
  /v1/stocks/{ticker}/ownership-flow/movers:
    get:
      responses:
        "200":
          description: Página de movimentações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundTickerMove"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listOwnershipMovers
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Competência do CDA (AAAA-MM-DD); default = mais recente COM
              movimentação.
        - in: query
          name: direction
          schema:
            type: string
            enum:
              - in
              - out
              - all
            default: all
            description: in = só quem aumentou/entrou; out = só quem reduziu/saiu; all =
              tudo, das maiores altas às maiores baixas.
      summary: Fundos que mais aumentaram e reduziram posição no papel
      description: >-
        Quem se mexeu, fundo a fundo, numa competência do CDA: posição anterior,
        posição corrente e a variação em QUANTIDADE (`qty_delta`), já na posição
        econômica — direta + cedida em empréstimo − obrigação por recebida, que
        é o que a carteira do fundo esconde (428 dos 2.770 pares fundo×ativo
        cedidos em 2026-05 existem SÓ como empréstimo). Por isso os números aqui
        divergem de `/fund-holders`, que serve a posição DECLARADA em carteira,
        sem empréstimo nem obrigação — as duas rotas respondem perguntas
        diferentes sobre o mesmo papel.


        Só aparecem fundos do PAINEL BALANCEADO, isto é, que divulgaram carteira
        nas duas competências. Sem esse corte a queda de divulgação entre um mês
        de trimestre (6.625 fundos) e o mês seguinte (3.278) seria lida como
        venda — em PETR4 o sinal chega a inverter. Por isso um papel legítimo
        pode responder página vazia: significa 'não há competência comparável',
        não 'papel inexistente'.
      x-capability: market-data
  /v1/stocks/{ticker}/ownership-flow:
    get:
      responses:
        "200":
          description: Página de competências mensais
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/TickerDemand"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listOwnershipFlow
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Quem está comprando o papel, por mês (fundos + insiders)
      description: >-
        Série mensal com o delta de posição dos fundos de investimento (CDA/CVM)
        e o fluxo de insiders (VLMO/CVM) no mesmo eixo temporal.


        Dois campos são guardas, não decoração. `funds_shares_delta` só existe
        quando o painel de fundos divulgantes é comparável entre as duas
        competências — quando não é, vem null com o motivo em
        `funds_delta_reason`, o TAMANHO do painel em `funds_panel_n` e a
        cobertura do universo (percentual 0–100) em `funds_panel_coverage_pct`
        (sem esse corte o sinal inverte em 27,8% dos tickers). E o insider é
        nível-COMPANHIA: a mesma linha se repete em todos os tickers do emissor,
        então agregue apenas onde `is_issuer_anchor_ticker` é true, ou o total
        infla 40,4%.


        Fluxo em R$ é `funds_value_flow_brl` (Δqtd × preço implícito do CDA); a
        variação do valor bruto NÃO serve como fluxo, porque embute marcação a
        mercado — em ITUB4 as duas contas dão sinais opostos.
        `insider_corporate_event_shares` nunca deve ser somado a
        `insider_net_shares`.


        Meses em que só há insider vêm com `comptc_date` nula: a série de VLMO é
        mais longa que a janela do CDA.
      x-capability: market-data
  /v1/stocks/{ticker}/trade-stats:
    get:
      responses:
        "200":
          description: Página de estatísticas de negociação
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/TradeStat"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listTradeStats
      tags:
        - Stocks
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: VWAP oficial e nº de negócios por pregão (B3 consolidado)
      description: Série diária de VWAP oficial (TradAvrgPric), nº de negócios e
        volume financeiro do instrumento, do arquivo consolidado da B3.
        Complementa o OHLC ajustado de /quotes.
      x-capability: market-data
  /v1/screener/stocks:
    get:
      responses:
        "200":
          description: Página de resultados
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ScreenerRow"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      unmatched_filters:
                        type: array
                        items:
                          type: string
                        description: "Os recortes desta consulta que não casaram NENHUMA companhia —
                          grafia errada, valor inexistente ou dois valores num
                          campo que aceita um. Vem sempre, não só na página
                          vazia: com dois recortes, um pode casar e o outro não,
                          e a página com resultado seria a interseção de um
                          filtro só sem nada avisar."
                      available_sectors:
                        type:
                          - array
                          - "null"
                        items:
                          type: string
                        description: O vocabulário fechado de `sector`, devolvido quando o setor pedido
                          não casou ninguém. Sem ele, descobrir os valores
                          custava paginar 2.566 companhias. Para cortar por
                          VÁRIOS setores de uma vez, ou por qualquer propriedade
                          categórica, use `rankObjects`/`aggregateObjects` com
                          `where` — lá o OU cabe dentro do campo.
                    required:
                      - next_cursor
                      - count
                      - unmatched_filters
                      - available_sectors
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: screenStocks
      tags:
        - Screener
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: pl_min
          schema:
            type: number
        - in: query
          name: pl_max
          schema:
            type: number
        - in: query
          name: pvp_min
          schema:
            type: number
        - in: query
          name: pvp_max
          schema:
            type: number
        - in: query
          name: dy_min
          schema:
            type: number
        - in: query
          name: roe_min
          schema:
            type: number
        - in: query
          name: ev_ebitda_max
          schema:
            type: number
        - in: query
          name: div_liq_ebitda_max
          schema:
            type: number
        - in: query
          name: min_volume
          schema:
            type: number
            description: "Piso de liquidez: volume financeiro médio dos últimos 2 meses, em
              R$/dia (ex.: 1000000 corta micro caps ilíquidas). Papel sem série
              de preço recente fica de fora quando o piso está ativo."
        - in: query
          name: sector
          schema:
            type: string
            description: Filtra por setor da empresa (match EXATO, case-sensitive). Valor
              desconhecido retorna página vazia, sem erro.
        - in: query
          name: segment
          schema:
            type: string
            description: "Filtra por segmento de listagem B3 (substring, case-insensitive),
              ex.: 'Novo Mercado'."
        - in: query
          name: sort
          schema:
            type: string
            default: -market_cap
            description: "Ordena por indicador. Valores: pl, pvp, psr, dy, dy_12m, payout,
              roe, roic, ev_ebitda, market_cap, margem_liquida, volume_medio_2m.
              Prefixe com '-' para decrescente (ex.: -dy_12m). Nomes livres como
              'dividend_yield' são inválidos. Default: -market_cap — o topo de
              -dy_12m é dominado por provento extraordinário sobre base pequena
              e não deve ser servido como resposta a uma consulta sem filtro."
      summary: Filtra ações por múltiplos critérios fundamentalistas
      description: >-
        Filtra o universo de ações por faixas de indicadores (pl_min/pl_max,
        pvp_min/pvp_max, dy_min, roe_min, ev_ebitda_max, div_liq_ebitda_max),
        liquidez mínima (min_volume), setor e segmento; ordena por `sort`
        (default `-market_cap`). Paginação por cursor (use meta.next_cursor).
        Retorna ticker, nome, setor e os indicadores principais por papel. Cada
        linha traz `quality_flags`: indicador estatisticamente implausível (DY >
        20%, P/L < 1, ROE > 100%, payout > 200%) é MARCADO com o motivo, nunca
        escondido nem nulado.


        `sector` casa por igualdade EXATA e aceita UM valor. Grafia errada e
        pedido de dois setores devolvem página vazia, como um setor legítimo sem
        ação nenhuma — por isso `meta.unmatched_filters` nomeia o recorte que
        não casou ninguém, e `meta.available_sectors` traz o vocabulário quando
        o setor não existe.


        **Para cortar por vários setores, ou por qualquer propriedade
        categórica, use `rankObjects` / `aggregateObjects` com `where`** — lá o
        OU cabe dentro do campo (`where=sector=Bancos|Seguradoras`) e o
        vocabulário vem anunciado em `filterable_properties`, sem uma chamada
        extra para descobri-lo.
      x-capability: market-data
  /v1/screener/fiis:
    get:
      responses:
        "200":
          description: Página de FIIs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FiiListRow"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: screenFiis
      tags:
        - Screener
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: segment
          schema:
            type: string
            description: "Filtra por segmento do FII (substring, case-insensitive), ex.:
              'Logística', 'Papel'."
        - in: query
          name: paper
          schema:
            type: string
            enum:
              - "true"
              - "false"
            description: true = papel (CRI), false = tijolo. Omitir = todos.
        - in: query
          name: min_pl
          schema:
            type: number
            description: Patrimônio líquido mínimo (R$) — corta micro-fundos ilíquidos.
        - in: query
          name: include_stale
          schema:
            type: string
            enum:
              - "true"
              - "false"
            description: "true = inclui fundos sem informe mensal recente
              (encerrados/incorporados). Default: só fundos com informe nos
              últimos 6 meses."
        - in: query
          name: sort
          schema:
            type: string
            default: -patrimonio_liquido
            description: "Ordena por: ticker, preco, dy, dy_12m, pvp, vacancia, pl,
              patrimonio_liquido. Prefixe com '-' para decrescente (ex.:
              -dy_12m). Default: -patrimonio_liquido."
      summary: Universo de FIIs filtrável por segmento e tijolo/papel
      description: Lista o universo de FIIs ATIVOS (com informe mensal nos últimos 6
        meses; include_stale=true inclui encerrados/incorporados), filtrável por
        segmento, tijolo/papel e PL mínimo (min_pl); ordena por `sort`.
        Paginação por cursor (meta.next_cursor). Retorna ticker, nome, segmento
        e indicadores (dy, pvp, vacância…).
      x-capability: market-data
  /v1/screener/funds:
    get:
      responses:
        "200":
          description: Página de fundos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundScreenerItem"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: screenFunds
      tags:
        - Screener
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: classificacao
          schema:
            type: string
            description: "Filtra por classificação CVM do fundo (match exato), ex.: 'Renda
              Fixa', 'Ações'."
        - in: query
          name: manager
          schema:
            type: string
            description: "Filtra pelo GESTOR, por substring da razão social publicada pela
              CVM (ex.: 'paramis')."
        - in: query
          name: min_net_worth
          schema:
            type: number
            description: Patrimônio líquido mínimo (R$).
        - in: query
          name: sort
          schema:
            type: string
            enum:
              - net_worth
              - return_1m
              - return_12m
              - return_ytd
              - shareholders
            default: net_worth
        - in: query
          name: order
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
      summary: Screener de fundos de investimento (CVM 175)
      description: Ranqueia o universo de fundos por patrimônio, retorno (1m/12m/YTD)
        ou nº de cotistas; filtra por classificação CVM e PL mínimo.
        `return_12m` fica null enquanto não houver 12m de histórico.
      x-capability: market-data
  /v1/screener/debentures:
    get:
      responses:
        "200":
          description: Página de debêntures ranqueadas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/DebentureScreenerRow"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: screenDebentures
      tags:
        - Screener
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: q
          schema:
            type: string
            description: Busca por código do papel ou nome do emissor (substring).
        - in: query
          name: indexer
          schema:
            type: string
            description: "Indexador as-filed: DI, IPCA, IGP-M, PRÉ, TR…"
        - in: query
          name: incentivada
          schema:
            type: boolean
            description: true = só incentivadas (Lei 12.431, isentas de IR para pessoa
              física).
        - in: query
          name: active
          schema:
            type: boolean
            default: true
            description: Default true = só emissões vivas. active=false lista as saídas
              (vencidas/resgatadas).
        - in: query
          name: maturityFrom
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Vencimento a partir de (YYYY-MM-DD).
        - in: query
          name: maturityTo
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Vencimento até (YYYY-MM-DD).
        - in: query
          name: minSpread
          schema:
            type: number
            description: Spread mínimo em % a.a. sobre o indexador.
        - in: query
          name: maxSpread
          schema:
            type: number
            description: Spread máximo em % a.a. sobre o indexador.
        - in: query
          name: minTrades30d
          schema:
            type: number
            description: "Piso de liquidez: nº mínimo de negócios no secundário na janela de
              30 dias."
        - in: query
          name: minFundHolders
          schema:
            type: number
            description: Nº mínimo de fundos com posição no papel na última carteira mensal
              divulgada.
        - in: query
          name: orderBy
          schema:
            type: string
            enum:
              - spread
              - maturity
              - volume_30d
              - trades_30d
              - fund_holders
              - pct_of_curve
            default: volume_30d
            description: "Critério de ordenação: spread (spread_pct_aa), maturity
              (vencimento), volume_30d, trades_30d, fund_holders ou pct_of_curve
              (last_pct_of_curve). Papel sem a métrica vai para o fim em
              qualquer sentido."
        - in: query
          name: order
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
      summary: "Screener de debêntures: indexação, spread, liquidez e demanda de fundos"
      description: "Filtra e ranqueia o universo de debêntures por indexador, spread,
        vencimento, liquidez do secundário e demanda institucional. Como ler
        cada métrica: `spread_pct_aa` é a taxa aditiva sobre o indexador
        (indexer='DI' + indexer_pct=100 + spread=1,6 lê-se DI a 100% + 1,6%
        a.a.); `years_to_maturity` é o prazo restante em anos (null = vencida ou
        perpétua); `last_pct_of_curve` é o preço do último negócio como % do PU
        teórico na curva — <100 = deságio (risco percebido ou iliquidez), >100 =
        ágio; `trades_30d`/`volume_brl_30d` (e 90d) medem liquidez em janelas
        contadas do último pregão disponível, com volume aproximado por PU médio
        × quantidade; `fund_holders`/`funds_value_brl` dizem quantos fundos
        detêm o papel e o valor somado dessas posições na carteira mensal mais
        recente (null = fora das carteiras divulgadas, não zero).
        `is_incentivada` = isenta de IR para pessoa física (Lei 12.431).
        Default: só emissões vivas (active=true), ordenadas por volume_brl_30d
        desc."
      x-capability: market-data
  /v1/macro/gears:
    get:
      responses:
        "200":
          description: Sinais por engrenagem
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/MacroGear"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      as_of:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - as_of
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getMacroGears
      tags:
        - Macro
      parameters:
        - in: query
          name: gear
          schema:
            type: string
            enum:
              - monetary
              - inflation
              - growth
              - employment
              - credit
              - fiscal
              - external
              - sovereign_risk
              - global
              - currency
              - cross_asset
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Engrenagens macro (sinais por seção)
      x-capability: market-data
  /v1/macro/regime:
    get:
      responses:
        "200":
          description: Snapshot do regime
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegimeSnapshot"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getMacroRegime
      tags:
        - Macro
      parameters:
        - in: query
          name: at
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Regime econômico (quadrante crescimento × inflação)
      x-capability: market-data
  /v1/macro/expectations:
    get:
      responses:
        "200":
          description: Expectativas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Expectation"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      indicator:
                        type: string
                      reference:
                        type:
                          - string
                          - "null"
                      realized:
                        type:
                          - number
                          - "null"
                      unit:
                        type: string
                        description: Unidade de `median`/`mean` para ESTE indicador.
                      dimension:
                        type: string
                        enum:
                          - rate
                          - currency
                        description: "`rate` para ipca/selic/pib, `currency` para cambio. Mesmo campo,
                          coisas incomparáveis."
                      period:
                        type: string
                        enum:
                          - annual
                          - none
                        description: Janela que o número cobre. `annual` para as taxas do ano de
                          `reference`; `none` para o câmbio, que é um nível de
                          fim de período.
                      frequency:
                        type: string
                        const: weekly
                        description: O Focus é semanal — `survey_date` é a data da coleta, não do fato.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo — sentido OPOSTO ao de
                          `getSeries`, que devolve crescente. Nenhuma das duas
                          declarava, e é a diferença que inverte o sinal de quem
                          calcula variação entre linhas consecutivas sem
                          conferir.
                      truncated:
                        type: boolean
                        description: "`latest` cortou pesquisas que a janela pedida contém. `true` = há
                          histórico antes da última linha."
                    required:
                      - next_cursor
                      - count
                      - indicator
                      - reference
                      - realized
                      - unit
                      - dimension
                      - period
                      - frequency
                      - order
                      - truncated
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getMarketExpectations
      tags:
        - Macro
      parameters:
        - in: query
          name: indicator
          schema:
            type: string
            enum:
              - ipca
              - selic
              - pib
              - cambio
          required: true
        - in: query
          name: reference
          schema:
            type: string
            description: "Ano de referência do consenso, ex.: '2026'. Omitir retorna TODAS
              as referências (mistura anos); informe um ano para uma série
              limpa."
          required: false
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
        - in: query
          name: latest
          schema:
            type: integer
            minimum: 1
            maximum: 5000
            description: As N pesquisas MAIS RECENTES. **Use quase sempre.** Sem isto a
              resposta é o histórico Focus inteiro do indicador, que já estourou
              o teto de tokens de uma tool. Combine com `reference` para uma
              série limpa de um ano só.
          required: false
      summary: Expectativas Focus (consenso por indicador)
      description: >-
        Consenso do Boletim Focus (BCB) para ipca, selic, pib ou cambio. Informe
        `reference` (ano) para uma série limpa; sem ele, mistura todos os anos
        de referência. As pesquisas vêm em `data` (o campo `surveys` da raiz não
        existe mais); indicador, referência e realizado em `meta`.


        **Ordem DECRESCENTE** — a pesquisa mais recente primeiro, declarado em
        `meta.order`. É o sentido OPOSTO ao de `getSeries`; conferir antes de
        calcular variação entre linhas.


        **Peça `latest=N`.** Sem ele a resposta é o histórico completo do
        indicador.
      x-capability: market-data
  /v1/bonds/tesouro/yield-curve:
    get:
      responses:
        "200":
          description: Curva de juros
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/YieldCurvePoint"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      date:
                        type:
                          - string
                          - "null"
                      kind:
                        type: string
                        enum:
                          - nominal
                          - real
                    required:
                      - next_cursor
                      - count
                      - date
                      - kind
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getYieldCurve
      tags:
        - Bonds
      parameters:
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Data de referência da curva (default: mais recente)."
        - in: query
          name: kind
          schema:
            type: string
            enum:
              - nominal
              - real
            default: nominal
            description: nominal = juros prefixados (LTN/NTN-F); real = juros reais IPCA+
              (NTN-B).
      summary: Curva de juros (Tesouro), um ponto por vencimento
      description: Pontos em `data` (ordenados por prazo); data-base e tipo da curva
        em `meta`.
      x-capability: market-data
  /v1/bonds/curves:
    get:
      responses:
        "200":
          description: Curva por vértices
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/BondCurvePoint"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      date:
                        type:
                          - string
                          - "null"
                      kind:
                        type: string
                        enum:
                          - di
                          - pre_ref
                          - ipca_ref
                          - implicita_ref
                    required:
                      - next_cursor
                      - count
                      - date
                      - kind
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getBondCurves
      tags:
        - Bonds
      parameters:
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Data de referência da curva (default: mais recente)."
        - in: query
          name: kind
          schema:
            type: string
            enum:
              - di
              - pre_ref
              - ipca_ref
              - implicita_ref
            default: di
            description: di = curva de juros futuros de mercado; pre_ref/ipca_ref = curvas
              de referência de mercado pré e IPCA+; implicita_ref = inflação
              implícita entre as duas.
      summary: Curvas de juros de mercado por vértice (DI, pré, IPCA+, implícita)
      description: "Um ponto por vértice de prazo, em % a.a. `di` vem dos juros
        futuros; as demais são curvas de referência de mercado. Complementa a
        getYieldCurve (que é a régua do Tesouro Direto, um ponto por título).
        ATENÇÃO: `tenor_days` é em dias CORRIDOS (o vértice de 1 ano-calendário
        fica em ~365-374 dias, não em 252) — para comparar prazos entre curvas
        use `tenor_years`, que normaliza a convenção."
      x-capability: market-data
  /v1/bonds/tesouro/auctions:
    get:
      responses:
        "200":
          description: Página de leilões
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/TesouroAuction"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listTesouroAuctions
      tags:
        - Bonds
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: type
          schema:
            type: string
            description: "Tipo do título: LTN, LFT, NTN-B, NTN-F…"
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Leilões primários da dívida pública federal (2000+)
      description: "Resultado leilão a leilão: ofertado × aceito, taxa de corte,
        financeiro e segunda volta. `bid_to_cover` = aceito/ofertado. Mais
        recente primeiro."
      x-capability: market-data
  /v1/bonds/tesouro:
    get:
      responses:
        "200":
          description: Página de títulos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/TesouroBondQuote"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listTesouroBonds
      tags:
        - Bonds
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: type
          schema:
            type: string
            enum:
              - LTN
              - NTN-F
              - NTN-B
              - NTN-B-Principal
              - LFT
              - RENDA+
              - EDUCA+
            description: "Tipo do título: LTN (prefixado), NTN-F (prefixado c/ juros),
              NTN-B(-Principal) (IPCA+), LFT (Selic), RENDA+, EDUCA+."
        - in: query
          name: maturity
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Filtra por vencimento (YYYY-MM-DD).
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Data de referência das taxas/preços (default: mais recente)."
      summary: Títulos do Tesouro Direto (taxas e preços)
      description: Identidade é `type + maturity`, e títulos do mesmo vencimento NÃO
        são intercambiáveis. `Tesouro IPCA+ 2035` (sem complemento) é
        `NTN-B-Principal`, `coupon=unique`; `Tesouro IPCA+ com Juros Semestrais
        2035` é `NTN-B`, `coupon=semiannual`. Quando a pergunta nomear um deles,
        filtre `type` em vez de escolher a primeira linha. Cada linha já devolve
        o `entity_id` do título exato.
      x-capability: market-data
  /v1/commodities:
    get:
      responses:
        "200":
          description: Mercadorias disponíveis
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Commodity"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCommodities
      tags:
        - Commodities
      parameters: []
      summary: Mercadorias com contrato futuro na B3
      description: "Catálogo das commodities cujo ajuste diário publicamos: boi gordo,
        milho, café arábica, etanol hidratado, soja (duas referências) e ouro.
        `contracts_open` é o número de vencimentos vivos no último pregão.
        `reference_owner` aparece quando o contrato é referenciado a um índice
        de terceiro (CME Group, Platts)."
      x-capability: market-data
  /v1/commodities/{commodity}/curve:
    get:
      responses:
        "200":
          description: Curva por vencimento
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CommodityCurvePoint"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      commodity:
                        type: string
                      product_name:
                        type:
                          - string
                          - "null"
                      reference_owner:
                        type:
                          - string
                          - "null"
                      trade_date:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - commodity
                      - product_name
                      - reference_owner
                      - trade_date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getCommodityCurve
      tags:
        - Commodities
      parameters:
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Pregão de referência (YYYY-MM-DD). Default: o mais recente."
        - schema:
            type: string
          in: path
          name: commodity
          required: true
      summary: Curva de ajuste de um pregão, por vencimento
      description: "Um ponto por vencimento aberto, do mais curto ao mais longo.
        ATENÇÃO: o vencimento mais curto NÃO é preço à vista — é base futura.
        Para spot de agro brasileiro a referência de mercado é o indicador
        CEPEA, que não distribuímos."
      x-capability: market-data
  /v1/commodities/{commodity}/settlements:
    get:
      responses:
        "200":
          description: Ajustes diários
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CommoditySettlement"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      commodity:
                        type: string
                      product_name:
                        type:
                          - string
                          - "null"
                      reference_owner:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - commodity
                      - product_name
                      - reference_owner
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCommoditySettlements
      tags:
        - Commodities
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: contract
          schema:
            type: string
            description: "Restringe a UM vencimento (ex.: BGIN26). Sem ele a série mistura
              vencimentos na mesma data."
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Início da janela (YYYY-MM-DD).
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Fim da janela (YYYY-MM-DD).
        - schema:
            type: string
          in: path
          name: commodity
          required: true
      summary: Série histórica de ajuste diário
      description: Ajuste por (pregão, contrato), do mais recente para o mais antigo.
        Para série comparável no tempo, filtre por `contract` — sem isso a mesma
        data traz vários vencimentos, o que serve para volume mas engana como
        preço.
      x-capability: market-data
  /v1/minerals:
    get:
      responses:
        "200":
          description: Página de minerais
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/MineralCommodity"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listMinerals
      tags:
        - Minerais
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: search
          schema:
            type: string
            description: Busca por nome em português, chave ou nome em inglês. Ignora
              acento.
        - in: query
          name: critical
          schema:
            type: boolean
            description: true = só minerais da lista de críticos do USGS.
        - in: query
          name: group
          schema:
            type: string
            description: "Grupo exato: Metais básicos, Metais preciosos, Metais ferrosos e
              ligas, Terras raras e críticos, Minerais industriais,
              Fertilizantes, Materiais de construção, Energia."
      summary: Catálogo de minerais com produção e reservas por país
      description: Lista as commodities minerais cobertas (lítio, cobre, nióbio,
        cobalto, terras raras, grafita, ferro, ouro, fosfato, potássio e
        outras), com nome em português, grupo, período coberto, se é mineral
        crítico e o maior produtor do ano mais recente. Use para descobrir a
        chave do mineral antes de pedir o ranking de países ou a série. A busca
        ignora acento, então 'litio', 'lítio' e 'lithium' chegam na mesma linha.
        `top_producer_share_pct` sai nulo quando a participação não é calculável
        para nenhum país da commodity.
      x-capability: market-data
  /v1/minerals/observations:
    get:
      responses:
        "200":
          description: Página de observações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/MineralObservation"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listMineralObservations
      tags:
        - Minerais
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: mineral
          schema:
            type: string
            description: "Chave do mineral, ex.: lithium."
        - in: query
          name: country
          schema:
            type: string
            pattern: ^[A-Za-z]{3}$
            description: "País em ISO 3166-1 alfa-3, ex.: BRA, CHN, AUS, CHL."
        - in: query
          name: statistic
          schema:
            type: string
            enum:
              - production
              - reserves
        - in: query
          name: year
          schema:
            type: integer
            minimum: 1900
            maximum: 2100
            description: Ano exato.
        - in: query
          name: from
          schema:
            type: integer
            minimum: 1900
            maximum: 2100
            description: Ano inicial (inclusive).
        - in: query
          name: to
          schema:
            type: integer
            minimum: 1900
            maximum: 2100
            description: Ano final (inclusive).
        - in: query
          name: include_world_total
          schema:
            type: boolean
            description: true inclui as linhas de total mundial, que por default ficam de
              fora por não serem um país.
      summary: Série longa de produção e reservas de minerais, por país e ano
      description: "Formato longo: uma linha por mineral, país, recorte e ano. É a
        rota para perguntas por PAÍS, do tipo 'quais minerais o Brasil produz'
        ou 'o que o Brasil tem de reservas', com `?country=BRA`, e também para a
        série histórica de um mineral com `?mineral=lithium&from=2021&to=2025`.
        Cada linha carrega o valor, a sentinela publicada quando não há valor
        (`value_raw`, `value_flag_reason`), a participação no total mundial com
        o motivo quando ela não existe (`share_of_world_pct` nulo +
        `share_basis`) e a edição da publicação. Edições diferentes não formam
        série comparável: o USGS reestima anos anteriores a cada safra. As
        linhas de total mundial ficam de fora por default. Sobre `reserves`: é
        estimativa geológica agregada por país, não reserva provada sob SEC S-K
        1300, JORC ou NI 43-101."
      x-capability: market-data
  /v1/minerals/{mineral}:
    get:
      responses:
        "200":
          description: Mineral
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MineralCommodity"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getMineral
      tags:
        - Minerais
      parameters:
        - in: path
          name: mineral
          schema:
            type: string
            pattern: ^[A-Za-z0-9_]+$
            minLength: 2
            maxLength: 64
            description: "Chave do mineral, ex.: lithium, copper, niobium_columbium,
              rare_earths."
          required: true
      summary: Ficha de um mineral
      description: "Perfil da commodity mineral: nome em português, grupo, se está na
        lista de minerais críticos do USGS, anos cobertos, unidades, quantos
        países aparecem e o maior produtor com sua participação. Ponto de
        entrada antes do ranking de países e da série histórica."
      x-capability: market-data
  /v1/minerals/{mineral}/producers:
    get:
      responses:
        "200":
          description: Ranking de países
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/MineralProducer"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      mineral:
                        type: string
                      label:
                        type:
                          - string
                          - "null"
                      statistic:
                        type: string
                        description: production ou reserves.
                      year:
                        type:
                          - number
                          - "null"
                        description: Ano do recorte. Nulo quando não há dado.
                      detail:
                        type:
                          - string
                          - "null"
                        description: Recorte usado no ranking. Nulo quando nenhum agregado do ano tem
                          total mundial e a resposta traz todos os recortes lado
                          a lado (cada linha com o seu `statistic_detail_base`).
                      available_details:
                        type: array
                        items:
                          $ref: "#/components/schemas/MineralDetailOption"
                        description: Outros agregados publicados para o mesmo ano. Troque com o
                          parâmetro `detail`.
                      unit:
                        type:
                          - string
                          - "null"
                      world_total:
                        type:
                          - number
                          - "null"
                        description: Total mundial do recorte. Nulo quando o USGS não publica ou o
                          declara `XX`.
                      world_total_flag:
                        type:
                          - string
                          - "null"
                        description: "`XX` significa que o próprio USGS declara o total mundial
                          incalculável."
                      countries_withheld:
                        type: number
                        description: Países com valor retido pelo USGS neste recorte (sigla `W`, dado
                          proprietário de empresa). Acima de zero o ranking está
                          INCOMPLETO e os shares não somam 100%.
                      is_estimate:
                        type: boolean
                        description: O ano do recorte é estimativa do USGS, não realizado.
                      is_critical:
                        type: boolean
                      edition:
                        type:
                          - string
                          - "null"
                      edition_year:
                        type:
                          - number
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - mineral
                      - label
                      - statistic
                      - year
                      - detail
                      - available_details
                      - unit
                      - world_total
                      - world_total_flag
                      - countries_withheld
                      - is_estimate
                      - is_critical
                      - edition
                      - edition_year
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listMineralProducers
      tags:
        - Minerais
      parameters:
        - in: path
          name: mineral
          schema:
            type: string
            pattern: ^[A-Za-z0-9_]+$
            minLength: 2
            maxLength: 64
            description: "Chave do mineral, ex.: lithium, copper, niobium_columbium,
              rare_earths."
          required: true
        - in: query
          name: statistic
          schema:
            type: string
            enum:
              - production
              - reserves
            default: production
            description: production = quantidade extraída/processada no ano. reserves =
              reserva estimada.
        - in: query
          name: year
          schema:
            type: integer
            minimum: 1900
            maximum: 2100
            description: "Ano do recorte. Default: o mais recente publicado."
        - in: query
          name: detail
          schema:
            type: string
            description: "Recorte do agregado quando a commodity tem mais de um, ex.: 'Mine
              production' ou 'Refinery production' no cobre. Sem ele a rota
              escolhe o recorte com total mundial e mais países, e lista os
              outros em `meta.available_details`."
      summary: Ranking de países produtores e detentores de reservas de um mineral
      description: >-
        Responde 'quem produz mais' e 'quem tem as maiores reservas': ranking de
        países produtores com a participação no total mundial, para lítio,
        cobre, nióbio, cobalto, terras raras, grafita, ferro, ouro, fosfato,
        potássio e as demais commodities do catálogo. Use `statistic=production`
        para produção do ano e `statistic=reserves` para reservas. É a rota
        certa para perguntas como 'quanto do nióbio do mundo vem do Brasil',
        'quem domina a produção de terras raras' ou 'quais países concentram as
        reservas de lítio'.


        Como ler o resultado sem errar:

        - `share_of_world_pct` é NULO quando a participação não pode ser
        calculada, e `share_basis` diz o motivo. Nunca trate ausência como zero.
        O motivo mais sutil é `incomparable_basis`: existe total mundial, mas os
        países reportam bases diferentes, como nas reservas de boro, em que a
        Turquia declara boratos refinados, o Chile ulexita e a China óxido
        bórico equivalente. Não são somáveis, e é por isso que o próprio USGS
        marca o total como `XX`.

        - `meta.countries_withheld` acima de zero significa ranking INCOMPLETO:
        o USGS reteve o valor de países onde ele revelaria dado proprietário de
        empresa, então os shares não somam 100%.

        - `meta.is_estimate` verdadeiro significa que o ano é estimativa do
        USGS, não realizado.

        - Quando a commodity tem mais de um agregado (cobre tem produção de mina
        e produção de refino, com totais mundiais distintos), o ranking usa um
        recorte só e informa os outros em `meta.available_details`; troque com
        `?detail=`.

        - Em `statistic=reserves` o número é ESTIMATIVA GEOLÓGICA AGREGADA POR
        PAÍS publicada pelo USGS. Não é reserva provada ou provável declarada
        por companhia sob SEC S-K 1300, JORC ou NI 43-101, não tem a mesma
        auditoria e não deve ser comparada com o relatório técnico de uma
        mineradora.
      x-capability: market-data
  /v1/credit/debentures:
    get:
      responses:
        "200":
          description: Página de debêntures
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Debenture"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listDebentures
      tags:
        - Credit
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: total
          schema:
            type: string
            description: true = inclui `meta.total` (contagem do universo filtrado). Custa
              uma consulta a mais.
        - in: query
          name: q
          schema:
            type: string
            description: Busca por código ou nome do emissor.
        - in: query
          name: issuerCnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ do emissor (14 dígitos, sem pontuação).
        - in: query
          name: indexer
          schema:
            type: string
            description: "Indexador as-filed: DI, IPCA, IGP-M, PRÉ, TR…"
        - in: query
          name: incentivada
          schema:
            type: boolean
            description: Somente incentivadas (Lei 12.431, isentas de IR p/ pessoa física).
        - in: query
          name: active
          schema:
            type: boolean
            description: "Estado do INSTRUMENTO por ISIN: true = há código registrado/vivo;
              false = nenhum alias do instrumento segue registrado. Código
              antigo excluído não torna inativo o ISIN que continua vivo sob
              código novo."
        - in: query
          name: maturityFrom
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Vencimento a partir de (YYYY-MM-DD).
        - in: query
          name: maturityTo
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Vencimento até (YYYY-MM-DD).
        - in: query
          name: issueFrom
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Data de emissão a partir de (YYYY-MM-DD).
        - in: query
          name: issueTo
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Data de emissão até (YYYY-MM-DD).
      summary: Catálogo de debêntures (universo completo, vivas e vencidas)
      description: "Uma linha por emissão: emissor, indexação as-filed (índice + % +
        spread), incentivada (Lei 12.431), garantia, vencimento, quantidade em
        mercado e o último negócio do secundário. `active=true` restringe às
        registradas."
      x-capability: credit-market-data
  /v1/credit/debentures/{code}:
    get:
      responses:
        "200":
          description: Debênture
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Debenture"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getDebenture
      tags:
        - Credit
      parameters:
        - in: path
          name: code
          schema:
            type: string
            minLength: 3
            description: "Código de negociação da debênture, ex.: CPTM11, VALE29."
          required: true
      summary: Detalhe de uma debênture
      x-capability: credit-market-data
  /v1/credit/debentures/{code}/quotes:
    get:
      responses:
        "200":
          description: Página de negócios
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/DebentureQuote"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                      code:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - order
                      - code
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listDebentureQuotes
      tags:
        - Credit
      parameters:
        - in: path
          name: code
          schema:
            type: string
            minLength: 3
            description: "Código de negociação da debênture, ex.: CPTM11, VALE29."
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Secundário de uma debênture (PU e % da curva por pregão)
      description: "Negócios consolidados por pregão desde 2013: PU
        mínimo/médio/máximo, número de negócios e preço como % do PU teórico na
        curva (>100 = ágio). Mais recente primeiro."
      x-capability: credit-market-data
  /v1/credit/debentures/{code}/holders:
    get:
      responses:
        "200":
          description: Página de fundos detentores
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/DebentureHolder"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      code:
                        type: string
                      comptc_date:
                        type:
                          - string
                          - "null"
                      funds_panel_n:
                        type:
                          - number
                          - "null"
                      available_dates:
                        type: array
                        items:
                          type: string
                        description: "TODAS as competências que o painel de carteiras tem, da mais
                          recente para a mais antiga. São poucas — quatro em
                          21/08/2026 — e viajam sempre, não só quando a resposta
                          é vazia: é esta lista que diz quais outras datas dá
                          para pedir em `date`, sem sondar mês a mês."
                      empty_reason:
                        type:
                          - string
                          - "null"
                        description: >-
                          POR QUE a página veio vazia, quando veio.
                          `date_not_in_panel`: a competência pedida não existe
                          no painel — afirmação sobre a DATA, não sobre o papel.
                          `no_holders_at_date`: a competência existe e nenhum
                          fundo declarou o papel nela. `no_panel_for_code`: o
                          papel nunca apareceu em carteira divulgada.


                          Nulo quando há linhas. Sem este campo as três
                          ausências têm a mesma cara, e a primeira se lê como a
                          segunda.
                    required:
                      - next_cursor
                      - count
                      - code
                      - comptc_date
                      - funds_panel_n
                      - available_dates
                      - empty_reason
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listDebentureHolders
      tags:
        - Credit
      parameters:
        - in: path
          name: code
          schema:
            type: string
            minLength: 3
            description: "Código de negociação da debênture, ex.: CPTM11, VALE29."
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (YYYY-MM-DD, fim de mês). Default: a mais
              recente divulgada."
      summary: Fundos que detêm a debênture (look-through da carteira mensal)
      description: 'Lista os fundos com posição no papel na carteira mensal mais
        recente divulgada, ordenados por valor de posição; `date` (YYYY-MM-DD,
        fim de mês) consulta uma competência histórica — e
        `meta.available_dates` diz quais existem, em vez de obrigar a descobrir
        sondando. Página vazia traz `meta.empty_reason`, que separa "a
        competência não existe" de "nenhum fundo declarou o papel nela".
        `buy_value_brl`/`sell_value_brl` são compras e vendas declaradas pelo
        próprio fundo NO mês da competência. `meta.funds_panel_n` = total de
        fundos divulgando qualquer debênture na competência: o universo de
        divulgação encolhe muito fora dos meses de fechamento de trimestre,
        então nunca compare a lista de fundos entre competências sem olhar esse
        denominador.'
      x-capability: credit-market-data
  /v1/credit/debentures/{code}/demand:
    get:
      responses:
        "200":
          description: Série mensal de demanda
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/DebentureDemandPoint"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      code:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - code
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listDebentureDemand
      tags:
        - Credit
      parameters:
        - in: path
          name: code
          schema:
            type: string
            minLength: 3
            description: "Código de negociação da debênture, ex.: CPTM11, VALE29."
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
      summary: Demanda institucional pela debênture, mês a mês
      description: "Série mensal da demanda de fundos pelo papel: nº de fundos
        detentores, quantidade e valor somados, e o fluxo declarado do mês
        (`net_traded_brl` = compras − vendas informadas pelos próprios fundos).
        CAVEAT essencial: o painel de fundos que divulga carteira muda de
        tamanho todo mês (`funds_panel_n` — meses de fim de trimestre têm quase
        o dobro de divulgantes), então queda de `funds_value_brl` entre meses
        geralmente mede queda de DIVULGAÇÃO, não venda. Para fluxo real use
        `net_traded_brl`, nunca a diferença de posição entre meses. Mais recente
        primeiro."
      x-capability: credit-market-data
  /v1/credit/otc:
    get:
      responses:
        "200":
          description: Página de negócios
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CreditOtcQuote"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listOtcQuotes
      tags:
        - Credit
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
          required: false
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
          required: false
        - in: query
          name: family
          schema:
            type: string
            minLength: 2
            description: "Família do instrumento: CDB, CCB, CRI, CRA, LCI, LCA, LF, LIG, NC,
              COE…"
          required: true
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Pregão exato (default: o mais recente com negócios)."
          required: false
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
      summary: Secundário de balcão por instrumento (CDB, CRI, CRA, LCI, LCA…)
      description: >-
        Negócios consolidados de renda fixa privada de balcão, instrumento a
        instrumento. Sem recorte temporal, responde o pregão mais recente da
        família; só entra linha com evidência de negócio (quantidade, número de
        negócios ou volume financeiro).


        CAVEAT de divulgação: em CCB, LCA, LCI, CCI, CCCB, CDAWA e LC a B3 OMITE
        o tamanho e publica apenas preços e volume financeiro. Nessas famílias
        `quantity` e `trade_count` vêm `null` — é sigilo de quantidade, não
        ausência de negócio, e nunca 0. Para dimensionar o giro use
        `volume_brl`, que é real; a quantidade implícita é `volume_brl /
        price_avg`.


        **A profundidade desta série é a do próprio balcão na nossa cobertura, e
        ainda é curta fora de debênture** (debênture tem fonte própria e
        histórico longo em `/v1/credit/debentures/{code}/quotes`). Ausência de
        um papel aqui é ausência nos pregões cobertos, não prova de que ele
        nunca negociou. `listCoes` publica a janela medida em
        `meta.secondary_window`.


        `family=COE` devolve os NEGÓCIOS de COE, não o catálogo. O cadastro
        (emissor, tamanho da emissão, vencimento, secundário agregado na janela
        coberta) fica em `/v1/structured/coe` — COE tem superfície própria
        porque não é renda fixa nem crédito puro: é payoff de derivativo sobre o
        risco do banco emissor.
      x-capability: credit-market-data
  /v1/credit/curve:
    get:
      responses:
        "200":
          description: Curva por bucket de prazo
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CreditCurvePoint"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      date:
                        type:
                          - string
                          - "null"
                      indexer:
                        type: string
                        enum:
                          - DI
                          - IPCA
                    required:
                      - next_cursor
                      - count
                      - date
                      - indexer
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getCreditCurve
      tags:
        - Credit
      parameters:
        - in: query
          name: indexer
          schema:
            type: string
            enum:
              - DI
              - IPCA
            default: DI
            description: "Família da curva: DI (DI a ~100% + spread) ou IPCA (IPCA + taxa
              real)."
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Data da curva (YYYY-MM-DD, pregão). Default: a mais recente
              materializada."
      summary: Curva de crédito observada no secundário de debêntures (spread por prazo)
      description: "Quanto o mercado exige de crédito privado por prazo, derivado dos
        negócios reais do secundário de debêntures: janela de 21 pregões
        terminando em `meta.date`, papéis da família que negociaram na janela,
        bucketados pelo prazo restante (0-2, 2-4, 4-7, 7+ anos), medianas por
        papel (não por volume). Como ler: `spread_median_pct_aa` é a mediana do
        spread DA EMISSÃO (as-filed) dos papéis em mercado — reflete o mix de
        safras negociadas, NÃO o yield implícito negociado; o termômetro do
        reapreçamento é `pct_of_curve_median` (mediana do último preço de cada
        papel como % do PU teórico da curva: <100 = estoque negociando com
        deságio = stress de crédito). Na família IPCA o spread é a taxa real da
        emissão (comparável à NTN-B). Bucket com menos de 5 papéis sai com
        medianas null e `n_papers` real."
      x-capability: credit-market-data
  /v1/credit/issuers/{cnpj}/risk:
    get:
      responses:
        "200":
          description: Série trimestral de solidez
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/IssuerRiskPoint"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      cnpj_root:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - cnpj_root
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getIssuerRisk
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{8}(\d{6})?$
            description: "CNPJ do emissor: raiz de 8 dígitos ou CNPJ completo de 14."
          required: true
      summary: Solidez do emissor bancário (Basileia, PR, carteira), trimestral
      description: "Histórico trimestral de solidez do conglomerado prudencial: índice
        de Basileia (em fração), patrimônio de referência, ativo total, PL,
        lucro e carteira de crédito. É o contexto de risco por trás de um CDB
        que paga acima do mercado. Cobertura: instituições supervisionadas pelo
        BCB; busca pela raiz do CNPJ."
      x-capability: credit-market-data
  /v1/credit/issuers/{cnpj}:
    get:
      responses:
        "200":
          description: Perfil do emissor
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/IssuerProfile"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      cnpj:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - cnpj
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getIssuerProfile
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
      summary: Perfil do emissor de dívida
      description: Quem é o emissor por trás de uma emissão e por onde aprofundar.
        `kind` distingue o emissor supervisionado pelo BCB (`bank`, com solidez
        prudencial em /credit/issuers/{cnpj}/risk) da companhia registrada na
        CVM (`corporate`, com demonstrações e documentos protocolados) e do
        emissor sem nenhum dos dois (`unknown`). `categ_reg` = 'Categoria B'
        marca a companhia registrada apenas para emitir dívida — concessionárias
        e SPEs que publicam balanço mas nunca terão ticker.
      x-capability: credit-market-data
  /v1/credit/ratings:
    get:
      responses:
        "200":
          description: Ratings vigentes
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CreditRatingAction"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCreditRatings
      tags:
        - Credit
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: assetCode
          schema:
            type: string
            minLength: 4
            description: ISIN do papel avaliado (CRI/CRA).
        - in: query
          name: issuerCnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ do emissor avaliado (14 dígitos, sem pontuação).
        - in: query
          name: agency
          schema:
            type: string
            enum:
              - fitch
              - moodys
              - sp
              - austin
              - sr
              - liberum
              - other
            description: Agência classificadora.
        - in: query
          name: entityType
          schema:
            type: string
            enum:
              - issuer
              - securitization
              - fidc
              - stock
            description: A que a nota se refere.
        - in: query
          name: scale
          schema:
            type: string
            enum:
              - national_br
              - global
            description: Escala. Fixe-a antes de comparar `rating_notch` entre papéis.
        - in: query
          name: confidence
          schema:
            type: string
            enum:
              - high
              - medium
              - low
            description: Confiança declarada da extração.
      summary: Rating de crédito vigente, com a fonte primária de cada nota
      description: >-
        Notas de agência observadas em documentos protocolados na CVM e no FNET,
        uma linha por (agência, papel) com a mais recente vigente. Toda resposta
        traz `agency`, `action_date` e `download_url`: a citação é parte do
        dado. Não emitimos rating — quando agências divergem, todas aparecem, e
        nenhuma nota é consolidada num rating nosso.


        `rating_notch` (1 = melhor) só é comparável **dentro da mesma `scale`**:
        um emissor brasileiro costuma ser AAA na escala nacional e BB na global
        ao mesmo tempo, e ordenar as duas juntas produz ranking que parece certo
        e está errado. Filtre por `scale` antes de comparar.


        `action_date_known=false` significa que a vigência foi decidida pela
        data de protocolo do documento (`doc_filed_at`), porque o relatório não
        afirma a data da ação — use-a para ordenar, nunca para exibir como data
        da agência.


        `declared` traz, quando existe, o rating que a própria securitizadora
        declarou no informe mensal da CVM para o mesmo ISIN. É segunda fonte
        para conferência: campo livre e mal preenchido, com `is_parseable=false`
        significando **não declarado no informe** — nunca 'o papel não tem
        rating'.


        Cobertura é parcial e cresce com a indexação: ausência de um papel aqui
        significa que ainda não observamos documento de rating dele, não que ele
        seja sem nota.
      x-capability: credit-market-data
  /v1/credit/ratings/history:
    get:
      responses:
        "200":
          description: Ações de rating observadas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CreditRatingHistoryRow"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      asset_code:
                        type:
                          - string
                          - "null"
                      issuer_cnpj:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - asset_code
                      - issuer_cnpj
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCreditRatingHistory
      tags:
        - Credit
      parameters:
        - in: query
          name: assetCode
          schema:
            type: string
            minLength: 4
            description: ISIN do papel avaliado.
        - in: query
          name: issuerCnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ do emissor avaliado (14 dígitos).
        - in: query
          name: agency
          schema:
            type: string
            enum:
              - fitch
              - moodys
              - sp
              - austin
              - sr
              - liberum
              - other
            description: Restringe a uma agência.
      summary: Histórico de ações de rating de um papel ou emissor
      description: >-
        Todas as ações observadas, append-only: um rating é evento datado, não
        atributo mutável — 'a Fitch atribuiu AA+ em 12/03/2025' continua verdade
        depois de um rebaixamento em 2026. Exige `assetCode` (ISIN) ou
        `issuerCnpj`.


        `superseded_by` preenchido marca a linha corrigida: correção cria linha
        nova em vez de editar o fato, e a linha antiga fica como trilha de
        auditoria. `extracted_at` é quando nós soubemos; `action_date` é quando
        a agência agiu — as duas datas são distintas de propósito.
      x-capability: credit-market-data
  /v1/credit/fidc:
    get:
      responses:
        "200":
          description: Classes de FIDC na competência
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcMonthly"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        enum:
                          - asc
                          - desc
                        description: Sentido no tempo, quando a resposta É uma série. `listFidcHistory`
                          devolve `desc` (a competência mais recente primeiro);
                          `listFidcs` é um corte transversal de UMA competência
                          e não traz o campo.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcs
      tags:
        - Credit
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: q
          schema:
            type: string
            description: Busca pela denominação social da classe.
        - in: query
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ da CLASSE (14 dígitos).
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (fim de mês). Default: a mais recente."
        - in: query
          name: minNetWorth
          schema:
            type: number
            description: Patrimônio líquido mínimo, em BRL.
      summary: "Classes de FIDC por competência: patrimônio, carteira e inadimplência"
      description: >-
        Fundos de direitos creditórios do informe mensal da CVM. O FIDC tem
        superfície própria e não aparece nas séries de `/funds` porque sua cota
        é marcada a **laudo** e não passa pelo informe diário — servi-lo por lá
        devolveria fundo sem série.


        A chave é a **classe**, não o fundo: um fundo multiclasse tem um CNPJ
        por classe, e as classes têm prioridades diferentes. `entity_kind` vem
        null nas competências anteriores a 2020-11, quando a fonte publicava o
        CNPJ do fundo e o grão era outro — é o marcador da era, não uma lacuna.


        As duas séries de inadimplência **não se somam**: em `*_with_risk` o
        cedente responde pelo crédito, em `*_no_risk` o fundo carrega a perda.
        `impaired_ratio` é fração da **carteira** (não do patrimônio) e vem null
        quando o inadimplente declarado excede a carteira — nesse caso
        `impaired_exceeds_portfolio` é true e os componentes brutos continuam na
        resposta. `overdue_*` (vencido e não pago) e `impaired_*` (reconhecido
        como inadimplente) também são rubricas distintas e nunca substituem uma
        à outra.


        Um FIDC feeder pode declarar recebíveis diretos e inadimplência iguais a
        zero porque carrega cotas de outros FIDCs. Isso significa risco em outra
        camada, não ausência de risco; `/funds/:cnpj/holdings` e
        `/funds/:cnpj/invested-funds` cobrem fundos ordinários e podem vir
        vazios nesse caso.


        Sem `date`, serve a competência mais recente com dado.


        **Os filtros daqui são de CADASTRO e tamanho** (`cnpj`, `q`,
        `minNetWorth`). Para cortar por uma MEDIDA — grande E inadimplente,
        inadimplência acima de um limiar, os N maiores de um recorte — use
        `rankObjects` / `aggregateObjects` com `where`: é uma chamada, contra
        paginar as 4.208 classes e calcular do lado de fora. Atenção à escala ao
        compor: `impaired_ratio` é FRAÇÃO (0,05 = 5%) e `top_originator_pct` é
        PERCENTUAL (35,2 = 35,2%).
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/history:
    get:
      responses:
        "200":
          description: Série mensal da classe
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcMonthly"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        enum:
                          - asc
                          - desc
                        description: Sentido no tempo, quando a resposta É uma série. `listFidcHistory`
                          devolve `desc` (a competência mais recente primeiro);
                          `listFidcs` é um corte transversal de UMA competência
                          e não traz o campo.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcHistory
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Série mensal de uma classe de FIDC
      description: >-
        Patrimônio, carteira e inadimplência mês a mês da classe. `overdue_*` é
        vencido e não pago; `impaired_*` é reconhecido como inadimplente. As
        rubricas não são sinônimas, e razão null com
        `impaired_exceeds_portfolio=true` preserva o valor bruto para
        reconciliação em vez de convertê-lo em zero.


        Leia junto de `entity_kind`: a série cruza a mudança de grão de 2020-11
        (fundo → classe), e um fundo multiclasse vira várias linhas a partir
        dali. Um zero em toda a resposta significa zero apenas no intervalo
        devolvido; só diga 'desde o início' se esse intervalo comprovadamente
        começar na constituição da classe.


        `top_originator_pct` é a fatia do maior cedente e **não passa de 100%
        por definição** — mas 7.045 competências vêm acima disso na fonte, com
        topo em 1,55 × 10¹⁰ por cento. O valor sai as-filed e
        `top_originator_pct_implausible` avisa; não use a fatia sem olhar a
        marca.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/series:
    get:
      responses:
        "200":
          description: Séries de cota da classe
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcSeries"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      cnpj:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - cnpj
                      - reference_date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcSeries
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (fim de mês). Default: a mais recente."
      summary: Séries de cota da classe (sênior, mezanino, subordinada)
      description: >-
        Sênior, mezanino e subordinada **não são o mesmo instrumento**: a sênior
        recebe primeiro e a subordinada absorve a perda antes dela. Por isso a
        série é parte do grão e a cota nunca é consolidada num número só do
        fundo.


        `seniority` é derivado do rótulo livre da fonte e vem null quando o
        texto não afirma prioridade ("Série 1") — o rótulo original fica em
        `series_label` para quem quiser julgar por conta própria.
        `return_month_pct` é as-filed, em percentual do mês, não acumulado.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/terms:
    get:
      responses:
        "200":
          description: Termos do regulamento
          content:
            application/json:
              schema:
                type: object
                properties:
                  cnpj:
                    type: string
                  protocol:
                    type: string
                    description: Protocolo do documento no FNET/CVM.
                  schema_id:
                    type: string
                    description: "Extrator que produziu os fatos, ex.: 'regulamento_fidc/v3'."
                  extractor_model:
                    type: string
                  extracted_at:
                    anyOf:
                      - type: string
                      - type: string
                  custo:
                    type: object
                    properties:
                      total_pct_aa:
                        type:
                          - number
                          - "null"
                        description: Custo anual somando SÓ o que incide sobre o patrimônio ao ano. null
                          = nada comparável declarado; nunca zero, que afirmaria
                          gratuidade.
                      parcelas:
                        type: array
                        items:
                          type: object
                          properties:
                            tipo:
                              type: string
                            pct_aa:
                              type: number
                            convertida_de_fixo:
                              type: boolean
                              description: True quando saiu de um valor fixo mensal convertido pelo
                                patrimônio.
                            pagina:
                              type:
                                - number
                                - "null"
                          required:
                            - tipo
                            - pct_aa
                            - convertida_de_fixo
                            - pagina
                      fora_da_soma:
                        type: array
                        items:
                          type: object
                          properties:
                            tipo:
                              type: string
                            base:
                              type: string
                            motivo:
                              type: string
                          required:
                            - tipo
                            - base
                            - motivo
                        description: Taxas declaradas que NÃO entram no custo anual, com o motivo.
                      parcial:
                        type: boolean
                        description: "True quando há taxa fora da soma: o total é um PISO."
                      suspeito:
                        type: boolean
                        description: True quando alguma parcela isolada passa de 3% a.a., o que
                          raramente é percentual anual sobre o PL.
                    required:
                      - total_pct_aa
                      - parcelas
                      - fora_da_soma
                      - parcial
                      - suspeito
                  termos:
                    type: object
                    additionalProperties: {}
                    description: "Objeto completo do extrator: gatilhos, carteira, liquidez,
                      prestadores."
                required:
                  - cnpj
                  - protocol
                  - schema_id
                  - extractor_model
                  - extracted_at
                  - custo
                  - termos
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getFidcRegulationTerms
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
      summary: Termos do regulamento da classe, extraídos com página e trecho
      description: >-
        O que o REGULAMENTO da classe estipula: índices de subordinação, eventos
        de avaliação e liquidação, limites de concentração, critérios de
        elegibilidade, taxas, cascata de alocação e prestadores. Cada fato
        carrega `pagina` e `trecho` literal — sem procedência o fato não é
        publicável.


        `custo.total_pct_aa` soma **apenas** o que incide sobre o patrimônio ao
        ano. Taxa sobre valor cedido ou recuperado NÃO entra: 3% sobre cada
        cessão não é 3% ao ano do patrimônio, e somá-las produz custo que não
        existe. Valor fixo mensal vira percentual pelo patrimônio quando ele é
        conhecido — R$ 9.000/mês são 2,2% a.a. num fundo de R$ 5 milhões e 0,02%
        num de R$ 500 milhões.


        `custo.parcial=true` significa que existe taxa declarada fora da soma: o
        total é um PISO, não o custo completo. `custo.suspeito=true` marca
        parcela isolada acima de 3% a.a., que quase nunca é percentual anual
        sobre o patrimônio — o número é servido assim mesmo, e a revisão é de
        quem lê o regulamento.


        `total_pct_aa: null` significa que nada comparável foi declarado. Nunca
        zero: zero afirmaria que o fundo é gratuito.


        COBERTURA HOJE: **nenhum** FIDC responde. Medido em 16/08/2026 contra
        produção — a varredura de documentos alcançou 15 classes de 4.207, com
        69 documentos ao todo, e a extração do regulamento é passo MANUAL, sem
        agendamento. Testados os 15 que têm documento e os 40 maiores por
        patrimônio: 404 em todos. A rota está correta e espera dado que ainda
        não é produzido em escala; enquanto isso, 404 aqui significa "não
        extraímos", nunca "o regulamento não estipula".


        Índice de subordinação acima de 100% é razão INVERTIDA (patrimônio sobre
        a subclasse) e vem com `invertida: true` — lido como piso direto, 333%
        seria impossível.


        Cobertura cresce com a extração: ausência aqui significa que ainda não
        lemos o regulamento desta classe, não que ela não tenha regulamento.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/delinquency:
    get:
      responses:
        "200":
          description: Aging da carteira
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcDelinquency"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      cnpj:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - cnpj
                      - reference_date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcDelinquency
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (fim de mês). Default: a mais recente."
      summary: Aging dos direitos creditórios por faixa de prazo e atraso
      description: >-
        Quanto vence em 30/60/90/…/1080+ dias e quanto já está vencido e não
        pago em cada faixa. Um fundo com atraso concentrado em 30 dias é um
        caso; o mesmo patrimônio com massa em 360+ é outro.


        `risk_retained` separa direitos com risco de recompra (o cedente
        responde) dos sem risco (o fundo carrega): **somar as duas visões conta
        o mesmo fundo duas vezes**. Ordene por `bucket_order`, nunca por
        `bucket` como texto — '1080' viria antes de '30'.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/scr:
    get:
      responses:
        "200":
          description: Mix de risco SCR da carteira
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcScrLevel"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      cnpj:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - cnpj
                      - reference_date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcScr
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (fim de mês). Default: a mais recente."
      summary: Mix de risco SCR da carteira (Res. CMN 2.682) — não é rating de agência
      description: >-
        Distribuição da carteira pelos níveis **SCR** da Resolução CMN 2.682 (AA
        a H), atribuídos pela própria instituição ao devedor e à operação.


        **Isto não é rating de agência classificadora e não é comparável com
        `brAAA`.** As duas escalas usam letras, e confundi-las é o erro que este
        endpoint mais convida a cometer — o rating de agência vive em
        `/credit/ratings`.


        `declares_scr: false` significa que a classe não declarou nível nenhum,
        o que é diferente de declarar zero numa faixa: menos da metade das
        classes preenche, e mostrar 0% para quem não declarou seria inventar
        dado. A série começa em 2023 — a tabela só passa a existir no informe
        depois da reforma de classes.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/quotes:
    get:
      responses:
        "200":
          description: Página de negócios da cota
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcSecondaryTrade"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                      cnpj:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - order
                      - cnpj
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcQuotes
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Secundário da cota do FIDC (negócios de balcão da classe)
      description: >-
        Por quanto a cota desta classe NEGOCIOU, pregão a pregão. É a outra
        metade do FIDC: o informe mensal diz o que o fundo tem e nunca o preço;
        o balcão diz o preço e nunca de que fundo é.


        Duas coisas que valem saber antes de concluir qualquer coisa da ausência
        de linhas. A primeira é que a B3 publica cota de FIDC sob a família
        **`CFF`** (cota de fundo fechado), junto com FII, FIP e FIF — procurar
        `family=FIDC` em `/credit/otc` devolve vazio, o que parece ausência de
        mercado e é só rótulo diferente. A segunda é que a esmagadora maioria
        das classes simplesmente não tem giro: em 2026, 379 emissores de FIDC
        apareceram no balcão, contra mais de 4.300 classes no informe. Silêncio
        aqui é característica do produto, não falha de cobertura.


        `match_level` viaja em toda linha porque o elo entre as duas fontes é o
        **nome**: o informe de FIDC não publica ISIN e o balcão não publica
        CNPJ. `exact_name` é denominação social normalizada idêntica — 95,3% dos
        emissores de FIDC do balcão resolvem assim, sem nenhum caso ambíguo.
        Quem preferir descartar a ponte fica com o preço, que é as-filed e
        correto de qualquer forma.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/sectors:
    get:
      responses:
        "200":
          description: Carteira por setor do lastro
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcSector"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      cnpj:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                      portfolio_total:
                        type:
                          - number
                          - "null"
                      classified_total:
                        type:
                          - number
                          - "null"
                      unclassified_total:
                        type:
                          - number
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - cnpj
                      - reference_date
                      - portfolio_total
                      - classified_total
                      - unclassified_total
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcSectors
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (fim de mês). Default: a mais recente."
      summary: Carteira do FIDC por setor do lastro
      description: >-
        Do que a carteira de direitos creditórios é FEITA. É o corte que separa
        fundos incomparáveis com o mesmo patrimônio: consignado, precatório e
        factoring corporativo têm perfis de perda que não se parecem, e sem ele
        o universo de FIDC é uma lista de patrimônios sem natureza.


        **Duas camadas convivem na resposta e NÃO se somam.** `is_subcategory:
        false` são as 11 categorias de topo e já cobrem a carteira inteira; as
        demais reabrem algumas delas em detalhe (consignado dentro de
        Financeiro, precatório dentro de Setor público), com `parent_code`
        apontando a mãe. Somar as duas juntas dobra a carteira — quem quer o mix
        filtra o topo.


        Setor sem exposição não vira linha: a fonte é formulário de
        preenchimento completo e a esmagadora maioria das células vem zerada.
        Ausência de linha significa sem exposição, nunca dado faltando.


        `unclassified_total` no `meta` é a parte da carteira que a classe não
        abriu por setor. Sem ele, um mix parcial passaria por completo, e a
        classe que não declara nada devolveria lista vazia — indistinguível de
        fundo sem carteira. `pct_of_portfolio` é fração da CARTEIRA, não do
        patrimônio: um FIDC alavancado tem carteira maior que o PL e usar PL
        faria a mesma exposição parecer menor.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/originators:
    get:
      responses:
        "200":
          description: Cedentes declarados na competência
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcOriginator"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      cnpj:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - cnpj
                      - reference_date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcOriginators
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (fim de mês). Default: a mais recente."
      summary: Cedentes declarados da classe — quem vende o recebível para o fundo
      description: >-
        Quem ORIGINOU os direitos creditórios que o fundo comprou. É a ponte
        entre o FIDC e o risco de verdade: o fundo é registrado, publica informe
        e tem auditor, mas quem vende os recebíveis para ele costuma ser empresa
        fechada — sem ação, sem balanço público e sem rating. O informe obriga a
        declarar o documento dos maiores cedentes, e é o único lugar em que
        esses CNPJs aparecem de graça.


        **As duas listas não se somam.** `origin_block` separa os cedentes com
        risco de recompra (`with_risk`, o cedente responde pelo crédito) dos sem
        risco (`no_risk`, o fundo carrega a perda), e **o mesmo cedente aparece
        nos dois blocos** — somá-los o conta duas vezes, a mesma armadilha das
        duas séries de inadimplência. O terceiro bloco, `legacy`, é a lista
        ÚNICA que a fonte publicava até 2019-10, antes de separar risco de
        recompra: `risk_retained` vem null nele porque ele não é nenhum dos
        dois, e atribuí-lo a um lado inventaria uma distinção que a fonte não
        fazia.


        `document_kind` é o campo a ler antes de qualquer conclusão. `invalido`
        é a MAIORIA das declarações e não é lixo: são os placeholders de "não
        divulgado" da própria fonte. Eles ficam na resposta porque é deles que
        sai `undisclosed_share_pct` — **quanto da carteira vem de cedente que a
        classe não identificou**, que é a leitura de concentração que não existe
        em nenhuma outra superfície. `cpf` marca cedente pessoa física e o
        documento **não viaja**: as fontes federais de enriquecimento publicam
        CPF mascarado, então não há o que cruzar, e carregar a PII adiante só
        criaria exposição.


        `share_pct` é as-filed, em percentual (35,2 = 35,2%). Uma parcela em
        3,8% das declarações cai fora de [0, 100] — quem preencheu reais no
        campo de percentual — e essas passam com `share_pct_implausible`
        marcado, em vez de corrigidas. `declared_share_total_implausible` marca
        o caso análogo no total do bloco.


        Para a pergunta inversa — em quais classes um cedente aparece — use
        `/credit/originators/{cnpj}`.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/flows:
    get:
      responses:
        "200":
          description: Série mensal de fluxo e liquidez
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcFlow"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcFlows
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Fluxo de cotas e cobertura de resgate, mês a mês
      description: >-
        Está entrando ou saindo dinheiro, e o fundo consegue honrar o que já foi
        pedido. São duas tabelas do informe que só valem juntas: a movimentação
        de cotas dá o resgate **solicitado e ainda não pago**, e a liquidez do
        ativo dá quanto da carteira vira caixa em até 30 dias.


        `redemption_coverage_30d` é a razão entre as duas. **Abaixo de 1 é
        pedido maior que capacidade** — um problema com data marcada, não uma
        métrica de conforto. Vem null quando não há resgate solicitado, e
        ausência de pedido é ausência de risco: ler isso como cobertura zero
        inverteria o sinal.


        **Amortização não é resgate**, e por isso não entra em `net_flow`.
        Amortização é devolução programada de principal prevista em regulamento
        — sinal de fundo funcionando —, enquanto resgate é decisão do cotista.
        Somadas, um fundo amortizando rigorosamente no prazo pareceria em fuga.
        Pelo mesmo cuidado, `net_flow` usa o resgate PAGO e não o solicitado: os
        dois se sobrepõem no tempo (o mesmo dinheiro é pedido num mês e pago no
        outro) e empilhá-los conta a saída duas vezes.


        As faixas de liquidez são **cumulativas**: `liquid_30d` é "até 30 dias"
        e já engloba `liquid_now`. Somar as faixas conta o mesmo ativo mais de
        uma vez.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/portfolio-ops:
    get:
      responses:
        "200":
          description: Série mensal de movimentação da carteira
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcPortfolioOps"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcPortfolioOps
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: "Movimentação da carteira: ticket médio, recompra e parte relacionada"
      description: >-
        Como a carteira de direitos creditórios se mexeu no mês — o que o fundo
        comprou, o que o cedente comprou de volta e o que foi vendido para quem.


        `avg_ticket` é a **pulverização**, que era premissa do produto ("riscos
        não correlacionados") e até aqui indemonstrável: 100 recebíveis de R$ 1
        milhão e 1 milhão de recebíveis de R$ 100 são a mesma carteira em valor
        e riscos opostos.


        `acquired_amount` e `acquired_count` somam o eixo de **transferência de
        risco** (com e sem aquisição substancial dos riscos). A fonte publica um
        segundo corte das mesmas aquisições, por status de pagamento, que abre
        apenas a parcela com risco — os dois eixos **não se somam**, mesma regra
        das duas séries de inadimplência. `acquired_impaired` vem desse segundo
        corte e por construção só enxerga inadimplente comprado dentro da
        parcela com risco.


        `repurchase_to_portfolio` é o cedente comprando de volta o recebível que
        já cedeu, tipicamente o que vencia e não ia pagar. É a forma clássica de
        uma carteira parecer saudável enquanto o excesso de spread é queimado
        para maquiar atraso — e como a recompra some da inadimplência declarada,
        ela não aparece em nenhuma outra métrica. O denominador é a
        **carteira**, não o patrimônio: fundo alavancado tem carteira maior que
        o PL, e usar PL faria a mesma recompra parecer menor.


        `related_party_sale_pct` é o fundo vendendo recebível de volta para o
        cedente ou para prestadores de serviço. Em volume relevante muda a
        natureza do veículo: deixa de ser comprador de risco e vira financiador
        do cedente. Venda para terceiro fica fora da conta de propósito — isso é
        mercado, não conflito. É raro, e é a raridade que faz a ocorrência valer
        alarme.


        **Comprar direito já inadimplente não é necessariamente defeito:** há
        fundo cuja tese é comprar carteira vencida com deságio. Vira sinal num
        fundo que se apresenta como performado, e essa distinção é de quem
        consome — por isso o campo se chama `acquired_impaired` e descreve o
        fato em vez de julgá-lo.


        Garantia tem cobertura baixíssima na fonte (180 classes em 13,5 anos):
        `collateral_amount` null é a fonte calada, **nunca zero de garantia**.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/investors:
    get:
      responses:
        "200":
          description: Cotistas por tipo e senioridade
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcInvestor"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      cnpj:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - cnpj
                      - reference_date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcInvestors
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (fim de mês). Default: a mais recente."
      summary: Quem é o passivo da classe, por tipo de cotista e senioridade
      description: >-
        De quem é o dinheiro que está dentro do fundo. Quem corre primeiro numa
        crise não é o mesmo em todo FIDC: uma sênior de fundo de pensão e RPPS
        tem passivo estável por mandato, uma de pessoa física reage a manchete.
        O informe já dava o total de cotistas; esta rota diz de quem são.


        `pct_of_seniority` usa como denominador o total da **própria
        senioridade**, não o da classe. O motivo é da fonte: mezanino não tem
        coluna nesta tabela, então um percentual sobre o total da classe não
        fecharia em 100 e ninguém saberia por quê.


        `is_institutional` é **classificação nossa, não da fonte**, e por isso
        viaja ao lado de `investor_type` em vez de substituí-lo. Agrupa quem tem
        mandato e prazo (previdência aberta e fechada, RPPS, seguradora,
        capitalização, banco). Fundo e corretora ficam de fora de propósito: são
        veículos de passagem e por trás deles pode haver varejo — contá-los
        inventaria uma estabilidade que não se sabe existir.


        A série **começa em 2019-11**, a mesma virada de formulário que separou
        os blocos de cedente. Antes disso o informe não abria o cotista por
        tipo, e a ausência marca a era, não é lacuna de coleta.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/performance:
    get:
      responses:
        "200":
          description: Prometido × entregue por série
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcPerformance"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      cnpj:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - cnpj
                      - reference_date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcPerformance
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (fim de mês). Default: a mais recente."
      summary: Prometido × entregue por série de cota
      description: >-
        A série entregou o que prometeu. É o desfecho na forma em que o
        investidor faz a pergunta, sem proxy — as demais leituras de
        deterioração usam "a inadimplência passou de tanto" como substituto de
        "deu errado", o que é razoável e ainda assim é um substituto.


        **Sênior e subordinada não se comparam.** A sênior promete taxa e a
        subordinada absorve o que sobrar: `performance_gap_pct` negativo na
        subordinada é o desenho funcionando, e na sênior é promessa quebrada.
        `seniority` usa o mesmo vocabulário e a mesma derivação de
        `/credit/fidc/{cnpj}/series`, e vem null quando o rótulo livre da fonte
        não afirma prioridade.


        `declares_target` **separa "não prometeu" de "prometeu zero"**, e sem
        ele o número engana: o campo de retorno esperado vem sempre preenchido
        na fonte e metade das séries traz zero, porque subordinada normalmente
        não promete retorno. Sem a marca, o gap dessas séries leria "superou a
        meta" onde não havia meta nenhuma.


        `shortfall_streak` conta **meses consecutivos** entregando abaixo do
        prometido. Um mês abaixo é ruído de marcação; a sequência é o que a
        série mensal de 13,5 anos permite afirmar. Desempenho negativo é dado e
        não é filtrado.


        `performance_implausible` marca o mês em que a fonte publica percentual
        fora de [-100, +100] — e ela publica: 1.396 linhas trazem realizado
        acima de 1.000%, com topo em 3,0 × 10¹⁶ por cento. O valor sai
        **as-filed**, nunca corrigido nem escondido, e o mês marcado fica FORA
        de `shortfall_streak`: um gap positivo gigante zeraria uma sequência
        real de meses abaixo da meta.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/returns:
    get:
      responses:
        "200":
          description: Série de rentabilidade contra o CDI
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcReturn"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                      cnpj:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - order
                      - cnpj
                      - reference_date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcReturns
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Competência inicial (inclusive).
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Competência final (inclusive).
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            description: "Máximo de linhas. Default: 240."
      summary: Rentabilidade da série com o CDI ao lado
      description: >-
        Quanto a série rendeu, **contra o CDI do mesmo período**.
        `/credit/fidc/{cnpj}/series` já dava `return_month_pct` as-filed, e isso
        responde "quanto rendeu no mês" sem responder nenhuma das perguntas que
        quem compra sênior faz de verdade: quanto no ano, quanto desde que
        entrei e quanto foi o CDI enquanto isso. Uma sênior que rende 1,45% no
        mês é ótima ou péssima conforme o CDI tenha sido 1,12% ou 1,60%.


        Diferente dos outros blocos do informe, esta rota devolve **série
        histórica**, não a foto de uma competência — a pergunta só existe ao
        longo do tempo.


        **Duas convenções de spread convivem no mercado e significam coisas
        diferentes.** `pct_of_cdi` é multiplicativo ("110% do CDI");
        `spread_annual_pp` é ADITIVO ("CDI + 3,5% a.a."), que é a forma em que o
        regulamento promete e portanto a única comparável com a meta. Publicar
        uma chamando-a da outra é erro que só aparece quando o CDI muda de
        patamar.


        **`is_reported` não é `has_position`.** A fonte continua publicando a
        série amortizada com cota 0 e rentabilidade 0; quem filtrar só pela
        primeira mostra série fantasma rendendo 0% e puxa a média da classe para
        baixo. A série ENCERRADA fica na resposta de propósito — omiti-la
        produziria viés de sobrevivente no track record da gestora.


        O acumulado encadeia só mês OBSERVADO: buraco na série não vira zero.
        Compare `months_in_window` com os meses do período antes de concluir que
        um acumulado está baixo. Mês com `return_implausible` (a fonte publica
        coisas como -1.165,85%) fica fora do encadeamento, mas continua as-filed
        na linha.
      x-capability: credit-market-data
  /v1/credit/originators/{cnpj}/profile:
    get:
      responses:
        "200":
          description: Perfil do cedente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OriginatorProfile"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getOriginatorProfile
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
      summary: Quem é o CNPJ que originou o recebível
      description: >-
        O informe mensal publica o **documento** do cedente e mais nada — sem
        razão social, sem situação cadastral, sem porte. É o único lugar em que
        esses CNPJs aparecem de graça, e é também onde a trilha pública
        terminava. Esta rota é a continuação dela: identidade, atividade,
        localização e situação cadastral pelo cadastro federal.


        `situacao_adversa` é **sinal, não sentença**: inapta costuma ser quem
        parou de entregar declaração e baixada pode ser reorganização societária
        normal. Serve como pergunta a fazer, não como veredito sobre a empresa.


        `capital_social` é **declarado e não auditado** — a empresa escreve,
        ninguém confere. Vale como ordem de grandeza e como sinal de mudança
        entre competências, nunca como patrimônio.


        `encontrado_no_cadastro` existe porque "não sabemos" e "achamos" são
        conclusões opostas.


        **Cedente pessoa física não é consultável**: a fonte publica CPF
        mascarado, então não existe cruzamento possível. O universo aqui é o dos
        cedentes declarados no informe — CNPJ fora dele responde 404.
      x-capability: credit-market-data
  /v1/credit/fidc/{cnpj}/pricing:
    get:
      responses:
        "200":
          description: Taxas de compra e venda, com o comparável
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcPricing"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      cnpj:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - cnpj
                      - reference_date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcPricing
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (fim de mês). Default: a mais recente."
      summary: Por quanto o fundo compra o risco, com a mediana dos pares ao lado
      description: >-
        O preço de ENTRADA — a que taxa o fundo comprou os direitos creditórios.
        É a peça que faltava para responder se um FIDC está caro ou barato: o
        informe já dizia o que o fundo tem e quanto disso não paga, e nunca
        dizia por quanto comprou. Sem isso não existe excesso de spread, o
        colchão que separa um fundo capaz de absorver perda de um que repassa
        direto para a cota.


        **As duas taxas não se somam.** `desconto_aquisicao` é o deságio pago na
        compra do recebível e `juros` é a remuneração embutida no papel;
        empilhá-las num "custo" único inventaria um número que não existe em
        lugar nenhum.


        `peer_median_buy_avg` viaja em toda linha porque **taxa isolada não
        informa nada**: a pergunta é "estou comprando a tanto, o setor está em
        quanto". É a mediana da mesma classe de ativo na mesma competência,
        calculada só sobre taxa plausível — incluir as marcadas puxaria a régua
        para um número que ninguém pagou. `buy_vs_peer_pp` positivo é comprar
        mais caro que os pares, o que pode ser risco maior **ou** negociação
        pior: a fonte não distingue os dois, e dizer qual seria inventar.


        `buy_spread` é a dispersão dentro da própria carteira. Um fundo que
        compra entre 10% e 300% não tem uma política de crédito, tem várias — e
        a média sozinha esconde isso.


        `rate_implausible` marca acima de 1.000 (valor em reais no campo de
        taxa). A faixa de 100 a 1.000 fica **sem marca de propósito**: desconto
        de factoring passa de 100% ao ano sem ser erro.


        Cerca de 27% das classes declaram alguma taxa numa competência típica.
        Resposta vazia é o normal da fonte, não falha de cobertura.
      x-capability: credit-market-data
  /v1/credit/originators/{cnpj}:
    get:
      responses:
        "200":
          description: Classes que declaram este cedente
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FidcOriginatorExposure"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      originator_cnpj:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                      class_count:
                        type:
                          - number
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - originator_cnpj
                      - reference_date
                      - class_count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFidcOriginatorExposure
      tags:
        - Credit
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
            pattern: ^\d{14}$
            description: CNPJ completo do emissor, 14 dígitos sem pontuação.
          required: true
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Competência exata (fim de mês). Default: a mais recente."
      summary: Em quais FIDCs este CNPJ aparece como cedente
      description: >-
        A pergunta inversa de `/credit/fidc/{cnpj}/originators`, e a que muda
        uma decisão: dado um CNPJ, **em quantas classes de FIDC ele originou os
        recebíveis, e com que peso em cada uma**.


        É o que revela concentração disfarçada de diversificação. Uma carteira
        com quatro FIDCs de gestores diferentes, lastros diferentes e prospectos
        diferentes pode depender do mesmo originador em três deles — e nenhum
        dos quatro relatórios mensais que o investidor recebe diz isso, porque
        cada um enxerga só o próprio fundo.


        `net_worth`, `portfolio` e `impaired_ratio` da classe viajam na resposta
        para que a exposição seja dimensionável sem uma segunda chamada: 5% de
        um fundo de R$ 3 bilhões e 60% de um de R$ 20 milhões não são o mesmo
        risco. Vêm null quando a classe declarou cedente e não tem linha de
        patrimônio na competência — a linha de exposição permanece, porque
        omiti-la esconderia justamente o que a rota existe para mostrar.


        `class_count` no `meta` conta **classes distintas**, não linhas: o mesmo
        cedente aparece nos dois blocos de risco da mesma classe e em mais de um
        slot da declaração, e contar linhas inflaria a exposição.


        Só CNPJ é consultável. Cedente pessoa física existe na fonte e o CPF não
        é indexado aqui — as fontes federais de enriquecimento publicam CPF
        mascarado, então não há cruzamento possível, e manter a PII consultável
        só criaria exposição sem uso.
      x-capability: credit-market-data
  /v1/credit/securities/{isin}/fund-holders:
    get:
      responses:
        "200":
          description: Fundos com posição resolvida neste papel
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundCreditHolding"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      summary:
                        oneOf:
                          - $ref: "#/components/schemas/CreditFundHoldersSummary"
                          - type: "null"
                    required:
                      - next_cursor
                      - count
                      - summary
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCreditFundHolders
      tags:
        - Credit
      parameters:
        - in: path
          name: isin
          schema:
            type: string
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Competência do CDA (AAAA-MM-DD); default = a mais recente com
              posição neste papel.
      summary: Quais fundos detêm um papel de crédito privado, e quanto
      description: >-
        A visão reversa da carteira: dado o ISIN de um CRI, CRA ou debênture,
        quais classes de fundo carregam o papel e com que valor a mercado, na
        competência do CDA.


        **O número é PISO, nunca total, e por dois motivos independentes.**
        Primeiro, o CDA não publica ISIN nos blocos de crédito privado: a
        posição só aparece aqui quando a identidade do papel foi resolvida a
        partir do que a fonte descreve (`match_level` `cetip` ou
        `issuer_maturity`). A mesma emissão pode estar em outros fundos que a
        descreveram de forma pobre demais para resolver, e essas posições
        existem em `/v1/funds/{cnpj}/credit-holdings` sem aparecer nesta lista.
        Segundo, o CDA cobre **fundo**: FII reporta por outro canal, e banco,
        seguradora, tesouraria e pessoa física não reportam carteira.
        `summary.resolved_only` é sempre `true` para que essa leitura não
        dependa de ninguém ter lido esta descrição.


        Para a exposição que NÃO depende de resolver o papel, consulte por
        emissor: a posição em `match_level: issuer` sabe qual casa emitiu mesmo
        sem saber qual série.


        Papel sem nenhuma posição resolvida responde página vazia com `summary`
        nulo — o que significa 'não sabemos de nenhuma', não 'nenhum fundo
        detém'.
      x-capability: credit-market-data
  /v1/structured/coe:
    get:
      responses:
        "200":
          description: Página de COEs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CoeInstrument"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      secondary_window:
                        $ref: "#/components/schemas/CoeSecondaryWindow"
                    required:
                      - next_cursor
                      - count
                      - secondary_window
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCoes
      tags:
        - Estruturados
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: issuer
          schema:
            type: string
            description: Busca pelo nome do emissor (parcial).
        - in: query
          name: maturityFrom
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Vencimento a partir de (YYYY-MM-DD).
        - in: query
          name: maturityTo
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Vencimento até (YYYY-MM-DD).
        - in: query
          name: minIssueSize
          schema:
            type: number
            description: Tamanho mínimo da emissão, em BRL.
        - in: query
          name: tradedOnly
          schema:
            type: boolean
            description: Somente os que tiveram negócio em secundário dentro da janela de
              pregões coberta (`meta.secondary_window`). Não é "já girou alguma
              vez".
      summary: "Catálogo de COE registrado no balcão: emissor, tamanho, prazo e
        secundário observado"
      description: >-
        COE (Certificado de Operações Estruturadas) do registro público de
        balcão da B3, um código por linha, com o cadastro as-filed mais recente
        e o agregado de negociação secundária **sobre os pregões de balcão que
        observamos** — a janela efetiva vem em `meta.secondary_window` (`from`,
        `to`, `sessions`).


        **Esta superfície é separada de `/v1/credit` de propósito.** O corte de
        `/credit` é quem deve — debênture, CDB, LCI/LCA, CRI/CRA são risco de
        emissor com retorno contratado. O COE não é isso: é um payoff de
        derivativo embrulhado no risco de crédito do banco emissor, e o
        investidor carrega os dois. Tratá-lo como renda fixa é o erro que a
        opacidade do produto explora.


        **O que este endpoint NÃO responde:** se um COE é bom, quanto ele rende,
        se tem capital protegido, qual o ativo subjacente ou quais os cenários
        de retorno. Isso vive no DIE (Documento de Informações Essenciais), que
        não é dado estruturado público.
        `indexer`/`indexer_pct`/`additional_rate_pct` são campos do REGISTRO e
        não descrevem o retorno do investidor — lê-los como taxa é o erro
        clássico aqui.


        **O que ele responde:** quanto cada emissor colocou no mercado, com que
        prazo, e o que girou em secundário nos pregões cobertos.


        **Como ler o secundário null.** Os campos
        `trade_days`/`trade_count_total`/`volume_brl_total`/`last_trade_date`
        vêm null para a maioria esmagadora dos papéis, e o único significado
        exato disso é: nenhum negócio registrado nos pregões de
        `meta.secondary_window`. **Leia sempre contra `sessions`** — enquanto a
        janela é curta, o null é dominado por ela e não mede a liquidez do
        papel. Que o COE seja feito para ser carregado até o vencimento, sem
        mercado de saída organizado, é propriedade do DESENHO do produto e está
        no DIE, não uma conclusão que este agregado sustente hoje.
        `tradedOnly=true` isola quem teve negócio na janela; não é um filtro de
        "já girou alguma vez na vida".


        Negócios instrumento a instrumento, por pregão, ficam em
        `/v1/credit/otc` com `family=COE`: lá é o fluxo, aqui é o catálogo.
      x-capability: market-data
  /v1/structured/coe/{code}:
    get:
      responses:
        "200":
          description: COE
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoeInstrument"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getCoe
      tags:
        - Estruturados
      parameters:
        - in: path
          name: code
          schema:
            type: string
            minLength: 3
            description: Código do instrumento no balcão B3.
          required: true
      summary: Detalhe de um COE
      description: >-
        A chave é o código do instrumento no balcão B3 — não o ISIN nem o nome
        comercial da campanha de venda, que não existem no registro.


        Os agregados de secundário cobrem só os pregões de balcão observados, os
        mesmos de `meta.secondary_window` no listing (`listCoes`) — este detalhe
        não repete a janela, então secundário null aqui significa "sem negócio
        na janela", não "nunca girou".
      x-capability: market-data
  /v1/indices:
    get:
      responses:
        "200":
          description: Lista de índices
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/IndexMeta"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listIndices
      tags:
        - Indices
      parameters: []
      summary: Índices disponíveis (IBOV, IFIX, ...)
      description: Catálogo dos índices da B3 cobertos (IBOV, IFIX, IBXX/IBrX-100,
        SMLL, IDIV, ICON…) com código e nome.
      x-capability: market-data
  /v1/indices/benchmarks:
    get:
      responses:
        "200":
          description: Página de observações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/BenchmarkIndexPoint"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listBenchmarkIndices
      tags:
        - Indices
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 20000
            default: 100
        - in: query
          name: code
          schema:
            type: string
            description: "Código do benchmark, ex.: IMA-B, IRF-M, IMA-S."
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Benchmarks de renda fixa (nível, retorno diário e duration)
      description: Índices de renda fixa de referência do mercado (famílias IMA-B,
        IRF-M, IMA-S…), desde 2002. Sem filtros responde o snapshot do dia mais
        recente com todos os códigos; com `code`, a série histórica dele.
      x-capability: market-data
  /v1/indices/{code}/quotes:
    get:
      responses:
        "200":
          description: Página de níveis
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Observation"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listIndexQuotes
      tags:
        - Indices
      parameters:
        - in: path
          name: code
          schema:
            type: string
            minLength: 1
            description: "Código do índice, ex.: IBOV, IFIX, IBXX, SMLL, IDIV, ICON. Ver
              listIndices."
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 20000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Série de níveis diários de um índice
      x-capability: market-data
  /v1/indices/{code}/intraday:
    get:
      responses:
        "200":
          description: Série intradiária
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/IntradayPoint"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      ticker:
                        type: string
                      session_date:
                        type: string
                      prev_close:
                        type:
                          - number
                          - "null"
                      sessions_behind:
                        type: integer
                        description: "Pregões entre `session_date` e a última sessão do mercado. 0 = a
                          série é do pregão corrente. > 0 = série ANTIGA: sem o
                          parâmetro `session`, esta rota devolve a última sessão
                          que o caminho intradiário conseguiu registrar, e ela
                          pode estar parada — em 07/2026 a B3 desativou o
                          endpoint que a alimentava (Comunicado Externo
                          01/2026-VTEC) e a série congelou. Os pontos seguem
                          fiéis ao pregão que descrevem; este campo diz QUAL
                          pregão é. O fechamento oficial não depende deste
                          caminho e continua fresco."
                    required:
                      - next_cursor
                      - count
                      - ticker
                      - session_date
                      - prev_close
                      - sessions_behind
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getIndexIntraday
      tags:
        - Quotes
      parameters:
        - in: path
          name: code
          schema:
            type: string
            minLength: 1
            description: "Código do índice, ex.: IBOV, IFIX, IBXX, SMLL, IDIV, ICON. Ver
              listIndices."
          required: true
        - in: query
          name: session
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Sessão (AAAA-MM-DD); default = sessão intradiária mais recente.
      summary: Série intradiária do índice (delay de 15 min)
      description: Níveis intradiários do pregão; `session` filtra o pregão (default =
        mais recente).
      x-capability: market-data
  /v1/indices/{code}/composition:
    get:
      responses:
        "200":
          description: Composição teórica
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/IndexComponent"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      code:
                        type: string
                      effective_date:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - code
                      - effective_date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getIndexComposition
      tags:
        - Indices
      parameters:
        - in: path
          name: code
          schema:
            type: string
            minLength: 1
            description: "Código do índice, ex.: IBOV, IFIX, IBXX, SMLL, IDIV, ICON. Ver
              listIndices."
          required: true
      summary: Composição teórica do índice (carteira teórica vigente)
      description: Constituintes em `data`; código e data de vigência em `meta`.
      x-capability: market-data
  /v1/series:
    get:
      responses:
        "200":
          description: Página do catálogo
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/SeriesMeta"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listSeries
      tags:
        - Macro
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: source
          schema:
            type: string
            enum:
              - bcb_sgs
              - fred
              - world_bank
              - benchmark
              - bcb_focus
              - curve
              - credit_spread
        - in: query
          name: search
          schema:
            type: string
      summary: Catálogo de séries macro (Brasil, EUA e mundo)
      description: >-
        Todas as séries servidas por getSeries, com período coberto e
        frequência. Use ANTES de chamar getSeries: é aqui que se descobre o
        `series_id` e até onde o histórico vai.


        Quatro fontes: `bcb_sgs` (Brasil — Selic, CDI, IPCA, câmbio, crédito,
        fiscal, contas externas), `fred` (EUA — curva Treasury de 1 mês a 30
        anos, CPI, PCE, payrolls, VIX, S&P 500, e os benchmarks de commodity
        Brent/minério/soja), `world_bank` (comparação entre economias, ANUAL,
        `series_id` no formato `<indicador>:<ISO2>`) e `benchmark` (CDI e IPCA
        em índice base-100, para comparar carteira contra referência).


        Fora deste catálogo, com porta própria: expectativas Focus em
        getMarketExpectations, títulos públicos em listTesouroBonds e
        getYieldCurve, índices da B3 em listIndexQuotes, e os indicadores macro
        já calculados em getMacroGears e getMacroRegime.
      x-capability: market-data
  /v1/series/{source}/{series_id}:
    get:
      responses:
        "200":
          description: Série com metadados e observações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Observation"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      source:
                        type: string
                      series_id:
                        type: string
                      name:
                        type:
                          - string
                          - "null"
                      label:
                        type:
                          - string
                          - "null"
                      unit:
                        type:
                          - string
                          - "null"
                        description: 'Rótulo de unidade COMO A FONTE PUBLICA, para exibição. Não use
                          para decidir se duas séries são comparáveis: no World
                          Bank ele traz o nome do indicador ("GDP (current
                          US$)") e no SGS convivem "%", "% a.a." e "% a.m.".
                          Para isso existem `dimension`, `scale` e `period`.'
                      frequency:
                        type:
                          - string
                          - "null"
                        description: "Cadência de PUBLICAÇÃO: daily, weekly, monthly, quarterly, annual.
                          Não confunda com `period`."
                      dimension:
                        type:
                          - string
                          - "null"
                        description: O que o número É. `rate` é taxa, `index` é número-índice (o CPI
                          americano vale 322, não 3,2%), `share` é participação,
                          `currency` é dinheiro, `count` é contagem. Comparar
                          séries de dimensões diferentes é aritmética correta
                          sobre coisas incomparáveis.
                      scale:
                        type:
                          - string
                          - "null"
                        description: "Em que escala o valor vem: 5,5 (percent), 0,055 (unit) e 550 (bps)
                          podem ser a mesma taxa. `thousand`/`million`/`billion`
                          multiplicam o valor servido."
                      period:
                        type:
                          - string
                          - "null"
                        description: Que JANELA o número cobre, que NÃO é a cadência de publicação. A
                          série 433 é o IPCA do MÊS (`monthly`, ~0,39) e a 13522
                          é o acumulado em 12 MESES (`ttm_12m`, ~4,72) —
                          responder "a inflação do ano" com a 433 é o erro que
                          este campo existe para impedir. A TR (226) e a
                          poupança (195) são publicadas todo dia e o número é do
                          mês.
                      seasonal_adjustment:
                        type:
                          - string
                          - "null"
                        description: '`nsa` sem ajuste sazonal, `sa` com, `saar` com ajuste E já
                          anualizado pela fonte (o PIB americano é assim: o
                          nível TRIMESTRAL já vem em ritmo anual e não se
                          compara com um trimestre brasileiro sem multiplicar
                          por quatro). Antes disso a informação vivia só no
                          sufixo do nome — `ibc_br` e `ibc_br_dessaz` têm a
                          mesma última data e quem pedisse "o IBC-Br" tinha 50%
                          de chance de pegar a série errada.'
                      expected_range:
                        type:
                          - object
                          - "null"
                        properties:
                          min:
                            type: number
                          max:
                            type: number
                        required:
                          - min
                          - max
                        description: "Faixa plausível declarada, NA MESMA UNIDADE dos valores servidos.
                          Guarda-corpo contra erro de unidade, não previsão:
                          valor fora dela é MARCADO, nunca omitido — ruptura de
                          regime é dado verdadeiro."
                      prior_note:
                        type:
                          - string
                          - "null"
                        description: Ressalva conhecida sobre a série, quando existe.
                      served_by:
                        type:
                          - string
                          - "null"
                        description: "QUAL operação serve as observações desta série — `getSeries`,
                          `getMarketExpectations` ou `getBondCurves`. O catálogo
                          é a lista de quem EXISTE, não a promessa de que
                          `getSeries` responde por todas: desde que a
                          expectativa Focus e o vértice de curva viraram objetos
                          do grafo, eles entraram aqui (é onde a régua declarada
                          mora) mas as observações continuam na tabela de
                          origem. Chamar a rota errada devolveria cabeçalho com
                          zero pontos, e zero pontos com 200 lê-se como 'o dado
                          não existe'."
                      first_date:
                        type:
                          - string
                          - "null"
                      last_date:
                        type:
                          - string
                          - "null"
                      order:
                        type: string
                        const: asc
                        description: "Do mais ANTIGO para o mais recente. Declarado porque `listQuotes`
                          devolve no sentido oposto e nenhuma das duas dizia:
                          quem varre o array calculando `linha[i+1] − linha[i]`
                          para achar a maior QUEDA acha a maior ALTA se supuser
                          o sentido errado — mesmo código, sinal trocado,
                          nenhuma exceção."
                      truncated:
                        type: boolean
                        description: "`latest` cortou observações que a janela pedida contém. `true` =
                          existe série antes de `data[0]`; peça de novo com `to`
                          anterior a essa data para varrer o resto."
                      out_of_prior_count:
                        type:
                          - number
                          - "null"
                        description: Quantas das observações DESTA resposta caem fora de
                          `expected_range`. Zero é o normal; a série inteira
                          fora é a assinatura de rótulo errado. Nulo quando não
                          há faixa declarada ou quando `accumulated=12m`
                          transformou os valores, caso em que a faixa da série
                          crua não descreve mais o que está sendo devolvido.
                      accumulation:
                        type:
                          - string
                          - "null"
                        description: >-
                          COMO as doze observações foram juntadas, quando
                          `accumulated=12m`. `compound` para variação percentual
                          do mês (doze meses de IPCA compõem: 4,44% e não 4,36%)
                          e `sum` para fluxo em moeda ou contagem. Nulo sem a
                          transformação.


                          O método sai de `dimension`+`period` e não de
                          heurística sobre `unit`. Quando os dois não sustentam
                          a conta — série que JÁ é `ttm_12m`, taxa anualizada,
                          nível, ou régua não declarada — a rota responde 400
                          dizendo qual é o impedimento, em vez de escolher entre
                          dois números diferentes em silêncio.


                          Com a transformação, `unit`, `period` e
                          `expected_range` no `meta` descrevem o valor SERVIDO,
                          não a série crua.
                    required:
                      - next_cursor
                      - count
                      - source
                      - series_id
                      - name
                      - label
                      - unit
                      - frequency
                      - dimension
                      - scale
                      - period
                      - seasonal_adjustment
                      - expected_range
                      - prior_note
                      - served_by
                      - first_date
                      - last_date
                      - order
                      - truncated
                      - out_of_prior_count
                      - accumulation
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getSeries
      tags:
        - Macro
      parameters:
        - in: path
          name: source
          schema:
            type: string
            minLength: 1
            description: "Fonte: bcb_sgs | fred | world_bank | benchmark."
          required: true
        - in: path
          name: series_id
          schema:
            type: string
            minLength: 1
            description: "ID da série na fonte. Em bcb_sgs: 432=Selic meta (%a.a.), 4389=CDI
              anualizado, 11=Selic diária, 433=IPCA mês, 13522=IPCA 12m,
              1=USD/BRL, 189=IGP-M. Use listSeries para descobrir outras."
          required: true
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: accumulated
          schema:
            type: string
            enum:
              - none
              - 12m
            default: none
            description: >-
              Transformação: `none` (default) ou `12m`, o acumulado em janela
              móvel de doze meses.


              COMO as doze observações se juntam depende do que o número é, e a
              rota decide pela régua declarada (`dimension` + `period`),
              devolvendo o método em `meta.accumulation`: variação percentual do
              mês COMPÕE (doze meses de IPCA dão 4,44% e não 4,36%), fluxo em
              moeda SOMA.


              Responde 400 onde a conta não tem sentido, em vez de servir um
              número plausível: série que JÁ é acumulada de doze meses (a 13522
              daria ~53%), taxa anualizada, nível/estoque, ou série sem régua
              declarada. Com a transformação, `meta.unit`, `meta.period` e
              `meta.expected_range` passam a descrever o valor servido — não a
              série crua.
        - in: query
          name: latest
          schema:
            type: integer
            minimum: 1
            maximum: 20000
            description: "As N observações MAIS RECENTES. **Use quase sempre.** Sem isto a
              resposta é a série INTEIRA: `bcb_sgs/11` devolve 6.688 linhas e
              ~91 KB numa chamada só, e quem tem teto de passos paga isso em
              orçamento de contexto. Compõe com `from`/`to` — as N últimas
              DENTRO da janela. Com `accumulated=12m` a folga de lookback é
              buscada por dentro, então `latest=12` devolve doze pontos
              acumulados de verdade, não um e onze nulos."
      summary: Observações de uma série (com acumulados opcionais)
      description: >-
        Série temporal (data, valor) de uma fonte macro. `source`+`series_id`
        identificam a série; `accumulated=12m` acumula variações e só vale para
        séries mensais — e nem para todas elas: veja o parâmetro. Ex.:
        bcb_sgs/432 = Selic meta, fred/DGS10 = Treasury de 10 anos,
        world_bank/NY.GDP.MKTP.KD.ZG:CN = crescimento do PIB da China. Descubra
        os ids em listSeries.


        **Ordem CRESCENTE** — do mais antigo para o mais recente, declarado em
        `meta.order`. Atenção ao comparar com `listQuotes`, que devolve no
        sentido OPOSTO: quem calcula `linha[i+1] − linha[i]` para achar a maior
        queda acha a maior alta se supuser o sentido errado, sem erro nenhum.


        **Peça `latest=N`.** Sem ele a resposta é a série inteira desde a
        primeira observação publicada — 6.688 linhas na Selic diária.
      x-capability: market-data
  /v1/fiis/{ticker}:
    get:
      responses:
        "200":
          description: FII
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Fii"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getFii
      tags:
        - FIIs
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
      summary: Perfil do fundo imobiliário
      x-capability: market-data
  /v1/fiis/{ticker}/indicators:
    get:
      responses:
        "200":
          description: Indicadores do FII
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/IndicatorValue"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      ticker:
                        type: string
                      reference_date:
                        type:
                          - string
                          - "null"
                      price_basis:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - ticker
                      - reference_date
                      - price_basis
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getFiiIndicators
      tags:
        - FIIs
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
      summary: Indicadores do FII (snapshot)
      description: "Snapshot dos indicadores do FII: dividend yield (mês e 12m), P/VP,
        vacância, patrimônio líquido, valor patrimonial por cota (vp_cota) e nº
        de cotistas. Para a série temporal use o histórico.
        preco/pvp/dy_12m/ffo_yield usam o PREÇO MAIS RECENTE disponível (ver
        `meta.price_basis`) — pode ser de um pregão posterior ao mês do informe
        em `meta.reference_date`, então divergem do histórico, que usa o
        fechamento do FIM DO MÊS de cada data (ambos corretos; bases de preço
        diferentes)."
      x-capability: market-data
  /v1/fiis/{ticker}/indicators/history:
    get:
      responses:
        "200":
          description: Série do indicador
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Observation"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: asc
                        description: Do mais ANTIGO para o mais recente — o oposto de `listQuotes`, que
                          é a rota vizinha sobre o mesmo papel. Confira antes de
                          calcular variação entre linhas.
                      ticker:
                        type: string
                      name:
                        type: string
                      label:
                        type: string
                      unit:
                        type: string
                        enum:
                          - ratio
                          - percent
                          - brl
                          - brl_per_share
                          - count
                          - days
                      reason:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - order
                      - ticker
                      - name
                      - label
                      - unit
                      - reason
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getFiiIndicatorHistory
      tags:
        - FIIs
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: name
          schema:
            type: string
            minLength: 1
            description: "Indicador do histórico mensal do FII. Da série mensal: vp_cota,
              dividend_yield_mes, patrimonio_liquido, cotistas, dy_12m
              (distribuições ajustadas por split ÷ fechamento do fim do mês,
              mesma base de cotas). Derivado (close raw mensal): pvp."
          required: true
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
          required: false
      summary: Histórico de um indicador do FII (informe mensal)
      description: Série mensal do informe CVM. Em `pvp` e `dy_12m`, o preço usado é o
        FECHAMENTO DO ÚLTIMO PREGÃO DE CADA MÊS (não o preço mais recente) — por
        isso podem divergir do snapshot (/fiis/{ticker}/indicators) na mesma
        reference_date; ver `meta.price_basis` no snapshot. `dy_12m` soma as
        distribuições dos 12 meses até o fim do mês AJUSTADAS por
        desdobramentos/grupamentos, contra o fechamento na mesma base de cotas.
      x-capability: market-data
  /v1/fiis/{ticker}/distributions:
    get:
      responses:
        "200":
          description: Página de distribuições
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FiiDistribution"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFiiDistributions
      tags:
        - FIIs
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Distribuições (rendimentos) do FII
      x-capability: market-data
  /v1/fiis/{ticker}/reports:
    get:
      responses:
        "200":
          description: Página de informes
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FiiMonthlyReport"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFiiReports
      tags:
        - FIIs
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Informes mensais do FII
      x-capability: market-data
  /v1/bdr:
    get:
      responses:
        "200":
          description: Página de BDRs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/BdrProfile"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listBdrs
      tags:
        - BDR
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: search
          schema:
            type: string
      summary: Catálogo de BDRs (recibos de ações estrangeiras)
      x-capability: market-data
  /v1/bdr/{ticker}:
    get:
      responses:
        "200":
          description: BDR
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BdrProfile"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getBdr
      tags:
        - BDR
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
      summary: Perfil de um BDR
      x-capability: market-data
  /v1/bdr/{ticker}/quotes:
    get:
      responses:
        "200":
          description: Página de cotações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/BdrQuote"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listBdrQuotes
      tags:
        - BDR
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Cotações EOD de um BDR
      description: Preços do BDR (events_only). BDR fica `adjust_quality='no_event_source'`.
      x-capability: market-data
  /v1/etfs:
    get:
      responses:
        "200":
          description: Página de ETFs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/EtfProfile"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listEtfs
      tags:
        - ETFs
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: search
          schema:
            type: string
        - in: query
          name: segment
          schema:
            type: string
      summary: Catálogo de ETFs listados na B3
      x-capability: market-data
  /v1/etfs/{ticker}:
    get:
      responses:
        "200":
          description: ETF
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EtfProfile"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getEtf
      tags:
        - ETFs
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
      summary: Perfil de um ETF
      x-capability: market-data
  /v1/funds:
    get:
      responses:
        "200":
          description: Página de fundos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundProfile"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFunds
      tags:
        - Funds
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: search
          schema:
            type: string
        - in: query
          name: classificacao
          schema:
            type: string
            description: "Classificação CVM (match exato), ex.: 'Renda Fixa'."
        - in: query
          name: manager
          schema:
            type: string
            description: "Filtra pelo GESTOR, por substring da razão social publicada pela
              CVM: 'paramis' casa 'PARAMIS BR INVESTIMENTOS LTDA'. Use
              /v1/funds/managers para descobrir a grafia exata."
      summary: Catálogo de fundos de investimento (CVM 175)
      x-capability: market-data
  /v1/funds/managers:
    get:
      responses:
        "200":
          description: Página de gestoras
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundManager"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFundManagers
      tags:
        - Funds
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: q
          schema:
            type: string
            description: Busca por substring da razão social do gestor.
      summary: "Gestoras: quantas classes cada casa gere e quanto administra"
      description: >-
        Agrega as classes de `/funds` pelo nome do gestor. `net_worth_net`
        desconta o que a casa aplica nela mesma (master/feeder), que é o que faz
        o número ser comparável entre gestoras.


        `registry` traz o cadastro da casa na CVM (categoria de registro,
        controle acionário, sede, site, desde quando opera), resolvido pelo CNPJ
        que as classes declaram — 98% das casas com fundo ativo. Use
        `registry.cnpj` como chave estável da gestora; o nome é o que a CVM
        publica e muda com reorganização societária.
      x-capability: market-data
  /v1/funds/{cnpj}:
    get:
      responses:
        "200":
          description: Fundo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FundProfile"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getFund
      tags:
        - Funds
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
          required: true
      summary: Perfil de um fundo pelo CNPJ da classe
      x-capability: market-data
  /v1/funds/{cnpj}/quotes:
    get:
      responses:
        "200":
          description: Página de cotas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundQuote"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFundQuotes
      tags:
        - Funds
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Série diária de cota/PL/fluxo de um fundo
      x-capability: market-data
  /v1/funds/{cnpj}/holdings:
    get:
      responses:
        "200":
          description: Página de posições
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundHolding"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFundHoldings
      tags:
        - Funds
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Competência (AAAA-MM-DD); default = mais recente.
      summary: Carteira (holdings) de um fundo
      description: "Posições do fundo (BLC_4 do CDA): ações, BDR, opções e debêntures
        com valor a mercado. Competência mais recente por default; use `date`
        (AAAA-MM-DD) p/ outra."
      x-capability: market-data
  /v1/funds/{cnpj}/credit-holdings:
    get:
      responses:
        "200":
          description: Página de posições de crédito
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundCreditHolding"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFundCreditHoldings
      tags:
        - Funds
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Competência (AAAA-MM-DD); default = mais recente.
        - in: query
          name: family
          schema:
            type: string
            description: "Família do papel: CRI, CRA, DEB, NP, CCB, CCI, CPR, CDCA, NCA,
              LCA. Match exato."
      summary: Crédito privado na carteira do fundo (CRI, CRA, CCB, nota promissória)
      description: >-
        As posições de crédito privado que a carteira codificada não mostra.
        `/holdings` cobre o BLC_4 do CDA, onde o papel tem código de negociação;
        esta rota cobre os blocos BLC_6 e BLC_8, onde o papel se identifica pelo
        **emissor**: CRI, CRA, CCB, CCI, CPR, CDCA, NCA, LCA, nota promissória e
        as debêntures sem código de negociação — que sozinhas são a maior fatia.


        **`match_level` é a coluna que sustenta a leitura de todas as outras.**
        O CDA não publica ISIN nestes blocos: o CRI vem descrito em texto livre
        e o CRA por (emissor, vencimento, indexador). O campo diz até onde a
        resolução de identidade chegou — `cetip` e `issuer_maturity` trazem
        `isin`; `issuer` resolve só a casa emissora; `none` não amarra em
        cadastro nenhum. Filtrar por `isin` não nulo devolve um recorte, nunca a
        carteira: somar `market_value` sem filtro é o que dá a exposição
        completa.


        Órfão não é falha de busca. Nota promissória e CCB **não têm cadastro
        público de série** — são dívida direta e contrato bilateral — e por
        construção nunca terão ISIN aqui. Em 2026-06 as duas famílias são 82% do
        valor sem match.


        Carteira sem crédito privado responde página vazia, que é o caso comum.
        Competência mais recente por default; use `date` p/ outra.
      x-capability: market-data
  /v1/funds/{cnpj}/invested-funds:
    get:
      responses:
        "200":
          description: Página de cotas investidas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundOfFundsEdge"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listInvestedFunds
      tags:
        - Funds
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Competência (AAAA-MM-DD); default = mais recente.
      summary: Cotas de outros fundos na carteira deste fundo (BLC_2)
      description: >-
        O lado investidor do grafo fundo-de-fundos: em quais fundos esta classe
        aplica e quanto (R$), na competência mais recente por default.


        `invested_weight_pct` usa o PL do investido NA COMPETÊNCIA, nunca o PL
        de hoje — casar carteira de março com PL de julho produziria peso errado
        sem nenhum sinal. O preço dessa honestidade é que 27,3% das arestas
        ficam sem peso, com o motivo em `weight_reason`. `weight_over_100` marca
        a posição declarada entre 1,0× e 1,5× o PL do investido: é descasamento
        entre a data da cota e a do informe, não erro de leitura.
      x-capability: market-data
  /v1/funds/{cnpj}/investors:
    get:
      responses:
        "200":
          description: Página de fundos investidores
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundOfFundsEdge"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFundInvestors
      tags:
        - Funds
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Competência (AAAA-MM-DD); default = mais recente.
      summary: Fundos que detêm cotas deste fundo (visão reversa)
      description: "A mesma aresta de `listInvestedFunds` lida do outro lado: quais
        fundos aplicam NESTA classe e quanto. Serve para ver de onde vem o
        passivo de um fundo — quanto do PL está em mãos de outros fundos do
        mesmo grupo, e o que uma retirada concentrada representaria.
        `investor_share_pct` diz quanto a posição pesa no PL de quem investiu."
      x-capability: market-data
  /v1/funds/{cnpj}/look-through:
    get:
      responses:
        "200":
          description: Página de exposições
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FundLookThrough"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listFundLookThrough
      tags:
        - Funds
      parameters:
        - in: path
          name: cnpj
          schema:
            type: string
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Competência (AAAA-MM-DD); default = mais recente.
        - in: query
          name: ticker
          schema:
            type: string
            description: "Filtra a exposição a um único papel (ex.: 'PETR4')."
      summary: Exposição a ativos listados somando a carteira direta e a via cotas
      description: >-
        Quanto o fundo tem de cada papel contando o que ele detém diretamente
        (`exposure_direct_brl`) MAIS a parcela que chega através das cotas de
        outros fundos (`exposure_indirect_brl`), com 1 salto de profundidade — o
        2º salto acrescentaria apenas 12,4% e o 1º já captura 88,9% do valor
        indireto conhecível.


        O número é um PISO, jamais um total: o BLC_4 divulgado cobre a mediana
        de 36,3% do PL dos fundos investidos e só 3,3% dos alvos diretos
        reportam ativo que resolve para ticker. `indirect_coverage_pct` diz que
        fração do valor em cotas foi possível abrir; quando ela é null, não
        sabemos abrir nada — o que não significa exposição zero.
      x-capability: market-data
  /v1/market/investor-flow:
    get:
      responses:
        "200":
          description: Página de fluxo diário
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/InvestorFlow"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listInvestorFlow
      tags:
        - Market
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: investor_type
          schema:
            type: string
            description: Investidor Estrangeiro | Institucionais | ...
      summary: Fluxo diário por perfil de investidor (participação na B3)
      description: "Participação MTD (mês a mês) de cada perfil — Estrangeiro,
        Institucionais, PF, Instituições Financeiras, Outros — em compras/vendas
        (R$ mil e %), com fluxo líquido diário derivado. Fonte: Boletim Diário
        do Mercado (BDM/BDI) da B3."
      x-capability: market-data
  /v1/market/investor-flow/monthly:
    get:
      responses:
        "200":
          description: Página de fluxo mensal
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/InvestorFlowMonthly"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listInvestorFlowMonthly
      tags:
        - Market
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: month
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: 1º dia do mês de referência, ex. 2026-05-01
        - in: query
          name: investor_type
          schema:
            type: string
        - in: query
          name: segment
          schema:
            type: string
            description: vista | termo | opcoes | exercicio_opcoes | blocos | total_geral
      summary: Participação mensal por perfil × segmento de mercado
      description: Fechamento mensal por perfil de investidor e segmento (à vista,
        termo, opções, exercício, blocos, total geral), em R$ e %.
      x-capability: market-data
  /v1/events:
    get:
      responses:
        "200":
          description: Página de eventos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PublicMarketEvent"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listMarketEvents
      tags:
        - Events
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: date
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: "Atalho: eventos de UM dia (equivale a from=to=date)."
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: layer
          schema:
            type: string
            enum:
              - estrutural
              - setorial
              - corporativa
        - in: query
          name: category
          schema:
            type: string
            enum:
              - macro
              - politica
              - internacional
              - commodities
              - corporate
              - fii
              - cripto
              - mercado
              - outros
        - in: query
          name: entity
          schema:
            type: string
            description: "Nome de ator/empresa (ex.: 'Banco Central', 'Raizen')."
        - in: query
          name: ticker
          schema:
            type: string
            description: Ticker B3 citado no evento.
        - in: query
          name: thread
          schema:
            type: string
            description: "Slug da thread (ex.: 'mp-1376')."
      summary: Eventos de mercado (ledger point-in-time)
      description: "O que foi relevante em cada dia: eventos estruturais (decisão do
        Copom, medida provisória, tarifa, geopolítica), setoriais e
        corporativos, detectados por ato oficial, cobertura de imprensa
        multi-fonte e anomalia de preço. Cada evento carrega as fontes
        (source_refs) e os canais de transmissão a preço. Histórico profundo via
        backfill de fontes oficiais. NÃO são eventos societários (para
        grupamentos/bonificações use listCorporateEvents) nem a agenda de
        resultados."
      x-capability: events
  /v1/events/anomalies:
    get:
      responses:
        "200":
          description: Página de dias anômalos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PublicMarketAnomaly"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listMarketAnomalies
      tags:
        - Events
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: series
          schema:
            type: string
            enum:
              - IBOV
              - IFIX
              - USDBRL
              - BRENT
              - VIX
              - SP500
              - NASDAQ
        - in: query
          name: min_z
          schema:
            type: number
            minimum: 3
            maximum: 20
            default: 4
            description: Corte de |z-score| (default 4).
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Dias anômalos de mercado (z-score) e o evento que os explica
      description: >-
        Dias em que uma série acompanhada (IBOV, IFIX, dólar, brent, VIX, S&P
        500, Nasdaq) se moveu N desvios-padrão além do regime dos 252 pregões
        anteriores (histórico desde 1968), com o evento explicador quando
        casado. Um dia anômalo sem explicação é lacuna de cobertura do ledger,
        não ausência de causa.


        Para IBOV e IFIX a anomalia é medida sobre o DESCOLAMENTO LOCAL
        (`local_zscore`), não sobre o retorno bruto: descontado o que Nasdaq e
        dólar explicam, sobra o movimento próprio do mercado brasileiro. Isso
        separa 'o mundo caiu' de 'o Brasil caiu' — um IBOV que cai 5% num dia de
        queda global de 5% não é evento brasileiro, e um que cai 3% enquanto o
        mundo sobe 5% é. O `min_z` corta pelo z efetivo (local quando existe,
        bruto caso contrário), que é o mesmo critério do detector. Séries
        globais e o período anterior a 2000 (sem os fatores disponíveis) seguem
        medidos pelo bruto, com `factor_model` nulo.
      x-capability: events
  /v1/events/threads/{slug}:
    get:
      responses:
        "200":
          description: Thread com timeline
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicEventThread"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getEventThread
      tags:
        - Events
      parameters:
        - in: path
          name: slug
          schema:
            type: string
            pattern: ^[a-z0-9-]+$
          required: true
      summary: Timeline de uma thread (evento-pai)
      description: "A história completa de um evento estrutural em ordem cronológica:
        rumor na imprensa, ato oficial, confirmação e análise. Ex.: 'mp-1376'
        (medida provisória), 'copom-279' (reunião), 'tarifas-eua-2026'."
      x-capability: events
  /v1/events/{id}:
    get:
      responses:
        "200":
          description: Evento
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicMarketEvent"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getMarketEvent
      tags:
        - Events
      parameters:
        - in: path
          name: id
          schema:
            type: integer
            exclusiveMinimum: 0
          required: true
      summary: Um evento com todas as evidências
      x-capability: events
  /v1/events/{id}/similar:
    get:
      responses:
        "200":
          description: Eventos semelhantes
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PublicSimilarEvent"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      reference_event_id:
                        type: number
                    required:
                      - next_cursor
                      - count
                      - reference_event_id
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: findSimilarMarketEvents
      tags:
        - Events
      parameters:
        - in: path
          name: id
          schema:
            type: integer
            exclusiveMinimum: 0
          required: true
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 25
            default: 10
      summary: Eventos históricos semelhantes (motor de analogias)
      description: '"Quando foi a última vez que algo assim aconteceu?" — vizinhos
        semânticos do evento no histórico do ledger, por similaridade de
        embedding. Combine com /v1/events/anomalies para medir como o mercado
        reagiu nos análogos.'
      x-capability: events
  /v1/offerings:
    get:
      responses:
        "200":
          description: Página de ofertas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PublicOffering"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listOfferings
      tags:
        - Primary Market
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: search
          schema:
            type: string
            description: Nome ou CNPJ do emissor.
        - in: query
          name: regime
          schema:
            type: string
            enum:
              - ICVM_400_476
              - RCVM_160
        - in: query
          name: instrumento
          schema:
            type: string
            enum:
              - acao
              - unit
              - bdr
              - bonus_subscricao
              - warrant
              - debenture
              - debenture_conversivel
              - debenture_permutavel
              - token_debenture
              - nota_comercial
              - letra_financeira
              - ccb
              - cpr_financeira
              - warrant_agropecuario
              - cri
              - cra
              - cr
              - cdca
              - outros_securitizacao
              - cota_fidc
              - cota_fii
              - cota_fip
              - cota_fiagro
              - cota_fundo
              - cota_fundo_infra
              - cota_funcine
              - token_cota_fundo
              - cert_audiovisual
              - cert_energia
              - tic
              - nao_mapeado
            description: Instrumento CANÔNICO — o filtro recomendado. A fonte usa um
              vocabulário por regime (60 grafias para ~28 instrumentos), então
              `instrumento=debenture` recupera tanto 'Debêntures' quanto
              'DEBÊNTURES SIMPLES'.
        - in: query
          name: familia
          schema:
            type: string
            enum:
              - divida
              - securitizacao
              - equity
              - fundo
              - outro
              - nao_mapeado
            description: "Agrupamento do instrumento: divida | securitizacao | equity |
              fundo | outro."
        - in: query
          name: tipo_ativo
          schema:
            type: string
            description: Grafia AS-FILED da fonte, casada sem acento e sem caixa. Não
              expande para as grafias irmãs do mesmo instrumento — para o
              conjunto completo use `instrumento`. Aceita também um valor de
              `instrumento`/`familia`, e nesse caso filtra pela coluna canônica.
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Recorta por `data_referencia`, a mesma coluna da ordenação default.
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Recorta por `data_referencia`, a mesma coluna da ordenação default.
        - in: query
          name: sort
          schema:
            type: string
            enum:
              - data_referencia
              - data_registro
              - data_processo
              - data_encerramento
              - valor_total
            default: data_registro
            description: "Campo de ordenação. `data_registro` e `data_referencia` são
              sinônimos e ordenam por data_referencia (data_registro >
              data_inicio > data_processo): o acervo ICVM 476 não tem data de
              registro, e ordenar pela coluna crua o empurrava todo para o fim."
        - in: query
          name: order
          schema:
            type: string
            enum:
              - asc
              - desc
            default: desc
        - in: query
          name: fields
          schema:
            type: string
            description: "Projeção: lista de campos separada por vírgula (ex.:
              `instrumento,valor_total,data_referencia`). `offering_id` sempre
              incluído. Sem o parâmetro, a resposta traz todos os 29 campos."
      summary: Ofertas públicas de distribuição (mercado primário)
      description: >-
        Registro de ofertas públicas (IPO/follow-on de ações, debêntures, cotas
        de fundos), unificando o histórico ICVM 400/476/555 e o rito automático
        RCVM 160. Grão: SÉRIE da oferta — um processo multissérie tem uma linha
        por série, e o valor pode diferir entre elas. Filtrável por emissor,
        regime, instrumento/família e período; ordena por `sort`/`order`
        (default: `data_referencia` decrescente). Fonte: CVM.


        COBERTURA. O acervo de esforços restritos (ICVM 476, 2008-2022) é 44% da
        base e não tem `data_registro` nem `numero_processo` — use
        `data_referencia` para série temporal e `regime_registro` para separar
        ofertas registradas de dispensadas.


        ANTES DE SOMAR `valor_total`: é o valor REGISTRADO, não o colocado, e é
        campo livre na fonte. Exclua `value_is_sentinel` (preenchimento com
        noves; um único registro de 2022 infla o ano em 40x) e
        `value_pre_real_currency` (cruzeiro/cruzado não convertido).
      x-capability: market-data
  /v1/options/{ticker}/chain:
    get:
      responses:
        "200":
          description: Cadeia de opções
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/OptionContract"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      underlying_ticker:
                        type: string
                      date:
                        type:
                          - string
                          - "null"
                    required:
                      - next_cursor
                      - count
                      - underlying_ticker
                      - date
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getOptionsChain
      tags:
        - Options
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
        - in: query
          name: expiry
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
            description: Filtra a cadeia por vencimento (YYYY-MM-DD). Ver
              listOptionExpiries.
        - in: query
          name: type
          schema:
            type: string
            enum:
              - call
              - put
            description: "Filtra por tipo: call ou put."
      summary: Cadeia de opções vigente de um subjacente
      description: Séries vivas (não vencidas) que negociaram; filtre por `expiry`/`type`.
      x-capability: market-data
  /v1/options/{ticker}/expiries:
    get:
      responses:
        "200":
          description: Vencimentos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/OptionExpiry"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      underlying_ticker:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - underlying_ticker
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listOptionExpiries
      tags:
        - Options
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z][A-Z0-9]{3}[0-9]{0,2}$
          required: true
      summary: Vencimentos disponíveis de um subjacente
      x-capability: market-data
  /v1/options/{option}/quotes:
    get:
      responses:
        "200":
          description: Página de cotações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/OptionQuote"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listOptionQuotes
      tags:
        - Options
      parameters:
        - in: path
          name: option
          schema:
            type: string
            pattern: ^[A-Z0-9]{4,14}$
            description: "Código da SÉRIE de opção (ex.: PETRF338), não o subjacente.
              Descubra via getOptionsChain do ativo."
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Histórico EOD de uma série de opção
      x-capability: market-data
  /v1/crypto:
    get:
      responses:
        "200":
          description: Página de criptoativos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CryptoAsset"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCrypto
      tags:
        - Crypto
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: search
          schema:
            type: string
      summary: Catálogo de criptoativos (velas diárias em BRL)
      x-capability: market-data
  /v1/crypto/live:
    get:
      responses:
        "200":
          description: Snapshot de cotações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CryptoLiveQuote"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      as_of:
                        type:
                          - string
                          - "null"
                        description: Timestamp mais recente do lote
                    required:
                      - next_cursor
                      - count
                      - as_of
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCryptoLive
      tags:
        - Crypto
      parameters: []
      summary: Cotações quase-live de cripto (snapshot ~1 min, 24/7)
      x-capability: market-data
  /v1/crypto/{symbol}:
    get:
      responses:
        "200":
          description: Criptoativo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CryptoAsset"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getCrypto
      tags:
        - Crypto
      parameters:
        - in: path
          name: symbol
          schema:
            type: string
            pattern: ^[A-Z0-9]+$
            minLength: 1
            maxLength: 16
            description: "Símbolo do criptoativo, ex.: BTC, ETH, SOL."
          required: true
      summary: Perfil de um criptoativo
      x-capability: market-data
  /v1/crypto/{symbol}/quotes:
    get:
      responses:
        "200":
          description: Página de candles
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CryptoCandle"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Da vela mais RECENTE para a mais antiga. `open_time`/`close_time`
                          são UTC — cripto negocia 24/7 e não tem pregão.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listCryptoQuotes
      tags:
        - Crypto
      parameters:
        - in: path
          name: symbol
          schema:
            type: string
            pattern: ^[A-Z0-9]+$
            minLength: 1
            maxLength: 16
            description: "Símbolo do criptoativo, ex.: BTC, ETH, SOL."
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 20000
            default: 100
        - in: query
          name: interval
          schema:
            type: string
            enum:
              - 1d
              - 1h
            default: 1d
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Candles de um criptoativo (diário; 1h vazio no v1)
      x-capability: market-data
  /v1/us/assets:
    get:
      responses:
        "200":
          description: Página de ativos
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/UsAsset"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listUsAssets
      tags:
        - EUA
      parameters:
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: search
          schema:
            type: string
        - in: query
          name: type
          schema:
            type: string
            enum:
              - stock
              - etf
        - in: query
          name: bdr
          schema:
            type: string
            description: "Filtra pelo BDR correspondente na B3 (ex.: AAPL34)"
      summary: Catálogo de ativos dos EUA (ações S&P 500/Nasdaq-100 + ETFs)
      x-capability: market-data
  /v1/us/assets/{ticker}:
    get:
      responses:
        "200":
          description: Ativo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsAssetDetail"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getUsAsset
      tags:
        - EUA
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z0-9.\-]+$
            minLength: 1
            maxLength: 10
            description: "Ticker do ativo nos EUA, ex.: AAPL, MSFT, SPY."
          required: true
      summary: Perfil de um ativo dos EUA (com fundamentos SEC inline)
      x-capability: market-data
  /v1/us/assets/{ticker}/quotes:
    get:
      responses:
        "200":
          description: Página de cotações
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/UsQuote"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      order:
                        type: string
                        const: desc
                        description: Do mais RECENTE para o mais antigo. Declarado porque as séries
                          deste contrato não têm um sentido único — `getSeries`
                          devolve crescente — e quem calcula variação entre
                          linhas consecutivas inverte o sinal sem erro nenhum.
                    required:
                      - next_cursor
                      - count
                      - order
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listUsAssetQuotes
      tags:
        - EUA
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z0-9.\-]+$
            minLength: 1
            maxLength: 10
            description: "Ticker do ativo nos EUA, ex.: AAPL, MSFT, SPY."
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 20000
            default: 100
        - in: query
          name: from
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
        - in: query
          name: to
          schema:
            type: string
            pattern: ^\d{4}-\d{2}-\d{2}$
      summary: Cotações EOD de um ativo dos EUA (USD)
      description: Preços diários em USD, ajustados por desdobramento na fonte.
      x-capability: market-data
  /v1/us/assets/{ticker}/filings:
    get:
      responses:
        "200":
          description: Página de filings
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/UsFiling"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: listUsFilings
      tags:
        - EUA
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
            pattern: ^[A-Z0-9.\-]+$
            minLength: 1
            maxLength: 10
            description: "Ticker do ativo nos EUA, ex.: AAPL, MSFT, SPY."
          required: true
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - in: query
          name: form
          schema:
            type: string
            description: Filtra por formulário (10-K, 10-Q, 8-K…)
      summary: Filings SEC de uma empresa dos EUA (10-K, 10-Q, 8-K…)
      description: Metadados e link do documento oficial no EDGAR (SEC).
      x-capability: market-data
  /v1/search:
    get:
      responses:
        "200":
          description: Resultados ranqueados
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/SearchResult"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                      count:
                        type: integer
                        description: Itens nesta página
                      total:
                        type: integer
                        description: Itens no universo filtrado, ignorando a paginação. Só com
                          `?total=true`.
                      subject:
                        type: object
                        additionalProperties:
                          type: string
                        description: Os parâmetros de caminho desta requisição, ecoados. Presente em
                          toda rota endereçada por um objeto. Use-o para
                          conferir a atribuição quando fizer chamadas
                          concorrentes — sem ele, resposta trocada e resposta
                          certa têm exatamente a mesma cara.
                      query:
                        type: string
                    required:
                      - next_cursor
                      - count
                      - query
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: search
      tags:
        - System
      parameters:
        - in: query
          name: q
          schema:
            type: string
            minLength: 1
            maxLength: 64
            description: Termo de busca (ticker ou nome) — ações, FIIs, ETFs, BDRs, índices,
              títulos do Tesouro, séries macro, cripto, ativos dos EUA e crédito
              privado (debêntures e CRI/CRA/LCI/LCA do balcão).
              Acento-insensível; ótimo p/ resolver ticker↔nome.
          required: true
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
          required: false
      summary: Busca unificada (typeahead) por
        ações/FIIs/ETFs/BDRs/índices/títulos/macro/cripto/EUA/crédito
      description: "Resolve ticker↔nome em todas as classes cobertas, incluindo
        crédito privado: debêntures pelo código de negociação (ELTN17) e
        CRI/CRA/LCI/LCA pelo código do instrumento no balcão. Para ações vem
        também o `cvm_code` da companhia, que abre os documentos e as
        demonstrações (esses endpoints também aceitam o ticker direto)."
      x-capability: system
  /v1/ingest:
    get:
      responses:
        "200":
          description: Saúde da ingestão
          content:
            application/json:
              schema:
                type: object
                properties:
                  latest:
                    oneOf:
                      - $ref: "#/components/schemas/IngestRunDetail"
                      - type: "null"
                  latest_by_trigger:
                    type: array
                    items:
                      $ref: "#/components/schemas/IngestRunDetail"
                  sources:
                    type: array
                    items:
                      $ref: "#/components/schemas/IngestSourceHealth"
                  pollers:
                    type: array
                    items:
                      $ref: "#/components/schemas/IngestPollerHealth"
                  recent_runs:
                    type: array
                    items:
                      $ref: "#/components/schemas/IngestRunSummary"
                required:
                  - latest
                  - latest_by_trigger
                  - sources
                  - pollers
                  - recent_runs
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
      operationId: getIngestHealth
      tags:
        - System
      parameters:
        - in: query
          name: source
          schema:
            type: string
            description: "Recorta `sources[]` por substring do nome da fonte: `oferta`,
              `cvm`, `b3`."
      summary: Saúde da ingestão (pipeline, fontes, pollers, histórico)
      description: >-
        `latest` é o último run do pipeline completo, não o último run a
        terminar: reparos e recargas pontuais têm trigger próprio e aparecem em
        `latest_by_trigger`, com as mensagens de erro em `errors`. Uma fonte
        pode estar fresca e oca ao mesmo tempo — `missing_ratio` mede a fração
        de partições sem dado e `status: incomplete` sinaliza quando ela deixa
        de ser sazonal. O frescor é julgado contra a cadência da própria fonte:
        `expected_max_age_days` é o intervalo de atualização que ela declara e
        `stale_after_days`, o silêncio tolerado antes de `status: stale`.


        **`latest.ok` fala do RUN, não da sua fonte.** Um run vermelho por causa
        de uma série do Banco Central não diz nada sobre as ofertas da CVM, que
        no mesmo run vieram inteiras — e quem lê só o booleano recusa uma
        pergunta que a base responde. `latest.error_sources` nomeia quais fontes
        falharam; `sources[]` traz a saúde de cada uma por si.


        **São 57 fontes.** Use `source` para recortar — casa por substring,
        então `oferta` acha `cvm_ofertas`. O recorte vale só para `sources[]`: o
        run e os pollers continuam inteiros, porque é cruzando os dois que se
        separa 'o pipeline falhou' de 'esta fonte falhou'.
      x-capability: system
  /v1/portfolio:
    get:
      operationId: getPortfolio
      tags:
        - Carteira
      x-capability: account
      summary: Sua carteira (consolidada)
      description: "Retorna a carteira consolidada do dono da chave de API:
        patrimônio, total investido, P&L realizado e não-realizado, variação do
        dia, composição por classe, TODAS as posições (`positions`, maiores
        primeiro) e quebra por carteira. Exige uma chave de API por usuário
        (crie em https://databolsa.com/conta)."
      responses:
        "200":
          description: Carteira consolidada do usuário.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioContext"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolio/history:
    get:
      operationId: getPortfolioHistory
      tags:
        - Carteira
      x-capability: account
      summary: Histórico mensal consolidado
      description: "Série mensal do patrimônio consolidado do dono da chave: valor de
        mercado, aporte acumulado, proventos recebidos e resultado realizado,
        mês a mês. Exclui carteiras marcadas como simulação."
      responses:
        "200":
          description: Pontos mensais em `data`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioHistoryResponse"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios:
    get:
      operationId: listPortfolios
      tags:
        - Carteira
      x-capability: account
      summary: Suas carteiras
      description: Lista as carteiras do dono da chave (id, nome, nº de linhas de
        ativo no ledger, flag de simulação). Use o `id` nas demais operações de
        carteira.
      responses:
        "200":
          description: Carteiras da conta.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PortfolioListItem"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                        description: "Sempre null: resposta em página única."
                      count:
                        type: integer
                        description: Itens nesta página.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    post:
      operationId: createPortfolio
      tags:
        - Carteira
      x-capability: account
      summary: Criar carteira
      description: CRIA uma carteira REAL na conta do dono da chave. O número de
        carteiras é limitado pelo plano (resposta 402 quando o teto é atingido).
        Nome único por conta (409 em duplicidade).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  description: Nome da carteira (até 60 caracteres, único na conta).
                visibility:
                  type: string
                  enum:
                    - private
                    - unlisted
                    - public
                  description: Visibilidade (default private).
      responses:
        "201":
          description: Carteira criada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioListItem"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/import-template:
    get:
      operationId: getPortfolioImportTemplate
      tags:
        - Carteira
      x-capability: account
      summary: Template CSV de import manual
      description: "Baixa o template CSV de preenchimento manual (colunas: ticker,
        operacao, data, quantidade, preco) aceito pelo import de planilha."
      responses:
        "200":
          description: Arquivo CSV.
          content:
            text/csv:
              schema:
                type: string
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}:
    get:
      operationId: getPortfolioDetail
      tags:
        - Carteira
      x-capability: account
      summary: Detalhe de uma carteira
      description: "Carteira computada: posições com preço médio, quantidade, valor de
        mercado, P&L, proventos e alocação, além dos totais e das duas medidas
        de retorno (`returns`). A resposta é COMPACTA por default: `holdings[]`
        traz só posições abertas + watchlist (sem as encerradas) e sem as séries
        mensais por posição — os totais e agregados mensais da carteira vêm
        sempre. Use `include` para compor as camadas opcionais. A posição é
        derivada do ledger de transações, que NÃO vem embutido: peça
        `include=transactions` quando precisar dele, ou use
        listPortfolioTransactions por ativo."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira (de listPortfolios).
          schema:
            type: string
        - name: include
          in: query
          required: false
          description: "Camadas opcionais, separadas por vírgula (compõem, ex.:
            `closed,monthly`). `closed` inclui as posições ENCERRADAS em
            holdings[] (default: só abertas + watchlist); `monthly` inclui as
            séries mensais por posição (realized_by_month/income_by_month — os
            agregados da carteira vêm sempre); `transactions` embute o ledger
            completo em cada posição — dezenas de vezes maior que o payload
            padrão numa carteira com histórico."
          schema:
            type: string
            example: closed,monthly
      responses:
        "200":
          description: Carteira computada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioDetail"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    patch:
      operationId: updatePortfolio
      tags:
        - Carteira
      x-capability: account
      summary: Editar carteira
      description: "ALTERA a carteira REAL do dono da chave: renomeia (`name`), muda a
        visibilidade (`visibility`) e/ou marca como simulação
        (`exclude_from_consolidated`, fora do consolidado). Informe ao menos um
        campo."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Novo nome (até 60 caracteres, único na conta).
                visibility:
                  type: string
                  enum:
                    - private
                    - unlisted
                    - public
                  description: Nova visibilidade.
                exclude_from_consolidated:
                  type: boolean
                  description: true = carteira de simulação (fora do consolidado).
      responses:
        "200":
          description: Campos alterados.
          content:
            application/json:
              schema:
                type: object
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    delete:
      operationId: deletePortfolio
      tags:
        - Carteira
      x-capability: account
      summary: Apagar carteira
      description: APAGA a carteira REAL do dono da chave, com TODOS os ativos e
        transações (cascata, irreversível). Confirme com o usuário antes de
        chamar.
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
      responses:
        "200":
          description: Carteira apagada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
                  id:
                    type: string
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}/history:
    get:
      operationId: getPortfolioHistoryById
      tags:
        - Carteira
      x-capability: account
      summary: Histórico mensal de uma carteira
      description: Série mensal (patrimônio, aporte, proventos, realizado) de UMA
        carteira específica, com o TWR time-weighted. A xirr money-weighted da
        mesma carteira está em getPortfolioDetail (`returns`).
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
      responses:
        "200":
          description: Pontos mensais em `data`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioHistoryDetailResponse"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}/xray:
    get:
      operationId: getPortfolioXray
      tags:
        - Carteira
      x-capability: account
      summary: Raio-X de concentração da carteira
      description: "Análise DETERMINÍSTICA de concentração da carteira em UMA chamada
        (substitui detalhe + perfil ativo a ativo): distribuição por classe
        (`by_class`), por setor/segmento das posições de bolsa (`by_sector`; sem
        classificação conhecida vira 'Sem classificação'), por indexador da
        renda fixa (`by_indexer` — Tesouro pelo tipo do título, CDB/debênture
        pelo contrato ou catálogo; pct sobre o TOTAL DE RENDA FIXA, não da
        carteira), por moeda (`by_currency`, BRL × USD) e as 10 maiores posições
        (`top_positions`). `flags` traz FATOS pt-BR quando um limite estoura
        (posição >10%, setor >25%, classe >60%, indexador >70% da RF, moeda
        estrangeira >30%) — sem opinião nem recomendação; a leitura é sua.
        Valores CRUS em BRL, percentuais 0-100. Considera só posições ABERTAS
        com valor (mercado, accrual de renda fixa ou custo); abertas sem valor
        nenhum são ignoradas e contadas em `meta.unpriced_ignored`."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira (de listPortfolios).
          schema:
            type: string
      responses:
        "200":
          description: Raio-X da carteira.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioXray"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}/costs:
    get:
      operationId: getPortfolioCosts
      tags:
        - Carteira
      x-capability: account
      summary: Raio-X de custos recorrentes da carteira
      description: "Estimativa DETERMINÍSTICA de quanto a carteira paga por ano, PARA
        QUEM, e o que está acima da referência — só a partir de dado público e
        do contrato informado: taxa de administração as-filed dos fundos
        (comparada à MEDIANA dos pares da mesma classificação CVM, quando há ≥20
        pares com ≥100 cotistas e informe fresco), custódia do Tesouro Direto na
        B3 (0,20% a.a.; Tesouro Selic isento até R$ 10 mil de estoque) e gap de
        renda fixa bancária TRIBUTÁVEL (CDB/RDB/LC/LF) contratada abaixo de 100%
        do CDI (estimado com o CDI acumulado 12m). `items` traz cada custo com
        destinatário (`recipient`), referência (`benchmark`) e a parcela acima
        dela (`excess_cost_brl`); `by_recipient` agrega por destinatário;
        `totals` soma tudo. O que NÃO é observável (LCI/LCA isentas, taxas
        internas de ETF/FII, spread de compra de debênture, custos de corretora)
        vira `not_estimated` com o motivo — nunca número inventado. `flags` são
        FATOS pt-BR (fundo acima da mediana; RF abaixo de 100% do CDI) — sem
        opinião nem recomendação; a leitura é sua. Considera só posições abertas
        com valor."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira (de listPortfolios).
          schema:
            type: string
      responses:
        "200":
          description: Raio-X de custos da carteira.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioCosts"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}/look-through:
    get:
      operationId: getPortfolioLookThrough
      tags:
        - Carteira
      x-capability: account
      summary: Exposição efetiva (abre as posições em fundos)
      description: "Exposição EFETIVA da carteira em UMA chamada: abre cada posição em
        FUNDO (símbolo = CNPJ) usando a última carteira mensal DIVULGADA do
        fundo e converte cada ação/debênture detida em exposição indireta
        proporcional (valor da posição × peso do ativo no fundo). Fundo que
        detém cotas de OUTRO fundo é aberto em mais UM nível e para aí.
        `exposures` agrega por ativo (top 50 por valor): posição direta,
        indireta, total, % da carteira e por quais fundos chega (`via`) — a
        sobreposição direto × fundo é o ponto. `funds_opened` diz a competência
        usada, quanto do fundo os ativos divulgados explicam (`coverage_pct`) e
        o denominador (`method`: patrimônio oficial na competência, ou soma dos
        ativos divulgados quando ele falta). `not_opened` lista fundos sem
        carteira divulgada. Valores CRUS em BRL, pct 0-100. CAVEAT: a carteira
        divulgada é defasada (semanas/meses) — NÃO é a posição de hoje do fundo;
        a cobertura costuma ser parcial, então a exposição indireta é PISO, não
        total. Carteira sem fundos responde 200 com arrays vazios e
        `meta.note`."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira (de listPortfolios).
          schema:
            type: string
      responses:
        "200":
          description: Exposição efetiva da carteira.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioLookThrough"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}/assets:
    post:
      operationId: addPortfolioAsset
      tags:
        - Carteira
      x-capability: account
      summary: Adicionar ativo à carteira
      description: "ADICIONA um ativo à carteira REAL do dono da chave (idempotente —
        repetir não duplica). Ativo sem transações funciona como watchlist;
        lance transações para rastrear posição. Para Tesouro Direto use o nome
        oficial do título (ex.: TESOURO SELIC 2029). Para fundo
        (asset_type=fund) use o CNPJ da classe (14 dígitos, com ou sem máscara);
        para debênture, o código do papel (ex.: ELTN17); para
        CRI/CRA/LCI/LCA/CDB use asset_type=renda_fixa com o código do
        instrumento no balcão (ex.: CRA021000RX) — com negócio recente no
        secundário a posição é marcada a mercado, senão vale o accrual da taxa
        contratada."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - asset_type
                - symbol
              properties:
                asset_type:
                  type: string
                  enum:
                    - stock
                    - fii
                    - etf
                    - bdr
                    - index
                    - tesouro
                    - crypto
                    - option
                    - renda_fixa
                    - debenture
                    - fund
                    - us
                  description: Tipo do ativo.
                symbol:
                  type: string
                  description: "Ticker (ex.: PETR4) ou nome oficial do título do Tesouro."
      responses:
        "201":
          description: Ativo na carteira.
          content:
            application/json:
              schema:
                type: object
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    delete:
      operationId: removePortfolioAsset
      tags:
        - Carteira
      x-capability: account
      summary: Remover ativo da carteira
      description: REMOVE um ativo da carteira REAL do dono da chave, APAGANDO o
        ledger de transações dele nesta carteira (cascata, irreversível).
        Confirme com o usuário antes de chamar.
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
        - name: asset_type
          in: query
          required: true
          description: Tipo do ativo.
          schema:
            type: string
            enum:
              - stock
              - fii
              - etf
              - bdr
              - index
              - tesouro
              - crypto
              - option
              - renda_fixa
              - debenture
              - fund
              - us
        - name: symbol
          in: query
          required: true
          description: Ticker ou nome do título.
          schema:
            type: string
      responses:
        "200":
          description: Ativo removido.
          content:
            application/json:
              schema:
                type: object
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    patch:
      operationId: updatePortfolioAsset
      tags:
        - Carteira
      x-capability: account
      summary: Taxa contratada de renda fixa
      description: "DEFINE a taxa contratada de uma posição de renda fixa (para o
        cálculo de rendimento): `rf_indexer` (cdi = % do CDI, prefixado = %
        a.a., ipca = IPCA + % a.a.) e `rf_rate`. Use `rf_indexer: none` para
        limpar."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
        - name: asset_type
          in: query
          required: true
          description: Tipo do ativo (renda_fixa/tesouro).
          schema:
            type: string
        - name: symbol
          in: query
          required: true
          description: Código do papel.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - rf_indexer
              properties:
                rf_indexer:
                  type: string
                  enum:
                    - cdi
                    - prefixado
                    - ipca
                    - none
                  description: Indexador (none limpa a taxa).
                rf_rate:
                  type: number
                  description: "Taxa (ex.: 110 = 110% do CDI; 6.2 = IPCA+6,2%)."
      responses:
        "200":
          description: Meta da posição.
          content:
            application/json:
              schema:
                type: object
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}/transactions:
    get:
      operationId: listPortfolioTransactions
      tags:
        - Carteira
      x-capability: account
      summary: Ledger de um ativo
      description: Lista as transações (compras, vendas, splits, ajustes) de um ativo
        da carteira, em ordem cronológica.
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
        - name: asset_type
          in: query
          required: true
          description: Tipo do ativo.
          schema:
            type: string
        - name: symbol
          in: query
          required: true
          description: Ticker ou nome do título.
          schema:
            type: string
      responses:
        "200":
          description: Transações do ativo.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PortfolioTransaction"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                        description: "Sempre null: resposta em página única."
                      count:
                        type: integer
                        description: Itens nesta página.
                      asset:
                        type: object
                        description: Ativo do ledger (id, asset_type, symbol).
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    post:
      operationId: addPortfolioTransaction
      tags:
        - Carteira
      x-capability: account
      summary: Lançar transação
      description: LANÇA uma transação na carteira REAL do dono da chave. Se o ativo
        ainda não está na carteira, ele é adicionado automaticamente — "compra
        de 100 PETR4" é uma única chamada. buy/sell exigem `quantity`; `price` é
        opcional (custo desconhecido); split usa `ratio` (2 = 2:1). Confirme
        valores com o usuário.
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - asset_type
                - symbol
                - kind
                - trade_date
              properties:
                asset_type:
                  type: string
                  enum:
                    - stock
                    - fii
                    - etf
                    - bdr
                    - index
                    - tesouro
                    - crypto
                    - option
                    - renda_fixa
                    - debenture
                    - fund
                    - us
                  description: Tipo do ativo.
                symbol:
                  type: string
                  description: "Ticker (ex.: PETR4) ou nome oficial do título do Tesouro."
                kind:
                  type: string
                  enum:
                    - buy
                    - sell
                    - split
                  description: Tipo da transação.
                trade_date:
                  type: string
                  description: Data do negócio (AAAA-MM-DD).
                quantity:
                  type: number
                  description: Quantidade (> 0; obrigatória em buy/sell).
                price:
                  type: number
                  description: Preço unitário em BRL (omita se desconhecido).
                fees:
                  type: number
                  description: Custos/corretagem em BRL (default 0).
                ratio:
                  type: number
                  description: Fator do split (2 = 2:1; 0.5 = grupamento 1:2).
                note:
                  type: string
                  description: Observação livre (até 280 caracteres).
      responses:
        "201":
          description: Transação lançada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  transaction:
                    $ref: "#/components/schemas/PortfolioTransaction"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}/transactions/{txId}:
    patch:
      operationId: updatePortfolioTransaction
      tags:
        - Carteira
      x-capability: account
      summary: Editar transação
      description: "ALTERA uma transação existente da carteira REAL do dono da chave
        (patch parcial: informe só os campos a mudar). Confirme com o usuário
        antes de chamar."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
        - name: txId
          in: path
          required: true
          description: Id da transação (de listPortfolioTransactions).
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                kind:
                  type: string
                  enum:
                    - buy
                    - sell
                    - split
                  description: Tipo da transação.
                trade_date:
                  type: string
                  description: Data do negócio (AAAA-MM-DD).
                quantity:
                  type: number
                  description: Quantidade.
                price:
                  type: number
                  description: Preço unitário em BRL.
                fees:
                  type: number
                  description: Custos em BRL.
                ratio:
                  type: number
                  description: Fator do split.
                note:
                  type: string
                  description: Observação.
      responses:
        "200":
          description: Transação atualizada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioTransaction"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    delete:
      operationId: deletePortfolioTransaction
      tags:
        - Carteira
      x-capability: account
      summary: Remover transação
      description: REMOVE uma transação do ledger da carteira REAL do dono da chave
        (irreversível). Confirme com o usuário antes de chamar.
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
        - name: txId
          in: path
          required: true
          description: Id da transação.
          schema:
            type: string
      responses:
        "200":
          description: Transação removida.
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
                  id:
                    type: string
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}/imports:
    post:
      operationId: importPortfolioFile
      tags:
        - Carteira
      x-capability: account
      summary: Importar planilha (B3 ou template manual)
      description: "IMPORTA uma planilha para a carteira REAL do dono da chave:
        Negociação ou Movimentação da B3 (.xlsx) ou o template manual
        (.csv/.xlsx) — o formato é detectado automaticamente. Envie o arquivo em
        `content_base64` (máx. 8 MB). Idempotente: reenviar o mesmo arquivo não
        duplica lançamentos. A resposta resume o que entrou (importadas,
        duplicadas, ignoradas) e traz avisos."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - content_base64
              properties:
                content_base64:
                  type: string
                  description: Conteúdo do arquivo em base64.
                filename:
                  type: string
                  description: Nome do arquivo (ajuda a trilha de auditoria).
      responses:
        "201":
          description: Resumo do import.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioImportSummary"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    get:
      operationId: listPortfolioImports
      tags:
        - Carteira
      x-capability: account
      summary: Histórico de imports
      description: Lista os imports já feitos nesta carteira (arquivo, fonte, data e
        resumo persistido).
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
      responses:
        "200":
          description: Imports da carteira.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PortfolioImportListItem"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                        description: "Sempre null: resposta em página única."
                      count:
                        type: integer
                        description: Itens nesta página.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}/imports/{importId}/rows:
    get:
      operationId: listPortfolioImportRows
      tags:
        - Carteira
      x-capability: account
      summary: Linhas de um import
      description: "Drill-down de um import: cada linha do arquivo com status
        (imported, ignored, duplicate, error) e motivo."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
        - name: importId
          in: path
          required: true
          description: Id do import (de listPortfolioImports).
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Filtra por status.
          schema:
            type: string
            enum:
              - imported
              - ignored
              - duplicate
              - error
        - name: limit
          in: query
          required: false
          description: Linhas por página (1–500, default 100).
          schema:
            type: integer
        - name: cursor
          in: query
          required: false
          description: Cursor opaco de meta.next_cursor — mesma paginação das demais
            listagens.
          schema:
            type: string
      responses:
        "200":
          description: Linhas do import.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PortfolioImportRow"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                        description: "Sempre null: resposta em página única."
                      count:
                        type: integer
                        description: Itens nesta página.
                      total:
                        type: integer
                        description: Linhas do import que casam o filtro, antes do recorte da página.
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/portfolios/{id}/reconcile:
    post:
      operationId: reconcilePortfolioAsset
      tags:
        - Carteira
      x-capability: account
      summary: Reconciliar posição à B3
      description: "AJUSTA a posição calculada de um ativo à quantidade declarada pela
        B3 (`target_qty` na data `as_of`), inserindo um ajuste idempotente —
        útil quando um evento (ex.: subscrição) não veio no extrato. Confirme a
        quantidade com o usuário antes de chamar."
      parameters:
        - name: id
          in: path
          required: true
          description: Id da carteira.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - asset_type
                - symbol
                - target_qty
                - as_of
              properties:
                asset_type:
                  type: string
                  description: Tipo do ativo.
                symbol:
                  type: string
                  description: Ticker.
                target_qty:
                  type: number
                  description: Quantidade declarada pela B3 (>= 0).
                as_of:
                  type: string
                  description: Data de referência (AAAA-MM-DD).
      responses:
        "200":
          description: Resultado do ajuste (delta aplicado).
          content:
            application/json:
              schema:
                type: object
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/suitability:
    get:
      operationId: getSuitability
      tags:
        - Carteira
      x-capability: account
      summary: Seu perfil de investidor
      description: "Retorna o perfil de investidor (suitability) do dono da chave:
        nível (Conservador/Moderado/Arrojado), a posição na régua 0–100 (não é
        uma nota) e as respostas do questionário. `profile` é nulo se o usuário
        ainda não definiu o perfil."
      responses:
        "200":
          description: Perfil de investidor do usuário (ou nulo).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuitabilityEnvelope"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/documents/search:
    get:
      operationId: searchDocuments
      tags:
        - Documentos
      x-capability: documents
      summary: Busca semântica em documentos
      description: >-
        Encontra TRECHOS relevantes no texto de documentos oficiais CVM/B3. Use
        para descobrir e investigar fatos qualitativos; não trate os primeiros
        hits como leitura exaustiva do documento. Fluxo autossuficiente no MCP:
        (1) chame getDocumentTaxonomy quando não souber as facetas; (2) faça
        busca ampla por entidade/família; (3) restrinja pelo protocol+source da
        citação e tente 3–5 formulações para alegações críticas; (4) chame
        readDocument e pagine até meta.next_cursor=null quando a conclusão
        depender do documento completo.


        Crédito privado não se identifica só por ticker: issuer_cnpj é o emissor
        cadastral (não necessariamente o devedor econômico), asset_code é o
        papel (ISIN/código) e asset_family separa DEB/CRI/CRA/OTS/FIDC/FIAGRO.
        Securitizadora, originador e devedor econômico não são sinônimos.
        Prefira document_kind, estável; category/doc_type são rótulos brutos e
        exatos da fonte.


        Cada hit traz text, text_expanded e citation. score mede proximidade
        semântica, não confiança factual. Cite protocol+source, página/heading e
        distinga reference_date (período coberto) de filed_at (quando se tornou
        público). Ausência de hit não prova ausência de documento ou evento;
        reconcilie números com as operações estruturadas.


        TEMPO: sem período na consulta nem nos filtros, um leve prior de
        recência desempata a favor do documento mais novo — mas semântica ainda
        manda, então para 'o relatório MAIS RECENTE' não confie só no ranking:
        fixe reference_from/reference_to (ou descubra o doc por
        getObjectEvidence, que lista por data) e só então busque dentro dele.
        Período explícito na consulta ('2T25', '2021') desliga o prior e promove
        o período pedido.
      parameters:
        - name: q
          in: query
          required: true
          description: Termo/pergunta em linguagem natural.
          schema:
            type: string
            minLength: 1
        - name: tickers
          in: query
          required: false
          description: "Filtra por papéis, ex.: PETR4,PRIO3."
          style: form
          explode: false
          schema:
            type: array
            minItems: 1
            items:
              type: string
              minLength: 1
        - name: category
          in: query
          required: false
          description: Categoria bruta, sensível ao rótulo exato da fonte. Descubra em
            getDocumentTaxonomy; prefira document_kind quando possível.
          schema:
            type: string
            minLength: 1
        - name: document_kind
          in: query
          required: false
          description: "Taxonomia estável do DataBolsa. Para crédito: debt_instrument
            reúne escrituras, termos de securitização e aditamentos;
            governing_document reúne regulamentos/documentos constitutivos."
          schema:
            $ref: "#/components/schemas/DocumentKind"
        - name: entity_type
          in: query
          required: false
          description: "Que tipo de entidade o documento descreve. `issuer` é a companhia
            registrada apenas para emitir dívida (concessionária, SPE): publica
            demonstrações e documentos, e não tem ticker. `securitization` é o
            certificado (CRI/CRA/OTS), identificado pelo ISIN e não pela
            securitizadora."
          schema:
            $ref: "#/components/schemas/DocumentEntityType"
        - name: issuer_cnpj
          in: query
          required: false
          description: CNPJ do emissor (14 dígitos; pontuação é aceita e ignorada). É a
            chave de entidade para documentos de crédito, que não têm ticker.
          schema:
            type: string
            minLength: 14
        - name: asset_code
          in: query
          required: false
          description: "Identificador do papel: ISIN, código ANBIMA da debênture ou código
            de instrumento da B3 — qualquer um dos que a emissão carrega."
          schema:
            type: string
            minLength: 3
        - name: asset_family
          in: query
          required: false
          description: Família do papel; eixo independente de entity_type.
          schema:
            type: string
            enum:
              - DEB
              - CRI
              - CRA
              - OTS
              - FIDC
              - FIAGRO
        - name: year
          in: query
          required: false
          description: Ano de referência do documento (AAAA).
          schema:
            type: integer
            minimum: 1900
            maximum: 2100
        - name: filed_before
          in: query
          required: false
          description: "Corte point-in-time: só documentos PROTOCOLADOS (filed_at) até
            esta data — o que estava publicamente disponível na data, não o
            período de referência. Documentos sem data de publicação conhecida
            são EXCLUÍDOS (a exclusão é sinalizada em warnings). A cobertura de
            filed_at do acervo histórico está em backfill; documentos ainda não
            cobertos ficam fora do corte em vez de aparecer com data inferida."
          schema:
            type: string
            format: date
        - name: filed_after
          in: query
          required: false
          description: Só documentos protocolados (filed_at) a partir desta data.
            Documentos sem data de publicação são excluídos.
          schema:
            type: string
            format: date
        - name: protocol
          in: query
          required: false
          description: Restringe a UM documento exato (protocolo CVM/FNET) — busca dentro
            do documento.
          schema:
            type: string
            minLength: 1
        - name: reference_date
          in: query
          required: false
          description: "Período de referência exato do documento (ex.: 2022-06-30 = 2T22)."
          schema:
            type: string
            format: date
        - name: reference_from
          in: query
          required: false
          description: Início do intervalo de reference_date (inclusivo).
          schema:
            type: string
            format: date
        - name: reference_to
          in: query
          required: false
          description: Fim do intervalo de reference_date (inclusivo).
          schema:
            type: string
            format: date
        - name: doc_type
          in: query
          required: false
          description: "Subtipo bruto, sensível ao rótulo exato da fonte (ex.: Relatório
            de Agência de Rating). Descubra em getDocumentTaxonomy."
          schema:
            type: string
            minLength: 1
        - name: tables_only
          in: query
          required: false
          description: Restringe a trechos de tabela (fatos numéricos).
          schema:
            type: boolean
        - name: financial_only
          in: query
          required: false
          description: Restringe a emissores financeiros quando true; false restringe aos
            demais.
          schema:
            type: boolean
        - name: include_context
          in: query
          required: false
          description: false remove o bloco duplicado meta.context e reduz tokens;
            data[].text/text_expanded e citation continuam completos. Em
            clientes MCP, prefira false salvo quando precisar de contexto pronto
            para colar em outro prompt.
          schema:
            type: boolean
            default: true
        - name: limit
          in: query
          required: false
          description: Máximo de trechos (1–25, default 8).
          schema:
            type: integer
            minimum: 1
            maximum: 25
            default: 8
      responses:
        "200":
          description: Trechos relevantes + bloco de contexto com citação.
          headers:
            Retry-After:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
            RateLimit-Limit:
              schema:
                type: integer
                minimum: 1
            RateLimit-Remaining:
              schema:
                type: integer
                minimum: 0
            RateLimit-Reset:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentSearchResponse"
        "429":
          description: Limite diário de requisições do plano atingido.
          headers:
            Retry-After:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
            RateLimit-Limit:
              schema:
                type: integer
                minimum: 1
            RateLimit-Remaining:
              schema:
                type: integer
                minimum: 0
            RateLimit-Reset:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/documents/{protocol}/chunks:
    get:
      operationId: readDocument
      tags:
        - Documentos
      x-capability: documents
      summary: Ler um documento indexado na ordem
      description: "Leitura determinística depois de searchDocuments: percorre TODOS
        os trechos do protocolo por chunk_index, sem ranking semântico. Passe
        também source da citação; se o protocolo existir em mais de uma fonte e
        source faltar, responde 409 com as opções. Comece sem cursor e repita
        com meta.next_cursor até null. A ordem dos chunks é a ordem extraída do
        documento; preserve a citação em meta e não interprete ausência de
        página em XML como falta de proveniência."
      parameters:
        - name: protocol
          in: path
          required: true
          description: Protocolo exato devolvido em
            searchDocuments.data[].citation.protocol.
          schema:
            type: string
            minLength: 1
        - name: source
          in: query
          required: false
          description: "Conector devolvido na citação (ex.: cvm_ipe, fnet_fii).
            Recomendado para identidade inequívoca."
          schema:
            type: string
            minLength: 1
        - name: cursor
          in: query
          required: false
          description: Valor opaco devolvido em meta.next_cursor; omita na primeira página.
          schema:
            type: string
            pattern: ^\d+$
        - name: limit
          in: query
          required: false
          description: Trechos por página (1–50, default 20).
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        "200":
          description: Trechos ordenados e citação única do documento.
          headers:
            Retry-After:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
            RateLimit-Limit:
              schema:
                type: integer
                minimum: 1
            RateLimit-Remaining:
              schema:
                type: integer
                minimum: 0
            RateLimit-Reset:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentReadResponse"
        "409":
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "429":
          description: Limite diário de requisições do plano atingido.
          headers:
            Retry-After:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
            RateLimit-Limit:
              schema:
                type: integer
                minimum: 1
            RateLimit-Remaining:
              schema:
                type: integer
                minimum: 0
            RateLimit-Reset:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/events/search:
    get:
      operationId: searchMarketEvents
      tags:
        - Events
      x-capability: events
      summary: Busca semântica no ledger de eventos de mercado
      description: "Busca por significado no histórico de eventos de mercado (decisões
        do Copom, medidas provisórias, tarifas, geopolítica, eventos
        corporativos relevantes). Complementa as leituras estruturadas de
        /v1/events: use quando a pergunta é temática ('renegociação de dívidas
        do agro', 'escalada no Oriente Médio') e depois aprofunde pelo
        event_id."
      parameters:
        - name: q
          in: query
          required: true
          description: Termo/pergunta em linguagem natural.
          schema:
            type: string
            minLength: 1
        - name: limit
          in: query
          required: false
          description: Máximo de eventos (1–25, default 8).
          schema:
            type: integer
            minimum: 1
            maximum: 25
            default: 8
      responses:
        "200":
          description: Eventos mais próximos semanticamente da consulta.
          headers:
            Retry-After:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
            RateLimit-Limit:
              schema:
                type: integer
                minimum: 1
            RateLimit-Remaining:
              schema:
                type: integer
                minimum: 0
            RateLimit-Reset:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarketEventSearchResponse"
        "429":
          description: Limite diário de requisições do plano atingido.
          headers:
            Retry-After:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
            RateLimit-Limit:
              schema:
                type: integer
                minimum: 1
            RateLimit-Remaining:
              schema:
                type: integer
                minimum: 0
            RateLimit-Reset:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/documents/taxonomy:
    get:
      operationId: getDocumentTaxonomy
      tags:
        - Documentos
      x-capability: documents
      summary: Taxonomia e cobertura de documentos
      description: "Descobre as facetas aceitas por searchDocuments: document_kind
        estável com contagem e seus rótulos brutos category/doc_type,
        entity_type, asset_family e intervalo válido de anos. Cada categoria
        declara `shape` — o que um SEGUNDO documento dela significa: `event`
        (nada substitui nada), `superseding` (o último é o vigente), `amending`
        (o vigente é o original mais os aditamentos) ou `periodic` (chaveado por
        competência). Leia isso ANTES de concluir qual documento vale: sete
        estatutos são um vigente e seis históricos, mas trinta e três fatos
        relevantes são trinta e três fatos. Para crédito privado, comece aqui
        quando não souber o rótulo da fonte; prefira document_kind e
        asset_family aos rótulos brutos. As contagens são por documento, não por
        trecho."
      responses:
        "200":
          description: Facetas disponíveis no corpus indexado.
          headers:
            Retry-After:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
            RateLimit-Limit:
              schema:
                type: integer
                minimum: 1
            RateLimit-Remaining:
              schema:
                type: integer
                minimum: 0
            RateLimit-Reset:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentTaxonomy"
        "429":
          description: Limite diário de requisições do plano atingido.
          headers:
            Retry-After:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
            RateLimit-Limit:
              schema:
                type: integer
                minimum: 1
            RateLimit-Remaining:
              schema:
                type: integer
                minimum: 0
            RateLimit-Reset:
              schema:
                type: integer
                minimum: 1
              description: Segundos até o reinício da janela.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/theses:
    get:
      operationId: listMyTheses
      tags:
        - Teses
      x-capability: account
      summary: Minhas teses
      description: "Lista as teses do dono da chave (resumo: título, ticker,
        visibilidade, status de export) e o teto do plano. O documento completo
        vem no getThesis."
      responses:
        "200":
          description: Teses + limite do plano (null = ilimitado).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ThesisSummary"
                  meta:
                    type: object
                    properties:
                      next_cursor:
                        type:
                          - string
                          - "null"
                        description: "Sempre null: resposta em página única."
                      count:
                        type: integer
                        description: Itens nesta página.
                      limit:
                        type: number
                        nullable: true
                        description: Teto de teses do plano (null = ilimitado).
                    required:
                      - next_cursor
                      - count
                required:
                  - data
                  - meta
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    post:
      operationId: createThesis
      tags:
        - Teses
      x-capability: account
      summary: Criar tese
      description: "Cria uma tese escrita em markdown (`md`). Título, ticker e tipo
        saem do frontmatter. Respeita o limite de teses do plano (402
        plan_required ao estourar). FORMATO: Markdown com frontmatter YAML.
        Obrigatórios no frontmatter: `kind`
        (equity|fii|macro|setor|carta|carteira) e `title`. Opcionais:
        `subtitle`, `ticker`, `date` (AAAA-MM-DD), `tags`. Autor, capa, ids de
        seção e disclaimer o servidor preenche — não escreva. No corpo: `##
        Título` abre uma seção; parágrafos, listas e `###` viram prosa em GFM;
        tabela GFM vira tabela do relatório; `>
        [!risco|tese|catalisador|atenção|info] Título` vira caixa de destaque
        (linhas `- item` viram bullets). Blocos ricos vão em cercas ```db:<tipo>
        com corpo YAML, onde <tipo> é rating, keyStats, summary, chart,
        scenarios, sources, quote, figure, table, cover ou disclaimer. Um bloco
        fora do formato NÃO derruba a tese: ele degrada para um aviso de atenção
        no documento e aparece em `warnings` na resposta, com linha e campo,
        para você corrigir depois. O campo `doc` (ReportDoc JSON pronto)
        continua aceito no lugar de `md`, para clientes antigos, mas ali a
        validação é tudo-ou-nada: um campo fora do schema recusa o documento
        inteiro."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - md
              properties:
                md:
                  type: string
                  description: "Markdown da tese. Markdown com frontmatter YAML. Obrigatórios no
                    frontmatter: `kind` (equity|fii|macro|setor|carta|carteira)
                    e `title`. Opcionais: `subtitle`, `ticker`, `date`
                    (AAAA-MM-DD), `tags`. Autor, capa, ids de seção e disclaimer
                    o servidor preenche — não escreva. No corpo: `## Título`
                    abre uma seção; parágrafos, listas e `###` viram prosa em
                    GFM; tabela GFM vira tabela do relatório; `>
                    [!risco|tese|catalisador|atenção|info] Título` vira caixa de
                    destaque (linhas `- item` viram bullets). Blocos ricos vão
                    em cercas ```db:<tipo> com corpo YAML, onde <tipo> é rating,
                    keyStats, summary, chart, scenarios, sources, quote, figure,
                    table, cover ou disclaimer. Um bloco fora do formato NÃO
                    derruba a tese: ele degrada para um aviso de atenção no
                    documento e aparece em `warnings` na resposta, com linha e
                    campo, para você corrigir depois."
                doc:
                  allOf:
                    - $ref: "#/components/schemas/ReportDocInput"
                  deprecated: true
                  description: "OBSOLETO — use `md`. ReportDoc JSON pronto, aceito para clientes
                    anteriores ao markdown. Aqui a validação é tudo-ou-nada: um
                    campo fora do schema recusa o documento inteiro."
                visibility:
                  type: string
                  enum:
                    - private
                    - unlisted
                    - public
                  description: "Default: private."
      responses:
        "201":
          description: Tese criada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  thesis:
                    $ref: "#/components/schemas/ThesisDetail"
                  warnings:
                    type: array
                    items:
                      $ref: "#/components/schemas/ThesisWarning"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/theses/{id}/edit:
    post:
      operationId: editThesis
      tags:
        - Teses
      x-capability: account
      summary: Editar um trecho da tese
      description: "Substitui UM trecho do markdown, sem reenviar o documento inteiro
        — é o caminho barato para corrigir um parágrafo, um número ou um bloco.
        `old_text` precisa aparecer EXATAMENTE UMA vez: se não aparecer, ou
        aparecer mais de uma, a operação falha dizendo quantas vezes achou, em
        vez de trocar a ocorrência errada em silêncio. Inclua as linhas ao redor
        até o trecho ficar único. `new_text` vazio remove o trecho. Para
        reescritas grandes, use updateThesis."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - old_text
                - new_text
              properties:
                old_text:
                  type: string
                  description: Trecho exato do markdown a substituir, como veio no `md` do
                    getThesis.
                new_text:
                  type: string
                  description: Texto que entra no lugar. Vazio remove o trecho.
      responses:
        "200":
          description: Tese atualizada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  thesis:
                    $ref: "#/components/schemas/ThesisDetail"
                  warnings:
                    type: array
                    items:
                      $ref: "#/components/schemas/ThesisWarning"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/theses/reorder:
    post:
      operationId: reorderTheses
      tags:
        - Teses
      x-capability: account
      summary: Reordenar a vitrine
      description: Define a ordem manual das suas teses (perfil público +
        gerenciador). Envie `ids` na ordem desejada; as teses listadas ganham
        posição 0,1,2…, as demais voltam a ordenar por data.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
              properties:
                ids:
                  type: array
                  items:
                    type: string
                  description: Ids das teses na ordem desejada.
      responses:
        "200":
          description: Ordem salva.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/theses/import:
    post:
      operationId: importThesisFile
      tags:
        - Teses
      x-capability: account
      summary: Importar tese de arquivo
      description: Cria uma tese enviando o ARQUIVO em base64 (`content_base64`) — o
        caminho natural para CLI e automações (`--file tese.md`). Aceita
        markdown (`.md` ou conteúdo abrindo com `---`) e o JSON do ReportDoc; o
        formato é detectado do arquivo. Mesma validação e limites do
        createThesis.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - content_base64
              properties:
                filename:
                  type: string
                  description: Nome do arquivo — a extensão decide o formato.
                content_base64:
                  type: string
                  description: Markdown da tese ou JSON do ReportDoc, em base64.
                visibility:
                  type: string
                  enum:
                    - private
                    - unlisted
                    - public
      responses:
        "201":
          description: Tese criada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  thesis:
                    $ref: "#/components/schemas/ThesisDetail"
                  warnings:
                    type: array
                    items:
                      $ref: "#/components/schemas/ThesisWarning"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/theses/{id}:
    get:
      operationId: getThesis
      tags:
        - Teses
      x-capability: account
      summary: Detalhe de uma tese
      description: Markdown da tese (`md`) + visibilidade + status do export (quando
        `ready`, `pdfUrl`/`previewUrl` apontam para a mídia pública). Para ler
        ou editar, use `md`; o documento compilado é derivado dele e só vem com
        `include=doc`, para quem vai renderizar.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: include
          in: query
          required: false
          description: Campos extras, separados por vírgula. Hoje só `doc` (o ReportDoc
            compilado).
          schema:
            type: string
            enum:
              - doc
      responses:
        "200":
          description: Tese.
          content:
            application/json:
              schema:
                type: object
                properties:
                  thesis:
                    $ref: "#/components/schemas/ThesisDetail"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    put:
      operationId: updateThesis
      tags:
        - Teses
      x-capability: account
      summary: Atualizar tese
      description: "Atualiza o conteúdo, a visibilidade, o arquivamento e/ou as tags.
        `md` substitui o markdown INTEIRO (não é patch): leia a tese com
        getThesis, edite o texto e devolva completo. `archived` esconde a tese
        do público sem apagar; `tags` são rótulos PRIVADOS de organização (só o
        dono vê, nunca aparecem na página pública)."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                md:
                  type: string
                  description: "Markdown COMPLETO da tese. Markdown com frontmatter YAML.
                    Obrigatórios no frontmatter: `kind`
                    (equity|fii|macro|setor|carta|carteira) e `title`.
                    Opcionais: `subtitle`, `ticker`, `date` (AAAA-MM-DD),
                    `tags`. Autor, capa, ids de seção e disclaimer o servidor
                    preenche — não escreva. No corpo: `## Título` abre uma
                    seção; parágrafos, listas e `###` viram prosa em GFM; tabela
                    GFM vira tabela do relatório; `>
                    [!risco|tese|catalisador|atenção|info] Título` vira caixa de
                    destaque (linhas `- item` viram bullets). Blocos ricos vão
                    em cercas ```db:<tipo> com corpo YAML, onde <tipo> é rating,
                    keyStats, summary, chart, scenarios, sources, quote, figure,
                    table, cover ou disclaimer. Um bloco fora do formato NÃO
                    derruba a tese: ele degrada para um aviso de atenção no
                    documento e aparece em `warnings` na resposta, com linha e
                    campo, para você corrigir depois."
                doc:
                  allOf:
                    - $ref: "#/components/schemas/ReportDocInput"
                  deprecated: true
                  description: OBSOLETO — use `md`. ReportDoc JSON pronto, aceito para clientes
                    anteriores ao markdown.
                visibility:
                  type: string
                  enum:
                    - private
                    - unlisted
                    - public
                archived:
                  type: boolean
                  description: true arquiva (some do público), false desarquiva.
                tags:
                  type: array
                  items:
                    type: string
                  description: Rótulos privados (substitui o conjunto; máx. 12).
      responses:
        "200":
          description: Tese atualizada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  thesis:
                    $ref: "#/components/schemas/ThesisDetail"
                  warnings:
                    type: array
                    items:
                      $ref: "#/components/schemas/ThesisWarning"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    delete:
      operationId: deleteThesis
      tags:
        - Teses
      x-capability: account
      summary: Excluir tese
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Excluída.
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/theses/{id}/publish:
    post:
      operationId: publishThesis
      tags:
        - Teses
      x-capability: account
      summary: Publicar / mudar visibilidade
      description: "Define a visibilidade: `public` aparece no perfil e tem página
        própria; `unlisted` só por link; `private` volta a ser só sua. O doc
        publicado vai INTEIRO (revisar antes é do autor); leitores anônimos veem
        só a capa e precisam entrar para ler o resto."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - visibility
              properties:
                visibility:
                  type: string
                  enum:
                    - private
                    - unlisted
                    - public
      responses:
        "200":
          description: Visibilidade atualizada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  thesis:
                    $ref: "#/components/schemas/ThesisDetail"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /v1/theses/{id}/export:
    post:
      operationId: exportThesis
      tags:
        - Teses
      x-capability: account
      summary: Exportar em PDF (plano pago)
      description: "Gera o PDF (A4, com preview PNG) da tese e publica num link
        público de mídia. Assíncrono: responde 202 {status: queued} e o
        getThesis passa a devolver `pdfUrl` quando pronto; se o conteúdo não
        mudou desde o último export, responde 200 direto com o link. O PDF traz
        o doc inteiro."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Já exportada (conteúdo inalterado).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThesisExportStatus"
        "202":
          description: Export enfileirado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThesisExportStatus"
        default:
          description: Erro (RFC 9457 application/problem+json)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
x-profiles:
  context:
    description: "O grafo de objetos como superfície única: resolver, ler
      fatos/propriedades/histórico, atravessar relações, ranquear e agregar
      coortes — mais documentos, eventos de mercado e saúde do serviço. É o
      perfil enxuto para agente; sem escrita."
    capabilities:
      - object-context
      - documents
      - events
      - system
  market:
    description: "Mercado público por rotas de domínio: preços, fundamentos,
      cadastros, macro, índices e screeners. Sem o grafo, sem conta, sem
      escrita."
    capabilities:
      - market-data
      - system
  credit:
    description: Crédito privado (debêntures, FIDC, ratings, curvas, balcão) MAIS o
      grafo de objetos — análise de crédito compõe emissor, instrumento e
      prestador pelo grafo. Sem escrita.
    capabilities:
      - credit-market-data
      - object-context
      - system
  account:
    description: "A conta do usuário dono da chave: carteiras, teses e suitability.
      Contém as operações de ESCRITA do contrato."
    capabilities:
      - account
      - system
  full:
    description: A superfície completa do contrato — o comportamento de uma sessão
      sem perfil.
    capabilities:
      - object-context
      - documents
      - events
      - market-data
      - credit-market-data
      - account
      - system
x-ontology:
  version: 1
  kinds:
    - company
    - equity_security
    - fund
    - service_provider
    - instrument
    - index
    - crypto_asset
    - commodity
    - country
    - indicator
    - data_series
    - offering
    - fund_share_class
    - market_event
  rels:
    - rel: administers
      shape: static
      max_gap_days: null
      domain_kinds:
        - service_provider
        - company
      range_kinds:
        - fund
      note: "ADMINISTRADOR FIDUCIÁRIO — o responsável legal pelo fundo: constitui e
        registra na CVM; calcula e divulga a cota; contrata os demais
        prestadores. NÃO é o gestor (`manages`) que decide a carteira — um
        administrador serve centenas de fundos de gestores diferentes. Único dos
        quatro prestadores que a CVM publica SEM CNPJ: chega por razão social e
        é resolvido contra o cadastro ADM_CART pelo nome normalizado — 134 de
        135 nomes e 36.914 dos 37.009 fundos medidos em 22/08/2026"
    - rel: assigned_to
      shape: snapshot
      max_gap_days: 45
      domain_kinds:
        - company
        - fund
      range_kinds:
        - fund
      note: Cessão de recebíveis declarada por competência no informe mensal do FIDC
    - rel: audits
      shape: static
      max_gap_days: null
      domain_kinds:
        - service_provider
        - company
      range_kinds:
        - fund
      note: Auditor independente vigente no registro da CVM
    - rel: contains
      shape: snapshot
      max_gap_days: 45
      domain_kinds:
        - index
        - fund
      range_kinds:
        - equity_security
        - fund
        - fund_share_class
      note: Composição da carteira teórica do índice (o índice contém o PAPEL e nunca
        a companhia) e estrutura da classe de fundo (a classe contém suas
        SUBCLASSES)
    - rel: covers
      shape: static
      max_gap_days: null
      domain_kinds:
        - data_series
      range_kinds:
        - country
      note: "A SÉRIE COBRE o país a que se refere — `world_bank:NY.GDP.MKTP.CD:DE`
        cobre a Alemanha. É o eixo que faltava para o país: sem ele, 113 séries
        do World Bank traziam o país DENTRO da chave (`...:DE`, ISO2) e nada no
        grafo dizia de quem elas falavam, enquanto o objeto do país existia ao
        lado sem uma única aresta. SÓ ONDE A FONTE DECLARA. `bcb_sgs` e `fred`
        ficam de fora de propósito: quem publica não é do que se trata — o Banco
        Central publica a PTAX, que é sobre o par e não sobre o Brasil, e
        deduzir país da fonte seria o vínculo inventado de sempre. Quando o
        catálogo declarar país para elas, elas entram sem mudar o verbo"
    - rel: custodies
      shape: static
      max_gap_days: null
      domain_kinds:
        - company
        - service_provider
      range_kinds:
        - fund
      note: Custodiante vigente no registro da CVM
    - rel: exposed_to_issuer
      shape: snapshot
      max_gap_days: 45
      domain_kinds:
        - fund
        - index
      range_kinds:
        - company
      note: Exposição do detentor à EMISSORA — derivada de holds/contains pelo papel;
        responde COM QUEM e nunca QUANTO (sem magnitude de propósito)
    - rel: forecasts
      shape: static
      max_gap_days: null
      domain_kinds:
        - data_series
      range_kinds:
        - indicator
        - index
      note: A série PROJETA o conceito em vez de medi-lo — Focus / breakeven da curva
        implícita / degrau esperado do Copom. Separado de `measures` em
        20/08/2026 porque as duas conviviam no mesmo verbo com source e shape e
        confidence IDÊNTICOS. Das 45 arestas do IPCA 41 eram projeção — quem
        atravessava a inflação medida recebia 41 previsões junto sem erro nenhum
    - rel: holds
      shape: snapshot
      max_gap_days: 45
      domain_kinds:
        - fund
      range_kinds:
        - instrument
        - fund
        - equity_security
      note: Posição em carteira declarada por competência — detentor detém INSTRUMENTO
        e nunca a emissora; company saiu do range em 17/08/2026 com zero arestas
        medidas em produção; fonte que só identifica o emissor vira
        exposed_to_issuer
    - rel: indexed_to
      shape: event
      max_gap_days: null
      domain_kinds:
        - instrument
      range_kinds:
        - indicator
      note: Indexador contratado na emissão do papel — aponta para o CONCEITO (IPCA) e
        nunca para a série que o publica
    - rel: issued
      shape: event
      max_gap_days: null
      domain_kinds:
        - company
        - fund
      range_kinds:
        - instrument
        - equity_security
        - offering
      note: "Emissão do papel ou da OFERTA — aconteceu numa data e permanece fato
        histórico. O domínio inclui `fund` porque quem oferta cotas de FII/FIDC
        é o próprio fundo: 26.690 das 45.462 arestas de oferta saem de um fundo"
    - rel: manages
      shape: static
      max_gap_days: null
      domain_kinds:
        - service_provider
        - company
      range_kinds:
        - fund
      note: GESTOR (CPF_CNPJ_Gestor da CVM 175) — NÃO é o administrador fiduciário
    - rel: measures
      shape: static
      max_gap_days: null
      domain_kinds:
        - data_series
      range_kinds:
        - indicator
        - index
      note: A série publicada MEDE o conceito OU o índice — bcb_sgs:433 mede o IPCA
        realizado e bcb_sgs:7 mede o IBOV. É declaração de vocabulário e não
        série temporal. NÃO inclui expectativa — para isso existe `forecasts`
    - rel: member_of
      shape: static
      max_gap_days: null
      domain_kinds:
        - instrument
      range_kinds:
        - instrument
      note: 'O TÍTULO PERTENCE à família — "Tesouro IPCA+ com Juros Semestrais 2035" é
        membro do tipo NTN-B. `from` é o título porque `from` é sempre quem
        pratica a ação do verbo; a leitura natural "os títulos desta família" é
        a direção `in`. É relação ESTRUTURAL e não observação datada: um NTN-B
        2035 nunca deixa de ser NTN-B, por isso `static` e sem competência.
        Nasceu em 24/08/2026 para o Tesouro Direto, quando o título ganhou
        identidade própria e a família (que era o único objeto) virou o nó
        agregador'
    - rel: mentions
      shape: event
      max_gap_days: null
      domain_kinds:
        - market_event
      range_kinds:
        - company
        - equity_security
        - fund
        - instrument
      note: "O EVENTO CITA o objeto — `from` é o evento porque `from` é sempre quem
        pratica a ação do verbo; a leitura natural os eventos que falam da
        Petrobras é a direção `in`. A ponta do objeto é carimbada pelo DETECTOR
        na escrita e não casada por nome em tempo de consulta — o casamento por
        razão social truncada acertava 3 de 60. A CONFIANÇA cai com a
        cardinalidade e o corte é em 9: até 8 objetos citados é `high`; 9 ou
        mais é `low` e descreve matéria panorâmica que cita meia bolsa — 35
        eventos produzem 640 das 2.071 arestas medidas em 22/08/2026. O número
        EXATO de citados viaja em `magnitude`; use-o para cortar mais fino que o
        binário do contrato"
    - rel: produces
      shape: snapshot
      max_gap_days: 400
      domain_kinds:
        - country
      range_kinds:
        - commodity
      note: "O PAÍS PRODUZ a commodity mineral, por ano de referência do USGS. `from`
        é o país porque `from` é sempre quem pratica a ação do verbo. NÃO liga
        empresa a produto: os produtores que a fonte publica são PAÍSES, e ligar
        Vale a minério de ferro por semelhança de nome seria o vínculo
        silencioso e errado de sempre — quando existir fonte que ligue companhia
        a produto, ela entra por aqui sem mudar o verbo. As duas linhas
        agregadas da fonte (`World total` e `Other countries`) ficam de fora:
        nenhuma das duas é um país. `magnitude` é a PARTICIPAÇÃO NO TOTAL
        MUNDIAL em pontos percentuais, não a quantidade: quantidade sai em
        toneladas, quilates ou quilos conforme a commodity e não se compara
        entre elas, enquanto a participação se compara sempre. Nula onde a
        própria fonte marca as bases como não somáveis (`incomparable_basis`:
        nas reservas de boro a Turquia declara borato refinado, o Chile ulexita
        e a China óxido bórico). Commodity com mais de um agregado (cobre tem
        produção de mina e de refino, com totais mundiais distintos) usa UM
        recorte só — o que mais países reportam —, e o país que só reporta o
        outro recorte mantém a aresta com magnitude nula: a relação é verdadeira
        mesmo quando o número não é comparável"
    - rel: rates
      shape: event
      max_gap_days: null
      domain_kinds:
        - service_provider
      range_kinds:
        - instrument
        - fund
        - company
      note: "A AGÊNCIA AVALIA o papel ou o emissor — `from` é a agência porque `from`
        é sempre quem pratica a ação do verbo; a leitura natural este CRI é
        avaliado pela Fitch é a direção `in`. Fonte é o relatório da própria
        agência lido por LLM e NÃO o campo declarado no informe da CVM (texto
        livre com ~4% de nota legível; serve para conferir e não para publicar).
        `event` porque a ação de rating tem DATA e permanece fato histórico:
        rebaixamento de 2023 continua tendo acontecido depois de a nota subir em
        2025"
    - rel: same_owner
      shape: static
      max_gap_days: null
      domain_kinds:
        - company
      range_kinds:
        - company
      note: Dois cedentes que compartilham sócio PJ — simétrica
    - rel: shareholder_of
      shape: static
      max_gap_days: null
      domain_kinds:
        - company
        - fund
        - service_provider
      range_kinds:
        - company
      note: Sócio pessoa jurídica na Receita — participação societária SEM percentual
        nem qualificação
    - rel: succeeded_by
      shape: event
      max_gap_days: null
      domain_kinds:
        - fund
        - company
        - equity_security
      range_kinds:
        - fund
        - company
        - equity_security
      note: Sucessão societária real (incorporação/cisão) — não é renome de código
  facts:
    - name: bank_basileia
      kind: company
      unit: ratio
      dimension: share
      scale: unit
      period: quarterly
      cadence: quarterly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: bank_capital_pr
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: bank_credit_portfolio
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: bank_equity
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: bank_net_income
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: quarterly
      cadence: quarterly
      grain: object
      concept: net_income_quarterly
      expected_range: null
    - name: bank_total_assets
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: bdr_sessions
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: beta
      kind: equity_security
      unit: x
      dimension: ratio
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range:
        min: -5
        max: 5
    - name: capex
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: cash
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: close
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: paper
      concept: close_price
      expected_range: null
    - name: close
      kind: crypto_asset
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: close_price
      expected_range: null
    - name: close
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: close_price
      expected_range: null
    - name: close
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: close_price
      expected_range: null
    - name: close
      kind: index
      unit: points
      dimension: index
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: close
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: close_price
      expected_range: null
    - name: close_raw
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: paper
      concept: null
      expected_range: null
    - name: close_raw
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: close_raw
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: close_raw
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: close_usd
      kind: crypto_asset
      unit: usd
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: coe_additional_rate_pct
      kind: instrument
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: coe_indexer_pct
      kind: instrument
      unit: pct
      dimension: share
      scale: percent
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: coe_issue_price
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: coe_issue_size
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: coe_qty_issued
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: coe_trade_count
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: coe_trade_days
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: coe_volume
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: company_market_cap
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: crypto_rank
      kind: crypto_asset
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: current_assets
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: current_liabilities
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: curve_rate
      kind: data_series
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: daily_change_pct
      kind: index
      unit: pct
      dimension: rate
      scale: percent
      period: daily
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: deb_funds_holders
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: deb_funds_qty
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: deb_funds_value_brl
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: deb_net_traded_brl
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: div_liquida_ebitda
      kind: company
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: div_liquida_pl
      kind: company
      unit: x
      dimension: ratio
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: dividend_per_share
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1000
    - name: dividend_per_share_net
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1000
    - name: dna
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: duration_bd
      kind: index
      unit: count
      dimension: duration
      scale: business_days
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: dy_12m
      kind: company
      unit: ratio
      dimension: rate
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: paper
      concept: dividend_yield_12m
      expected_range:
        min: 0
        max: 0.36
    - name: dy_12m
      kind: equity_security
      unit: ratio
      dimension: rate
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: dividend_yield_12m
      expected_range:
        min: 0
        max: 0.36
    - name: earnings_cagr_3y
      kind: company
      unit: ratio
      dimension: rate
      scale: unit
      period: cagr_3y
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: ebit
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: ebitda
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: equity
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: etf_quota_count_authorized
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: etf_sessions
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: ev_ebit
      kind: company
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: paper
      concept: null
      expected_range: null
    - name: ev_ebit
      kind: equity_security
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: ev_ebitda
      kind: company
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: paper
      concept: null
      expected_range: null
    - name: ev_ebitda
      kind: equity_security
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: face_value_current
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: face_value_issue
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: fcf
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: fiagro_sessions
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: fidc_acquired_amount
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_acquired_impaired_pct
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_aging_1080d
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_aging_120d
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_aging_150d
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_aging_180d
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_aging_30d
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_aging_360d
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_aging_60d
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_aging_720d
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_aging_90d
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_aging_acima_1080d
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_allowance_ratio
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_amortizations
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_avg_ticket
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_buy_rate_avg
      kind: fund
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 100
    - name: fidc_collateral_pct
      kind: fund
      unit: pct
      dimension: share
      scale: percent
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1000
    - name: fidc_impaired_ratio
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_investors
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: holders_count
      expected_range: null
    - name: fidc_liquid_30d
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_net_flow
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_net_worth
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: net_worth
      expected_range: null
    - name: fidc_overdue_ratio
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_portfolio
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_portfolio_to_net_worth
      kind: fund
      unit: x
      dimension: ratio
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_redemption_coverage_30d
      kind: fund
      unit: x
      dimension: ratio
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 100
    - name: fidc_redemptions_paid
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_redemptions_requested
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_related_party_sale_pct
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_repurchase_to_portfolio
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_scr_alto_risco
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_scr_baixo_risco
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_sector_hhi
      kind: fund
      unit: points
      dimension: index
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 10000
    - name: fidc_segment_months_stable
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_subscriptions
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fidc_top_originator_pct
      kind: fund
      unit: pct
      dimension: share
      scale: percent
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 100
    - name: fidc_top_sector_share
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fidc_top_segment_share
      kind: fund
      unit: ratio
      dimension: share
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1
    - name: fii_area_m2
      kind: fund
      unit: count
      dimension: area
      scale: square_meters
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fii_cap_rate
      kind: fund
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 30
    - name: fii_distribution_per_share
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1000
    - name: fii_distribution_per_share_adj
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1000
    - name: fii_dividend_yield_month
      kind: fund
      unit: pct
      dimension: rate
      scale: percent
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fii_dy_12m
      kind: fund
      unit: pct
      dimension: rate
      scale: percent
      period: ttm_12m
      cadence: monthly
      grain: object
      concept: dividend_yield_12m
      expected_range:
        min: 0
        max: 36
    - name: fii_fee_effective_pct_aa
      kind: fund
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 10
    - name: fii_fee_month_max_annualized
      kind: fund
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fii_fee_months_observed
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fii_ffo_yield
      kind: fund
      unit: pct
      dimension: rate
      scale: percent
      period: ttm_12m
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 50
    - name: fii_net_asset_value
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: net_worth
      expected_range: null
    - name: fii_price_m2
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fii_properties
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fii_pvp
      kind: fund
      unit: x
      dimension: ratio
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: price_to_book
      expected_range:
        min: 0
        max: 5
    - name: fii_rent_m2
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fii_shareholders
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: holders_count
      expected_range: null
    - name: fii_shares_issued
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: fii_vacancy
      kind: fund
      unit: pct
      dimension: share
      scale: percent
      period: none
      cadence: monthly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 100
    - name: fii_value_per_share
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: monthly
      grain: object
      concept: book_value_per_share
      expected_range: null
    - name: focus_mean
      kind: data_series
      unit: native
      dimension: null
      scale: null
      period: null
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: focus_median
      kind: data_series
      unit: native
      dimension: null
      scale: null
      period: null
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: focus_respondents
      kind: data_series
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: focus_std_dev
      kind: data_series
      unit: native
      dimension: null
      scale: null
      period: null
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: funds_entered
      kind: equity_security
      unit: count
      dimension: count
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: funds_exited
      kind: equity_security
      unit: count
      dimension: count
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: funds_holders
      kind: equity_security
      unit: count
      dimension: count
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: funds_panel_coverage_pct
      kind: equity_security
      unit: pct
      dimension: share
      scale: percent
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: funds_shares
      kind: equity_security
      unit: count
      dimension: count
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: funds_value_brl
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: funds_value_flow_brl
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: funds_value_price_effect_brl
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: gross_debt
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: gross_profit
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: index_value
      kind: index
      unit: points
      dimension: index
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: indexer_pct
      kind: instrument
      unit: pct
      dimension: share
      scale: percent
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: indicator_value
      kind: data_series
      unit: native
      dimension: null
      scale: null
      period: null
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: insider_buy_value_brl
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: insider_net_shares
      kind: company
      unit: count
      dimension: count
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: insider_net_value_brl
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: insider_net_value_brl
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: insider_sell_value_brl
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: monthly
      cadence: monthly
      grain: object
      concept: null
      expected_range: null
    - name: invested_capital
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: jcp_per_share
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1000
    - name: liquidez_corrente
      kind: company
      unit: x
      dimension: ratio
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: lpa
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: paper
      concept: null
      expected_range: null
    - name: lpa
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: margem_bruta
      kind: company
      unit: ratio
      dimension: share
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range:
        min: -3
        max: 1.05
    - name: margem_ebit
      kind: company
      unit: ratio
      dimension: share
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range:
        min: -10
        max: 3
    - name: margem_liquida
      kind: company
      unit: ratio
      dimension: share
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range:
        min: -10
        max: 3
    - name: market_cap
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: paper
      concept: null
      expected_range: null
    - name: market_cap
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: net_debt
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: net_flow
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: net_income
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: net_worth
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: net_worth
      expected_range: null
    - name: nopat
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: ocf
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: offering_amount
      kind: offering
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 1000000000000
    - name: offering_quantity
      kind: offering
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: offering_unit_price
      kind: offering
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: otc_price_avg
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: otc_price_last
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: otc_price_max
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: otc_price_min
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: otc_quantity
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: daily
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: otc_trade_count
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: daily
      cadence: irregular
      grain: object
      concept: trade_count_daily
      expected_range: null
    - name: otc_volume
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: daily
      cadence: irregular
      grain: object
      concept: financial_volume_daily
      expected_range: null
    - name: p_ebit
      kind: company
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: paper
      concept: null
      expected_range: null
    - name: p_ebit
      kind: equity_security
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: payout
      kind: company
      unit: ratio
      dimension: share
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: paper
      concept: null
      expected_range:
        min: 0
        max: 5
    - name: payout
      kind: equity_security
      unit: ratio
      dimension: share
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 5
    - name: pct_of_curve
      kind: instrument
      unit: pct
      dimension: share
      scale: percent
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range:
        min: 20
        max: 200
    - name: pl
      kind: company
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: paper
      concept: null
      expected_range: null
    - name: pl
      kind: equity_security
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: portfolio_value_kbrl
      kind: index
      unit: brl
      dimension: currency
      scale: thousand
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: price_at_eval
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: paper
      concept: null
      expected_range: null
    - name: price_at_eval
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: psr
      kind: company
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: paper
      concept: null
      expected_range: null
    - name: psr
      kind: equity_security
      unit: x
      dimension: ratio
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: pu_avg
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: pvp
      kind: company
      unit: x
      dimension: ratio
      scale: unit
      period: none
      cadence: quarterly
      grain: paper
      concept: price_to_book
      expected_range: null
    - name: pvp
      kind: equity_security
      unit: x
      dimension: ratio
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: price_to_book
      expected_range: null
    - name: qty_issued
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: qty_outstanding
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: quota
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: book_value_per_share
      expected_range: null
    - name: quota_raw
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: retorno_12m
      kind: equity_security
      unit: pct
      dimension: rate
      scale: percent
      period: ttm_12m
      cadence: daily
      grain: object
      concept: null
      expected_range:
        min: -100
        max: 1000
    - name: revenue
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: revenue_cagr_3y
      kind: company
      unit: ratio
      dimension: rate
      scale: unit
      period: cagr_3y
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: roa
      kind: company
      unit: ratio
      dimension: rate
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range:
        min: -3
        max: 3
    - name: roe
      kind: company
      unit: ratio
      dimension: rate
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range:
        min: -5
        max: 5
    - name: roic
      kind: company
      unit: ratio
      dimension: rate
      scale: unit
      period: ttm_12m
      cadence: quarterly
      grain: object
      concept: null
      expected_range:
        min: -5
        max: 5
    - name: shareholders
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: holders_count
      expected_range: null
    - name: spread_median
      kind: data_series
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: spread_n_papers
      kind: data_series
      unit: count
      dimension: count
      scale: unit
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: spread_pct_aa
      kind: instrument
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: spread_pct_of_curve
      kind: data_series
      unit: pct
      dimension: share
      scale: percent
      period: none
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: spread_volume_brl
      kind: data_series
      unit: brl
      dimension: currency
      scale: unit
      period: daily
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: tesouro_buy_price
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: tesouro_buy_rate
      kind: instrument
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: daily
      grain: object
      concept: null
      expected_range:
        min: -2
        max: 25
    - name: tesouro_sell_price
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: tesouro_sell_rate
      kind: instrument
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: daily
      grain: object
      concept: null
      expected_range:
        min: -2
        max: 25
    - name: total_assets
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: trade_count
      kind: equity_security
      unit: count
      dimension: count
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: trade_count_daily
      expected_range: null
    - name: trade_count
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: daily
      cadence: irregular
      grain: object
      concept: trade_count_daily
      expected_range: null
    - name: trade_price_last
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: trade_price_max
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: trade_price_min
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: trade_quantity
      kind: equity_security
      unit: count
      dimension: count
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: trade_volume
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: financial_volume_daily
      expected_range: null
    - name: trade_vwap
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: traded_quantity
      kind: instrument
      unit: count
      dimension: count
      scale: unit
      period: daily
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: trades
      kind: crypto_asset
      unit: count
      dimension: count
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: trade_count_daily
      expected_range: null
    - name: us_change_pct_1y
      kind: company
      unit: pct
      dimension: rate
      scale: percent
      period: ttm_12m
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: us_change_pct_1y
      kind: fund
      unit: pct
      dimension: rate
      scale: percent
      period: ttm_12m
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: us_close
      kind: company
      unit: usd
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: close_price
      expected_range: null
    - name: us_close
      kind: fund
      unit: usd
      dimension: currency
      scale: unit
      period: none
      cadence: daily
      grain: object
      concept: close_price
      expected_range: null
    - name: us_eps_diluted_fy
      kind: company
      unit: usd
      dimension: currency
      scale: unit
      period: annual
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: us_eps_diluted_fy
      kind: fund
      unit: usd
      dimension: currency
      scale: unit
      period: annual
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: us_eps_diluted_q
      kind: company
      unit: usd
      dimension: currency
      scale: unit
      period: quarterly
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: us_eps_diluted_q
      kind: fund
      unit: usd
      dimension: currency
      scale: unit
      period: quarterly
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: us_high_52w
      kind: company
      unit: usd
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: us_high_52w
      kind: fund
      unit: usd
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: us_low_52w
      kind: company
      unit: usd
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: us_low_52w
      kind: fund
      unit: usd
      dimension: currency
      scale: unit
      period: ttm_12m
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: us_net_income_fy
      kind: company
      unit: usd
      dimension: currency
      scale: unit
      period: annual
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: us_net_income_fy
      kind: fund
      unit: usd
      dimension: currency
      scale: unit
      period: annual
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: us_net_income_q
      kind: company
      unit: usd
      dimension: currency
      scale: unit
      period: quarterly
      cadence: quarterly
      grain: object
      concept: net_income_quarterly
      expected_range: null
    - name: us_net_income_q
      kind: fund
      unit: usd
      dimension: currency
      scale: unit
      period: quarterly
      cadence: quarterly
      grain: object
      concept: net_income_quarterly
      expected_range: null
    - name: us_revenue_fy
      kind: company
      unit: usd
      dimension: currency
      scale: unit
      period: annual
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: us_revenue_fy
      kind: fund
      unit: usd
      dimension: currency
      scale: unit
      period: annual
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: us_revenue_q
      kind: company
      unit: usd
      dimension: currency
      scale: unit
      period: quarterly
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: us_revenue_q
      kind: fund
      unit: usd
      dimension: currency
      scale: unit
      period: quarterly
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
    - name: us_volume
      kind: company
      unit: count
      dimension: count
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: us_volume
      kind: fund
      unit: count
      dimension: count
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: value
      kind: data_series
      unit: native
      dimension: null
      scale: null
      period: null
      cadence: irregular
      grain: object
      concept: null
      expected_range: null
    - name: volatilidade
      kind: equity_security
      unit: pct
      dimension: rate
      scale: percent
      period: annual
      cadence: daily
      grain: object
      concept: null
      expected_range:
        min: 0
        max: 300
    - name: volume
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: daily
      cadence: daily
      grain: paper
      concept: financial_volume_daily
      expected_range: null
    - name: volume
      kind: crypto_asset
      unit: count
      dimension: count
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: volume
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: financial_volume_daily
      expected_range: null
    - name: volume
      kind: fund
      unit: brl
      dimension: currency
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: financial_volume_daily
      expected_range: null
    - name: volume
      kind: instrument
      unit: brl
      dimension: currency
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: financial_volume_daily
      expected_range: null
    - name: volume_medio_2m
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: daily
      cadence: daily
      grain: object
      concept: null
      expected_range: null
    - name: vpa
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: paper
      concept: book_value_per_share
      expected_range: null
    - name: vpa
      kind: equity_security
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: book_value_per_share
      expected_range: null
    - name: working_capital
      kind: company
      unit: brl
      dimension: currency
      scale: unit
      period: none
      cadence: quarterly
      grain: object
      concept: null
      expected_range: null
  properties:
    - name: sector
      kind: company
      source: Cadastro de companhias abertas (CVM)
      description: Setor de atuação classificado pela CVM. São 70 valores vivos, por
        isso sem vocabulário declarado — lista congelada aqui ficaria errada na
        primeira revisão da CVM.
      vocabulary: null
    - name: status
      kind: company
      source: Cadastro de companhias abertas (CVM)
      description: "Situação do REGISTRO da companhia na CVM. Não diz nada sobre
        pregão: companhia com status ATIVO pode ter todos os papéis parados há
        anos."
      vocabulary:
        - ATIVO
        - CANCELADA
        - SUSPENSO(A) - DECISÃO ADM
    - name: issuer_status
      kind: company
      source: Cadastro de companhias abertas (CVM)
      description: Situação OPERACIONAL do emissor — é aqui que recuperação judicial e
        falência aparecem, e não em `status`. Confundir os dois é ler uma
        empresa falida como ativa.
      vocabulary:
        - FASE OPERACIONAL
        - FASE PRÉ-OPERACIONAL
        - EM RECUPERAÇÃO JUDICIAL OU EQUIVALENTE
        - EM RECUPERAÇÃO EXTRAJUDICIAL
        - EM LIQUIDAÇÃO JUDICIAL
        - LIQUIDAÇÃO EXTRAJUDICIAL
        - FALIDA
        - PARALISADA
    - name: ownership_control
      kind: company
      source: Cadastro de companhias abertas (CVM)
      description: Natureza do controle acionário declarada à CVM. É a CLASSIFICAÇÃO
        da fonte, não uma apuração de quem controla — para isso existem as
        arestas do grafo.
      vocabulary:
        - PRIVADO
        - PRIVADO HOLDING
        - ESTATAL
        - ESTATAL HOLDING
        - ESTRANGEIRO
        - ESTRANGEIRO HOLDING
    - name: listing_segment
      kind: company
      source: Cadastro de companhias abertas (CVM)
      description: Segmento de listagem na B3, do Básico ao Novo Mercado. É exigência
        de governança, não tamanho nem liquidez.
      vocabulary:
        - Básico
        - Bovespa Mais
        - Nível 1 de Governança Corporativa
        - Nível 2 de Governança Corporativa
        - Novo Mercado
    - name: registration_category
      kind: company
      source: Cadastro de companhias abertas (CVM)
      description: Categoria A pode ter ação listada; Categoria B só emite dívida.
        Explica companhia aberta sem papel nenhum, que não é erro de dado.
      vocabulary:
        - Categoria A
        - Categoria B
    - name: has_active_ticker
      kind: company
      source: Cadastro de companhias abertas (CVM)
      description: "Tem ao menos um papel negociando hoje. `false` junto de `status:
        ATIVO` é o caso comum de companhia registrada e não listada — as duas
        coisas são verdadeiras ao mesmo tempo."
      vocabulary:
        - "true"
        - "false"
    - name: us_filings_count
      kind: company
      source: SEC EDGAR (acervo de filings)
      description: Quantos filings a SEC tem deste papel no acervo. É o tamanho da
        ficha, não do histórico da empresa — o acervo começa em
        `us_filings_since`.
      vocabulary: null
    - name: us_filings_since
      kind: company
      source: SEC EDGAR (acervo de filings)
      description: Desde quando há filing no acervo. Cobertura da FONTE, não idade da
        companhia.
      vocabulary: null
    - name: us_last_filing_at
      kind: company
      source: SEC EDGAR (acervo de filings)
      description: Data do filing mais recente, de qualquer formulário. É o frescor do
        acervo.
      vocabulary: null
    - name: us_last_annual_report_at
      kind: company
      source: SEC EDGAR (acervo de filings)
      description: "Último relatório ANUAL: 10-K (e emenda) para a companhia
        americana, 20-F para a estrangeira listada nos EUA. Mais de um ano atrás
        com `us_last_filing_at` recente = a empresa protocola por outro
        formulário — leia `listUsFilings`."
      vocabulary: null
    - name: us_last_quarterly_report_at
      kind: company
      source: SEC EDGAR (acervo de filings)
      description: Último relatório TRIMESTRAL (10-Q e emenda). Emissor estrangeiro
        não entrega 10-Q — ausência aqui com acervo vivo não é atraso.
      vocabulary: null
    - name: us_last_current_report_at
      kind: company
      source: SEC EDGAR (acervo de filings)
      description: Último fato relevante (8-K, ou 6-K do emissor estrangeiro) — o
        formulário de evento, não o periódico.
      vocabulary: null
    - name: trading_status
      kind: equity_security
      source: CODBDI do pregão (B3, COTAHIST)
      description: "Situação EXCEPCIONAL da emissora, carimbada neste papel pela B3 no
        CODBDI do pregão. NÃO é suspensão: a maioria segue negociando todo dia.
        A propriedade só existe quando há exceção — papel em operação regular
        não tem esta folha, e a ausência É a resposta. É da COMPANHIA, carimbada
        no papel: atravesse `issued` para chegar à emissora."
      vocabulary:
        - sancionada
        - concordataria
        - recuperacao_extrajudicial
        - recuperacao_judicial
    - name: trading_status_since
      kind: equity_security
      source: CODBDI do pregão (B3, COTAHIST)
      description: "Início do PERÍODO ATUAL da situação, não a primeira ocorrência
        dela: quem entra, sai e reentra em recuperação reporta a data da
        reentrada. Derivado de ilhas de pregões contíguos com o mesmo carimbo."
      vocabulary: null
    - name: fund_type
      kind: fund
      source: Cadastro de fundos (CVM, RCVM 175)
      description: "A sigla do tipo: FIF, FIDC, FII, FIP, FIAGRO. É o mesmo eixo que
        `subkind` publica no objeto, e serve para conferir um contra o outro."
      vocabulary: null
    - name: class_type
      kind: fund
      source: Cadastro de fundos (CVM, RCVM 175)
      description: O tipo por extenso, como a RCVM 175 nomeia a CLASSE de cotas —
        'Classes de Cotas de Fundos FIDC' e afins.
      vocabulary: null
    - name: situation
      kind: fund
      source: Cadastro de fundos (CVM, RCVM 175)
      description: Situação do registro. NENHUM dos valores é 'Encerrado' — referência
        de mercado que exibe isso está derivando de outro lugar, não da CVM.
      vocabulary:
        - Em Funcionamento Normal
        - Fase Pré-Operacional
        - Em Liquidação
        - Em Análise
        - Cancelado
    - name: classification
      kind: fund
      source: Cadastro de fundos (CVM, RCVM 175)
      description: Classificação CVM por fator de risco predominante. É grossa de
        propósito; para o corte fino existe a da ANBIMA, ao lado.
      vocabulary:
        - Ações
        - Cambial
        - FMP-FGTS
        - Multimercado
        - Renda Fixa
    - name: anbima_classification
      kind: fund
      source: Cadastro de fundos (CVM, RCVM 175)
      description: "Classificação ANBIMA, bem mais fina que a da CVM: 66 valores
        vivos, por isso sem vocabulário declarado aqui."
      vocabulary: null
    - name: condominium_type
      kind: fund
      source: Cadastro de fundos (CVM, RCVM 175)
      description: Aberto aceita resgate; fechado só sai vendendo a cota. Muda o que
        'liquidez' significa para este fundo, e é a primeira coisa a olhar antes
        de comparar retorno.
      vocabulary:
        - Aberto
        - Fechado
    - name: target_investor
      kind: fund
      source: Cadastro de fundos (CVM, RCVM 175)
      description: Público-alvo regulatório. 'Profissional' e 'Qualificado' têm
        barreira de entrada LEGAL — não é preferência comercial nem sugestão.
      vocabulary:
        - Público Geral
        - Qualificado
        - Profissional
    - name: is_exclusive
      kind: fund
      source: Cadastro de fundos (CVM, RCVM 175)
      description: Fundo exclusivo, de um único cotista. Era `S`/`N` da CVM até
        20/08/2026; a normalização foi para o mart, e não para esta rota, para
        que `getFund` e as propriedades nunca discordem sobre a mesma coluna.
      vocabulary:
        - "true"
        - "false"
    - name: is_investment_entity
      kind: fund
      source: Cadastro de fundos (CVM, RCVM 175)
      description: Entidade de investimento na acepção contábil (CPC 18/IFRS 10), o
        que muda como as participadas são avaliadas. Nulo é ausência de
        declaração na fonte, nunca `false`.
      vocabulary:
        - "true"
        - "false"
    - name: fee_source
      kind: fund
      source: Cadastro de fundos (CVM, RCVM 175)
      description: "De ONDE veio a taxa de administração servida: `extrato` é a fonte
        viva, `cad_fi_legado` é o cadastro antigo. Não é atributo do fundo — é
        procedência do número, e está aqui para quem compara taxas saber o que
        está comparando."
      vocabulary:
        - extrato
        - cad_fi_legado
    - name: fii_segment
      kind: fund
      source: Cadastro de FII (B3 + CVM)
      description: Segmento imobiliário do FII — logística, shoppings, lajes, papel,
        híbrido. É o primeiro corte de qualquer triagem de FII e não existe no
        registro da CVM, que descreve o veículo e não o que ele possui.
      vocabulary: null
    - name: fii_is_paper
      kind: fund
      source: Cadastro de FII (B3 + CVM)
      description: "Se o fundo é de PAPEL (carteira de CRI e recebível) em vez de
        TIJOLO (imóvel físico). Muda o que significa vacância, cap rate e P/VP:
        fundo de papel não tem imóvel, e comparar os dois pelos mesmos
        indicadores compara coisas diferentes. Nulo em 30 dos 546 — a fonte não
        classificou."
      vocabulary:
        - "true"
        - "false"
    - name: etf_segment
      kind: fund
      source: Cadastro de ETF (B3)
      description: Segmento do ETF conforme a B3 — o índice ou a classe de ativo que
        ele replica. Prefixado porque o cadastro de FII publica um `segment`
        próprio, com vocabulário diferente.
      vocabulary: null
    - name: etf_trading_code_others
      kind: fund
      source: Cadastro de ETF (B3)
      description: Outros códigos de negociação do mesmo ETF. Existe porque um ETF
        pode negociar sob mais de um código, e quem casa por ticker precisa
        saber disso antes de concluir que são fundos diferentes.
      vocabulary: null
    - name: etf_first_traded
      kind: fund
      source: Cadastro de ETF (B3)
      description: "Primeiro pregão observado. É início de OBSERVAÇÃO e não de
        existência: ETF anterior à janela do lake aparece com data mais recente
        que a real."
      vocabulary: null
    - name: etf_last_traded
      kind: fund
      source: Cadastro de ETF (B3)
      description: Último pregão observado. Distância grande para hoje indica ETF que
        parou de negociar, não ausência de dado.
      vocabulary: null
    - name: fiagro_first_traded
      kind: fund
      source: Cadastro de FIAGRO (B3 + CVM)
      description: Primeiro pregão observado do FIAGRO na série.
      vocabulary: null
    - name: fiagro_last_traded
      kind: fund
      source: Cadastro de FIAGRO (B3 + CVM)
      description: Último pregão observado do FIAGRO. Distância grande para hoje
        indica papel que parou de negociar, e num universo de 46 fundos isso é a
        maior parte da triagem.
      vocabulary: null
    - name: registration_category
      kind: service_provider
      source: Cadastro de administradores de carteira (CVM, ADM_CART)
      description: O que a casa PODE fazer perante a CVM — não o que ela faz.
        'Administrador Fiduciário' puro administra o fundo sem gerir a carteira;
        a diferença decide quem responde por quê.
      vocabulary:
        - Gestor de Carteira
        - Administrador Fiduciário
        - Administrador Fiduciário e Gestor de Carteira
    - name: registration_subcategory
      kind: service_provider
      source: Cadastro de administradores de carteira (CVM, ADM_CART)
      description: Sob que regime de capital a casa se registrou. 'Instituição
        Financeira' é banco ou DTVM; 'Capital Mínimo' é a gestora independente.
      vocabulary:
        - Capital Mínimo
        - Fundos Especiais
        - Instituição Financeira
    - name: situation
      kind: service_provider
      source: Cadastro de administradores de carteira (CVM, ADM_CART)
      description: Situação do registro na CVM. 'AFASTADO - TERMO DE COMPROMISSO' e
        'SUSPENSO(A)' são estados de sanção, e nenhum dos dois é cancelamento.
      vocabulary:
        - EM FUNCIONAMENTO NORMAL
        - CANCELADA
        - SUSPENSO(A) - DECISÃO ADM
        - AFASTADO - TERMO DE COMPROMISSO
        - LIQUIDAÇÃO EXTRAJUDICIAL
    - name: is_active
      kind: service_provider
      source: Cadastro de administradores de carteira (CVM, ADM_CART)
      description: "Derivada de `situation`: verdadeira só em 'EM FUNCIONAMENTO
        NORMAL'. Os outros quatro estados são diferentes entre si — leia
        `situation` para separá-los."
      vocabulary:
        - "true"
        - "false"
    - name: manages_portfolios
      kind: service_provider
      source: Cadastro de administradores de carteira (CVM, ADM_CART)
      description: A casa gere carteira de terceiros. Falso com registro ativo é o
        administrador fiduciário puro, que existe e não decide alocação —
        confundir os dois atribui decisão a quem não a toma.
      vocabulary:
        - "true"
        - "false"
    - name: ownership_control
      kind: service_provider
      source: Cadastro de administradores de carteira (CVM, ADM_CART)
      description: Natureza do controle declarada à CVM. É classificação da FONTE, não
        apuração de quem controla — para isso existem as arestas do grafo.
      vocabulary:
        - PRIVADO
        - PRIVADO HOLDING
        - ESTATAL
    - name: state
      kind: service_provider
      source: Cadastro de administradores de carteira (CVM, ADM_CART)
      description: UF da sede declarada. Concentração geográfica de gestoras é
        informação de mercado, não detalhe cadastral.
      vocabulary: null
    - name: advisor_situation
      kind: service_provider
      source: Cadastro de assessores de investimento (CVM, AGENTE_AUTON)
      description: "Situação do registro de ASSESSOR na CVM. Vocabulário próprio,
        diferente do da gestora: no acervo PJ só existem os dois estados — 1.421
        em funcionamento e 3.323 canceladas, e o cancelado é histórico legítimo,
        não lixo."
      vocabulary:
        - EM FUNCIONAMENTO NORMAL
        - CANCELADA
    - name: advisor_registered_at
      kind: service_provider
      source: Cadastro de assessores de investimento (CVM, AGENTE_AUTON)
      description: Data do registro na CVM. Idade de registro é o único proxy de
        experiência que o cadastro público oferece.
      vocabulary: null
    - name: advisor_canceled_at
      kind: service_provider
      source: Cadastro de assessores de investimento (CVM, AGENTE_AUTON)
      description: Data do cancelamento, quando houve. Junto de `advisor_situation`,
        separa o ativo do histórico sem adivinhar.
      vocabulary: null
    - name: advisor_city
      kind: service_provider
      source: Cadastro de assessores de investimento (CVM, AGENTE_AUTON)
      description: Município da sede declarada. Com `advisor_state`, é o eixo
        geográfico do censo de assessorias.
      vocabulary: null
    - name: advisor_state
      kind: service_provider
      source: Cadastro de assessores de investimento (CVM, AGENTE_AUTON)
      description: UF da sede declarada à CVM. Concentração geográfica de assessorias
        é informação de mercado, não detalhe cadastral.
      vocabulary: null
    - name: situation
      kind: fund_share_class
      source: Registro de subclasses (CVM, RCVM 175)
      description: "Situação do registro da SUBCLASSE, que pode diferir da classe-mãe:
        subclasse em liquidação dentro de classe em funcionamento é caso
        normal."
      vocabulary:
        - Em Funcionamento Normal
        - Fase Pré-Operacional
        - Em Liquidação
        - Em Análise
        - Cancelado
    - name: is_operating
      kind: fund_share_class
      source: Registro de subclasses (CVM, RCVM 175)
      description: "Derivada de `situation`: verdadeira só em 'Em Funcionamento
        Normal'. Os outros quatro estados são diferentes entre si — leia
        `situation` para separá-los, porque o booleano os colapsa."
      vocabulary:
        - "true"
        - "false"
    - name: condominium_type
      kind: fund_share_class
      source: Registro de subclasses (CVM, RCVM 175)
      description: Aberto aceita resgate; fechado só sai vendendo a cota. Muda o que
        liquidez significa para esta subclasse, e 7.085 das 9.119 são fechadas.
      vocabulary:
        - Aberto
        - Fechado
    - name: target_investor
      kind: fund_share_class
      source: Registro de subclasses (CVM, RCVM 175)
      description: Público-alvo regulatório desta subclasse. É onde a estrutura
        sênior/subordinada costuma se separar do varejo, com barreira legal e
        não comercial.
      vocabulary:
        - Público Geral
        - Qualificado
        - Profissional
    - name: is_exclusive
      kind: fund_share_class
      source: Registro de subclasses (CVM, RCVM 175)
      description: Subclasse exclusiva, de um único cotista. Nulo é ausência de
        declaração na fonte, nunca 'não é exclusiva' — por isso a linha some em
        vez de vir falsa.
      vocabulary:
        - "true"
        - "false"
    - name: is_pension
      kind: fund_share_class
      source: Registro de subclasses (CVM, RCVM 175)
      description: Subclasse previdenciária. 475 das 9.119 não declaram, e nulo não é
        'não é previdenciária' — a linha some em vez de afirmar o que a fonte
        não disse.
      vocabulary:
        - "true"
        - "false"
    - name: parent_class_name
      kind: fund_share_class
      source: Registro de subclasses (CVM, RCVM 175)
      description: "O nome da CLASSE-mãe como o registro a chama. Atenção: o nome
        comercial da subclasse NÃO casa com o rótulo do informe mensal — o
        registro diz 'SEN 2' e o informe diz 'Subclasse Sênior Série 2'. As duas
        listas viajam lado a lado de propósito; casá-las por similaridade
        produziria vínculo errado em silêncio."
      vocabulary: null
    - name: indexer
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: "O indexador CONTRATADO na emissão. `DI` e `PRÉ` não são a mesma
        família de risco que `IPCA`: o primeiro acompanha juro nominal e o
        segundo carrega inflação. `SEM-ÍNDICE` é declaração da fonte, não
        ausência de dado."
      vocabulary:
        - DI
        - IPCA
        - IGP-M
        - SEM-ÍNDICE
        - TR
        - PRÉ
        - ANBID
        - INPC
        - TJLP
        - DOLAR
        - IGP-DI
        - TR-REAL
        - US$ COMERCIAL
        - PREFIXADO
        - PÓS
        - BTN
        - IPC-R
        - IPC-M
        - UFIR
        - TBF
        - SELIC
        - EURO
        - FDS
        - IPC-FIPE
    - name: guarantee_type
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: Espécie da garantia. `Quirografária` é SEM garantia real e é a
        maioria (5.819 de 9.887); `Subordinada` fica atrás dos demais credores
        na ordem de pagamento. Não é nota de risco — é posição na fila.
      vocabulary:
        - Quirografária
        - Real
        - Subordinada
        - Flutuante
    - name: debenture_class
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: Conversibilidade em ação. `-` é como a fonte publica quando não
        declara, e viaja sem tradução.
      vocabulary:
        - Simples
        - Conversível
        - Permutável
        - "-"
        - Não Conversível Permutável
    - name: situation
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: Situação no registro. `Excluído` são 5.148 de 9.887 e significa
        papel que saiu do sistema (vencido, resgatado), não erro de cadastro — a
        maioria da base é histórico, e uma triagem que não filtre isto mistura
        papel vivo com papel morto.
      vocabulary:
        - Excluído
        - Registrado
    - name: registration_regime
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: Rito pelo qual a emissão chegou ao mercado. `dispensa_icvm_476`
        (esforços restritos) é o maior grupo e é DISPENSADO de registro
        ordinário — por isso emissor sem registro CVM não é anomalia.
      vocabulary:
        - dispensa_icvm_476
        - rito_automatico_rcvm_160
        - registro_ordinario
        - dispensa_outra
        - pendente
        - outro
    - name: is_incentivada
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: Se é incentivada (Lei 12.431), com isenção de IR para pessoa
        física. 1.411 de 9.887. Muda o retorno LÍQUIDO sem mudar a taxa
        contratada, então comparar taxa bruta entre incentivada e comum compara
        coisas diferentes.
      vocabulary:
        - "true"
        - "false"
    - name: is_active
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: "Se o papel está ativo no registro. Ler junto com `situation`: as
        duas vêm da fonte e respondem perguntas próximas, mas não idênticas."
      vocabulary:
        - "true"
        - "false"
    - name: maturity_date
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: "Vencimento COMO ARQUIVADO. Pode ser sentinela — ver
        `maturity_is_sentinel`. É texto: para cortar por prazo use data, não
        comparação de string, e o corte numérico por prazo remanescente ainda
        não existe como medida."
      vocabulary: null
    - name: effective_maturity_date
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: Vencimento EFETIVO, já resolvida a sentinela. É este que deve
        entrar em conta de prazo; `maturity_date` fica para auditoria contra a
        fonte.
      vocabulary: null
    - name: maturity_is_sentinel
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: Se o vencimento arquivado é valor-sentinela (ano ≥ 2090) e não data
        real. São 55 papéis. Sem esta flag, eles entram em bucket de prazo
        longuíssimo e distorcem qualquer curva.
      vocabulary:
        - "true"
        - "false"
    - name: spread_absent_reason
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: "Por que `spread_pct_aa` está ausente. Existe em 17,7% dos papéis,
        e 82,3% + 17,7% = 100%: não há buraco na base, há ausência DECLARADA.
        `spread_pct_aa = 0` nunca é omissão."
      vocabulary:
        - not_filed
        - filed_zero_unverifiable
        - no_index_no_rate
        - unparseable_legacy
    - name: issuer_match_level
      kind: instrument
      source: SND/ANBIMA + registro CVM
      description: "Como o emissor foi casado com o registro da CVM.
        `none_issuer_not_in_cvm_registry` NÃO é falha nossa: emissão por
        esforços restritos dispensa registro, e o emissor legitimamente não está
        lá."
      vocabulary:
        - exact
        - none_issuer_not_in_cvm_registry
        - none_cnpj_root_only
    - name: bdr_kind
      kind: instrument
      source: Cadastro B3
      description: Tipo do programa. `nao_patrocinado` é emitido pela depositária SEM
        acordo com a companhia estrangeira e é a maioria (878 de 1.194);
        `patrocinado` tem acordo (21); `etf` é BDR de fundo de índice (295), e
        aí o lastro é uma carteira, não uma empresa.
      vocabulary:
        - nao_patrocinado
        - etf
        - patrocinado
    - name: bdr_spec
      kind: instrument
      source: Cadastro B3
      description: Especificação do papel como a B3 publica. `ED` e `EB` no sufixo
        marcam situações de negociação, não classe — e por isso a sigla viaja
        inteira, sem ser partida em campos que a fonte não separa.
      vocabulary:
        - DRN
        - DRE
        - DRN ED
        - DRE ED
        - DR1
        - DRN EB
        - DR3
        - DR2
        - DRN EDB
        - UNT
        - DRN A
        - DIR
        - DRN C
    - name: first_traded
      kind: instrument
      source: Cadastro B3
      description: "Primeiro pregão observado do papel na nossa série. É início de
        OBSERVAÇÃO, não de existência: papel anterior à janela do lake aparece
        com data mais recente do que a real."
      vocabulary: null
    - name: last_traded
      kind: instrument
      source: Cadastro B3
      description: Último pregão observado. Distância grande para hoje indica papel
        que parou de negociar, não ausência de dado.
      vocabulary: null
    - name: coe_indexer
      kind: instrument
      source: BDI InstrumentRegistration (B3)
      description: Indexador DE REGISTRO da emissão — NÃO é o retorno do investidor. O
        COE é estruturado e o que se recebe depende de barreira e cenário, que a
        fonte não publica. `SEM REMUNERACAO` e `NO PERIODO` são rótulos do
        formulário, não promessa de zero.
      vocabulary:
        - NO PERIODO
        - SEM REMUNERACAO
        - IPCA VCP
        - PRE
        - DI
    - name: coe_issue_type
      kind: instrument
      source: BDI InstrumentRegistration (B3)
      description: Modalidade da oferta no registro. Nos 957 COEs com objeto hoje,
        todos vêm `PUBLICA` — mas isso é o que a janela observada mostra, não um
        vocabulário fechado da fonte.
      vocabulary: null
    - name: coe_is_incentivada
      kind: instrument
      source: BDI InstrumentRegistration (B3)
      description: "Se o registro marca incentivo fiscal. Campo de REGISTRO: confirme
        no documento da emissão antes de concluir isenção."
      vocabulary:
        - "true"
        - "false"
    - name: coe_maturity_date
      kind: instrument
      source: BDI InstrumentRegistration (B3)
      description: Vencimento no registro da emissão. Prefixado porque `maturity_date`
        já existe na ficha da debênture e as duas abrem pela mesma chave.
      vocabulary: null
    - name: coe_last_trade_date
      kind: instrument
      source: BDI InstrumentRegistration (B3)
      description: Último pregão com negócio no balcão. Nulo significa registrado e
        nunca negociado — que é o caso comum em COE, não anomalia.
      vocabulary: null
    - name: family
      kind: offering
      source: Ofertas públicas (CVM)
      description: A família CANÔNICA do que está sendo ofertado — o mesmo eixo que
        `subkind` publica no objeto. É por aqui que se filtra, não por
        `asset_type_as_filed`.
      vocabulary:
        - fundo
        - divida
        - securitizacao
        - equity
        - outro
    - name: instrument
      kind: offering
      source: Ofertas públicas (CVM)
      description: O instrumento canônico, um nível mais fino que a família — também
        derivado, também seguro para filtrar.
      vocabulary: null
    - name: asset_type_as_filed
      kind: offering
      source: Ofertas públicas (CVM)
      description: "O tipo do ativo COMO A FONTE ESCREVEU. Está aqui por procedência,
        não para filtrar: são 60 grafias para ~28 instrumentos, porque cada
        regime usa um vocabulário. Filtrar por ele devolvia 1.841 de 2.801
        debêntures — 34% de perda silenciosa. Use `family` ou `instrument`."
      vocabulary: null
    - name: offering_type
      kind: offering
      source: Ofertas públicas (CVM)
      description: Primária (dinheiro novo para o emissor), secundária (venda de quem
        já tinha) ou mista. Muda QUEM recebe o volume, que é a diferença entre a
        empresa capitalizar e um sócio sair.
      vocabulary: null
    - name: rite
      kind: offering
      source: Ofertas públicas (CVM)
      description: Rito de registro. Automático e ordinário têm exigências e prazos
        diferentes, e isso explica ofertas de mesmo tamanho com histórias
        regulatórias distintas.
      vocabulary: null
    - name: regime
      kind: offering
      source: Ofertas públicas (CVM)
      description: O regime regulatório sob o qual a oferta foi registrada — é o que
        separa o acervo ICVM 476 do resto, e o 476 não tem data de registro.
      vocabulary: null
    - name: status
      kind: offering
      source: Ofertas públicas (CVM)
      description: "Situação da oferta. REGISTRADA NÃO É DISTRIBUÍDA: o volume servido
        é o pretendido no registro, não o efetivamente colocado — somar
        registros e chamar de captação do ano é o erro comum aqui."
      vocabulary: null
    - name: registration_modality
      kind: offering
      source: Ofertas públicas (CVM)
      description: Modalidade do registro na CVM. Junto de `registration_regime`,
        descreve por qual porta a oferta entrou.
      vocabulary: null
    - name: registration_regime
      kind: offering
      source: Ofertas públicas (CVM)
      description: Regime do registro — dispensa, análise prévia e afins. É atributo
        do PROCESSO, não do papel ofertado.
      vocabulary: null
    - name: series
      kind: offering
      source: Ofertas públicas (CVM)
      description: "A série dentro da emissão, e ela é parte do GRÃO: uma emissão com
        três séries são três ofertas aqui, com volumes diferentes. Somá-las sem
        perceber conta a mesma emissão três vezes."
      vocabulary: null
    - name: issue
      kind: offering
      source: Ofertas públicas (CVM)
      description: O número da emissão do emissor. Emissão alta com séries curtas é
        emissor recorrente, e isso é informação sobre o emissor, não sobre esta
        oferta.
      vocabulary: null
    - name: tax_incentive
      kind: offering
      source: Ofertas públicas (CVM)
      description: "Papel incentivado (Lei 12.431 e afins). Muda a comparação de taxa:
        incentivado rende líquido de IR para pessoa física, então comparar a
        taxa crua com a de um papel comum favorece o comum sem motivo."
      vocabulary:
        - "true"
        - "false"
    - name: value_is_sentinel
      kind: offering
      source: Ofertas públicas (CVM)
      description: O volume declarado é lixo da fonte — três registros vêm preenchidos
        com noves, até R$ 100 tri. A flag NÃO altera o valor servido; existe
        para você poder excluí-lo ao agregar.
      vocabulary:
        - "true"
        - "false"
    - name: value_pre_real_currency
      kind: offering
      source: Ofertas públicas (CVM)
      description: O volume está em moeda ANTERIOR ao Plano Real e nunca foi
        convertido — 693 ofertas. Somá-lo com o resto produz um total sem
        significado nenhum.
      vocabulary:
        - "true"
        - "false"
    - name: dimension
      kind: data_series
      source: Catálogo de séries
      description: O que o número É. Sem isto o `unit` da fonte não decide nada — no
        World Bank ele traz o NOME do indicador em 113 de 185 séries.
      vocabulary:
        - rate
        - index
        - currency
        - count
        - share
        - points
        - ratio
    - name: scale
      kind: data_series
      source: Catálogo de séries
      description: Em que escala o número vem. É o eixo que separa 4,44 de 0,0444 para
        a MESMA inflação, e ler errado erra por 100× sem nada acusar.
      vocabulary:
        - unit
        - thousand
        - million
        - billion
        - percent
        - bps
    - name: period
      kind: data_series
      source: Catálogo de séries
      description: Que JANELA o número cobre, e NÃO é a frequência de publicação. A TR
        é publicada todo dia e o número é DO MÊS. É a diferença entre o IPCA do
        mês (bcb_sgs:433) e o acumulado em 12 meses (bcb_sgs:13522), que é o
        erro macro mais caro que existe.
      vocabulary:
        - none
        - daily
        - monthly
        - quarterly
        - annual
        - ttm_12m
    - name: seasonal_adjustment
      kind: data_series
      source: Catálogo de séries
      description: Ajuste sazonal. Vivia só no sufixo do nome (`ibc_br` ×
        `ibc_br_dessaz`), o que obrigava a ler o rótulo para saber o que se
        estava comparando.
      vocabulary:
        - nsa
        - sa
        - saar
    - name: frequency
      kind: data_series
      source: Catálogo de séries
      description: "Com que frequência a fonte PUBLICA. Compare com `period` antes de
        concluir qualquer coisa: as duas divergem de propósito em várias séries,
        e é essa divergência que engana."
      vocabulary: null
    - name: served_by
      kind: data_series
      source: Catálogo de séries
      description: Qual operação serve as observações desta série. O catálogo une três
        casas desde 19/08/2026, e sem isto quem lesse a lista chamaria
        `getSeries` para todas — cabeçalho e ZERO pontos nas que moram noutra
        tabela, que se lê como 'o dado não existe'.
      vocabulary: null
    - name: group
      kind: commodity
      source: USGS Mineral Commodity Summaries
      description: A família da commodity. É o eixo por onde se pergunta "quais são os
        críticos" ou "quais são os metais preciosos" — o nome da commodity
        sozinho não diz a que grupo ela pertence.
      vocabulary:
        - Minerais industriais
        - Terras raras e críticos
        - Metais básicos
        - Metais ferrosos e ligas
        - Materiais de construção
        - Metais preciosos
        - Fertilizantes
        - Energia
    - name: is_critical
      kind: commodity
      source: USGS Mineral Commodity Summaries
      description: Se está na lista de MINERAIS CRÍTICOS do USGS — a designação
        oficial que sustenta política industrial e restrição de exportação nos
        EUA. Não é juízo sobre escassez geológica nem sobre preço.
      vocabulary:
        - "true"
        - "false"
    - name: units
      kind: commodity
      source: USGS Mineral Commodity Summaries
      description: 'A unidade em que as observações desta commodity vêm, e ela NÃO é
        única: 2 das 97 publicam em duas unidades ao mesmo tempo ("metric tons |
        thousand metric tons"), porque produção e reserva usam escalas
        diferentes. Somar as duas sem ler isto erra por mil.'
      vocabulary: null
    - name: usgs_label
      kind: commodity
      source: USGS Mineral Commodity Summaries
      description: "O nome COMO O USGS ESCREVEU, em inglês. Está aqui por procedência:
        a grafia muda entre edições do relatório, e é por isso que a chave do
        objeto é o slug e não este texto."
      vocabulary: null
    - name: area_type
      kind: country
      source: ISO 3166-1 (pacote iso-codes)
      description: 'País ou BLOCO. A única pergunta que esta propriedade responde é
        "posso somar esta linha com as outras", e ela existe porque a resposta
        às vezes é não: a União Europeia é objeto do mesmo tipo que a Alemanha,
        com o mesmo formato de chave, e somá-las conta a Alemanha duas vezes
        devolvendo um número inteiramente plausível.'
      vocabulary:
        - country
        - bloc
    - name: name_en
      kind: country
      source: ISO 3166-1 (pacote iso-codes)
      description: O nome em inglês da norma. Serve para casar com fonte estrangeira
        que publica por nome e não por código — o nome servido pelo objeto é o
        português.
      vocabulary: null
    - name: layer
      kind: market_event
      source: Ledger de eventos de mercado
      description: "A CAMADA do evento: `estrutural` move o cenário inteiro (juro,
        câmbio, regulação), `setorial` move um setor e `corporativa` move uma
        empresa. É o corte que separa 'o que muda a tese' de 'o que muda a
        notícia'."
      vocabulary:
        - estrutural
        - setorial
        - corporativa
    - name: category
      kind: market_event
      source: Ledger de eventos de mercado
      description: O assunto do evento, no vocabulário do detector.
      vocabulary:
        - macro
        - politica
        - internacional
        - commodities
        - corporate
        - fii
        - cripto
        - mercado
        - outros
    - name: event_status
      kind: market_event
      source: Ledger de eventos de mercado
      description: Situação do evento no ledger. Prefixado porque `status` já é
        propriedade de outros tipos e o vocabulário é outro — sem o prefixo, um
        corte por `status` responderia duas coisas diferentes conforme o tipo da
        coorte.
      vocabulary:
        - ativo
        - arquivado
  aspects:
    - kind: company
      name: quotes
      description: Cotações diárias de {ticker}, ajustadas por proventos por default
      operation: listQuotes
      parameter: ticker
      grain: paper
    - kind: company
      name: indicators
      description: Indicadores fundamentalistas de {ticker}, TTM e consolidados
      operation: getStockIndicators
      parameter: ticker
      grain: paper
    - kind: company
      name: dividends
      description: Proventos e JCP de {ticker}, com data-ex e data de pagamento
      operation: listDividends
      parameter: ticker
      grain: paper
    - kind: company
      name: trading_status
      description: Situação da emissora carimbada em {ticker} (recuperação judicial e
        afins). NÃO é suspensão de pregão
      operation: getStock
      parameter: ticker
      grain: object
    - kind: company
      name: corporate_events
      description: Eventos societários MECÂNICOS de {ticker} — desdobramento,
        grupamento, bonificação, com o factor de ajuste por data-ex. Não é o
        ledger editorial de `getObjectEvents`
      operation: listCorporateEvents
      parameter: ticker
      grain: paper
    - kind: company
      name: documents
      description: Documentos entregues à CVM, por data de referência e de entrega. É
        da COMPANHIA, não do papel
      operation: listCompanyDocuments
      parameter: company
      grain: object
    - kind: company
      name: debentures
      description: Debêntures emitidas por esta companhia
      operation: listDebentures
      parameter: issuerCnpj
      grain: object
    - kind: company
      name: insider
      description: "Fluxo mensal de insiders (compras menos vendas de mercado). É do
        EMISSOR: qualquer papel desta companhia devolve os MESMOS números, então
        somar papéis conta a mesma movimentação duas vezes"
      operation: listInsiderMoves
      parameter: ticker
      grain: object
    - kind: equity_security
      name: quotes
      description: Cotações diárias de {ticker}, ajustadas por proventos por default
      operation: listQuotes
      parameter: ticker
      grain: object
    - kind: equity_security
      name: indicators
      description: Indicadores fundamentalistas de {ticker}, TTM e consolidados
      operation: getStockIndicators
      parameter: ticker
      grain: object
    - kind: equity_security
      name: dividends
      description: Proventos e JCP de {ticker}, com data-ex e data de pagamento
      operation: listDividends
      parameter: ticker
      grain: object
    - kind: equity_security
      name: trading_status
      description: "Situação da emissora carimbada em {ticker} (recuperação judicial e
        afins). NÃO é suspensão de pregão. É da COMPANHIA: atravesse `issued`
        para chegar nela"
      operation: getStock
      parameter: ticker
      grain: object
    - kind: equity_security
      name: corporate_events
      description: Eventos societários MECÂNICOS de {ticker} — desdobramento,
        grupamento, bonificação, com o factor de ajuste por data-ex. A rota
        resolve os códigos antigos do papel. Não é o ledger editorial de
        `getObjectEvents`
      operation: listCorporateEvents
      parameter: ticker
      grain: object
    - kind: fund
      name: profile
      description: "Cadastro do fundo: administrador, gestor, taxas, situação"
      operation: getFund
      parameter: cnpj
      grain: object
    - kind: fund
      name: quotes
      description: Cota diária, patrimônio e captação/resgate
      operation: listFundQuotes
      parameter: cnpj
      grain: object
    - kind: fund
      name: holdings
      description: Carteira declarada (CDA), por competência
      operation: listFundHoldings
      parameter: cnpj
      grain: object
    - kind: instrument
      name: quotes
      description: Negócios do secundário de balcão
      operation: listDebentureQuotes
      parameter: code
      grain: object
    - kind: index
      name: composition
      description: Carteira teórica do índice, com peso por papel
      operation: getIndexComposition
      parameter: code
      grain: object
    - kind: data_series
      name: series
      description: A série histórica do indicador
      operation: getSeries
      parameter: series_id
      grain: object
